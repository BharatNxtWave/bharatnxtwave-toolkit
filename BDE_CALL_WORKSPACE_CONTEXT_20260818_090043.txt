BHARATNXT BDE CALL WORKSPACE
TARGETED CURRENT SOURCE CONTEXT

PROJECT ROOT: /mnt/d/BharatNXT Wave/BharatNXTwave Toolkit
GENERATED: 2026-08-18T09:00:43
FILES INCLUDED: 15

This export contains source text only.
Database contents, uploaded client data and environment files are excluded.

====================================================================================================
FILE: static/css/bde_workspace.css
LINES: 2473
SHA256: f034c6fbe9a1f48c14051045b08193055d980c665486177caecf750095847be8
====================================================================================================
/* =========================================================
   BharatNXT Wave BDE Call Console
   Call-first experience layer. Admin Center intentionally
   remains on admin_base.html and is not affected by this file.
========================================================= */

:root {
    --bde-ink: #111318;
    --bde-muted: #676d77;
    --bde-soft: #f4f6f8;
    --bde-panel: #ffffff;
    --bde-line: #e3e7ec;
    --bde-blue: #1769ff;
    --bde-blue-dark: #0b4dd8;
    --bde-blue-soft: #eef4ff;
    --bde-yellow: #f6c945;
    --bde-green: #168a5b;
    --bde-green-soft: #eaf8f1;
    --bde-amber: #a96600;
    --bde-amber-soft: #fff6dc;
    --bde-red: #b63232;
    --bde-red-soft: #fff0f0;
    --bde-radius: 18px;
    --bde-radius-sm: 12px;
    --sidebar-width: 270px;
}

body {
    background: #f5f6f7;
    color: var(--bde-ink);
}

.bde-shell {
    min-height: 100vh;
}

.bde-sidebar {
    width: var(--sidebar-width);
    background: #111318;
    border-right: 0;
    color: #fff;
    padding: 18px 14px;
}

.bde-brand {
    min-height: 72px;
    padding: 10px 12px 18px;
    margin-bottom: 8px;
    border-bottom: 1px solid rgba(255,255,255,.1);
}

.bde-brand .brand-mark {
    width: 40px;
    height: 40px;
    border-radius: 12px;
    display: grid;
    place-items: center;
    font-weight: 800;
    letter-spacing: -.03em;
    background: #fff;
    color: #111318;
}

.bde-brand .brand-name {
    color: #fff;
    font-weight: 760;
    letter-spacing: -.02em;
}

.bde-brand .brand-subtitle {
    color: rgba(255,255,255,.52);
    font-size: 10px;
    font-weight: 750;
    letter-spacing: .12em;
}

.bde-navigation {
    flex: 1;
    overflow-y: auto;
    padding: 4px 0 12px;
}

.bde-navigation .nav-section-label {
    color: rgba(255,255,255,.38);
    padding: 16px 12px 7px;
    font-size: 10px;
    letter-spacing: .14em;
    font-weight: 800;
}

.bde-nav-secondary-label {
    margin-top: 14px;
}

.bde-navigation .nav-item {
    min-height: 48px;
    display: flex;
    align-items: center;
    gap: 11px;
    margin: 3px 0;
    padding: 0 12px;
    border-radius: 12px;
    color: rgba(255,255,255,.72);
    font-size: 14px;
    font-weight: 650;
    transition: background .16s ease, color .16s ease, transform .16s ease;
}

.bde-navigation .nav-item:hover {
    background: rgba(255,255,255,.07);
    color: #fff;
    transform: translateX(2px);
}

.bde-navigation .nav-item.active {
    background: #fff;
    color: #111318;
}

.bde-navigation .nav-icon {
    width: 22px;
    text-align: center;
    font-size: 18px;
}

.bde-sidebar-hint {
    margin: auto 6px 12px;
    padding: 13px 14px;
    border: 1px solid rgba(255,255,255,.1);
    background: rgba(255,255,255,.045);
    border-radius: 12px;
    display: grid;
    gap: 4px;
}

.bde-sidebar-hint strong {
    font-size: 11px;
    color: #fff;
}

.bde-sidebar-hint span {
    font-size: 10px;
    line-height: 1.45;
    color: rgba(255,255,255,.48);
}

.bde-sidebar-hint kbd {
    padding: 1px 4px;
    border: 1px solid rgba(255,255,255,.18);
    border-radius: 4px;
    background: rgba(255,255,255,.06);
    color: rgba(255,255,255,.8);
    font: inherit;
}

.bde-sidebar .sidebar-account-actions {
    position: static;
    padding: 8px 6px 0;
    border-top: 1px solid rgba(255,255,255,.1);
}

.bde-sidebar .sidebar-logout-button {
    color: rgba(255,255,255,.62);
    background: transparent;
    border: 0;
}

.bde-main-area {
    margin-left: var(--sidebar-width);
}

.bde-topbar {
    height: 70px;
    background: rgba(255,255,255,.94);
    backdrop-filter: blur(14px);
    border-bottom: 1px solid var(--bde-line);
    padding: 0 32px;
}

.bde-global-search {
    width: min(720px, 62vw);
    height: 46px;
    border: 1px solid #dde2e8;
    background: #f7f8fa;
    border-radius: 14px;
    padding: 0 12px;
}

.bde-global-search:focus-within {
    border-color: rgba(23,105,255,.5);
    box-shadow: 0 0 0 4px rgba(23,105,255,.08);
    background: #fff;
}

.bde-global-search input {
    font-size: 14px;
}

.bde-global-search .search-shortcut {
    background: #fff;
    border: 1px solid var(--bde-line);
    color: #7a808a;
    border-radius: 7px;
    padding: 4px 7px;
    font-size: 10px;
    font-weight: 750;
}

.bde-content {
    padding: 34px 36px 90px;
    max-width: 1600px;
    width: 100%;
    margin: 0 auto;
}

.bde-page {
    width: 100%;
}

.bde-page-hero {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    gap: 28px;
    margin-bottom: 26px;
}

.bde-page-hero > div:first-child {
    max-width: 900px;
}

.bde-eyebrow {
    margin-bottom: 8px;
    color: var(--bde-blue);
    font-size: 11px;
    font-weight: 820;
    letter-spacing: .14em;
}

.bde-page-hero h1,
.bde-detail-hero h1 {
    margin: 0;
    max-width: 980px;
    color: var(--bde-ink);
    font-size: clamp(34px, 4.1vw, 56px);
    line-height: .99;
    letter-spacing: -.05em;
    font-weight: 820;
}

.bde-page-hero h1 span {
    color: #8a9099;
    font-weight: 660;
}

.bde-page-hero p {
    max-width: 760px;
    margin: 14px 0 0;
    color: var(--bde-muted);
    font-size: 17px;
    line-height: 1.55;
}

.bde-hero-link {
    flex: 0 0 auto;
    color: var(--bde-blue);
    font-size: 14px;
    font-weight: 750;
    text-decoration: none;
}

.bde-search-command {
    margin-bottom: 18px;
    padding: 14px;
    border: 1px solid var(--bde-line);
    background: #fff;
    border-radius: 18px;
    box-shadow: 0 9px 35px rgba(20,27,40,.05);
}

.bde-search-command-form {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 10px;
}

.bde-command-input-wrap {
    position: relative;
    min-width: 0;
}

.bde-command-icon {
    position: absolute;
    top: 50%;
    left: 17px;
    transform: translateY(-50%);
    z-index: 2;
    color: #777f89;
    font-size: 21px;
    pointer-events: none;
}

.bde-command-input,
.bde-library-search-form input[type="search"] {
    width: 100%;
    min-height: 58px;
    padding: 0 18px 0 48px;
    border: 1px solid #dfe4ea;
    border-radius: 13px;
    background: #f8f9fb;
    color: var(--bde-ink);
    font-size: 18px;
    font-weight: 560;
    outline: none;
}

.bde-command-input:focus,
.bde-library-search-form input[type="search"]:focus {
    background: #fff;
    border-color: rgba(23,105,255,.56);
    box-shadow: 0 0 0 4px rgba(23,105,255,.08);
}

.bde-command-button {
    min-width: 120px;
    min-height: 58px;
    border-radius: 13px;
    font-size: 16px;
}

.bde-command-suggestions {
    top: calc(100% + 8px) !important;
    border-radius: 14px;
    border: 1px solid var(--bde-line);
    box-shadow: 0 20px 55px rgba(15,22,34,.14);
}

.bde-search-examples {
    display: flex;
    align-items: center;
    gap: 7px;
    flex-wrap: wrap;
    padding: 10px 4px 0;
}

.bde-search-examples > span {
    margin-right: 2px;
    color: #9399a2;
    font-size: 11px;
    font-weight: 750;
    text-transform: uppercase;
    letter-spacing: .08em;
}

.bde-search-examples a {
    padding: 6px 9px;
    border-radius: 9px;
    background: var(--bde-soft);
    color: #535963;
    text-decoration: none;
    font-size: 12px;
    font-weight: 650;
}

.bde-search-examples a:hover {
    background: var(--bde-blue-soft);
    color: var(--bde-blue);
}

.bde-filter-panel {
    margin-bottom: 28px;
    border: 1px solid var(--bde-line);
    border-radius: 15px;
    background: #fff;
    overflow: hidden;
}

.bde-filter-panel summary {
    min-height: 52px;
    padding: 0 16px;
    display: flex;
    align-items: center;
    gap: 10px;
    cursor: pointer;
    list-style: none;
}

.bde-filter-panel summary::-webkit-details-marker {
    display: none;
}

.bde-filter-panel summary span {
    font-weight: 740;
}

.bde-filter-panel summary small {
    color: #8a909a;
}

.bde-filter-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr)) auto;
    gap: 10px;
    padding: 14px 16px 16px;
    border-top: 1px solid var(--bde-line);
}

.bde-filter-grid label {
    display: grid;
    gap: 6px;
}

.bde-filter-grid label > span {
    color: #7d838c;
    font-size: 10px;
    font-weight: 800;
    letter-spacing: .09em;
    text-transform: uppercase;
}

.bde-filter-grid select,
.bde-library-search-form select {
    min-height: 44px;
    padding: 0 11px;
    border: 1px solid var(--bde-line);
    border-radius: 10px;
    background: #fff;
    font-size: 13px;
}

.bde-results-head,
.bde-library-pane-head {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 20px;
}

.bde-results-head {
    margin-bottom: 13px;
}

.bde-results-head h2 {
    margin: 2px 0 0;
    font-size: 23px;
    letter-spacing: -.03em;
}

.bde-service-list {
    display: grid;
    gap: 9px;
}

.bde-service-row {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(180px, 260px) auto;
    gap: 24px;
    align-items: center;
    padding: 20px 20px 20px 22px;
    border: 1px solid var(--bde-line);
    border-radius: 16px;
    background: #fff;
    transition: border .15s ease, box-shadow .15s ease, transform .15s ease;
}

.bde-service-row:hover {
    border-color: #cfd6df;
    box-shadow: 0 12px 30px rgba(18,27,42,.055);
    transform: translateY(-1px);
}

.bde-service-row-topline {
    display: flex;
    gap: 7px;
    align-items: center;
    flex-wrap: wrap;
    margin-bottom: 7px;
}

.bde-service-code {
    color: #969ca5;
    font-size: 10px;
    font-weight: 800;
    letter-spacing: .08em;
}

.bde-pill,
.bde-preview-tags span {
    padding: 4px 7px;
    border-radius: 999px;
    background: #f2f4f7;
    color: #5c626c;
    font-size: 10px;
    font-weight: 750;
}

.bde-pitch-state {
    padding: 4px 7px;
    border-radius: 999px;
    font-size: 10px;
    font-weight: 800;
}

.bde-pitch-state.active,
.bde-pitch-state.pitchable,
.bde-pitch-state.open {
    background: var(--bde-green-soft);
    color: var(--bde-green);
}

.bde-pitch-state.scheduled,
.bde-pitch-state.upcoming,
.bde-pitch-state.warning {
    background: var(--bde-amber-soft);
    color: var(--bde-amber);
}

.bde-pitch-state.closed,
.bde-pitch-state.expired,
.bde-pitch-state.inactive {
    background: var(--bde-red-soft);
    color: var(--bde-red);
}

.bde-service-row-title {
    display: inline-block;
    color: var(--bde-ink);
    text-decoration: none;
    font-size: 20px;
    line-height: 1.18;
    font-weight: 790;
    letter-spacing: -.025em;
}

.bde-service-row-title:hover {
    color: var(--bde-blue);
}

.bde-service-row-path {
    margin-top: 5px;
    color: #8a9099;
    font-size: 12px;
    font-weight: 560;
}

.bde-service-row-main p {
    margin: 9px 0 0;
    max-width: 760px;
    color: #626873;
    font-size: 13px;
    line-height: 1.5;
}

.bde-service-row-facts {
    display: grid;
    gap: 9px;
}

.bde-service-row-facts > div {
    display: grid;
    gap: 2px;
}

.bde-service-row-facts span,
.bde-side-fact span,
.bde-glance-card > span,
.bde-preview-fact-grid span {
    color: #9298a1;
    font-size: 9px;
    font-weight: 820;
    letter-spacing: .09em;
    text-transform: uppercase;
}

.bde-service-row-facts strong {
    color: #30343a;
    font-size: 12px;
    line-height: 1.35;
}

.bde-service-row-actions {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 7px;
    flex-wrap: wrap;
}

.bde-service-row-actions .primary-button,
.bde-service-row-actions .secondary-button,
.bde-external-button {
    min-height: 40px;
    padding: 0 12px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 10px;
    white-space: nowrap;
    font-size: 12px;
    font-weight: 740;
    text-decoration: none;
}

.bde-external-button {
    border: 1px solid rgba(23,105,255,.25);
    background: var(--bde-blue-soft);
    color: var(--bde-blue);
}

.bde-external-button:hover {
    background: var(--bde-blue);
    color: #fff;
}

.bde-save-icon {
    width: 40px;
    height: 40px;
    display: grid;
    place-items: center;
    border: 1px solid var(--bde-line);
    border-radius: 10px;
    background: #fff;
    color: #7b818a;
    font-size: 18px;
    cursor: pointer;
}

.bde-save-icon.saved {
    background: #fff8df;
    border-color: #f1d37d;
    color: #9b6b00;
}

.bde-empty-state {
    padding: 54px 30px;
    border: 1px dashed #cfd5dd;
    border-radius: 18px;
    background: #fff;
    text-align: center;
}

.bde-empty-state > span {
    display: block;
    margin-bottom: 10px;
    color: #9298a1;
    font-size: 34px;
}

.bde-empty-state h3 {
    margin: 0 0 6px;
    font-size: 23px;
}

.bde-empty-state p {
    margin: 0 auto 16px;
    max-width: 520px;
    color: var(--bde-muted);
}

.bde-empty-state.compact {
    margin: 14px;
    padding: 30px 16px;
}

/* =========================================================
   SERVICE LIBRARY
========================================================= */

.bde-library-hero {
    align-items: center;
}

.bde-library-total {
    min-width: 154px;
    padding: 16px 18px;
    border: 1px solid var(--bde-line);
    border-radius: 16px;
    background: #fff;
    text-align: right;
}

.bde-library-total strong {
    display: block;
    font-size: 32px;
    letter-spacing: -.05em;
}

.bde-library-total span {
    color: #8a9099;
    font-size: 11px;
}

.bde-library-searchbar {
    margin-bottom: 12px;
    padding: 10px;
    border: 1px solid var(--bde-line);
    border-radius: 15px;
    background: #fff;
}

.bde-library-search-form {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 200px auto;
    gap: 9px;
}

.bde-library-search-form input[type="search"] {
    min-height: 48px;
    padding-left: 46px;
    font-size: 15px;
}

.bde-library-search-form select {
    min-height: 48px;
}

.bde-library-grid {
    height: calc(100vh - 265px);
    min-height: 600px;
    display: grid;
    grid-template-columns: 250px minmax(360px, .86fr) minmax(360px, 1.14fr);
    border: 1px solid var(--bde-line);
    border-radius: 18px;
    background: #fff;
    overflow: hidden;
}

.bde-library-categories,
.bde-library-results,
.bde-library-preview {
    min-width: 0;
    min-height: 0;
}

.bde-library-categories {
    display: flex;
    flex-direction: column;
    border-right: 1px solid var(--bde-line);
    background: #f9fafb;
}

.bde-library-results {
    display: flex;
    flex-direction: column;
    border-right: 1px solid var(--bde-line);
}

.bde-library-preview {
    padding: 26px;
    overflow-y: auto;
    background: #fff;
}

.bde-library-pane-head {
    min-height: 58px;
    padding: 0 16px;
    border-bottom: 1px solid var(--bde-line);
}

.bde-library-pane-head span,
.bde-preview-kicker {
    color: #8d939c;
    font-size: 9px;
    font-weight: 820;
    letter-spacing: .11em;
}

.bde-library-pane-head strong {
    font-size: 12px;
}

.bde-category-scroll,
.bde-library-list {
    overflow-y: auto;
}

.bde-category-scroll {
    flex: 1;
    padding: 4px 8px 12px;
}

.bde-category-group-label {
    padding: 14px 9px 5px;
    color: #999fa8;
    font-size: 9px;
    font-weight: 820;
    letter-spacing: .09em;
    text-transform: uppercase;
}

.bde-category-link {
    min-height: 42px;
    margin: 3px 8px;
    padding: 0 10px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    border-radius: 9px;
    color: #535963;
    text-decoration: none;
    font-size: 12px;
    font-weight: 650;
}

.bde-category-link strong {
    color: #999fa8;
    font-size: 10px;
}

.bde-category-link:hover {
    background: #eef1f5;
    color: var(--bde-ink);
}

.bde-category-link.active {
    background: #111318;
    color: #fff;
}

.bde-category-link.active strong {
    color: rgba(255,255,255,.6);
}

.bde-library-list {
    flex: 1;
}

.bde-library-row {
    width: 100%;
    min-height: 92px;
    padding: 15px 16px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    border: 0;
    border-bottom: 1px solid #edf0f3;
    background: #fff;
    color: inherit;
    text-align: left;
    cursor: pointer;
}

.bde-library-row:hover {
    background: #f8faff;
}

.bde-library-row.active {
    background: var(--bde-blue-soft);
    box-shadow: inset 3px 0 0 var(--bde-blue);
}

.bde-library-row > div:first-child {
    min-width: 0;
    display: grid;
    gap: 4px;
}

.bde-library-row > div:first-child strong {
    overflow: hidden;
    color: #20242a;
    font-size: 14px;
    line-height: 1.25;
    text-overflow: ellipsis;
}

.bde-library-row > div:first-child small {
    overflow: hidden;
    color: #8a9099;
    font-size: 10px;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.bde-library-row-side {
    min-width: 94px;
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 9px;
    color: #8a9099;
    font-size: 10px;
}

.bde-library-row-side i {
    color: #adb2b9;
    font-size: 20px;
    font-style: normal;
}

.bde-preview-code {
    margin-top: 12px;
    color: #9aa0a8;
    font-size: 10px;
    font-weight: 800;
    letter-spacing: .09em;
}

.bde-library-preview h2 {
    margin: 6px 0 10px;
    color: var(--bde-ink);
    font-size: clamp(26px, 2.5vw, 36px);
    line-height: 1.02;
    letter-spacing: -.04em;
}

.bde-preview-tags {
    display: flex;
    gap: 6px;
    flex-wrap: wrap;
}

.bde-preview-fact-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 9px;
    margin: 22px 0;
}

.bde-preview-fact-grid > div {
    min-height: 88px;
    padding: 14px;
    display: grid;
    align-content: space-between;
    border-radius: 12px;
    background: #f5f7f9;
}

.bde-preview-fact-grid strong {
    margin-top: 8px;
    font-size: 13px;
    line-height: 1.3;
}

.bde-preview-section {
    padding: 18px 0;
    border-top: 1px solid var(--bde-line);
}

.bde-preview-section > span {
    color: #8a9099;
    font-size: 9px;
    font-weight: 820;
    letter-spacing: .1em;
}

.bde-preview-section p {
    margin: 8px 0 0;
    color: #4e545d;
    font-size: 14px;
    line-height: 1.55;
    white-space: pre-line;
}

.bde-preview-actions {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    margin-top: 18px;
}

.bde-preview-empty {
    height: 100%;
    display: grid;
    place-content: center;
    text-align: center;
    color: #8a9099;
}

.bde-preview-empty > span {
    font-size: 38px;
}

.bde-preview-empty h3 {
    margin: 8px 0 5px;
    color: #3b4047;
}

/* =========================================================
   SERVICE DETAIL
========================================================= */

.bde-detail-topbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    margin-bottom: 24px;
}

.bde-back-link {
    color: #676d77;
    text-decoration: none;
    font-size: 13px;
    font-weight: 680;
}

.bde-detail-actions {
    display: flex;
    gap: 8px;
    align-items: center;
}

.bde-save-text.saved {
    background: #fff8df;
    border-color: #f1d37d;
    color: #8d6200;
}

.bde-detail-hero {
    max-width: 1120px;
    margin-bottom: 26px;
}

.bde-detail-id-row {
    display: flex;
    align-items: center;
    gap: 7px;
    margin-bottom: 13px;
    flex-wrap: wrap;
}

.bde-detail-id-row > span:first-child {
    color: #90969f;
    font-size: 10px;
    font-weight: 820;
    letter-spacing: .1em;
}

.bde-detail-hero h1 {
    font-size: clamp(40px, 5vw, 68px);
}

.bde-detail-breadcrumb {
    display: flex;
    gap: 7px;
    flex-wrap: wrap;
    margin-top: 12px;
    color: #898f98;
    font-size: 12px;
    font-weight: 600;
}

.bde-detail-lede {
    max-width: 900px;
    margin: 20px 0 0;
    color: #4e545d;
    font-size: 19px;
    line-height: 1.55;
}

.bde-at-glance {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 10px;
    margin: 28px 0 22px;
}

.bde-glance-card {
    min-height: 150px;
    padding: 17px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    border: 1px solid var(--bde-line);
    border-radius: 15px;
    background: #fff;
}

.bde-glance-card strong {
    margin-top: 22px;
    color: #20242a;
    font-size: 16px;
    line-height: 1.36;
    letter-spacing: -.015em;
}

.bde-detail-layout {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 330px;
    gap: 18px;
    align-items: start;
}

.bde-detail-main {
    display: grid;
    gap: 12px;
}

.bde-detail-section {
    padding: 26px 28px;
    border: 1px solid var(--bde-line);
    border-radius: 18px;
    background: #fff;
}

.bde-detail-highlight {
    background: #111318;
    border-color: #111318;
    color: #fff;
}

.bde-section-heading {
    display: flex;
    align-items: flex-start;
    gap: 13px;
    margin-bottom: 20px;
}

.bde-section-heading > span {
    width: 34px;
    height: 34px;
    flex: 0 0 auto;
    display: grid;
    place-items: center;
    border-radius: 9px;
    background: #f1f4f7;
    color: #79808a;
    font-size: 11px;
    font-weight: 800;
}

.bde-detail-highlight .bde-section-heading > span {
    background: rgba(255,255,255,.1);
    color: rgba(255,255,255,.7);
}

.bde-section-heading small {
    display: block;
    margin-bottom: 3px;
    color: #9298a1;
    font-size: 9px;
    font-weight: 820;
    letter-spacing: .11em;
}

.bde-section-heading h2 {
    margin: 0;
    color: var(--bde-ink);
    font-size: 22px;
    line-height: 1.1;
    letter-spacing: -.025em;
}

.bde-detail-highlight .bde-section-heading h2 {
    color: #fff;
}

.bde-readable-copy {
    color: #454b54;
    font-size: 17px;
    line-height: 1.72;
}

.bde-detail-highlight .bde-readable-copy {
    color: rgba(255,255,255,.8);
    font-size: 19px;
    line-height: 1.65;
}

.bde-readable-copy p {
    margin: 0 0 12px;
}

.bde-eligibility-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 9px;
    margin-top: 18px;
}

.bde-eligibility-grid > div {
    min-height: 96px;
    padding: 14px;
    display: grid;
    align-content: start;
    gap: 7px;
    border-radius: 12px;
    background: #f5f7f9;
}

.bde-eligibility-grid span {
    color: #8c929b;
    font-size: 9px;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: .08em;
}

.bde-eligibility-grid strong {
    font-size: 13px;
    line-height: 1.45;
}

.bde-rule-list,
.bde-checklist,
.bde-process-list {
    display: grid;
    gap: 8px;
    margin-top: 18px;
}

.bde-rule-item,
.bde-checklist > div {
    padding: 13px 14px;
    display: flex;
    align-items: flex-start;
    gap: 10px;
    border: 1px solid #edf0f3;
    border-radius: 11px;
}

.bde-rule-item {
    display: grid;
}

.bde-rule-item strong,
.bde-checklist strong {
    font-size: 13px;
}

.bde-rule-item span,
.bde-checklist small {
    color: #6f7580;
    font-size: 12px;
    line-height: 1.45;
}

.bde-checklist > div > span {
    color: var(--bde-green);
    font-weight: 800;
}

.bde-process-item {
    display: grid;
    grid-template-columns: 42px 1fr;
    gap: 12px;
}

.bde-process-item > span {
    width: 38px;
    height: 38px;
    display: grid;
    place-items: center;
    border-radius: 11px;
    background: var(--bde-blue-soft);
    color: var(--bde-blue);
    font-weight: 800;
}

.bde-process-item > div {
    padding: 7px 0 15px;
    border-bottom: 1px solid #edf0f3;
}

.bde-process-item strong {
    font-size: 14px;
}

.bde-process-item p {
    margin: 5px 0 0;
    color: #707681;
    font-size: 12px;
    line-height: 1.5;
}

.bde-call-script {
    border-color: rgba(23,105,255,.22);
    background: #f6f9ff;
}

.bde-not-recorded {
    color: #9298a1;
    font-size: 13px;
    font-style: italic;
}

.bde-detail-aside {
    position: sticky;
    top: 88px;
    display: grid;
    gap: 10px;
}

.bde-call-card {
    padding: 18px;
    border: 1px solid var(--bde-line);
    border-radius: 15px;
    background: #fff;
}

.bde-side-fact {
    display: grid;
    gap: 5px;
    padding: 13px 0;
    border-bottom: 1px solid #edf0f3;
}

.bde-side-fact:last-child {
    border-bottom: 0;
    padding-bottom: 0;
}

.bde-side-fact strong {
    font-size: 13px;
    line-height: 1.35;
}

.bde-source-list {
    display: grid;
    gap: 7px;
    margin-top: 12px;
}

.bde-source-list a {
    padding: 11px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 8px;
    border-radius: 10px;
    background: #f5f7f9;
    color: var(--bde-ink);
    text-decoration: none;
}

.bde-source-list a:hover {
    background: var(--bde-blue-soft);
    color: var(--bde-blue);
}

.bde-source-list a > div {
    display: grid;
    gap: 2px;
}

.bde-source-list strong {
    font-size: 12px;
}

.bde-source-list span {
    max-width: 210px;
    overflow: hidden;
    color: #8a9099;
    font-size: 10px;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.bde-sticky-actions {
    position: fixed;
    z-index: 30;
    left: calc(var(--sidebar-width) + 36px);
    right: 36px;
    bottom: 18px;
    min-height: 66px;
    padding: 10px 12px 10px 16px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    border: 1px solid rgba(17,19,24,.13);
    border-radius: 17px;
    background: rgba(17,19,24,.94);
    box-shadow: 0 20px 50px rgba(10,15,22,.24);
    backdrop-filter: blur(16px);
    color: #fff;
}

.bde-sticky-actions > div:first-child {
    min-width: 0;
    display: grid;
}

.bde-sticky-actions > div:first-child strong {
    overflow: hidden;
    font-size: 13px;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.bde-sticky-actions > div:first-child span {
    color: rgba(255,255,255,.52);
    font-size: 10px;
}

.bde-sticky-actions > div:last-child {
    display: flex;
    gap: 8px;
}

/* =========================================================
   QUICK DRAWER
========================================================= */

.bde-quick-overlay {
    position: fixed;
    inset: 0;
    z-index: 90;
    background: rgba(8,11,16,.25);
    backdrop-filter: blur(2px);
}

.bde-quick-drawer {
    position: fixed;
    z-index: 91;
    top: 0;
    right: 0;
    width: min(510px, 92vw);
    height: 100vh;
    display: flex;
    flex-direction: column;
    background: #fff;
    box-shadow: -24px 0 70px rgba(11,18,29,.2);
}

.bde-quick-drawer-header {
    min-height: 74px;
    padding: 0 20px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    border-bottom: 1px solid var(--bde-line);
}

.bde-quick-drawer-header > div {
    display: grid;
}

.bde-quick-drawer-header span {
    color: var(--bde-blue);
    font-size: 9px;
    font-weight: 820;
    letter-spacing: .11em;
}

.bde-quick-drawer-header strong {
    font-size: 14px;
}

.bde-quick-drawer-header button {
    width: 38px;
    height: 38px;
    border: 0;
    border-radius: 10px;
    background: #f2f4f7;
    font-size: 24px;
    cursor: pointer;
}

.bde-quick-drawer-content {
    flex: 1;
    overflow-y: auto;
    padding: 24px;
}

.bde-quick-loading {
    color: #8a9099;
    padding: 30px 0;
}

.bde-quick-title {
    margin: 6px 0 8px;
    font-size: 31px;
    line-height: 1.02;
    letter-spacing: -.04em;
}

.bde-quick-summary {
    margin: 14px 0;
    color: #555b65;
    font-size: 14px;
    line-height: 1.58;
    white-space: pre-line;
}

.bde-quick-block {
    padding: 16px 0;
    border-top: 1px solid var(--bde-line);
}

.bde-quick-block > span {
    display: block;
    margin-bottom: 7px;
    color: #8a9099;
    font-size: 9px;
    font-weight: 820;
    letter-spacing: .1em;
}

.bde-quick-block p {
    margin: 0;
    color: #484e57;
    font-size: 13px;
    line-height: 1.55;
    white-space: pre-line;
}

.bde-quick-links {
    display: grid;
    gap: 7px;
    margin-top: 8px;
}

.bde-quick-links a {
    min-height: 44px;
    padding: 0 12px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    border: 1px solid var(--bde-line);
    border-radius: 10px;
    color: #333840;
    text-decoration: none;
    font-size: 12px;
    font-weight: 680;
}

.bde-quick-links a:hover {
    border-color: rgba(23,105,255,.32);
    background: var(--bde-blue-soft);
    color: var(--bde-blue);
}

.bde-quick-footer {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    padding-top: 18px;
}

/* =========================================================
   CLIENT FINDER — CALL-FIRST OVERRIDES
========================================================= */

.client-matcher-page .page-title {
    max-width: 850px;
    font-size: clamp(34px, 4vw, 52px);
    line-height: 1;
    letter-spacing: -.045em;
}

.client-matcher-page .matcher-v2-description {
    max-width: 720px;
    font-size: 16px;
}

.client-matcher-page .matcher-v2-guide {
    max-width: 940px;
    margin-bottom: 18px;
}

.client-matcher-page .matcher-v2-card {
    padding: 24px;
    border-radius: 18px;
    box-shadow: none;
}

.client-matcher-page .matcher-v2-step {
    gap: 18px;
}

.client-matcher-page .matcher-v2-step-content h2 {
    font-size: 24px;
    letter-spacing: -.03em;
}

.client-matcher-page .matcher-v2-main-search input,
.client-matcher-page select,
.client-matcher-page input {
    min-height: 48px;
}

.client-matcher-page .matcher-v2-basic-grid {
    gap: 10px;
}

.client-matcher-page .matcher-v2-action {
    padding-top: 18px;
}

.client-matcher-page .matcher-results-grid {
    grid-template-columns: 1fr !important;
    gap: 9px;
}

.client-matcher-page .matcher-result-card {
    padding: 19px 20px;
    border-radius: 15px;
    box-shadow: none;
}

.client-matcher-page .matcher-service-title {
    font-size: 20px;
    line-height: 1.2;
    letter-spacing: -.02em;
}

.client-matcher-page .matcher-summary-copy {
    max-width: 820px;
    font-size: 13px;
}

.client-matcher-page .matcher-call-section {
    padding: 11px 12px;
}

.client-matcher-page .matcher-card-actions {
    gap: 7px;
}

.client-matcher-page .matcher-summary-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 9px;
}

.client-matcher-page .matcher-summary-card {
    min-height: 110px;
    padding: 15px;
}

.client-matcher-page .search-kpi-value {
    font-size: 30px;
}

/* =========================================================
   SHARED EXISTING PAGE TYPOGRAPHY
========================================================= */

.bde-shell .page-title {
    font-size: clamp(34px, 4vw, 52px);
    line-height: 1;
    letter-spacing: -.045em;
}

.bde-shell .page-description {
    max-width: 760px;
    font-size: 16px;
    line-height: 1.55;
}

.bde-shell .card {
    border-radius: 16px;
    box-shadow: none;
}

/* =========================================================
   RESPONSIVE
========================================================= */

@media (max-width: 1180px) {
    :root {
        --sidebar-width: 230px;
    }

    .bde-library-grid {
        grid-template-columns: 210px minmax(330px, 1fr);
    }

    .bde-library-preview {
        display: none;
    }

    .bde-detail-layout {
        grid-template-columns: 1fr;
    }

    .bde-detail-aside {
        position: static;
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .bde-at-glance {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .bde-service-row {
        grid-template-columns: minmax(0, 1fr) auto;
    }

    .bde-service-row-facts {
        display: none;
    }
}

@media (max-width: 860px) {
    :root {
        --sidebar-width: 76px;
    }

    .bde-sidebar {
        padding: 12px 8px;
    }

    .bde-brand .brand-text,
    .bde-navigation .nav-item > span:last-child,
    .bde-navigation .nav-section-label,
    .bde-sidebar-hint,
    .sidebar-logout-button > span:last-child {
        display: none;
    }

    .bde-brand {
        padding: 8px 0 14px;
        justify-content: center;
    }

    .bde-navigation .nav-item {
        justify-content: center;
        padding: 0;
    }

    .bde-main-area {
        margin-left: var(--sidebar-width);
    }

    .bde-topbar {
        padding: 0 16px;
    }

    .bde-global-search {
        width: min(520px, 72vw);
    }

    .bde-content {
        padding: 24px 18px 90px;
    }

    .bde-page-hero {
        align-items: flex-start;
        flex-direction: column;
    }

    .bde-filter-grid {
        grid-template-columns: 1fr 1fr;
    }

    .bde-library-grid {
        grid-template-columns: 170px minmax(0, 1fr);
    }

    .bde-library-search-form {
        grid-template-columns: 1fr;
    }

    .bde-service-row {
        grid-template-columns: 1fr;
        gap: 14px;
    }

    .bde-service-row-actions {
        justify-content: flex-start;
    }

    .bde-sticky-actions {
        left: calc(var(--sidebar-width) + 18px);
        right: 18px;
    }
}

@media (max-width: 620px) {
    .bde-topbar-actions,
    .security-status,
    .user-info,
    .search-shortcut {
        display: none !important;
    }

    .bde-global-search {
        width: 100%;
    }

    .bde-search-command-form {
        grid-template-columns: 1fr;
    }

    .bde-command-button {
        width: 100%;
    }

    .bde-filter-grid {
        grid-template-columns: 1fr;
    }

    .bde-library-grid {
        display: block;
        height: auto;
        min-height: 0;
    }

    .bde-library-categories {
        max-height: 280px;
        border-right: 0;
        border-bottom: 1px solid var(--bde-line);
    }

    .bde-library-results {
        max-height: 620px;
        border-right: 0;
    }

    .bde-at-glance,
    .bde-eligibility-grid,
    .bde-detail-aside {
        grid-template-columns: 1fr;
    }

    .bde-sticky-actions {
        left: calc(var(--sidebar-width) + 10px);
        right: 10px;
    }

    .bde-sticky-actions > div:first-child {
        display: none;
    }

    .bde-sticky-actions > div:last-child {
        width: 100%;
        justify-content: flex-end;
    }
}


/* =========================================================
   QUICK VIEW VISIBILITY FIX
   The drawer uses the HTML hidden attribute as its state.
========================================================= */

.bde-quick-overlay[hidden],
.bde-quick-drawer[hidden] {
    display: none !important;
}


/* =========================================================
   BDE REDESIGN V1.2 — ONE SEARCH + CALM INTERACTION FEEDBACK
========================================================= */

.topbar > .global-search.bde-global-search {
    position: relative !important;
    overflow: visible !important;
    max-width: 860px !important;
    z-index: 80 !important;
}

.bde-global-search .search-shortcut {
    pointer-events: none;
    user-select: none;
}

.bde-global-search[aria-busy="true"] {
    opacity: .78;
}

.bde-global-suggestions {
    position: absolute;
    top: calc(100% + 9px);
    left: 0;
    right: 0;
    z-index: 2500;
    max-height: min(510px, 65vh);
    overflow: auto;
    padding: 7px;
    border: 1px solid #dce1e7;
    border-radius: 14px;
    background: #fff;
    box-shadow: 0 24px 70px rgba(17, 24, 39, .18);
}

.bde-global-suggestions[hidden] {
    display: none !important;
}

.bde-smart-suggestion {
    width: 100%;
    min-height: 66px;
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: center;
    gap: 16px;
    padding: 11px 12px;
    border: 0;
    border-radius: 10px;
    background: transparent;
    color: #16191e;
    text-align: left;
    cursor: pointer;
}

.bde-smart-suggestion:hover,
.bde-smart-suggestion.active {
    background: #f2f6ff;
}

.bde-suggestion-main,
.bde-suggestion-side {
    min-width: 0;
    display: grid;
    gap: 4px;
}

.bde-suggestion-main strong {
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    font-size: 14px;
    font-weight: 760;
}

.bde-suggestion-main small,
.bde-suggestion-side small {
    color: #7d8490;
    font-size: 11px;
}

.bde-suggestion-side {
    justify-items: end;
}

.bde-suggestion-side b {
    color: #1769ff;
    font-size: 11px;
}

.bde-suggestion-state {
    min-height: 62px;
    display: flex;
    align-items: center;
    gap: 9px;
    padding: 10px 12px;
    color: #646b76;
    font-size: 12px;
}

.bde-suggestion-state.empty,
.bde-suggestion-state.warning {
    display: grid;
    gap: 3px;
}

.bde-suggestion-state strong {
    color: #20242a;
}

.bde-suggestion-state.warning strong {
    color: #9a6816;
}

.bde-one-search-strip,
.bde-library-toolbar {
    margin-bottom: 22px;
    padding: 13px 15px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 18px;
    border: 1px solid #e0e4e9;
    border-radius: 14px;
    background: #fff;
}

.bde-one-search-copy,
.bde-library-search-hint {
    min-width: 0;
    display: flex;
    align-items: center;
    gap: 11px;
}

.bde-one-search-icon {
    width: 36px;
    height: 36px;
    flex: 0 0 auto;
    display: grid;
    place-items: center;
    border-radius: 10px;
    background: #eef3ff;
    color: #1769ff;
    font-size: 17px;
}

.bde-one-search-copy > div,
.bde-library-search-hint > div {
    display: grid;
    gap: 2px;
}

.bde-one-search-copy strong,
.bde-library-search-hint strong {
    color: #20242a;
    font-size: 13px;
}

.bde-one-search-copy small,
.bde-library-search-hint small {
    color: #777e88;
    font-size: 11px;
    line-height: 1.45;
}

.bde-one-search-strip kbd,
.bde-library-toolbar kbd {
    padding: 1px 4px;
    border: 1px solid #d8dde4;
    border-radius: 5px;
    background: #f7f8fa;
    color: #535a64;
    font: inherit;
}

.bde-library-filter-form {
    flex: 0 0 auto;
    display: flex;
    align-items: end;
    gap: 9px;
}

.bde-library-filter-form label {
    display: grid;
    gap: 4px;
}

.bde-library-filter-form label > span {
    color: #8a9099;
    font-size: 9px;
    font-weight: 800;
    letter-spacing: .08em;
    text-transform: uppercase;
}

.bde-library-filter-form select {
    min-width: 190px;
    min-height: 42px;
    padding: 0 10px;
    border: 1px solid #dfe3e8;
    border-radius: 10px;
    background: #fff;
    color: #25292f;
}

.bde-clear-library {
    min-height: 42px;
    display: inline-flex;
    align-items: center;
    padding: 0 8px;
    color: #6c737e;
    font-size: 11px;
    font-weight: 700;
    text-decoration: none;
}

.bde-clear-library:hover {
    color: #1769ff;
}

.bde-active-search-line {
    margin: -9px 0 14px;
    display: flex;
    align-items: center;
    gap: 8px;
    color: #747b85;
    font-size: 11px;
}

.bde-active-search-line span {
    color: #969ca4;
    font-weight: 750;
    text-transform: uppercase;
    letter-spacing: .08em;
}

.bde-active-search-line strong {
    color: #20242a;
    font-size: 13px;
}

.bde-active-search-line b {
    padding: 4px 7px;
    border-radius: 999px;
    background: #eef3ff;
    color: #1769ff;
    font-size: 10px;
}

.bde-live-feedback {
    position: fixed;
    top: 82px;
    right: 22px;
    z-index: 5000;
    width: min(330px, calc(100vw - 32px));
    display: grid;
    gap: 8px;
    pointer-events: none;
}

.bde-live-toast {
    min-height: 48px;
    display: grid;
    grid-template-columns: 24px minmax(0, 1fr);
    align-items: center;
    gap: 9px;
    padding: 9px 11px;
    border: 1px solid #dfe4e9;
    border-radius: 12px;
    background: rgba(255,255,255,.97);
    box-shadow: 0 14px 40px rgba(17,24,39,.12);
    color: #333941;
    font-size: 12px;
    font-weight: 650;
    animation: bde-feedback-in .16s ease-out;
}

.bde-live-toast.leaving {
    opacity: 0;
    transform: translateY(-5px);
    transition: .18s ease;
}

.bde-live-toast-icon {
    width: 24px;
    height: 24px;
    display: grid;
    place-items: center;
    border-radius: 8px;
    background: #eef3ff;
    color: #1769ff;
    font-weight: 900;
}

.bde-live-toast.success .bde-live-toast-icon {
    background: #e9f8f1;
    color: #168a5b;
}

.bde-live-toast.warning .bde-live-toast-icon {
    background: #fff4dc;
    color: #a66c11;
}

.bde-live-toast.error .bde-live-toast-icon {
    background: #fff0f0;
    color: #c83a3a;
}

.bde-preview-ready {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    margin-bottom: 9px;
    color: #168a5b;
    font-size: 9px;
    font-weight: 800;
    letter-spacing: .08em;
    text-transform: uppercase;
}

.bde-preview-ready span {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: #168a5b;
}

.bde-skeleton-wrap {
    align-content: start;
}

.bde-mini-spinner {
    width: 15px;
    height: 15px;
    display: inline-block;
    border: 2px solid #dfe5ec;
    border-top-color: #1769ff;
    border-radius: 50%;
    animation: bde-spin .7s linear infinite;
}

.bde-skeleton-line {
    height: 10px;
    margin-top: 10px;
    border-radius: 999px;
    background: linear-gradient(90deg,#edf0f4 25%,#f7f8fa 50%,#edf0f4 75%);
    background-size: 200% 100%;
    animation: bde-shimmer 1.15s infinite linear;
}

.bde-skeleton-line.wide { width: 92%; }
.bde-skeleton-line.short { width: 58%; }

@keyframes bde-spin {
    to { transform: rotate(360deg); }
}

@keyframes bde-shimmer {
    to { background-position: -200% 0; }
}

@keyframes bde-feedback-in {
    from { opacity: 0; transform: translateY(-5px); }
    to { opacity: 1; transform: translateY(0); }
}

@media (max-width: 980px) {
    .bde-one-search-strip,
    .bde-library-toolbar {
        align-items: stretch;
        flex-direction: column;
    }

    .bde-library-filter-form {
        justify-content: space-between;
    }

    .topbar > .global-search.bde-global-search {
        max-width: none !important;
    }
}



/* =========================================================
   BDE SEARCH V1.3
   Contextual page search — top search removed
========================================================= */


/* Keep user/security controls on right after global search removal */

.bde-topbar > .topbar-actions {
    margin-left: auto;
}


/* ---------------------------------------------------------
   PRIMARY PAGE SEARCH
--------------------------------------------------------- */

.bde-primary-search-shell {
    margin-bottom: 24px;
    padding: 16px;

    border:
        1px solid #dde2e8;

    border-radius:
        18px;

    background:
        #ffffff;
}


.bde-page-search {
    position: relative;

    width: 100%;
    min-height: 64px;

    display: flex;
    align-items: center;
    gap: 12px;

    padding:
        0 16px;

    border:
        1px solid #d8dee7;

    border-radius:
        15px;

    background:
        #f9fafc;

    transition:
        border-color .16s ease,
        box-shadow .16s ease,
        background .16s ease;
}


.bde-page-search:focus-within {
    border-color:
        #1769ff;

    background:
        #ffffff;

    box-shadow:
        0 0 0 4px
        rgba(23,105,255,.08);
}


.bde-page-search-icon {
    flex:
        0 0 auto;

    color:
        #1769ff;

    font-size:
        21px;
}


.bde-page-search input[type="search"] {
    width:
        100%;

    min-width:
        0;

    border:
        0 !important;

    outline:
        0 !important;

    box-shadow:
        none !important;

    background:
        transparent !important;

    color:
        #16191e;

    font-size:
        17px;

    font-weight:
        520;
}


.bde-page-search input::placeholder {
    color:
        #8b929d;
}


.bde-page-search-shortcut {
    flex:
        0 0 auto;

    padding:
        6px 9px;

    border:
        1px solid #dce1e7;

    border-radius:
        8px;

    background:
        #ffffff;

    color:
        #676e78;

    font-size:
        11px;

    font-weight:
        750;
}


/* ---------------------------------------------------------
   RECOMMENDATIONS
--------------------------------------------------------- */

.bde-page-suggestions {
    position:
        absolute;

    top:
        calc(100% + 9px);

    left:
        0;

    right:
        0;

    z-index:
        3000;

    max-height:
        min(480px, 60vh);

    overflow:
        auto;

    padding:
        7px;

    border:
        1px solid #dce1e7;

    border-radius:
        14px;

    background:
        #ffffff;

    box-shadow:
        0 24px 70px
        rgba(17,24,39,.16);
}


.bde-page-suggestions[hidden] {
    display:
        none !important;
}


/* ---------------------------------------------------------
   LIBRARY TOOLBAR
--------------------------------------------------------- */

.bde-library-toolbar {
    align-items:
        center;
}


.bde-library-page-search {
    flex:
        1 1 auto;

    min-width:
        360px;
}


.bde-library-filter-form {
    flex:
        0 0 auto;
}


/* Old global search is intentionally no longer used */

.bde-global-search {
    display:
        none !important;
}


@media (max-width: 980px) {

    .bde-library-page-search {
        min-width:
            0;
    }

    .bde-page-search {
        min-height:
            58px;
    }

    .bde-page-search-shortcut {
        display:
            none;
    }
}



====================================================================================================
FILE: static/css/ui_stability.css
LINES: 1405
SHA256: 6dc46b92fecc4baefb4a3e66f1221248583b660e846c23b2e0873943d8ed1047
====================================================================================================

/* =========================================================
   BHARATNXT WAVE
   FINAL UI STABILITY / HUMAN INTERACTION LAYER

   Loaded AFTER app.css.
   Do not duplicate these rules back into app.css.
========================================================= */


/* =========================================================
   GLOBAL STABILITY
========================================================= */

html,
body {
    overflow-x: hidden;
}


.main-area,
.content,
.content > *,
.bn-home-v2,
.bn-home-main-grid,
.bn-home-secondary-grid,
.toolkit-v3-page,
.search-console-page,
.pitch-ops-page,
.bn-dashboard-panel {
    min-width: 0;
}


img,
svg {
    max-width: 100%;
}



/* =========================================================
   TOPBAR
   Authoritative layout:
   SEARCH | ACCOUNT CONTROLS | SIGN OUT
========================================================= */

.topbar {
    position: sticky !important;
    top: 0 !important;

    z-index: 100 !important;

    width: 100% !important;
    height: 64px !important;
    min-height: 64px !important;

    display: flex !important;
    flex-direction: row !important;
    flex-wrap: nowrap !important;

    align-items: center !important;
    justify-content: flex-start !important;

    gap: 14px !important;

    padding: 0 24px !important;

    overflow: visible !important;

    background: rgba(255,255,255,.98) !important;

    border-bottom: 1px solid var(--border) !important;
}



/* =========================================================
   GLOBAL SEARCH
========================================================= */

.topbar > .global-search {
    width: auto !important;

    min-width: 0 !important;
    max-width: 780px !important;

    flex: 1 1 580px !important;

    height: 42px !important;

    display: grid !important;

    grid-template-columns:
        22px
        minmax(0, 1fr)
        auto !important;

    align-items: center !important;

    gap: 9px !important;

    margin: 0 !important;

    padding: 0 13px !important;

    border: 1px solid var(--border) !important;

    border-radius: 7px !important;

    background: #FFFFFF !important;

    overflow: hidden !important;
}


.global-search:focus-within {
    border-color: #9CB9FF !important;

    box-shadow:
        0 0 0 3px
        rgba(23,105,255,.08) !important;
}


.global-search input {
    width: 100% !important;

    min-width: 0 !important;

    height: 40px !important;

    margin: 0 !important;

    padding: 0 !important;

    border: 0 !important;

    outline: 0 !important;

    background: transparent !important;

    color: var(--text) !important;

    font-size: 14px !important;

    font-weight: 400 !important;
}


.global-search input::placeholder {
    color: #8A9098 !important;
}


.global-search .search-shortcut {
    position: static !important;

    margin: 0 !important;

    flex-shrink: 0;

    color: var(--text-secondary) !important;

    font-size: 11px !important;

    white-space: nowrap !important;
}



/* =========================================================
   TOPBAR RIGHT
========================================================= */

.topbar > .topbar-actions {
    min-width: 0 !important;

    display: flex !important;

    flex: 0 1 auto !important;

    align-items: center !important;

    gap: 10px !important;

    margin-left: auto !important;

    white-space: nowrap !important;
}


.topbar > .topbar-logout-form {
    display: block !important;

    flex: 0 0 auto !important;

    margin: 0 !important;

    padding: 0 !important;
}


.topbar-logout-button {
    min-width: 82px !important;
    min-height: 38px !important;

    display: inline-flex !important;

    align-items: center !important;
    justify-content: center !important;

    padding: 0 14px !important;

    border: 1px solid var(--border) !important;

    border-radius: 7px !important;

    background: #FFFFFF !important;

    color: var(--text) !important;

    font-size: 12px !important;

    font-weight: 500 !important;

    white-space: nowrap !important;

    cursor: pointer !important;
}


.topbar-logout-button:hover {
    border-color: #E2B5B5 !important;

    background: #FFF8F8 !important;

    color: var(--danger) !important;
}



/* =========================================================
   ADMIN TOPBAR
   No search, so right controls align naturally right.
========================================================= */

.topbar:not(:has(.global-search))
.topbar-actions {
    margin-left: auto !important;
}



/* =========================================================
   USER PROFILE
========================================================= */

.user-profile {
    min-width: 0;

    display: flex !important;

    align-items: center !important;

    gap: 9px !important;
}


.user-info {
    min-width: 0;
}


.user-name {
    max-width: 150px;

    overflow: hidden;

    text-overflow: ellipsis;

    white-space: nowrap;
}



/* =========================================================
   MEMORABLE / RECOGNISABLE ICONS
========================================================= */

.bn-search-icon {
    width: 20px;
    height: 20px;

    display: inline-flex;

    align-items: center;
    justify-content: center;

    color: #6C7480;
}


.bn-search-icon svg {
    width: 17px;
    height: 17px;

    stroke: currentColor;

    stroke-width: 1.8;

    stroke-linecap: round;
    stroke-linejoin: round;
}


.bn-nav-svg-wrap {
    width: 23px;
    height: 23px;

    display: inline-flex;

    align-items: center;
    justify-content: center;
}


.bn-nav-svg {
    width: 19px;
    height: 19px;

    stroke: currentColor;

    stroke-width: 1.7;

    stroke-linecap: round;
    stroke-linejoin: round;
}



/* =========================================================
   CLICK TARGETS
========================================================= */

.nav-item {
    min-height: 44px !important;
}


.primary-button,
.secondary-button {
    min-height: 40px !important;

    display: inline-flex !important;

    align-items: center !important;
    justify-content: center !important;
}



/* =========================================================
   HOME RECENT ACTIVITY
   Replace tiny arrow appearance with a large clear action.
========================================================= */

.bn-recent-item > a {
    width: auto !important;
    min-width: 70px !important;

    min-height: 36px !important;

    display: inline-flex !important;

    align-items: center !important;
    justify-content: center !important;

    gap: 5px !important;

    padding: 0 13px !important;

    border: 1px solid #D4A514 !important;

    border-radius: 18px !important;

    background: #F6C945 !important;

    color: #17191C !important;

    font-size: 0 !important;

    font-weight: 500 !important;

    text-decoration: none !important;

    cursor: pointer !important;

    transition:
        background .13s ease,
        transform .13s ease,
        border-color .13s ease !important;
}


.bn-recent-item > a::before {
    content: "View";

    font-size: 12px !important;
}


.bn-recent-item > a::after {
    content: "›";

    margin-left: 3px;

    font-size: 18px !important;

    line-height: 1;
}


.bn-recent-item > a:hover {
    background: #F0BE2E !important;

    border-color: #C7970B !important;

    transform: translateY(-1px);
}


.bn-recent-item > a:active {
    transform: translateY(0);
}



/* =========================================================
   PITCH OPERATIONS ROW ACTION
========================================================= */

.pitch-row-arrow {
    min-width: 68px !important;
    min-height: 36px !important;

    display: inline-flex !important;

    align-items: center !important;
    justify-content: center !important;

    gap: 5px !important;

    padding: 0 12px !important;

    border: 1px solid #D4A514 !important;

    border-radius: 18px !important;

    background: #F6C945 !important;

    color: #17191C !important;

    font-size: 0 !important;

    font-weight: 500 !important;

    cursor: pointer !important;
}


.pitch-row-arrow::before {
    content: "View";

    font-size: 11px;
}


.pitch-row-arrow::after {
    content: "›";

    font-size: 17px;
}



/* =========================================================
   FEEDBACK / SUCCESS NOTIFICATIONS
========================================================= */

.bn-feedback-stack {
    position: fixed;

    top: 78px;
    right: 22px;

    z-index: 1000;

    width:
        min(
            380px,
            calc(100vw - 32px)
        );

    display: grid;

    gap: 9px;

    pointer-events: none;
}


.bn-feedback-toast {
    min-height: 64px;

    display: grid;

    grid-template-columns:
        34px
        minmax(0, 1fr)
        30px;

    align-items: center;

    gap: 10px;

    padding: 11px 11px 11px 13px;

    border: 1px solid var(--border);

    border-left: 4px solid var(--blue);

    border-radius: 8px;

    background: #FFFFFF;

    box-shadow:
        0 10px 30px
        rgba(19,24,32,.12);

    pointer-events: auto;

    animation:
        bn-feedback-enter
        .22s ease-out;
}


.bn-feedback-toast.success {
    border-left-color: var(--success);
}


.bn-feedback-toast.warning {
    border-left-color: var(--warning);
}


.bn-feedback-toast.error {
    border-left-color: var(--danger);
}


.bn-feedback-icon {
    width: 30px;
    height: 30px;

    display: flex;

    align-items: center;
    justify-content: center;

    border-radius: 50%;

    background: #EDF4FF;

    color: var(--blue);

    font-size: 14px;

    font-weight: 600;
}


.bn-feedback-toast.success
.bn-feedback-icon {
    background: #EAF7F0;

    color: var(--success);
}


.bn-feedback-toast.warning
.bn-feedback-icon {
    background: #FFF6E4;

    color: var(--warning);
}


.bn-feedback-toast.error
.bn-feedback-icon {
    background: #FFF0F0;

    color: var(--danger);
}


.bn-feedback-copy {
    min-width: 0;
}


.bn-feedback-copy strong,
.bn-feedback-copy span {
    display: block;
}


.bn-feedback-copy strong {
    font-size: 13px;

    font-weight: 600;
}


.bn-feedback-copy span {
    margin-top: 2px;

    color: var(--text-secondary);

    font-size: 12px;

    line-height: 1.4;
}


.bn-feedback-close {
    width: 30px;
    height: 30px;

    display: flex;

    align-items: center;
    justify-content: center;

    padding: 0;

    border: 0;

    border-radius: 50%;

    background: transparent;

    color: var(--text-secondary);

    font-size: 18px;

    cursor: pointer;
}


.bn-feedback-close:hover {
    background: #F1F3F5;

    color: var(--text);
}


.bn-feedback-toast.leaving {
    opacity: 0;

    transform: translateY(-6px);

    transition:
        opacity .18s ease,
        transform .18s ease;
}


@keyframes bn-feedback-enter {

    from {
        opacity: 0;

        transform: translateY(-7px);
    }

    to {
        opacity: 1;

        transform: translateY(0);
    }
}



/* =========================================================
   WORKING / PROCESSING FEEDBACK
========================================================= */

.bn-button-loading {
    cursor: wait !important;

    opacity: .76 !important;
}


.bn-loading-spinner {
    width: 14px;
    height: 14px;

    display: inline-block;

    margin-right: 7px;

    border: 2px solid rgba(255,255,255,.45);

    border-top-color: currentColor;

    border-radius: 50%;

    animation:
        bn-ui-spin
        .7s linear infinite;
}


@keyframes bn-ui-spin {

    to {
        transform: rotate(360deg);
    }
}



/* =========================================================
   KEYBOARD / FOCUS CLARITY
========================================================= */

a:focus-visible,
button:focus-visible,
input:focus-visible,
select:focus-visible,
textarea:focus-visible,
summary:focus-visible {
    outline:
        3px solid rgba(23,105,255,.23) !important;

    outline-offset:
        2px !important;
}



/* =========================================================
   PREVENT CONTENT CRUMBLING
========================================================= */

.toolkit-v3-search-form,
.toolkit-v3-filters,
.bn-home-kpi-strip,
.bn-activity-pie-layout,
.bn-home-main-grid,
.bn-home-secondary-grid {
    min-width: 0 !important;
}


.toolkit-v3-search-form > *,
.toolkit-v3-filters > *,
.bn-home-main-grid > *,
.bn-home-secondary-grid > *,
.bn-activity-pie-layout > * {
    min-width: 0 !important;
}


.toolkit-v3-search-input {
    width: 100% !important;
}


.toolkit-v3-filter select {
    width: 100% !important;

    min-width: 0 !important;
}



/* =========================================================
   RESPONSIVE TOPBAR
========================================================= */

@media (max-width: 1180px) {

    .topbar {
        gap: 10px !important;

        padding:
            0 16px !important;
    }


    .security-status {
        display: none !important;
    }


    .global-search
    .search-shortcut {
        display: none !important;
    }

}


@media (max-width: 920px) {

    .user-info {
        display: none !important;
    }


    .topbar > .global-search {
        min-width: 150px !important;
    }


    .topbar-actions {
        gap: 7px !important;
    }


    .topbar-logout-button {
        min-width: 68px !important;

        padding:
            0 10px !important;
    }

}


@media (max-width: 650px) {

    .notification-button {
        display: none !important;
    }


    .bn-feedback-stack {
        top: 72px;

        right: 10px;

        width:
            calc(100vw - 20px);
    }

}


/* =========================================================
   SIDEBAR SIGN OUT
   Final location for authentication exit action
========================================================= */

.sidebar {
    display: flex !important;

    flex-direction: column !important;
}


.sidebar-account-actions {
    width: 100%;

    margin-top: auto;

    padding-top: 14px;

    border-top: 1px solid var(--border);
}


.sidebar-logout-form {
    width: 100%;

    margin: 0;
}


.sidebar-logout-button {
    width: 100%;

    min-height: 44px;

    display: flex;

    align-items: center;

    gap: 10px;

    padding: 0 12px;

    border: 1px solid transparent;

    border-radius: 7px;

    background: transparent;

    color: var(--text-secondary);

    font-family: inherit;

    font-size: 13px;

    font-weight: 400;

    text-align: left;

    cursor: pointer;

    transition:
        background .14s ease,
        border-color .14s ease,
        color .14s ease;
}


.sidebar-logout-button:hover {
    border-color: #F0D5D5;

    background: #FFF7F7;

    color: var(--danger);
}


.sidebar-logout-button:active {
    background: #FDEEEE;
}


.sidebar-logout-icon {
    width: 24px;
    height: 24px;

    display: inline-flex;

    align-items: center;
    justify-content: center;

    flex-shrink: 0;
}


.sidebar-logout-icon svg {
    width: 18px;
    height: 18px;

    stroke: currentColor;

    stroke-width: 1.8;

    stroke-linecap: round;
    stroke-linejoin: round;
}


/* Old header logout must never appear */

.topbar-logout-form,
.topbar-logout-button {
    display: none !important;
}


/* Collapsed sidebar */

@media (max-width: 900px) {

    .sidebar-account-actions {
        display: flex;

        justify-content: center;
    }


    .sidebar-logout-button {
        width: 44px;

        padding: 0;

        justify-content: center;
    }


    .sidebar-logout-button > span:last-child {
        display: none;
    }

}


/* =========================================================
   SOURCE HISTORY — FILE INTERACTIONS
========================================================= */

.source-file-link {
    display: inline-flex;

    align-items: center;

    color: var(--text);

    font-weight: 600;

    text-decoration: none;

    border-bottom:
        1px solid transparent;

    transition:
        color .14s ease,
        border-color .14s ease;
}


.source-file-link::before {
    content: "";

    width: 15px;
    height: 18px;

    flex-shrink: 0;

    margin-right: 7px;

    border: 1.5px solid #6D7682;

    border-radius: 2px;

    background:
        linear-gradient(
            135deg,
            transparent 0 68%,
            #E9EBEE 68% 100%
        );
}


.source-file-link:hover {
    color: var(--blue);

    border-bottom-color:
        rgba(23,105,255,.35);
}



/* =========================================================
   SOURCE ROW ACTIONS
========================================================= */

.source-delete-form {
    display: inline-flex;

    margin-left: 8px;

    vertical-align: middle;
}


.source-delete-button {
    min-height: 32px;

    display: inline-flex;

    align-items: center;
    justify-content: center;

    gap: 5px;

    padding: 0 9px;

    border:
        1px solid transparent;

    border-radius: 6px;

    background: transparent;

    color: #8A4B4B;

    font-family: inherit;

    font-size: 10px;

    font-weight: 500;

    cursor: pointer;
}


.source-delete-button:hover {
    border-color: #E9C8C8;

    background: #FFF5F5;

    color: var(--danger);
}


.source-delete-button svg {
    width: 14px;
    height: 14px;

    stroke: currentColor;

    stroke-width: 1.6;

    stroke-linecap: round;
    stroke-linejoin: round;
}



/* =========================================================
   SOURCE DETAIL DELETE
========================================================= */

.source-detail-delete-form {
    display: inline-flex;

    margin-left: 8px;
}


.source-detail-delete-button {
    min-height: 40px;

    display: inline-flex;

    align-items: center;
    justify-content: center;

    gap: 7px;

    padding: 0 13px;

    border: 1px solid #E6C5C5;

    border-radius: 7px;

    background: #FFFFFF;

    color: #A33A3A;

    font-family: inherit;

    font-size: 12px;

    font-weight: 500;

    cursor: pointer;
}


.source-detail-delete-button:hover {
    border-color: #D99C9C;

    background: #FFF5F5;

    color: var(--danger);
}


.source-detail-delete-button svg {
    width: 16px;
    height: 16px;

    stroke: currentColor;

    stroke-width: 1.7;

    stroke-linecap: round;
    stroke-linejoin: round;
}



/* =========================================================
   SOURCE HISTORY DETAILS LINK
   Larger click target
========================================================= */

.import-history-table a,
.source-history-table a {
    min-height: 32px;
}



/* =========================================================
   MOBILE
========================================================= */

@media (max-width: 760px) {

    .source-delete-form {
        margin-left: 4px;
    }


    .source-delete-button span {
        display: none;
    }


    .source-delete-button {
        width: 34px;

        padding: 0;
    }

}


/* =========================================================
   BNX_CANONICAL_BOOKMARK_V21
   One Saved Services icon across the entire BDE workspace.
========================================================= */


.bn-bookmark-icon {

    display:
        inline-block;

    width:
        16px;

    height:
        16px;

    flex:
        0 0 16px;

    vertical-align:
        -2px;

    background:
        currentColor;


    -webkit-mask:
        url("data:image/svg+xml,%3Csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2024%2024'%20fill='none'%20stroke='black'%20stroke-width='1.8'%20stroke-linecap='round'%20stroke-linejoin='round'%3E%3Cpath%20d='M7%204.5C7%203.7%207.7%203%208.5%203H15.5C16.3%203%2017%203.7%2017%204.5V20L12%2016.8L7%2020V4.5Z'/%3E%3C/svg%3E")
        center / contain
        no-repeat;


    mask:
        url("data:image/svg+xml,%3Csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2024%2024'%20fill='none'%20stroke='black'%20stroke-width='1.8'%20stroke-linecap='round'%20stroke-linejoin='round'%3E%3Cpath%20d='M7%204.5C7%203.7%207.7%203%208.5%203H15.5C16.3%203%2017%203.7%2017%204.5V20L12%2016.8L7%2020V4.5Z'/%3E%3C/svg%3E")
        center / contain
        no-repeat;

}


/* Filled bookmark = Saved */

.bn-bookmark-icon.is-saved {

    -webkit-mask:
        url("data:image/svg+xml,%3Csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2024%2024'%3E%3Cpath%20fill='black'%20d='M8.5%202.2h7A2.3%202.3%200%200%201%2017.8%204.5V21.4L12%2017.7l-5.8%203.7V4.5A2.3%202.3%200%200%201%208.5%202.2Z'/%3E%3C/svg%3E")
        center / contain
        no-repeat;


    mask:
        url("data:image/svg+xml,%3Csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2024%2024'%3E%3Cpath%20fill='black'%20d='M8.5%202.2h7A2.3%202.3%200%200%201%2017.8%204.5V21.4L12%2017.7l-5.8%203.7V4.5A2.3%202.3%200%200%201%208.5%202.2Z'/%3E%3C/svg%3E")
        center / contain
        no-repeat;

}


/* Sidebar */

.nav-icon
.bn-bookmark-icon {

    width:
        18px;

    height:
        18px;

    flex-basis:
        18px;

}


/* Text save buttons */

.bde-save-text,
.matcher-save-button {

    display:
        inline-flex !important;

    align-items:
        center !important;

    justify-content:
        center;

    gap:
        6px !important;

}


/* Icon-only button */

.bde-save-icon {

    display:
        inline-flex !important;

    align-items:
        center !important;

    justify-content:
        center !important;

}


.bde-save-icon
.bn-bookmark-icon {

    margin:
        0;

}


/* Saved page empty state */

.bde-empty-bookmark {

    display:
        inline-flex;

    align-items:
        center;

    justify-content:
        center;

}


.bde-empty-bookmark
.bn-bookmark-icon {

    width:
        24px;

    height:
        24px;

    flex-basis:
        24px;

}


/* Dashboard quick action */

.bnx-dashboard-quick-actions
.bn-bookmark-icon,
.bn-dashboard-quick-actions
.bn-bookmark-icon {

    width:
        14px;

    height:
        14px;

    flex-basis:
        14px;

    margin-right:
        5px;

}



====================================================================================================
FILE: static/js/bde_workspace.js
LINES: 1103
SHA256: 6ff2ee259cc653627a9e3632c80a9c0250bb94707dd7eb094725846d8523e528
====================================================================================================
(function () {
    "use strict";

    const drawer = document.getElementById("bde-quick-drawer");
    const overlay = document.getElementById("bde-quick-overlay");
    const drawerContent = document.getElementById("bde-quick-drawer-content");
    const closeButton = document.getElementById("bde-quick-close");

    const globalForm = document.querySelector(".bde-global-search");
    const globalInput = globalForm
        ? globalForm.querySelector('input[type="search"][name="q"]')
        : null;
    const globalShortcut = globalForm
        ? globalForm.querySelector(".search-shortcut")
        : null;

    let suggestionTimer = null;
    let suggestionController = null;
    let suggestionActiveIndex = -1;
    let suggestionBox = null;

    function escapeHtml(value) {
        return String(value || "")
            .replaceAll("&", "&amp;")
            .replaceAll("<", "&lt;")
            .replaceAll(">", "&gt;")
            .replaceAll('"', "&quot;")
            .replaceAll("'", "&#039;");
    }

    function safeUrl(value) {
        try {
            const url = new URL(value, window.location.origin);

            if (url.protocol === "http:" || url.protocol === "https:") {
                return url.href;
            }
        }
        catch (error) {
            return "";
        }

        return "";
    }

    function feedbackHost() {
        let host = document.getElementById("bde-live-feedback");

        if (!host) {
            host = document.createElement("div");
            host.id = "bde-live-feedback";
            host.className = "bde-live-feedback";
            host.setAttribute("aria-live", "polite");
            host.setAttribute("aria-atomic", "true");
            document.body.appendChild(host);
        }

        return host;
    }

    function notify(message, type, duration) {
        const host = feedbackHost();
        const toast = document.createElement("div");
        const mode = type || "info";
        const timeout = Number(duration || 2400);

        toast.className = "bde-live-toast " + mode;
        toast.innerHTML = `
            <span class="bde-live-toast-icon" aria-hidden="true">
                ${mode === "success" ? "✓" : mode === "error" ? "!" : mode === "warning" ? "!" : "i"}
            </span>
            <span>${escapeHtml(message)}</span>
        `;

        host.appendChild(toast);

        while (host.children.length > 3) {
            host.firstElementChild.remove();
        }

        window.setTimeout(function () {
            toast.classList.add("leaving");
            window.setTimeout(function () { toast.remove(); }, 180);
        }, timeout);
    }

    function paragraph(value) {
        const text = String(value || "").trim();

        if (!text) {
            return '<p class="bde-not-recorded">Not available in Toolkit.</p>';
        }

        return "<p>" + escapeHtml(text) + "</p>";
    }

    function sourceLinks(items) {
        if (!Array.isArray(items) || !items.length) {
            return '<p class="bde-not-recorded">No external scheme link is recorded.</p>';
        }

        return `
            <div class="bde-quick-links">
                ${items.map(function (item) {
                    const href = safeUrl(item.url);

                    if (!href) {
                        return "";
                    }

                    return `
                        <a href="${escapeHtml(href)}"
                           target="_blank"
                           rel="noopener noreferrer"
                           class="bde-feedback-external">
                            <span>${escapeHtml(item.label || item.name || "External Link")}</span>
                            <b>↗</b>
                        </a>
                    `;
                }).join("")}
            </div>
        `;
    }

    function serviceMarkup(data) {
        const primary = safeUrl(data.primary_url);
        const detail = safeUrl(data.detail_url);

        return `
            <div class="bde-preview-ready"><span></span> Details loaded</div>
            <div class="bde-preview-kicker">QUICK DETAILS</div>
            <div class="bde-preview-code">${escapeHtml(data.service_id)}</div>
            <h2 class="bde-quick-title">${escapeHtml(data.title)}</h2>

            <div class="bde-preview-tags">
                <span>${escapeHtml(data.kind)}</span>
                <span>${escapeHtml(data.category)}</span>
            </div>

            <div class="bde-preview-fact-grid">
                <div>
                    <span>Deadline</span>
                    <strong>${escapeHtml(data.deadline || "Not recorded")}</strong>
                </div>
                <div>
                    <span>Pitch status</span>
                    <strong>${escapeHtml(data.pitch_state || "—")}</strong>
                </div>
            </div>

            ${data.summary ? `
                <div class="bde-quick-block">
                    <span>WHAT IT DOES</span>
                    <div class="bde-quick-summary">${escapeHtml(data.summary)}</div>
                </div>
            ` : ""}

            <div class="bde-quick-block">
                <span>ELIGIBILITY</span>
                ${paragraph(data.eligibility || data.applicable_for)}
            </div>

            <div class="bde-quick-block">
                <span>BENEFITS</span>
                ${paragraph(data.benefits)}
            </div>

            ${data.funding_organisation ? `
                <div class="bde-quick-block">
                    <span>RUN BY</span>
                    <p>${escapeHtml(data.funding_organisation)}</p>
                </div>
            ` : ""}

            <div class="bde-quick-block">
                <span>SCHEME LINKS</span>
                ${sourceLinks(data.sources)}
            </div>

            <div class="bde-quick-footer">
                ${primary ? `
                    <a href="${escapeHtml(primary)}"
                       target="_blank"
                       rel="noopener noreferrer"
                       class="bde-external-button bde-feedback-external">
                        Open Scheme ↗
                    </a>
                ` : ""}

                ${detail ? `
                    <a href="${escapeHtml(detail)}"
                       class="primary-button">
                        Full Details
                    </a>
                ` : ""}
            </div>
        `;
    }

    function loadingMarkup(label) {
        return `
            <div class="bde-quick-loading bde-skeleton-wrap" aria-live="polite">
                <span class="bde-mini-spinner"></span>
                <strong>${escapeHtml(label || "Loading service…")}</strong>
                <div class="bde-skeleton-line wide"></div>
                <div class="bde-skeleton-line"></div>
                <div class="bde-skeleton-line short"></div>
            </div>
        `;
    }

    async function loadService(serviceId) {
        const response = await fetch(
            "/toolkit/library/quick/" + encodeURIComponent(serviceId) + "/",
            {
                credentials: "same-origin",
                headers: { "X-Requested-With": "XMLHttpRequest" }
            }
        );

        if (!response.ok) {
            throw new Error("Quick view request failed");
        }

        return response.json();
    }

    function openDrawer() {
        if (!drawer || !overlay) {
            return;
        }

        drawer.hidden = false;
        overlay.hidden = false;
        document.body.style.overflow = "hidden";
    }

    function closeDrawer() {
        if (!drawer || !overlay) {
            return;
        }

        drawer.hidden = true;
        overlay.hidden = true;
        document.body.style.overflow = "";
    }

    async function showInDrawer(serviceId) {
        if (!drawerContent) {
            return;
        }

        openDrawer();
        drawerContent.innerHTML = loadingMarkup("Loading service details…");

        try {
            const data = await loadService(serviceId);
            drawerContent.innerHTML = serviceMarkup(data);
            notify("Service details ready", "success", 1600);
        }
        catch (error) {
            drawerContent.innerHTML = `
                <div class="bde-empty-state compact">
                    <h3>Could not load this service</h3>
                    <p>Please open the full service page and try again.</p>
                </div>
            `;
            notify("Could not load service details", "error", 4200);
        }
    }

    async function showInLibrary(serviceId, button) {
        const target = document.getElementById("bde-library-preview");

        if (!target) {
            return;
        }

        document.querySelectorAll(".bde-library-row.active").forEach(function (item) {
            item.classList.remove("active");
        });

        if (button) {
            button.classList.add("active");
            button.setAttribute("aria-busy", "true");
        }

        target.innerHTML = loadingMarkup("Loading service details…");

        try {
            const data = await loadService(serviceId);
            target.innerHTML = serviceMarkup(data);
        }
        catch (error) {
            target.innerHTML = `
                <div class="bde-empty-state compact">
                    <h3>Could not load service details</h3>
                    <p>Use the full details page instead.</p>
                </div>
            `;
            notify("Could not load Service Library preview", "error", 4200);
        }
        finally {
            if (button) {
                button.removeAttribute("aria-busy");
            }
        }
    }

    function initGlobalSearch() {
        if (!globalForm || !globalInput) {
            return;
        }

        const libraryMode = window.location.pathname.startsWith("/toolkit/library/");
        globalForm.action = libraryMode ? "/toolkit/library/" : "/toolkit/";
        globalForm.classList.add("bde-smart-search");
        globalInput.id = "bde-global-search-input";
        globalInput.placeholder = libraryMode
            ? "Search all 163 published services…"
            : "Search services, schemes, sectors or client needs…";

        const currentQuery = new URLSearchParams(window.location.search).get("q");
        if (currentQuery !== null) {
            globalInput.value = currentQuery;
        }

        if (globalShortcut) {
            globalShortcut.textContent = "Ctrl K";
            globalShortcut.setAttribute("aria-hidden", "true");
        }

        suggestionBox = document.createElement("div");
        suggestionBox.id = "bde-global-suggestions";
        suggestionBox.className = "bde-global-suggestions";
        suggestionBox.hidden = true;
        globalForm.appendChild(suggestionBox);

        function items() {
            return Array.from(suggestionBox.querySelectorAll(".bde-smart-suggestion"));
        }

        function setActive(index) {
            const list = items();
            list.forEach(function (item) { item.classList.remove("active"); });

            if (!list.length) {
                suggestionActiveIndex = -1;
                return;
            }

            suggestionActiveIndex = Math.max(0, Math.min(index, list.length - 1));
            const active = list[suggestionActiveIndex];
            active.classList.add("active");
            active.scrollIntoView({ block: "nearest" });
        }

        function closeSuggestions() {
            suggestionActiveIndex = -1;
            suggestionBox.hidden = true;
            suggestionBox.innerHTML = "";
        }

        function renderLoading() {
            suggestionBox.innerHTML = `
                <div class="bde-suggestion-state">
                    <span class="bde-mini-spinner"></span>
                    Finding services…
                </div>
            `;
            suggestionBox.hidden = false;
        }

        function renderSuggestions(results) {
            suggestionActiveIndex = -1;

            if (!Array.isArray(results) || !results.length) {
                suggestionBox.innerHTML = `
                    <div class="bde-suggestion-state empty">
                        <strong>No quick recommendation</strong>
                        <span>Press Enter to run the full catalog search.</span>
                    </div>
                `;
                suggestionBox.hidden = false;
                return;
            }

            suggestionBox.innerHTML = results.map(function (item) {
                return `
                    <button type="button"
                            class="bde-smart-suggestion"
                            data-service-id="${Number(item.pk) || 0}"
                            data-service-title="${escapeHtml(item.title)}">
                        <span class="bde-suggestion-main">
                            <strong>${escapeHtml(item.title)}</strong>
                            <small>${escapeHtml(item.kind)} · ${escapeHtml(item.category)}</small>
                        </span>
                        <span class="bde-suggestion-side">
                            <small>${escapeHtml(item.deadline || "")}</small>
                            <b>Quick View</b>
                        </span>
                    </button>
                `;
            }).join("");
            suggestionBox.hidden = false;
        }

        async function requestSuggestions(query) {
            if (suggestionController) {
                suggestionController.abort();
            }

            suggestionController = new AbortController();
            renderLoading();

            try {
                const response = await fetch(
                    "/toolkit/search/suggestions/?q=" + encodeURIComponent(query),
                    {
                        credentials: "same-origin",
                        signal: suggestionController.signal,
                        headers: { "X-Requested-With": "XMLHttpRequest" }
                    }
                );

                if (!response.ok) {
                    throw new Error("Suggestion request failed");
                }

                const data = await response.json();
                renderSuggestions(data.results || []);
            }
            catch (error) {
                if (error.name === "AbortError") {
                    return;
                }

                suggestionBox.innerHTML = `
                    <div class="bde-suggestion-state warning">
                        <strong>Recommendations unavailable</strong>
                        <span>Press Enter — full search still works.</span>
                    </div>
                `;
                suggestionBox.hidden = false;
            }
        }

        globalInput.addEventListener("input", function () {
            window.clearTimeout(suggestionTimer);
            const query = globalInput.value.trim();

            if (query.length < 2) {
                closeSuggestions();
                return;
            }

            suggestionTimer = window.setTimeout(function () {
                requestSuggestions(query);
            }, 60);
        });

        globalInput.addEventListener("focus", function () {
            if (globalInput.value.trim().length >= 2 && suggestionBox.innerHTML.trim()) {
                suggestionBox.hidden = false;
            }
        });

        globalInput.addEventListener("keydown", function (event) {
            const list = items();

            if (event.key === "ArrowDown" && list.length) {
                event.preventDefault();
                setActive(suggestionActiveIndex < 0 ? 0 : suggestionActiveIndex + 1);
                return;
            }

            if (event.key === "ArrowUp" && list.length) {
                event.preventDefault();
                setActive(suggestionActiveIndex < 0 ? list.length - 1 : suggestionActiveIndex - 1);
                return;
            }

            if (event.key === "Enter" && suggestionActiveIndex >= 0 && list[suggestionActiveIndex]) {
                event.preventDefault();
                list[suggestionActiveIndex].click();
                return;
            }

            if (event.key === "Escape") {
                closeSuggestions();
            }
        });

        suggestionBox.addEventListener("click", function (event) {
            const item = event.target.closest(".bde-smart-suggestion");

            if (!item) {
                return;
            }

            const serviceId = item.dataset.serviceId;
            const title = item.dataset.serviceTitle || "Service";

            globalInput.value = title;
            closeSuggestions();
            notify("Opening quick view", "info", 1100);
            showInDrawer(serviceId);
        });

        globalForm.addEventListener("submit", function () {
            const query = globalInput.value.trim();
            closeSuggestions();

            if (query) {
                globalForm.setAttribute("aria-busy", "true");
                if (globalShortcut) {
                    globalShortcut.textContent = "Searching…";
                }
            }
        });

        document.addEventListener("click", function (event) {
            if (!globalForm.contains(event.target)) {
                closeSuggestions();
            }
        });
    }


    /* BDE_PAGE_SEARCH_V13 */

    function initPageSearch() {

        const forms = Array.from(
            document.querySelectorAll(
                ".bde-page-search"
            )
        );

        if (!forms.length) {
            return;
        }

        forms.forEach(function (form) {

            const input =
                form.querySelector(
                    'input[type="search"][name="q"]'
                );

            const suggestions =
                form.querySelector(
                    ".bde-page-suggestions"
                );

            if (!input || !suggestions) {
                return;
            }

            let timer = null;
            let controller = null;
            let activeIndex = -1;


            function suggestionItems() {
                return Array.from(
                    suggestions.querySelectorAll(
                        ".bde-smart-suggestion"
                    )
                );
            }


            function closeSuggestions() {
                suggestions.hidden = true;
                suggestions.innerHTML = "";
                activeIndex = -1;
            }


            function setActive(index) {

                const items = suggestionItems();

                items.forEach(function (item) {
                    item.classList.remove("active");
                });

                if (!items.length) {
                    activeIndex = -1;
                    return;
                }

                activeIndex = Math.max(
                    0,
                    Math.min(
                        index,
                        items.length - 1
                    )
                );

                items[activeIndex]
                    .classList.add("active");

                items[activeIndex]
                    .scrollIntoView({
                        block: "nearest"
                    });
            }


            function renderLoading() {

                suggestions.innerHTML = `
                    <div class="bde-suggestion-state">
                        <span class="bde-mini-spinner"></span>
                        Finding the best matching services…
                    </div>
                `;

                suggestions.hidden = false;
            }


            function renderResults(results) {

                activeIndex = -1;

                if (
                    !Array.isArray(results)
                    ||
                    !results.length
                ) {

                    suggestions.innerHTML = `
                        <div class="bde-suggestion-state empty">
                            <strong>No instant recommendation</strong>
                            <span>
                                Press Enter to run a complete search.
                            </span>
                        </div>
                    `;

                    suggestions.hidden = false;
                    return;
                }


                suggestions.innerHTML =
                    results.map(function (item) {

                        return `
                            <button
                                type="button"
                                class="bde-smart-suggestion"
                                data-service-id="${Number(item.pk) || 0}"
                                data-service-title="${escapeHtml(item.title)}">

                                <span class="bde-suggestion-main">

                                    <strong>
                                        ${escapeHtml(item.title)}
                                    </strong>

                                    <small>
                                        ${escapeHtml(item.kind)}
                                        ·
                                        ${escapeHtml(item.category)}
                                    </small>

                                </span>

                                <span class="bde-suggestion-side">

                                    <small>
                                        ${escapeHtml(item.deadline || "")}
                                    </small>

                                    <b>
                                        Quick View →
                                    </b>

                                </span>

                            </button>
                        `;
                    }).join("");

                suggestions.hidden = false;
            }


            async function fetchSuggestions(query) {

                if (controller) {
                    controller.abort();
                }

                controller =
                    new AbortController();

                renderLoading();

                try {

                    const response =
                        await fetch(
                            "/toolkit/search/suggestions/?q="
                            + encodeURIComponent(query),
                            {
                                credentials:
                                    "same-origin",

                                signal:
                                    controller.signal,

                                headers: {
                                    "X-Requested-With":
                                        "XMLHttpRequest"
                                }
                            }
                        );


                    if (!response.ok) {
                        throw new Error(
                            "Suggestion request failed"
                        );
                    }


                    const data =
                        await response.json();

                    renderResults(
                        data.results || []
                    );

                }
                catch (error) {

                    if (
                        error.name ===
                        "AbortError"
                    ) {
                        return;
                    }

                    suggestions.innerHTML = `
                        <div class="bde-suggestion-state warning">
                            <strong>
                                Recommendations unavailable
                            </strong>
                            <span>
                                Press Enter — normal search still works.
                            </span>
                        </div>
                    `;

                    suggestions.hidden = false;
                }
            }


            input.addEventListener(
                "input",
                function () {

                    window.clearTimeout(timer);

                    const query =
                        input.value.trim();

                    if (query.length < 2) {
                        closeSuggestions();
                        return;
                    }

                    timer =
                        window.setTimeout(
                            function () {
                                fetchSuggestions(query);
                            },
                            180
                        );
                }
            );


            input.addEventListener(
                "keydown",
                function (event) {

                    const items =
                        suggestionItems();


                    if (
                        event.key === "ArrowDown"
                        &&
                        items.length
                    ) {

                        event.preventDefault();

                        setActive(
                            activeIndex < 0
                                ? 0
                                : activeIndex + 1
                        );

                        return;
                    }


                    if (
                        event.key === "ArrowUp"
                        &&
                        items.length
                    ) {

                        event.preventDefault();

                        setActive(
                            activeIndex < 0
                                ? items.length - 1
                                : activeIndex - 1
                        );

                        return;
                    }


                    if (
                        event.key === "Enter"
                        &&
                        activeIndex >= 0
                        &&
                        items[activeIndex]
                    ) {

                        event.preventDefault();

                        items[
                            activeIndex
                        ].click();

                        return;
                    }


                    if (
                        event.key === "Escape"
                    ) {

                        closeSuggestions();
                    }
                }
            );


            suggestions.addEventListener(
                "click",
                function (event) {

                    const item =
                        event.target.closest(
                            ".bde-smart-suggestion"
                        );

                    if (!item) {
                        return;
                    }


                    const serviceId =
                        item.dataset.serviceId;

                    const title =
                        item.dataset.serviceTitle
                        || "Service";


                    input.value =
                        title;

                    closeSuggestions();


                    if (
                        form.dataset.searchContext
                        === "library"
                    ) {

                        notify(
                            "Opening service preview…",
                            "info",
                            1200
                        );

                        showInLibrary(
                            serviceId,
                            null
                        );
                    }

                    else {

                        notify(
                            "Opening quick view…",
                            "info",
                            1200
                        );

                        showInDrawer(
                            serviceId
                        );
                    }
                }
            );


            document.addEventListener(
                "click",
                function (event) {

                    if (
                        !form.contains(
                            event.target
                        )
                    ) {
                        closeSuggestions();
                    }
                }
            );

        });
    }


    initGlobalSearch();
    initPageSearch();

    document.addEventListener("click", function (event) {
        const trigger = event.target.closest("[data-service-quick]");

        if (trigger) {
            event.preventDefault();
            const serviceId = trigger.dataset.serviceQuick;

            if (!serviceId) {
                return;
            }

            if (trigger.dataset.quickMode === "library") {
                showInLibrary(serviceId, trigger);
            }
            else {
                showInDrawer(serviceId);
            }
            return;
        }

        const external = event.target.closest('a[target="_blank"]');
        if (external && external.href) {
            notify("Opening scheme in a new tab", "info", 1800);
        }
    });

    if (closeButton) {
        closeButton.addEventListener("click", closeDrawer);
    }

    if (overlay) {
        overlay.addEventListener("click", closeDrawer);
    }

    document.addEventListener("keydown", function (event) {
        if (event.key === "Escape" && drawer && !drawer.hidden) {
            closeDrawer();
        }

        if ((event.ctrlKey || event.metaKey) && event.key.toLowerCase() === "k") {

            event.preventDefault();

            const pageInput =
                document.querySelector(
                    '.bde-page-search input[type="search"][name="q"]'
                );

            if (pageInput) {

                pageInput.focus();
                pageInput.select();

                notify(
                    "Search ready",
                    "info",
                    900
                );
            }

            else if (globalInput) {

                globalInput.focus();
                globalInput.select();
            }
        }
    });

    document.querySelectorAll(".bde-auto-submit").forEach(function (control) {
        control.addEventListener("change", function () {
            const form = control.form;
            if (!form) {
                return;
            }
            notify(form.dataset.feedbackMessage || "Updating view…", "info", 1200);
            form.submit();
        });
    });

    document.addEventListener("submit", function (event) {
        const feedbackForm = event.target.closest(".bde-feedback-form");
        if (feedbackForm && !event.defaultPrevented) {
            notify(feedbackForm.dataset.feedbackMessage || "Updating view…", "info", 1200);

            const button = feedbackForm.querySelector('button[type="submit"]');
            if (button && !button.disabled) {
                button.dataset.originalText = button.textContent;
                button.textContent = "Working…";
                button.disabled = true;
            }
        }

        const matcherForm = event.target.closest("#client-match-form form");
        if (matcherForm && !event.defaultPrevented) {
            notify("Checking the client against Toolkit services…", "info", 1800);
        }
    });

    document.addEventListener("submit", async function (event) {
        const form = event.target.closest(".bde-ajax-save");

        if (!form) {
            return;
        }

        event.preventDefault();

        const button = form.querySelector("button[type='submit']");

        if (!button || button.disabled) {
            return;
        }

        const originalText = button.textContent;
        const iconMode = button.classList.contains("bde-save-icon");
        button.disabled = true;
        button.setAttribute("aria-busy", "true");
        button.textContent = iconMode ? "…" : "Saving…";

        try {
            const response = await fetch(
                form.action,
                {
                    method: "POST",
                    credentials: "same-origin",
                    headers: { "X-Requested-With": "XMLHttpRequest" },
                    body: new FormData(form)
                }
            );

            if (!response.ok) {
                throw new Error("Save request failed");
            }

            const data = await response.json();

            if (iconMode) {
                button.innerHTML = data.saved ? '<span class="bn-bookmark-icon is-saved" aria-hidden="true"></span>' : '<span class="bn-bookmark-icon" aria-hidden="true"></span>';
            }
            else {
                button.innerHTML = data.saved ? '<span class="bn-bookmark-icon is-saved" aria-hidden="true"></span><span>Saved</span>' : '<span class="bn-bookmark-icon" aria-hidden="true"></span><span>Save</span>';
            }

            button.classList.toggle("saved", Boolean(data.saved));
            notify(
                data.saved ? "Saved for quick access" : "Removed from Saved Services",
                "success",
                2200
            );
        }
        catch (error) {
            button.textContent = originalText;
            notify("Could not update Saved Services. Please try again.", "error", 4200);
        }
        finally {
            button.disabled = false;
            button.removeAttribute("aria-busy");
        }
    });
})();


====================================================================================================
FILE: static/js/saved_collections.js
LINES: 1019
SHA256: 946afc6e7f2e3735e7e0ca0ca7c94e0287da1a7dd60de73247169b8dd9ed302f
====================================================================================================
(function () {

    "use strict";


    const SAVE_ACTION_PATTERN =
        /\/toolkit\/service\/(\d+)\/save\/?$/;


    let modal = null;
    let currentServiceId = null;


    function csrfToken() {

        const input =
            document.querySelector(
                'input[name="csrfmiddlewaretoken"]'
            );


        if (input) {
            return input.value;
        }


        const cookies =
            document.cookie
                ? document.cookie.split(";")
                : [];


        for (const cookie of cookies) {

            const value = cookie.trim();

            if (
                value.startsWith(
                    "csrftoken="
                )
            ) {

                return decodeURIComponent(
                    value.substring(
                        "csrftoken=".length
                    )
                );

            }

        }


        return "";

    }


    function escapeHtml(value) {

        const element =
            document.createElement(
                "div"
            );


        element.textContent =
            value || "";


        return element.innerHTML;

    }


    function ensureModal() {

        if (modal) {
            return modal;
        }


        modal =
            document.createElement(
                "div"
            );


        modal.className =
            "bnx-save-collection-modal";


        modal.hidden = true;


        modal.innerHTML = `
            <div
                class="bnx-modal-backdrop"
                data-close-save-collections>
            </div>

            <div
                class="bnx-save-collection-dialog"
                role="dialog"
                aria-modal="true"
                aria-labelledby="bnx-save-collection-title">

                <div class="bnx-modal-handle"></div>

                <header>

                    <div>

                        <small>
                            SAVE SERVICE
                        </small>

                        <h2 id="bnx-save-collection-title">
                            Save to client
                        </h2>

                        <p
                            class="bnx-save-service-name"
                            data-save-service-name>
                        </p>

                    </div>

                    <button
                        type="button"
                        class="bnx-modal-close"
                        data-close-save-collections>
                        ×
                    </button>

                </header>


                <div
                    class="bnx-save-collection-content"
                    data-save-collection-content>

                    <div class="bnx-modal-loading">
                        Loading collections…
                    </div>

                </div>

            </div>
        `;


        document.body.appendChild(
            modal
        );


        modal
            .querySelectorAll(
                "[data-close-save-collections]"
            )
            .forEach(
                function (element) {

                    element.addEventListener(
                        "click",
                        closeModal
                    );

                }
            );


        return modal;

    }


    function closeModal() {

        if (!modal) {
            return;
        }


        modal.hidden = true;

        document.body.classList.remove(
            "bnx-modal-open"
        );


        currentServiceId = null;

    }


    async function loadCollections(
        serviceId
    ) {

        const response =
            await fetch(
                "/toolkit/service/"
                + serviceId
                + "/collections/",
                {
                    credentials:
                        "same-origin",
                }
            );


        if (!response.ok) {

            throw new Error(
                "Could not load collections."
            );

        }


        return response.json();

    }


    function collectionRow(
        collection
    ) {

        return `
            <button
                type="button"
                class="bnx-save-collection-row ${
                    collection.selected
                        ? "selected"
                        : ""
                }"
                data-toggle-collection="${
                    collection.id
                }">

                <div
                    class="bnx-save-collection-avatar">

                    ${
                        escapeHtml(
                            collection.name
                                .slice(
                                    0,
                                    1
                                )
                                .toUpperCase()
                        )
                    }

                </div>


                <div
                    class="bnx-save-collection-name">

                    <strong>
                        ${
                            escapeHtml(
                                collection.name
                            )
                        }
                    </strong>

                    <span>
                        ${
                            collection.note
                                ? escapeHtml(
                                    collection.note
                                )
                                : "Client collection"
                        }
                    </span>

                </div>


                <span
                    class="bnx-save-collection-check">

                    ${
                        collection.selected
                            ? "✓"
                            : "+"
                    }

                </span>

            </button>
        `;

    }


    function renderState(
        data
    ) {

        const name =
            modal.querySelector(
                "[data-save-service-name]"
            );


        const content =
            modal.querySelector(
                "[data-save-collection-content]"
            );


        name.textContent =
            data.service.title;


        let collections = "";


        if (data.collections.length) {

            collections =
                data.collections
                    .map(
                        collectionRow
                    )
                    .join("");

        }

        else {

            collections = `
                <div class="bnx-no-collections">
                    No client collections yet.
                    Create one below.
                </div>
            `;

        }


        content.innerHTML = `
            <div class="bnx-save-collections-heading">
                Client collections
            </div>

            <div class="bnx-save-collection-list">
                ${collections}
            </div>


            <button
                type="button"
                class="bnx-create-inline-collection"
                data-create-inline-collection>

                <span>+</span>

                Create new client collection

            </button>


            <form
                class="bnx-inline-collection-form"
                data-inline-collection-form
                hidden>

                <label>
                    Client / collection name

                    <input
                        type="text"
                        name="name"
                        maxlength="150"
                        placeholder="e.g. Deepak"
                        required>
                </label>

                <label>
                    Note
                    <small>Optional</small>

                    <input
                        type="text"
                        name="note"
                        maxlength="500"
                        placeholder="e.g. Agriculture business">
                </label>

                <div class="bnx-inline-actions">

                    <button
                        type="button"
                        class="secondary-button"
                        data-cancel-inline>
                        Cancel
                    </button>

                    <button
                        type="submit"
                        class="primary-button">
                        Create & save
                    </button>

                </div>

            </form>


            ${
                data.saved
                    ? `
                        <div class="bnx-save-modal-divider"></div>

                        <button
                            type="button"
                            class="bnx-remove-entire-save"
                            data-remove-entire-save>

                            Remove from Saved Services

                        </button>
                    `
                    : ""
            }
        `;


        bindModalActions();

    }


    async function postAction(
        params
    ) {

        const body =
            new URLSearchParams(
                params
            );


        const response =
            await fetch(
                "/toolkit/service/"
                + currentServiceId
                + "/collections/action/",
                {
                    method: "POST",

                    credentials:
                        "same-origin",

                    headers: {
                        "Content-Type":
                            "application/x-www-form-urlencoded",

                        "X-CSRFToken":
                            csrfToken(),

                        "X-Requested-With":
                            "XMLHttpRequest",
                    },

                    body:
                        body.toString(),
                }
            );


        const data =
            await response.json();


        if (!response.ok) {

            throw new Error(
                data.error
                || "Could not update saved service."
            );

        }


        return data;

    }


    function updateBookmarkButtons(
        serviceId,
        saved
    ) {

        document
            .querySelectorAll(
                'form[action*="/toolkit/service/'
                + serviceId
                + '/save/"]'
            )
            .forEach(
                function (form) {

                    const button =
                        form.querySelector(
                            "button"
                        );


                    if (!button) {
                        return;
                    }


                    button.classList.toggle(
                        "saved",
                        saved
                    );


                    const icon =
                        button.querySelector(
                            ".bnx-bookmark-icon"
                        );


                    if (icon) {

                        icon.classList.toggle(
                            "is-saved",
                            saved
                        );

                    }


                    button.setAttribute(
                        "aria-label",
                        saved
                            ? "Manage saved service"
                            : "Save service"
                    );

                }
            );

    }


    function bindModalActions() {

        modal
            .querySelectorAll(
                "[data-toggle-collection]"
            )
            .forEach(
                function (button) {

                    button.addEventListener(
                        "click",
                        async function () {

                            const id =
                                button.dataset
                                    .toggleCollection;


                            button.disabled =
                                true;


                            try {

                                const data =
                                    await postAction(
                                        {
                                            action:
                                                "toggle_collection",

                                            collection_id:
                                                id,
                                        }
                                    );


                                button.classList.toggle(
                                    "selected",
                                    data.selected
                                );


                                const check =
                                    button.querySelector(
                                        ".bnx-save-collection-check"
                                    );


                                if (check) {

                                    check.textContent =
                                        data.selected
                                            ? "✓"
                                            : "+";

                                }


                                updateBookmarkButtons(
                                    currentServiceId,
                                    true
                                );

                            }

                            catch (error) {

                                alert(
                                    error.message
                                );

                            }

                            finally {

                                button.disabled =
                                    false;

                            }

                        }
                    );

                }
            );


        const createButton =
            modal.querySelector(
                "[data-create-inline-collection]"
            );


        const form =
            modal.querySelector(
                "[data-inline-collection-form]"
            );


        if (
            createButton
            &&
            form
        ) {

            createButton.addEventListener(
                "click",
                function () {

                    createButton.hidden =
                        true;

                    form.hidden =
                        false;


                    const input =
                        form.querySelector(
                            'input[name="name"]'
                        );


                    if (input) {

                        input.focus();

                    }

                }
            );


            const cancel =
                form.querySelector(
                    "[data-cancel-inline]"
                );


            if (cancel) {

                cancel.addEventListener(
                    "click",
                    function () {

                        form.hidden =
                            true;

                        createButton.hidden =
                            false;

                    }
                );

            }


            form.addEventListener(
                "submit",
                async function (event) {

                    event.preventDefault();


                    const formData =
                        new FormData(
                            form
                        );


                    try {

                        await postAction(
                            {
                                action:
                                    "create_and_add",

                                name:
                                    formData.get(
                                        "name"
                                    )
                                    || "",

                                note:
                                    formData.get(
                                        "note"
                                    )
                                    || "",
                            }
                        );


                        updateBookmarkButtons(
                            currentServiceId,
                            true
                        );


                        const state =
                            await loadCollections(
                                currentServiceId
                            );


                        renderState(
                            state
                        );

                    }

                    catch (error) {

                        alert(
                            error.message
                        );

                    }

                }
            );

        }


        const remove =
            modal.querySelector(
                "[data-remove-entire-save]"
            );


        if (remove) {

            remove.addEventListener(
                "click",
                async function () {

                    const confirmed =
                        window.confirm(
                            "Remove this service from all saved collections?"
                        );


                    if (!confirmed) {
                        return;
                    }


                    try {

                        await postAction(
                            {
                                action:
                                    "remove_saved",
                            }
                        );


                        updateBookmarkButtons(
                            currentServiceId,
                            false
                        );


                        closeModal();


                        if (
                            window.location.pathname
                            === "/toolkit/saved/"
                        ) {

                            window.location.reload();

                        }

                    }

                    catch (error) {

                        alert(
                            error.message
                        );

                    }

                }
            );

        }

    }


    async function openForService(
        serviceId
    ) {

        currentServiceId =
            String(
                serviceId
            );


        ensureModal();


        modal.hidden =
            false;


        document.body.classList.add(
            "bnx-modal-open"
        );


        const content =
            modal.querySelector(
                "[data-save-collection-content]"
            );


        content.innerHTML = `
            <div class="bnx-modal-loading">
                Loading collections…
            </div>
        `;


        try {

            const state =
                await loadCollections(
                    currentServiceId
                );


            renderState(
                state
            );

        }

        catch (error) {

            content.innerHTML = `
                <div class="bnx-modal-error">
                    ${
                        escapeHtml(
                            error.message
                        )
                    }
                </div>
            `;

        }

    }


    // -------------------------------------------------------
    // Capture existing bookmark forms BEFORE older save JS.
    // No existing search / matcher code has to be rewritten.
    // -------------------------------------------------------

    document.addEventListener(
        "submit",
        function (event) {

            const form =
                event.target;


            if (
                !form
                ||
                form.tagName !== "FORM"
            ) {

                return;

            }


            const action =
                form.getAttribute(
                    "action"
                )
                || "";


            const match =
                action.match(
                    SAVE_ACTION_PATTERN
                );


            if (!match) {
                return;
            }


            event.preventDefault();

            event.stopPropagation();

            event.stopImmediatePropagation();


            openForService(
                match[1]
            );

        },
        true
    );


    document.addEventListener(
        "click",
        function (event) {

            const manual =
                event.target.closest(
                    "[data-bnx-manage-saved]"
                );


            if (!manual) {
                return;
            }


            event.preventDefault();


            openForService(
                manual.dataset.serviceId
            );

        }
    );


    document.addEventListener(
        "keydown",
        function (event) {

            if (
                event.key === "Escape"
                &&
                modal
                &&
                !modal.hidden
            ) {

                closeModal();

            }

        }
    );

})();


====================================================================================================
FILE: templates/base.html
LINES: 1442
SHA256: 99a7c714cb1ab4495af38e321b77d43bccb07998804d73a041eb1b707c28ea8c
====================================================================================================
{% load static %}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>{% block title %}BharatNXT Wave Internal{% endblock %}</title>

    <link rel="stylesheet" href="{% static 'css/app.css' %}?v=20260818_shell_recovery_v1">
<link rel="stylesheet" href="{% static 'css/ui_stability.css' %}?v=20260818_shell_recovery_v1">
    <link rel="stylesheet" href="{% static 'css/bde_workspace.css' %}?v=20260818_shell_recovery_v1">

<style id="premium-bde-sidebar-v11">

/* =========================================================
   COLLAPSIBLE BDE SIDEBAR
========================================================= */

.sidebar{
    transition:
        width .22s ease,
        padding .22s ease;
}


.main-area{
    transition:
        margin-left .22s ease;
}


.bnx-sidebar-toggle{

    position:absolute;

    top:30px;
    right:-14px;

    width:30px;
    height:30px;

    padding:0;

    display:flex;
    align-items:center;
    justify-content:center;

    border:1px solid #d0d5dd;
    border-radius:50%;

    background:#ffffff;
    color:#475467;

    cursor:pointer;

    box-shadow:
        0 4px 12px
        rgba(16,24,40,.13);

    z-index:200;

    transition:
        color .15s ease,
        background .15s ease,
        transform .15s ease;
}


.bnx-sidebar-toggle:hover{

    color:#1769ff;

    background:#f8faff;
}


.bnx-sidebar-toggle svg{

    width:14px;
    height:14px;

    fill:none;
    stroke:currentColor;
    stroke-width:2;

    transition:
        transform .20s ease;
}


/* =========================================================
   COMPACT MODE
========================================================= */

body.bnx-sidebar-collapsed
.sidebar{

    width:78px !important;

    padding-left:10px !important;
    padding-right:10px !important;
}


body.bnx-sidebar-collapsed
.main-area{

    margin-left:78px !important;
}


body.bnx-sidebar-collapsed
.brand{

    justify-content:center !important;

    padding-left:0 !important;
    padding-right:0 !important;
}


body.bnx-sidebar-collapsed
.brand-text,

body.bnx-sidebar-collapsed
.nav-section-label{

    display:none !important;
}


body.bnx-sidebar-collapsed
.navigation{

    gap:7px !important;
}


body.bnx-sidebar-collapsed
.nav-item{

    position:relative;

    width:48px !important;
    height:46px !important;

    margin-left:auto !important;
    margin-right:auto !important;

    padding:0 !important;

    display:flex !important;

    align-items:center !important;
    justify-content:center !important;

    border-radius:12px !important;
}


body.bnx-sidebar-collapsed
.nav-item > span:not(.nav-icon){

    display:none !important;
}


body.bnx-sidebar-collapsed
.nav-item .nav-icon{

    width:22px !important;

    margin:0 !important;

    display:flex !important;

    align-items:center !important;
    justify-content:center !important;
}


body.bnx-sidebar-collapsed
.sidebar-account-actions{

    display:flex !important;

    justify-content:center !important;
}


body.bnx-sidebar-collapsed
.sidebar-logout-button{

    width:48px !important;

    padding-left:0 !important;
    padding-right:0 !important;

    justify-content:center !important;
}


body.bnx-sidebar-collapsed
.sidebar-logout-button > span:last-child{

    display:none !important;
}


body.bnx-sidebar-collapsed
.bnx-sidebar-toggle svg{

    transform:
        rotate(180deg);
}


/* HOVER TOOLTIP */

body.bnx-sidebar-collapsed
.nav-item:hover::after{

    content:
        attr(data-bnx-label);

    position:absolute;

    left:59px;
    top:50%;

    transform:
        translateY(-50%);

    padding:7px 10px;

    border-radius:8px;

    background:#101828;
    color:#ffffff;

    white-space:nowrap;

    font-size:11px;
    font-weight:650;

    box-shadow:
        0 7px 18px
        rgba(16,24,40,.18);

    z-index:500;
}


@media(max-width:760px){

    .bnx-sidebar-toggle{
        display:none;
    }

}

</style>



<!-- SIDEBAR_TOGGLE_VISIBILITY_V12 -->

<style id="sidebar-toggle-visibility-v12">

.bnx-sidebar-toggle{

    width:36px !important;
    height:36px !important;

    top:27px !important;
    right:-18px !important;

    border:
        3px solid #ffffff !important;

    border-radius:
        11px !important;

    background:
        #1769ff !important;

    color:
        #ffffff !important;

    box-shadow:
        0 5px 16px
        rgba(23,105,255,.32),
        0 2px 6px
        rgba(16,24,40,.18) !important;

    opacity:1 !important;

    visibility:visible !important;

    overflow:visible !important;
}


.bnx-sidebar-toggle:hover{

    background:
        #0f5de8 !important;

    color:
        #ffffff !important;

    transform:
        scale(1.06);

    box-shadow:
        0 7px 20px
        rgba(23,105,255,.38),
        0 3px 8px
        rgba(16,24,40,.20) !important;
}


.bnx-sidebar-toggle:active{

    transform:
        scale(.96);
}


.bnx-sidebar-toggle-icon{

    width:20px !important;
    height:20px !important;

    fill:none !important;

    stroke:
        currentColor !important;

    stroke-width:
        1.8 !important;

    stroke-linecap:
        round !important;

    stroke-linejoin:
        round !important;

    transition:
        transform .2s ease;
}


/*
When sidebar is collapsed,
the entire panel icon reverses direction.
*/

body.bnx-sidebar-collapsed
.bnx-sidebar-toggle-icon{

    transform:
        rotate(180deg);
}


/*
Cancel old V1.1 rotation rule
so the icon only rotates once.
*/

body.bnx-sidebar-collapsed
.bnx-sidebar-toggle svg{

    transform:
        none !important;
}


.bnx-sidebar-toggle:focus-visible{

    outline:
        3px solid
        rgba(23,105,255,.25);

    outline-offset:
        3px;
}

</style>



<!-- SIDEBAR_TOGGLE_FINAL_FIX -->

<style id="bnx-sidebar-toggle-final-fix">

/* =========================================================
   FINAL SIDEBAR TOGGLE
   Completely inside sidebar — nothing can clip it
========================================================= */

.sidebar{
    overflow:visible !important;
}


/*
Expanded sidebar:
place control inside the black sidebar,
not across the white-content boundary.
*/

#bnx-sidebar-toggle{

    position:absolute !important;

    top:96px !important;
    right:16px !important;

    left:auto !important;

    width:34px !important;
    height:34px !important;

    min-width:34px !important;
    min-height:34px !important;

    margin:0 !important;
    padding:0 !important;

    display:flex !important;
    align-items:center !important;
    justify-content:center !important;

    border:
        1px solid
        rgba(255,255,255,.16) !important;

    border-radius:
        10px !important;

    background:
        #20242c !important;

    color:
        #ffffff !important;

    box-shadow:
        none !important;

    opacity:1 !important;
    visibility:visible !important;

    overflow:hidden !important;

    cursor:pointer !important;

    z-index:9999 !important;

    transform:none !important;

    transition:
        background .16s ease,
        border-color .16s ease,
        transform .16s ease !important;
}


#bnx-sidebar-toggle:hover{

    background:
        #1769ff !important;

    border-color:
        #1769ff !important;

    color:
        #ffffff !important;

    transform:
        translateY(-1px) !important;

    box-shadow:
        none !important;
}


#bnx-sidebar-toggle:active{

    transform:
        scale(.95) !important;
}


/* =========================================================
   REMOVE EVERY OLD SVG EFFECT
========================================================= */

#bnx-sidebar-toggle svg{

    display:none !important;

    width:0 !important;
    height:0 !important;

    transform:none !important;
}


/* =========================================================
   CRISP NON-SVG CHEVRON
========================================================= */

.bnx-sidebar-final-chevron{

    position:relative;

    width:16px;
    height:16px;

    display:block;

    flex:none;
}


.bnx-sidebar-final-chevron::before,
.bnx-sidebar-final-chevron::after{

    content:"";

    position:absolute;

    left:5px;

    width:8px;
    height:2px;

    background:
        currentColor;

    border-radius:
        999px;

    transform-origin:
        1px center;
}


.bnx-sidebar-final-chevron::before{

    top:5px;

    transform:
        rotate(-45deg);
}


.bnx-sidebar-final-chevron::after{

    bottom:5px;

    transform:
        rotate(45deg);
}


/* =========================================================
   COLLAPSED SIDEBAR
========================================================= */

body.bnx-sidebar-collapsed
#bnx-sidebar-toggle{

    top:96px !important;
    right:22px !important;

    width:34px !important;
    height:34px !important;

    transform:none !important;
}


/*
Reverse the chevron when sidebar
is collapsed so it means "expand".
*/

body.bnx-sidebar-collapsed
.bnx-sidebar-final-chevron{

    transform:
        rotate(180deg);
}


body.bnx-sidebar-collapsed
#bnx-sidebar-toggle:hover{

    transform:
        translateY(-1px) !important;
}


/* =========================================================
   FOCUS
========================================================= */

#bnx-sidebar-toggle:focus-visible{

    outline:
        3px solid
        rgba(23,105,255,.30) !important;

    outline-offset:
        3px !important;
}


/* =========================================================
   MOBILE
========================================================= */

@media(max-width:760px){

    #bnx-sidebar-toggle{
        display:none !important;
    }

}

</style>



<!-- SIMPLE_SIDEBAR_ARROW_FINAL -->

<style id="simple-sidebar-arrow-final">

/* =========================================================
   SIMPLE SIDEBAR TOGGLE
========================================================= */

#bnx-sidebar-toggle{

    width:34px !important;
    height:34px !important;

    border:
        1px solid
        #3a414c !important;

    border-radius:
        9px !important;

    background:
        #1d222a !important;

    color:
        #ffffff !important;

    box-shadow:
        none !important;

    display:flex !important;

    align-items:center !important;
    justify-content:center !important;

    padding:0 !important;

    transform:none !important;

    cursor:pointer !important;
}


#bnx-sidebar-toggle:hover{

    background:
        #252b35 !important;

    border-color:
        #596273 !important;

    transform:none !important;
}


/* REMOVE OLD DRAWN ARROW */

.bnx-sidebar-final-chevron{

    width:auto !important;
    height:auto !important;

    display:flex !important;

    align-items:center !important;
    justify-content:center !important;

    font-family:
        Arial,
        sans-serif !important;

    font-size:
        27px !important;

    font-weight:
        300 !important;

    line-height:
        1 !important;

    color:
        #ffffff !important;

    transform:none !important;
}


/* kill old broken line drawing */

.bnx-sidebar-final-chevron::before,
.bnx-sidebar-final-chevron::after{

    display:none !important;

    content:none !important;
}


/* simple left arrow */

.bnx-sidebar-final-chevron{

    font-size:0 !important;
}


.bnx-sidebar-final-chevron::after{

    display:block !important;

    content:"‹" !important;

    position:static !important;

    width:auto !important;
    height:auto !important;

    background:none !important;

    transform:none !important;

    font-size:
        27px !important;

    font-weight:
        300 !important;

    line-height:
        28px !important;

    color:
        #ffffff !important;
}


/* collapsed = simple right arrow */

body.bnx-sidebar-collapsed
.bnx-sidebar-final-chevron{

    transform:none !important;
}


body.bnx-sidebar-collapsed
.bnx-sidebar-final-chevron::after{

    content:"›" !important;

    transform:none !important;
}


/* no old SVG */

#bnx-sidebar-toggle svg{

    display:none !important;
}

</style>



<style id="BNX_GLOBAL_SHELL_RECOVERY_V1">

/* =========================================================
   GLOBAL SOFTWARE SHELL RECOVERY

   The application's intended architecture is:

   FIXED SIDEBAR
   +
   MAIN AREA BESIDE IT

   Do not change sidebar widths here.
   Existing expanded/collapsed width rules remain authoritative.
========================================================= */


/* ---------------------------------------------------------
   SIDEBAR MUST NEVER ENTER NORMAL DOCUMENT FLOW
--------------------------------------------------------- */

.sidebar,
body.bnx-sidebar-collapsed .sidebar {

    position:
        fixed !important;

    top:
        0 !important;

    left:
        0 !important;

    bottom:
        0 !important;

    margin-top:
        0 !important;

    margin-bottom:
        0 !important;

}


/* ---------------------------------------------------------
   MAIN APPLICATION MUST START AT TOP OF VIEWPORT
--------------------------------------------------------- */

.main-area,
body.bnx-sidebar-collapsed .main-area {

    margin-top:
        0 !important;

    min-height:
        100vh !important;

}


/* ---------------------------------------------------------
   SHARED APPLICATION SHELL
--------------------------------------------------------- */

.app-shell {

    min-height:
        100vh !important;

}


/* ---------------------------------------------------------
   TOPBAR MUST REMAIN AT TOP OF MAIN AREA
--------------------------------------------------------- */

.main-area > .topbar {

    top:
        0 !important;

    margin-top:
        0 !important;

}


/* ---------------------------------------------------------
   BDE VARIANTS USE THE SAME GEOMETRY
--------------------------------------------------------- */

.bde-sidebar,
body.bnx-sidebar-collapsed .bde-sidebar {

    position:
        fixed !important;

    top:
        0 !important;

    left:
        0 !important;

    bottom:
        0 !important;

}


.bde-main-area,
body.bnx-sidebar-collapsed .bde-main-area {

    margin-top:
        0 !important;

    min-height:
        100vh !important;

}

</style>



<link rel="stylesheet" href="{% static 'css/saved_collections.css' %}?v=20260818_v11">
</head>

<body>

<div class="app-shell bde-shell">

    <!-- SIDEBAR -->
    <aside class="sidebar bde-sidebar">

<!-- PREMIUM_BDE_SIDEBAR_V11 -->
<button
    type="button"
    id="bnx-sidebar-toggle"
    class="bnx-sidebar-toggle"
    aria-label="Collapse sidebar"
    aria-expanded="true"
    title="Collapse sidebar">
    <span
        class="bnx-sidebar-final-chevron"
        aria-hidden="true">
    </span>
</button>


        <!-- BRAND -->
        <div class="brand">

            <div class="brand-mark">
                BN
            </div>

            <div class="brand-text">
                <div class="brand-name">
                    BharatNXT Wave
                </div>

                <div class="brand-subtitle">
                    INTERNAL
                </div>
            </div>

        </div>


        <!-- MAIN NAVIGATION -->
        <nav class="navigation bde-navigation">

            <div class="nav-section-label">
                WORKSPACE
            </div>

            <a href="{% url 'dashboard:home' %}"
               class="nav-item {% if request.path == '/' %}active{% endif %}">
                <span class="nav-icon">⌂</span>
                <span>Home</span>
            </a>

            <a href="{% url 'toolkit:home' %}"
               class="nav-item {% if request.path == '/toolkit/' or '/toolkit/service/' in request.path %}active{% endif %}">
                <span class="nav-icon">◫</span>
                <span>Business Toolkit</span>
            </a>

            
            <a href="{% url 'toolkit:service_library' %}"
               class="nav-item {% if '/toolkit/library/' in request.path %}active{% endif %}">
                <span class="nav-icon">▦</span>
                <span>Service Library</span>
            </a>

            <a href="{% url 'toolkit:recent_services' %}"
               class="nav-item {% if '/toolkit/recent/' in request.path %}active{% endif %}">
                <span class="nav-icon">◷</span>
                <span>Recent</span>
            </a>

<a href="{% url 'toolkit:saved_services' %}"
               class="nav-item {% if '/toolkit/saved/' in request.path %}active{% endif %}">
                <span class="nav-icon"><span class="bn-bookmark-icon" aria-hidden="true"></span></span>
                <span>Saved Services</span>
            </a>

            <a
                href="{% url 'toolkit:client_matcher' %}"
                class="nav-item {% if '/toolkit/matcher/' in request.path %}active{% endif %}">

                
                <span
                    class="nav-icon bn-nav-svg-wrap"
                    aria-hidden="true">

                    <svg
                        class="bn-nav-svg"
                        viewBox="0 0 24 24"
                        fill="none">

                        <circle
                            cx="8"
                            cy="7"
                            r="3">
                        </circle>

                        <path
                            d="M3.5 16.5C4.1 13.8 5.7 12.5 8 12.5C9.2 12.5 10.2 12.8 11 13.4">
                        </path>

                        <circle
                            cx="16.5"
                            cy="15.5"
                            r="4">
                        </circle>

                        <path
                            d="M19.5 18.5L22 21">
                        </path>

                    </svg>

                </span>

                <span>
                    Find Services for Client
                </span>


            </a>


        </nav>

    
    <!-- SIDEBAR ACCOUNT ACTIONS -->
    <div class="sidebar-account-actions">

        <form
            method="post"
            action="{% url 'accounts:logout' %}"
            class="sidebar-logout-form">

            {% csrf_token %}

            <button
                type="submit"
                class="sidebar-logout-button">

                <span
                    class="sidebar-logout-icon"
                    aria-hidden="true">

                    <svg
                        viewBox="0 0 24 24"
                        fill="none">

                        <path
                            d="M10 5H6.5C5.7 5 5 5.7 5 6.5V17.5C5 18.3 5.7 19 6.5 19H10">
                        </path>

                        <path
                            d="M14 8L18 12L14 16">
                        </path>

                        <path
                            d="M18 12H9">
                        </path>

                    </svg>

                </span>

                <span>
                    Sign Out
                </span>

            </button>

        </form>

    </div>

</aside>


    <!-- MAIN APPLICATION AREA -->
    <main class="main-area bde-main-area">


        <!-- TOP NAVIGATION BAR -->
        <header class="topbar bde-topbar">


            <!-- GLOBAL SEARCH -->


            <!-- TOP RIGHT -->
            <div class="topbar-actions">

                <!-- USER PROFILE -->
                <div class="user-profile">

                    <div class="user-avatar">

                        {{ request.user.username|slice:":1"|upper }}

                    </div>


                    <div class="user-info">

                        <div class="user-name">

                            {{ request.user.username }}

                        </div>

                        <div class="user-role">

                            {{ request.user.get_role_display }}

                        </div>

                    </div>

                </div>

            </div>

        
            

</header>


        <!-- MAIN PAGE CONTENT -->
        
        <!-- =================================================
             GLOBAL HUMAN FEEDBACK
        ================================================== -->

        {% if messages %}

            <div
                class="bn-feedback-stack"
                aria-live="polite"
                aria-atomic="true">

                {% for message in messages %}

                    <div
                        class="bn-feedback-toast {{ message.tags|default:'info' }}"
                        data-feedback-type="{{ message.tags|default:'info' }}"
                        role="status">


                        <div
                            class="bn-feedback-icon"
                            aria-hidden="true">

                            {% if "success" in message.tags %}
                                ✓
                            {% elif "error" in message.tags %}
                                !
                            {% elif "warning" in message.tags %}
                                !
                            {% else %}
                                i
                            {% endif %}

                        </div>


                        <div class="bn-feedback-copy">

                            {% if "success" in message.tags %}

                                <strong>
                                    Done
                                </strong>

                            {% elif "error" in message.tags %}

                                <strong>
                                    Something went wrong
                                </strong>

                            {% elif "warning" in message.tags %}

                                <strong>
                                    Please check
                                </strong>

                            {% else %}

                                <strong>
                                    Update
                                </strong>

                            {% endif %}


                            <span>
                                {{ message }}
                            </span>

                        </div>


                        <button
                            type="button"
                            class="bn-feedback-close"
                            aria-label="Dismiss notification">

                            ×

                        </button>

                    </div>

                {% endfor %}

            </div>

        {% endif %}


<section class="content bde-content">

            {% block content %}

            {% endblock %}

        </section>


    </main>

</div>

    <script src="{% static 'js/session_location.js' %}"></script>


    <script src="{% static 'js/ui_feedback.js' %}"></script>


<div id="bde-quick-overlay"
     class="bde-quick-overlay"
     hidden></div>

<aside id="bde-quick-drawer"
       class="bde-quick-drawer"
       aria-label="Service quick view"
       hidden>
    <div class="bde-quick-drawer-header">
        <div>
            <span>CALL ASSIST</span>
            <strong>Service quick view</strong>
        </div>
        <button id="bde-quick-close"
                type="button"
                aria-label="Close quick view">×</button>
    </div>
    <div id="bde-quick-drawer-content"
         class="bde-quick-drawer-content">
        <div class="bde-quick-loading">Loading service…</div>
    </div>
</aside>

    <script src="{% static 'js/bde_workspace.js' %}"></script>

<script id="premium-bde-sidebar-script-v11">

(function(){

    const toggle =
        document.getElementById(
            "bnx-sidebar-toggle"
        );

    if(!toggle){
        return;
    }


    const storageKey =
        "bharatnxt_bde_sidebar_collapsed";


    document
        .querySelectorAll(
            ".sidebar .nav-item"
        )
        .forEach(
            function(item){

                const text =
                    item.innerText
                    .replace(/\s+/g," ")
                    .trim();

                if(text){

                    item.dataset.bnxLabel =
                        text;

                    item.title =
                        text;
                }

            }
        );


    function setSidebarState(
        collapsed,
        remember
    ){

        document.body
            .classList
            .toggle(
                "bnx-sidebar-collapsed",
                collapsed
            );


        toggle.setAttribute(
            "aria-expanded",
            collapsed
                ? "false"
                : "true"
        );


        toggle.setAttribute(
            "aria-label",
            collapsed
                ? "Expand sidebar"
                : "Collapse sidebar"
        );


        toggle.title =
            collapsed
                ? "Expand sidebar"
                : "Collapse sidebar";


        if(remember){

            try{

                localStorage.setItem(
                    storageKey,
                    collapsed
                        ? "1"
                        : "0"
                );

            }catch(error){
                /* preference storage optional */
            }

        }

    }


    let initialState = false;


    try{

        initialState =
            localStorage.getItem(
                storageKey
            ) === "1";

    }catch(error){

        initialState =
            false;
    }


    setSidebarState(
        initialState,
        false
    );


    toggle.addEventListener(
        "click",
        function(){

            setSidebarState(
                !document.body
                    .classList
                    .contains(
                        "bnx-sidebar-collapsed"
                    ),
                true
            );

        }
    );

})();

</script>


<script src="{% static 'js/saved_collections.js' %}?v=20260818_v1"></script>
</body>

</html>

====================================================================================================
FILE: toolkit/migrations/0006_saved_client_collections_v1.py
LINES: 58
SHA256: 5861f3f5e17892e4aff548568c0d8704fa344408b1449877d0c610cca8bbc769
====================================================================================================
# Generated by Django 6.1 on 2026-08-18 06:55

import django.db.models.deletion
from django.conf import settings
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('toolkit', '0005_final_import_knowledge_architecture'),
        migrations.swappable_dependency(settings.AUTH_USER_MODEL),
    ]

    operations = [
        migrations.CreateModel(
            name='SavedCollection',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('name', models.CharField(max_length=150)),
                ('note', models.CharField(blank=True, max_length=500)),
                ('created_at', models.DateTimeField(auto_now_add=True)),
                ('updated_at', models.DateTimeField(auto_now=True)),
                ('user', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='toolkit_saved_collections', to=settings.AUTH_USER_MODEL)),
            ],
            options={
                'ordering': ['-updated_at', 'name'],
            },
        ),
        migrations.CreateModel(
            name='SavedCollectionItem',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('created_at', models.DateTimeField(auto_now_add=True)),
                ('collection', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='items', to='toolkit.savedcollection')),
                ('saved_service', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='collection_items', to='toolkit.savedservice')),
            ],
            options={
                'ordering': ['-created_at'],
            },
        ),
        migrations.AddIndex(
            model_name='savedcollection',
            index=models.Index(fields=['user', 'updated_at'], name='toolkit_sav_user_id_16a817_idx'),
        ),
        migrations.AddConstraint(
            model_name='savedcollection',
            constraint=models.UniqueConstraint(fields=('user', 'name'), name='unique_saved_collection_name_per_user'),
        ),
        migrations.AddIndex(
            model_name='savedcollectionitem',
            index=models.Index(fields=['collection', 'created_at'], name='toolkit_sav_collect_895ec4_idx'),
        ),
        migrations.AddConstraint(
            model_name='savedcollectionitem',
            constraint=models.UniqueConstraint(fields=('collection', 'saved_service'), name='unique_saved_service_per_collection'),
        ),
    ]


====================================================================================================
FILE: toolkit/models.py
LINES: 1980
SHA256: 451e117ded0d257f60db78e92aeadb0976be911bb85f6dc727a3816596d325c1
====================================================================================================
from django.conf import settings
from django.db import models


# =========================================================
# SERVICE DOMAIN
# Examples:
# Government Schemes & Grants
# Business Incorporation & Launch
# Compliance Management
# Funding & Equity Support
# =========================================================

class ServiceDomain(models.Model):
    name = models.CharField(
        max_length=150,
        unique=True
    )

    slug = models.SlugField(
        max_length=170,
        unique=True
    )

    description = models.TextField(
        blank=True
    )

    display_order = models.PositiveIntegerField(
        default=0
    )

    is_active = models.BooleanField(
        default=True
    )

    created_at = models.DateTimeField(
        auto_now_add=True
    )

    updated_at = models.DateTimeField(
        auto_now=True
    )

    class Meta:
        ordering = ["display_order", "name"]

    def __str__(self):
        return self.name


# =========================================================
# CATEGORY
# Examples:
# Company Registration
# Government Grants
# MSME Loans
# Certifications
# =========================================================

class Category(models.Model):
    domain = models.ForeignKey(
        ServiceDomain,
        on_delete=models.PROTECT,
        related_name="categories"
    )

    name = models.CharField(
        max_length=150
    )

    slug = models.SlugField(
        max_length=170
    )

    description = models.TextField(
        blank=True
    )

    display_order = models.PositiveIntegerField(
        default=0
    )

    is_active = models.BooleanField(
        default=True
    )

    created_at = models.DateTimeField(
        auto_now_add=True
    )

    updated_at = models.DateTimeField(
        auto_now=True
    )

    class Meta:
        ordering = ["display_order", "name"]

        constraints = [
            models.UniqueConstraint(
                fields=["domain", "slug"],
                name="unique_category_slug_per_domain"
            )
        ]

    def __str__(self):
        return f"{self.domain.name} → {self.name}"


# =========================================================
# MAIN SERVICE / SCHEME
# Examples:
# Startup India
# PMEGP
# CGTMSE
# Pvt Ltd Registration
# ISO Certification
# FSSAI Licence
# =========================================================

class Service(models.Model):

    STATUS_CHOICES = [
        ("DRAFT", "Draft"),
        ("UNDER_REVIEW", "Under Review"),
        ("APPROVED", "Approved"),
        ("PUBLISHED", "Published"),
        ("EXPIRING", "Expiring"),
        ("ARCHIVED", "Archived"),
    ]

    SERVICE_KIND_CHOICES = [
        ("REGISTRATION", "Registration"),
        ("COMPLIANCE", "Compliance"),
        ("CERTIFICATION", "Certification"),
        ("GOVT_SCHEME", "Government Scheme"),
        ("GRANT", "Grant"),
        ("LOAN", "Loan"),
        ("DEBT", "Debt Funding"),
        ("EQUITY", "Equity Funding"),
        ("VC", "VC Funding"),
        ("SUBSIDY", "Subsidy"),
        ("LEGAL", "Legal Service"),
        ("DIGITAL", "Digital Service"),
        ("CONSULTING", "Consulting"),
        ("OTHER", "Other"),
    ]

    PRIORITY_CHOICES = [
        ("LOW", "Low"),
        ("NORMAL", "Normal"),
        ("HIGH", "High"),
        ("CRITICAL", "Critical"),
    ]

    # -----------------------------
    # Identity
    # -----------------------------

    service_id = models.CharField(
        max_length=40,
        unique=True
    )

    title = models.CharField(
        max_length=255
    )

    slug = models.SlugField(
        max_length=280,
        unique=True
    )

    domain = models.ForeignKey(
        ServiceDomain,
        on_delete=models.PROTECT,
        related_name="services"
    )

    category = models.ForeignKey(
        Category,
        on_delete=models.PROTECT,
        related_name="services"
    )

    # Primary category remains above for backward compatibility.
    # This relationship allows one real Service to also belong
    # to multiple workbook classifications without duplication.
    classifications = models.ManyToManyField(
        Category,
        through="ServiceClassification",
        related_name="classified_services",
        blank=True
    )

    service_kind = models.CharField(
        max_length=30,
        choices=SERVICE_KIND_CHOICES
    )

    status = models.CharField(
        max_length=30,
        choices=STATUS_CHOICES,
        default="DRAFT"
    )

    priority = models.CharField(
        max_length=20,
        choices=PRIORITY_CHOICES,
        default="NORMAL"
    )

    # -----------------------------
    # BDE-facing information
    # -----------------------------

    bde_summary = models.TextField(
        blank=True,
        help_text="Short explanation BDE can quickly read during a call."
    )

    overview = models.TextField(
        blank=True
    )

    benefits = models.TextField(
        blank=True
    )

    restrictions = models.TextField(
        blank=True
    )

    important_notes = models.TextField(
        blank=True
    )

    # -----------------------------
    # Eligibility information
    # -----------------------------

    eligibility_summary = models.TextField(
        blank=True
    )

    business_types = models.JSONField(
        default=list,
        blank=True
    )

    business_stages = models.JSONField(
        default=list,
        blank=True
    )

    industries = models.JSONField(
        default=list,
        blank=True
    )

    applicable_states = models.JSONField(
        default=list,
        blank=True
    )

    founder_categories = models.JSONField(
        default=list,
        blank=True
    )

    # Original source value is retained because "Applicable for"
    # may contain business type, founder category, stage, state,
    # or mixed eligibility information.
    applicable_for_raw = models.TextField(
        blank=True
    )

    min_business_age_months = models.PositiveIntegerField(
        null=True,
        blank=True
    )

    max_business_age_months = models.PositiveIntegerField(
        null=True,
        blank=True
    )

    min_turnover = models.DecimalField(
        max_digits=16,
        decimal_places=2,
        null=True,
        blank=True
    )

    max_turnover = models.DecimalField(
        max_digits=16,
        decimal_places=2,
        null=True,
        blank=True
    )

    # -----------------------------
    # Funding / financial details
    # -----------------------------

    funding_min = models.DecimalField(
        max_digits=16,
        decimal_places=2,
        null=True,
        blank=True
    )

    funding_max = models.DecimalField(
        max_digits=16,
        decimal_places=2,
        null=True,
        blank=True
    )

    funding_type = models.CharField(
        max_length=120,
        blank=True
    )

    funding_organisation = models.CharField(
        max_length=255,
        blank=True
    )

    interest_rate_min = models.DecimalField(
        max_digits=6,
        decimal_places=2,
        null=True,
        blank=True
    )

    interest_rate_max = models.DecimalField(
        max_digits=6,
        decimal_places=2,
        null=True,
        blank=True
    )

    collateral_required = models.BooleanField(
        null=True,
        blank=True
    )

    tenure = models.CharField(
        max_length=150,
        blank=True
    )

    subsidy_details = models.TextField(
        blank=True
    )

    # -----------------------------
    # BharatNXT commercial info
    # -----------------------------

    government_fee = models.DecimalField(
        max_digits=12,
        decimal_places=2,
        null=True,
        blank=True
    )

    bharatnxt_fee = models.DecimalField(
        max_digits=12,
        decimal_places=2,
        null=True,
        blank=True
    )

    pricing_notes = models.TextField(
        blank=True
    )

    # -----------------------------
    # Timeline
    # -----------------------------

    estimated_processing_time = models.CharField(
        max_length=150,
        blank=True
    )

    effective_from = models.DateField(
        null=True,
        blank=True,
        help_text="Date from which BDEs should start pitching this service."
    )

    pitch_until = models.DateField(
        null=True,
        blank=True,
        db_index=True,
        help_text="Internal last date until which BDEs should pitch this service."
    )

    application_deadline = models.DateField(
        null=True,
        blank=True
    )

    DEADLINE_STATUS_CHOICES = [
        ("UNKNOWN", "Unknown"),
        ("DATED", "Specific Date"),
        ("ROLLING", "Rolling"),
        ("ONGOING", "Ongoing"),
        ("OPEN", "Open"),
        ("CLOSED", "Closed"),
        ("NO_DEADLINE", "No Deadline"),
        ("OTHER", "Other"),
    ]

    # Preserve the exact source text. application_deadline is
    # populated only when the value can be confidently normalized.
    application_deadline_raw = models.TextField(
        blank=True
    )

    deadline_status = models.CharField(
        max_length=20,
        choices=DEADLINE_STATUS_CHOICES,
        default="UNKNOWN",
        db_index=True
    )

    # -----------------------------
    # Internal company information
    # -----------------------------

    internal_notes = models.TextField(
        blank=True
    )

    sales_pitch = models.TextField(
        blank=True
    )

    escalation_notes = models.TextField(
        blank=True
    )

    # -----------------------------
    # Verification
    # -----------------------------

    version = models.PositiveIntegerField(
        default=1
    )

    last_verified_at = models.DateTimeField(
        null=True,
        blank=True
    )

    verified_by = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="verified_services"
    )

    created_by = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="created_services"
    )

    created_at = models.DateTimeField(
        auto_now_add=True
    )

    updated_at = models.DateTimeField(
        auto_now=True
    )

    class Meta:
        ordering = ["title"]

        indexes = [
            models.Index(fields=["status"]),
            models.Index(fields=["service_kind"]),
            models.Index(fields=["domain"]),
            models.Index(fields=["category"]),
            models.Index(fields=["updated_at"]),
            models.Index(fields=["last_verified_at"]),
        ]

    def __str__(self):
        return f"{self.service_id} - {self.title}"


# =========================================================
# ELIGIBILITY RULE
# Allows multiple clean eligibility conditions
# instead of one giant paragraph.
# =========================================================

class EligibilityRule(models.Model):

    RULE_TYPE_CHOICES = [
        ("REQUIRED", "Required"),
        ("OPTIONAL", "Optional"),
        ("DISQUALIFIER", "Disqualifier"),
        ("INFORMATION", "Information"),
    ]

    service = models.ForeignKey(
        Service,
        on_delete=models.CASCADE,
        related_name="eligibility_rules"
    )

    rule_type = models.CharField(
        max_length=20,
        choices=RULE_TYPE_CHOICES,
        default="REQUIRED"
    )

    title = models.CharField(
        max_length=200
    )

    description = models.TextField()

    display_order = models.PositiveIntegerField(
        default=0
    )

    class Meta:
        ordering = ["display_order", "id"]

    def __str__(self):
        return f"{self.service.title} - {self.title}"


# =========================================================
# DOCUMENT REQUIREMENTS
# =========================================================

class DocumentRequirement(models.Model):

    service = models.ForeignKey(
        Service,
        on_delete=models.CASCADE,
        related_name="document_requirements"
    )

    name = models.CharField(
        max_length=200
    )

    description = models.TextField(
        blank=True
    )

    is_mandatory = models.BooleanField(
        default=True
    )

    display_order = models.PositiveIntegerField(
        default=0
    )

    class Meta:
        ordering = ["display_order", "name"]

    def __str__(self):
        return f"{self.service.title} - {self.name}"


# =========================================================
# PROCESS STEPS
# Example:
# 1. Eligibility check
# 2. Document collection
# 3. Application
# 4. Approval
# =========================================================

class ProcessStep(models.Model):

    service = models.ForeignKey(
        Service,
        on_delete=models.CASCADE,
        related_name="process_steps"
    )

    step_number = models.PositiveIntegerField()

    title = models.CharField(
        max_length=200
    )

    description = models.TextField(
        blank=True
    )

    estimated_time = models.CharField(
        max_length=120,
        blank=True
    )

    class Meta:
        ordering = ["step_number"]

        constraints = [
            models.UniqueConstraint(
                fields=["service", "step_number"],
                name="unique_step_number_per_service"
            )
        ]

    def __str__(self):
        return f"{self.service.title} - Step {self.step_number}"


# =========================================================
# SOURCES
# Official links / verification references
# =========================================================

class ServiceSource(models.Model):

    SOURCE_KIND_CHOICES = [
        ("OFFICIAL_PORTAL", "Official Portal"),
        ("APPLICATION", "Application Link"),
        ("FLYER", "Flyer"),
        ("REFERENCE", "Reference"),
        ("OTHER", "Other"),
    ]

    service = models.ForeignKey(
        Service,
        on_delete=models.CASCADE,
        related_name="sources"
    )

    source_name = models.CharField(
        max_length=255
    )

    source_url = models.URLField(
        max_length=1000,
        blank=True
    )

    source_kind = models.CharField(
        max_length=30,
        choices=SOURCE_KIND_CHOICES,
        default="REFERENCE",
        db_index=True
    )

    # Optional row-level provenance for imported workbook data.
    import_row = models.ForeignKey(
        "ImportRow",
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="service_sources"
    )

    source_reference = models.CharField(
        max_length=255,
        blank=True
    )

    is_official = models.BooleanField(
        default=False
    )

    last_checked_at = models.DateTimeField(
        null=True,
        blank=True
    )

    notes = models.TextField(
        blank=True
    )

    def __str__(self):
        return f"{self.service.title} - {self.source_name}"


# =========================================================
# RELATED SERVICES
#
# Example:
# Startup India → Udyam → GST → Funding Scheme
# =========================================================

class RelatedService(models.Model):

    RELATION_CHOICES = [
        ("RECOMMENDED", "Recommended"),
        ("PREREQUISITE", "Prerequisite"),
        ("ALTERNATIVE", "Alternative"),
        ("FOLLOW_UP", "Follow-up Service"),
    ]

    service = models.ForeignKey(
        Service,
        on_delete=models.CASCADE,
        related_name="related_from"
    )

    related_service = models.ForeignKey(
        Service,
        on_delete=models.CASCADE,
        related_name="related_to"
    )

    relation_type = models.CharField(
        max_length=20,
        choices=RELATION_CHOICES,
        default="RECOMMENDED"
    )

    notes = models.TextField(
        blank=True
    )

    class Meta:
        constraints = [
            models.UniqueConstraint(
                fields=[
                    "service",
                    "related_service",
                    "relation_type"
                ],
                name="unique_service_relationship"
            )
        ]

    def __str__(self):
        return (
            f"{self.service.title} → "
            f"{self.related_service.title}"
        )


# =========================================================
# SEARCH EVENT
# Structured search analytics for BDE dashboards.
# This is separate from ActivityLog because analytics
# should not depend on parsing human-readable audit text.
# =========================================================

class SearchEvent(models.Model):

    EVENT_CHOICES = [
        ("SEARCH", "Search"),
        ("SUGGESTION_CLICK", "Suggestion Click"),
        ("FILTER", "Filter"),
        ("CLIENT_MATCH", "Client Match"),
    ]

    user = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.CASCADE,
        related_name="toolkit_search_events"
    )

    event_type = models.CharField(
        max_length=30,
        choices=EVENT_CHOICES,
        default="SEARCH"
    )

    query = models.CharField(
        max_length=255,
        blank=True
    )

    filters = models.JSONField(
        default=dict,
        blank=True
    )

    result_count = models.PositiveIntegerField(
        default=0
    )

    selected_service = models.ForeignKey(
        Service,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="search_selections"
    )

    created_at = models.DateTimeField(
        auto_now_add=True,
        db_index=True
    )

    class Meta:
        ordering = ["-created_at"]

        indexes = [
            models.Index(
                fields=["user", "created_at"]
            ),
            models.Index(
                fields=["event_type", "created_at"]
            ),
        ]

    def __str__(self):
        return (
            f"{self.user.username} - "
            f"{self.event_type} - "
            f"{self.query}"
        )


# =========================================================
# SAVED SERVICE
# Personal starred/saved toolkit services for each BDE.
# =========================================================

class SavedService(models.Model):

    user = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.CASCADE,
        related_name="saved_toolkit_services"
    )

    service = models.ForeignKey(
        Service,
        on_delete=models.CASCADE,
        related_name="saved_by_users"
    )

    note = models.CharField(
        max_length=500,
        blank=True
    )

    created_at = models.DateTimeField(
        auto_now_add=True
    )

    class Meta:
        ordering = ["-created_at"]

        constraints = [
            models.UniqueConstraint(
                fields=["user", "service"],
                name="unique_saved_service_per_user"
            )
        ]

        indexes = [
            models.Index(
                fields=["user", "created_at"]
            ),
        ]

    def __str__(self):
        return (
            f"{self.user.username} → "
            f"{self.service.service_id}"
        )




# =========================================================
# BNX_SAVED_COLLECTIONS_V1
# CLIENT / SAVED COLLECTIONS
#
# SavedService remains the global bookmark owned by a BDE.
# A single SavedService can be placed into multiple client
# collections without duplicating the underlying bookmark.
# =========================================================

class SavedCollection(models.Model):

    user = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.CASCADE,
        related_name="toolkit_saved_collections",
    )

    name = models.CharField(
        max_length=150,
    )

    note = models.CharField(
        max_length=500,
        blank=True,
    )

    created_at = models.DateTimeField(
        auto_now_add=True,
    )

    updated_at = models.DateTimeField(
        auto_now=True,
    )

    class Meta:

        ordering = [
            "-updated_at",
            "name",
        ]

        constraints = [
            models.UniqueConstraint(
                fields=[
                    "user",
                    "name",
                ],
                name=(
                    "unique_saved_collection_"
                    "name_per_user"
                ),
            )
        ]

        indexes = [
            models.Index(
                fields=[
                    "user",
                    "updated_at",
                ]
            ),
        ]

    def __str__(self):

        return (
            f"{self.user.username} → "
            f"{self.name}"
        )


class SavedCollectionItem(models.Model):

    collection = models.ForeignKey(
        SavedCollection,
        on_delete=models.CASCADE,
        related_name="items",
    )

    saved_service = models.ForeignKey(
        SavedService,
        on_delete=models.CASCADE,
        related_name="collection_items",
    )

    created_at = models.DateTimeField(
        auto_now_add=True,
    )

    class Meta:

        ordering = [
            "-created_at",
        ]

        constraints = [
            models.UniqueConstraint(
                fields=[
                    "collection",
                    "saved_service",
                ],
                name=(
                    "unique_saved_service_"
                    "per_collection"
                ),
            )
        ]

        indexes = [
            models.Index(
                fields=[
                    "collection",
                    "created_at",
                ]
            ),
        ]

    def __str__(self):

        return (
            f"{self.collection.name} → "
            f"{self.saved_service.service.service_id}"
        )


# =========================================================
# IMPORT BATCH
# Metadata/audit history for Excel, CSV and Google Sheets.
#
# IMPORTANT:
# This stores import metadata, NOT the original uploaded
# confidential spreadsheet file.
# =========================================================

class ImportBatch(models.Model):

    SOURCE_CHOICES = [
        ("XLSX", "Excel"),
        ("CSV", "CSV"),
        ("GOOGLE_SHEET", "Google Sheet"),
    ]

    STATUS_CHOICES = [
        ("PREVIEWED", "Previewed"),
        ("VALIDATED", "Validated"),
        ("IMPORTING", "Importing"),
        ("IMPORTED", "Imported"),
        ("ROLLED_BACK", "Rolled Back"),
        ("FAILED", "Failed"),
    ]

    source_type = models.CharField(
        max_length=30,
        choices=SOURCE_CHOICES
    )

    source_name = models.CharField(
        max_length=255
    )

    source_identifier = models.CharField(
        max_length=500,
        blank=True
    )

    source_modified_at = models.DateTimeField(
        null=True,
        blank=True
    )

    file_sha256 = models.CharField(
        max_length=64,
        blank=True,
        db_index=True
    )

    sheet_count = models.PositiveIntegerField(
        default=0
    )

    row_count = models.PositiveIntegerField(
        default=0
    )

    status = models.CharField(
        max_length=30,
        choices=STATUS_CHOICES,
        default="PREVIEWED"
    )

    imported_by = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="toolkit_import_batches"
    )

    metadata = models.JSONField(
        default=dict,
        blank=True
    )

    created_at = models.DateTimeField(
        auto_now_add=True,
        db_index=True
    )

    imported_at = models.DateTimeField(
        null=True,
        blank=True
    )

    rolled_back_at = models.DateTimeField(
        null=True,
        blank=True
    )

    rolled_back_by = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="toolkit_rolled_back_import_batches"
    )

    class Meta:
        ordering = ["-created_at"]

        indexes = [
            models.Index(
                fields=["source_type", "created_at"]
            ),
            models.Index(
                fields=["status", "created_at"]
            ),
        ]

    def __str__(self):
        return (
            f"{self.source_type} - "
            f"{self.source_name} - "
            f"{self.created_at}"
        )


# ============================================================
# BHARATNXT WORKBOOK IMPORT ARCHITECTURE V1
# ============================================================


class ImportRow(models.Model):

    VALIDATION_STATUS_CHOICES = [
        ("PENDING", "Pending"),
        ("VALID", "Valid"),
        ("WARNING", "Warning"),
        ("INVALID", "Invalid"),
        ("PROCESSED", "Processed"),
    ]

    CANDIDATE_ACTION_CHOICES = [
        ("UNDECIDED", "Undecided"),
        ("CREATE", "Create"),
        ("UPDATE", "Update"),
        ("MERGE_REVIEW", "Merge Review"),
        ("SKIP", "Skip"),
        ("INVALID", "Invalid"),
    ]

    import_batch = models.ForeignKey(
        ImportBatch,
        on_delete=models.CASCADE,
        related_name="rows"
    )

    sheet_name = models.CharField(
        max_length=255,
        db_index=True
    )

    source_row_number = models.PositiveIntegerField()

    source_key = models.CharField(
        max_length=255,
        blank=True,
        db_index=True
    )

    row_hash = models.CharField(
        max_length=64,
        db_index=True
    )

    # JSON-safe representation of the source row.
    # Import code must convert dates/formulas/etc. to safe values.
    raw_data = models.JSONField(
        default=dict,
        blank=True
    )

    validation_status = models.CharField(
        max_length=20,
        choices=VALIDATION_STATUS_CHOICES,
        default="PENDING",
        db_index=True
    )

    validation_errors = models.JSONField(
        default=list,
        blank=True
    )

    validation_warnings = models.JSONField(
        default=list,
        blank=True
    )

    candidate_action = models.CharField(
        max_length=20,
        choices=CANDIDATE_ACTION_CHOICES,
        default="UNDECIDED",
        db_index=True
    )

    matched_service = models.ForeignKey(
        Service,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="matched_import_rows"
    )

    imported_service = models.ForeignKey(
        Service,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="imported_source_rows"
    )

    created_at = models.DateTimeField(
        auto_now_add=True
    )

    processed_at = models.DateTimeField(
        null=True,
        blank=True
    )

    # Explicit source-row accounting after the controlled workbook import.
    # One source row may legitimately produce multiple outcomes.
    import_outcomes = models.JSONField(
        default=list,
        blank=True
    )

    class Meta:
        ordering = [
            "import_batch",
            "sheet_name",
            "source_row_number",
        ]

        constraints = [
            models.UniqueConstraint(
                fields=[
                    "import_batch",
                    "sheet_name",
                    "source_row_number",
                ],
                name="unique_import_row_per_sheet"
            )
        ]

        indexes = [
            models.Index(
                fields=[
                    "import_batch",
                    "validation_status",
                ]
            ),
            models.Index(
                fields=[
                    "import_batch",
                    "candidate_action",
                ]
            ),
        ]

    def __str__(self):
        return (
            f"{self.import_batch_id} - "
            f"{self.sheet_name} - "
            f"row {self.source_row_number}"
        )


class ServiceClassification(models.Model):

    service = models.ForeignKey(
        Service,
        on_delete=models.CASCADE,
        related_name="classification_links"
    )

    category = models.ForeignKey(
        Category,
        on_delete=models.PROTECT,
        related_name="service_classification_links"
    )

    source_import_row = models.ForeignKey(
        ImportRow,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="created_classifications"
    )

    created_at = models.DateTimeField(
        auto_now_add=True
    )

    class Meta:
        constraints = [
            models.UniqueConstraint(
                fields=[
                    "service",
                    "category",
                ],
                name="unique_service_classification"
            )
        ]

    def __str__(self):
        return (
            f"{self.service.title} → "
            f"{self.category.name}"
        )


class ServiceCommercial(models.Model):

    VISIBILITY_CHOICES = [
        ("ADMIN_ONLY", "Admin Only"),
        ("BDE_ALLOWED", "BDE Allowed"),
    ]

    service = models.ForeignKey(
        Service,
        on_delete=models.CASCADE,
        related_name="commercial_terms"
    )

    label = models.CharField(
        max_length=255,
        blank=True
    )

    minimum_charge_raw = models.TextField(
        blank=True
    )

    minimum_charge = models.DecimalField(
        max_digits=14,
        decimal_places=2,
        null=True,
        blank=True
    )

    government_fee_raw = models.TextField(
        blank=True
    )

    government_fee = models.DecimalField(
        max_digits=14,
        decimal_places=2,
        null=True,
        blank=True
    )

    vendor_cost_raw = models.TextField(
        blank=True
    )

    vendor_cost = models.DecimalField(
        max_digits=14,
        decimal_places=2,
        null=True,
        blank=True
    )

    bdm_deduction_raw = models.TextField(
        blank=True
    )

    bdm_deduction = models.DecimalField(
        max_digits=14,
        decimal_places=2,
        null=True,
        blank=True
    )

    remarks = models.TextField(
        blank=True
    )

    visibility = models.CharField(
        max_length=20,
        choices=VISIBILITY_CHOICES,
        default="ADMIN_ONLY",
        db_index=True
    )

    source_import_row = models.ForeignKey(
        ImportRow,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="commercial_terms"
    )

    is_active = models.BooleanField(
        default=True
    )

    created_at = models.DateTimeField(
        auto_now_add=True
    )

    updated_at = models.DateTimeField(
        auto_now=True
    )

    def __str__(self):
        return (
            f"{self.service.title} - "
            f"{self.label or 'Commercial'}"
        )


class ServiceContentSection(models.Model):

    SECTION_TYPE_CHOICES = [
        ("OVERVIEW", "Overview"),
        ("BENEFITS", "Benefits"),
        ("ELIGIBILITY", "Eligibility"),
        ("FUNDING", "Funding"),
        ("SCOPE", "Scope of Work"),
        ("PROCESS", "Process"),
        ("DOCUMENTS", "Documents"),
        ("TIMELINE", "Timeline"),
        ("NOTES", "Notes"),
        ("GLOSSARY", "Glossary"),
        ("COMMERCIAL", "Commercial"),
        ("OTHER", "Other"),
    ]

    VISIBILITY_CHOICES = [
        ("ADMIN_ONLY", "Admin Only"),
        ("BDE", "BDE"),
    ]

    service = models.ForeignKey(
        Service,
        on_delete=models.CASCADE,
        related_name="content_sections"
    )

    section_type = models.CharField(
        max_length=20,
        choices=SECTION_TYPE_CHOICES,
        default="OTHER",
        db_index=True
    )

    title = models.CharField(
        max_length=255,
        blank=True
    )

    content = models.TextField()

    display_order = models.PositiveIntegerField(
        default=0
    )

    visibility = models.CharField(
        max_length=20,
        choices=VISIBILITY_CHOICES,
        default="ADMIN_ONLY",
        db_index=True
    )

    source_import_row = models.ForeignKey(
        ImportRow,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="content_sections"
    )

    created_at = models.DateTimeField(
        auto_now_add=True
    )

    updated_at = models.DateTimeField(
        auto_now=True
    )

    class Meta:
        ordering = [
            "display_order",
            "id",
        ]

    def __str__(self):
        return (
            f"{self.service.title} - "
            f"{self.title or self.section_type}"
        )


class ImportChange(models.Model):

    ACTION_CHOICES = [
        ("SERVICE_CREATE", "Service Create"),
        ("SERVICE_UPDATE", "Service Update"),
        ("CLASSIFICATION_ADD", "Classification Add"),
        ("SOURCE_ADD", "Source Add"),
        ("COMMERCIAL_CREATE", "Commercial Create"),
        ("COMMERCIAL_UPDATE", "Commercial Update"),
        ("CONTENT_CREATE", "Content Create"),
        ("REFERENCE_CREATE", "Reference Create"),
        ("OTHER", "Other"),
    ]

    import_batch = models.ForeignKey(
        ImportBatch,
        on_delete=models.PROTECT,
        related_name="changes"
    )

    import_row = models.ForeignKey(
        ImportRow,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="changes"
    )

    service = models.ForeignKey(
        Service,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="import_changes"
    )

    action = models.CharField(
        max_length=30,
        choices=ACTION_CHOICES,
        db_index=True
    )

    # Generic destination identity makes every created object reversible,
    # including Categories, comparisons, knowledge and communications.
    object_model = models.CharField(
        max_length=120,
        blank=True
    )

    object_pk = models.CharField(
        max_length=80,
        blank=True
    )

    before_snapshot = models.JSONField(
        default=dict,
        blank=True
    )

    after_snapshot = models.JSONField(
        default=dict,
        blank=True
    )

    is_reversed = models.BooleanField(
        default=False,
        db_index=True
    )

    created_at = models.DateTimeField(
        auto_now_add=True
    )

    reversed_at = models.DateTimeField(
        null=True,
        blank=True
    )

    class Meta:
        ordering = [
            "created_at",
            "id",
        ]

    def __str__(self):
        return (
            f"Batch {self.import_batch_id} - "
            f"{self.action}"
        )


class ReferenceItem(models.Model):

    VISIBILITY_CHOICES = [
        ("ADMIN_ONLY", "Admin Only"),
        ("BDE", "BDE"),
    ]

    dataset_name = models.CharField(
        max_length=150,
        db_index=True
    )

    key = models.CharField(
        max_length=255,
        blank=True
    )

    value = models.TextField()

    metadata = models.JSONField(
        default=dict,
        blank=True
    )

    visibility = models.CharField(
        max_length=20,
        choices=VISIBILITY_CHOICES,
        default="ADMIN_ONLY",
        db_index=True
    )

    source_import_row = models.ForeignKey(
        ImportRow,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="reference_items"
    )

    created_at = models.DateTimeField(
        auto_now_add=True
    )

    def __str__(self):
        return (
            f"{self.dataset_name} - "
            f"{self.key or self.pk}"
        )


class ComparisonMatrix(models.Model):

    name = models.CharField(
        max_length=255
    )

    source_sheet = models.CharField(
        max_length=255,
        blank=True
    )

    import_batch = models.ForeignKey(
        ImportBatch,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="comparison_matrices"
    )

    metadata = models.JSONField(
        default=dict,
        blank=True
    )

    created_at = models.DateTimeField(
        auto_now_add=True
    )

    updated_at = models.DateTimeField(
        auto_now=True
    )

    def __str__(self):
        return self.name


class ComparisonEntry(models.Model):

    matrix = models.ForeignKey(
        ComparisonMatrix,
        on_delete=models.CASCADE,
        related_name="entries"
    )

    row_number = models.PositiveIntegerField()

    column_name = models.CharField(
        max_length=255
    )

    row_label = models.CharField(
        max_length=255,
        blank=True
    )

    value_raw = models.TextField(
        blank=True
    )

    service = models.ForeignKey(
        Service,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="comparison_entries"
    )

    source_import_row = models.ForeignKey(
        ImportRow,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="comparison_entries"
    )

    class Meta:
        ordering = [
            "row_number",
            "id",
        ]

        constraints = [
            models.UniqueConstraint(
                fields=[
                    "matrix",
                    "row_number",
                    "column_name",
                ],
                name="unique_comparison_matrix_cell"
            )
        ]

    def __str__(self):
        return (
            f"{self.matrix.name} - "
            f"{self.row_number}:{self.column_name}"
        )


class CommunicationTemplate(models.Model):

    STATUS_CHOICES = [
        ("DRAFT", "Draft"),
        ("ACTIVE", "Active"),
        ("ARCHIVED", "Archived"),
    ]

    VISIBILITY_CHOICES = [
        ("ADMIN_ONLY", "Admin Only"),
        ("BDE", "BDE"),
    ]

    template_key = models.CharField(
        max_length=120,
        unique=True
    )

    title = models.CharField(
        max_length=255
    )

    subject = models.CharField(
        max_length=255,
        blank=True
    )

    body = models.TextField()

    status = models.CharField(
        max_length=20,
        choices=STATUS_CHOICES,
        default="DRAFT",
        db_index=True
    )

    visibility = models.CharField(
        max_length=20,
        choices=VISIBILITY_CHOICES,
        default="ADMIN_ONLY",
        db_index=True
    )

    source_import_batch = models.ForeignKey(
        ImportBatch,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="communication_templates"
    )

    source_sheet = models.CharField(
        max_length=255,
        blank=True
    )

    created_at = models.DateTimeField(
        auto_now_add=True
    )

    updated_at = models.DateTimeField(
        auto_now=True
    )

    def __str__(self):
        return self.title



# ============================================================
# KNOWLEDGE DOCUMENTS
# Dedicated storage for source knowledge that is not safely attributable
# to one Service. Service linkage is deliberately optional.
# ============================================================

class KnowledgeDocument(models.Model):

    STATUS_CHOICES = [
        ("DRAFT", "Draft"),
        ("ACTIVE", "Active"),
        ("ARCHIVED", "Archived"),
    ]

    VISIBILITY_CHOICES = [
        ("ADMIN_ONLY", "Admin Only"),
        ("BDE", "BDE"),
    ]

    document_key = models.CharField(
        max_length=180,
        unique=True
    )

    title = models.CharField(
        max_length=255
    )

    source_sheet = models.CharField(
        max_length=255,
        db_index=True
    )

    source_import_batch = models.ForeignKey(
        ImportBatch,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="knowledge_documents"
    )

    status = models.CharField(
        max_length=20,
        choices=STATUS_CHOICES,
        default="DRAFT",
        db_index=True
    )

    visibility = models.CharField(
        max_length=20,
        choices=VISIBILITY_CHOICES,
        default="ADMIN_ONLY",
        db_index=True
    )

    metadata = models.JSONField(
        default=dict,
        blank=True
    )

    created_at = models.DateTimeField(
        auto_now_add=True
    )

    updated_at = models.DateTimeField(
        auto_now=True
    )

    class Meta:
        ordering = ["title", "id"]

    def __str__(self):
        return self.title


class KnowledgeSection(models.Model):

    SECTION_TYPE_CHOICES = [
        ("OVERVIEW", "Overview"),
        ("BENEFITS", "Benefits"),
        ("ELIGIBILITY", "Eligibility"),
        ("FUNDING", "Funding"),
        ("SCOPE", "Scope of Work"),
        ("PROCESS", "Process"),
        ("DOCUMENTS", "Documents"),
        ("TIMELINE", "Timeline"),
        ("NOTES", "Notes"),
        ("GLOSSARY", "Glossary"),
        ("COMMERCIAL", "Commercial"),
        ("OTHER", "Other"),
    ]

    VISIBILITY_CHOICES = [
        ("ADMIN_ONLY", "Admin Only"),
        ("BDE", "BDE"),
    ]

    document = models.ForeignKey(
        KnowledgeDocument,
        on_delete=models.CASCADE,
        related_name="sections"
    )

    linked_service = models.ForeignKey(
        Service,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="knowledge_sections"
    )

    section_type = models.CharField(
        max_length=20,
        choices=SECTION_TYPE_CHOICES,
        default="OTHER",
        db_index=True
    )

    title = models.CharField(
        max_length=255,
        blank=True
    )

    content = models.TextField()

    display_order = models.PositiveIntegerField(
        default=0
    )

    is_heading = models.BooleanField(
        default=False
    )

    visibility = models.CharField(
        max_length=20,
        choices=VISIBILITY_CHOICES,
        default="ADMIN_ONLY",
        db_index=True
    )

    source_import_row = models.OneToOneField(
        ImportRow,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="knowledge_section"
    )

    created_at = models.DateTimeField(
        auto_now_add=True
    )

    updated_at = models.DateTimeField(
        auto_now=True
    )

    class Meta:
        ordering = ["document", "display_order", "id"]
        constraints = [
            models.UniqueConstraint(
                fields=["document", "display_order"],
                name="unique_knowledge_section_order_per_document"
            )
        ]

    def __str__(self):
        return (
            f"{self.document.title} - "
            f"{self.title or self.section_type}"
        )


====================================================================================================
FILE: toolkit/templates/toolkit/client_matcher.html
LINES: 1157
SHA256: 9904a7c3b54de1e94445d41303f3c8f1ec2045c18b75faf54c08072a4b83f896
====================================================================================================
{% extends "base.html" %}
{% load static %}

{% block title %}Client Service Checker | BharatNXT Wave{% endblock %}

{% block content %}

<div class="client-matcher-page">


<!-- =====================================================
     PAGE HEADER
====================================================== -->

<div class="page-eyebrow">
    CLIENT SERVICE FINDER
</div>

<h1 class="page-title">
    Find Services for a Client
</h1>

<p class="page-description matcher-v2-description">
    Enter what the client is looking for and the company details you know. The Toolkit will show services worth discussing.
</p>


<div class="matcher-v2-guide">

    <div>
        <span>1</span>
        <strong>Enter client requirement</strong>
    </div>

    <div>
        <span>2</span>
        <strong>Add company details</strong>
    </div>

    <div>
        <span>3</span>
        <strong>Review suitable services</strong>
    </div>

</div>



<!-- =====================================================
     CLIENT MATCH FORM
====================================================== -->

<section
    class="card matcher-v2-card"
    id="client-match-form">

    <form
        method="post"
        autocomplete="off">

        {% csrf_token %}


        <!-- STEP 1 -->

        <div class="matcher-v2-step">

            <div class="matcher-v2-step-number">
                1
            </div>


            <div class="matcher-v2-step-content">

                <h2>
                    What is the client looking for?
                </h2>

                <p>
                    Type naturally. The client does not need to know the exact scheme or service name.
                </p>


                <div class="matcher-v2-main-search">
                    {{ form.need_query }}
                </div>


                <div class="matcher-v2-examples">

                    <span class="matcher-example-label">
                        Examples
                    </span>

                    <span>Technology schemes</span>
                    <span>Startup funding</span>
                    <span>MSME loan</span>
                    <span>Startup India</span>

                </div>

            </div>

        </div>


        <div class="matcher-v2-divider"></div>


        <!-- STEP 2 -->

        <div class="matcher-v2-step">

            <div class="matcher-v2-step-number">
                2
            </div>


            <div class="matcher-v2-step-content">

                <h2>
                    What do you know about the company?
                </h2>

                <p>
                    Fill only what the client tells you. More information can improve eligibility checking.
                </p>


                <div class="matcher-v2-basic-grid">


                    <div class="matcher-v2-field">

                        <label>
                            Industry / Sector
                        </label>

                        {{ form.industry }}

                    </div>


                    <div class="matcher-v2-field">

                        <label>
                            Business Stage
                        </label>

                        {{ form.business_stage }}

                    </div>


                    <div class="matcher-v2-field">

                        <label>
                            State
                        </label>

                        {{ form.state }}

                    </div>


                    <div class="matcher-v2-field">

                        <label>
                            Business Type
                        </label>

                        {{ form.business_type }}

                    </div>

                </div>



                <!-- OPTIONAL ELIGIBILITY -->

                <details class="matcher-v2-advanced">

                    <summary>

                        <span>
                            + Add more eligibility details
                        </span>

                        <small>
                            Optional
                        </small>

                    </summary>


                    <div class="matcher-v2-advanced-content">


                        <div class="matcher-v2-field">

                            <label>
                                Founder Category
                            </label>

                            {{ form.founder_category }}

                        </div>


                        <div class="matcher-v2-field">

                            <label>
                                Business Age
                            </label>


                            <div class="matcher-v2-input-suffix">

                                {{ form.business_age_years }}

                                <span>
                                    Years
                                </span>

                            </div>

                        </div>


                        <div class="matcher-v2-field">

                            <label>
                                Annual Turnover
                            </label>


                            <div class="matcher-v2-turnover">

                                {{ form.turnover_amount }}

                                {{ form.turnover_unit }}

                            </div>

                        </div>


                        <div class="matcher-v2-service-types">

                            <label>
                                Limit results to service types
                            </label>

                            <div class="matcher-v2-kind-grid">
                                {{ form.service_kinds }}
                            </div>

                        </div>

                    </div>

                </details>

            </div>

        </div>



        {% if form.errors %}

            <div class="matcher-v2-error">
                Please check the information above and try again.
            </div>

        {% endif %}



        <!-- ACTION -->

        <div class="matcher-v2-action">

            <div class="matcher-v2-privacy">

                <span>◇</span>

                <div>

                    <strong>
                        Client profile isn't saved
                    </strong>

                    <small>
                        The supplied details are used only for this matching request.
                    </small>

                </div>

            </div>


            <button
                type="submit"
                class="primary-button matcher-v2-submit">

                Find Matching Services →

            </button>

        </div>

    </form>

</section>



{% if submitted %}


<!-- =====================================================
     RESULT CONTEXT
====================================================== -->

<section
    class="matcher-result-context"
    id="match-results">

    <div>

        <div class="page-eyebrow">
            MATCH RESULTS
        </div>


        <h2>
            Services worth discussing with the client
        </h2>


        <div class="matcher-context-query">

            <span>
                MATCHING FOR
            </span>

            <strong class="matcher-query-chip">

                {% if form.need_query.value %}

                    {{ form.need_query.value }}

                {% else %}

                    Client company profile

                {% endif %}

            </strong>

        </div>


        <div class="matcher-context-details">

            {% if form.industry.value %}
                <span>
                    Industry: {{ form.industry.value }}
                </span>
            {% endif %}

            {% if form.business_stage.value %}
                <span>
                    Stage: {{ form.business_stage.value }}
                </span>
            {% endif %}

            {% if form.state.value %}
                <span>
                    State: {{ form.state.value }}
                </span>
            {% endif %}

            {% if form.business_type.value %}
                <span>
                    Type: {{ form.business_type.value }}
                </span>
            {% endif %}

        </div>

    </div>


    <div class="matcher-result-actions">

        <a
            href="#client-match-form"
            class="secondary-button">

            Edit Client Details

        </a>


        <a
            href="{% url 'toolkit:client_matcher' %}"
            class="primary-button">

            New Client Service Check

        </a>

    </div>

</section>



<!-- =====================================================
     NEXT ACTION
====================================================== -->

<div class="matcher-next-action">

    {% if results.high_count %}

        <div class="matcher-next-action-icon strong">
            ✓
        </div>

        <div>

            <strong>
                {{ results.high_count }}
                strong match{{ results.high_count|pluralize:"es" }}
                found
            </strong>

            <span>
                Start with these services during the client conversation.
            </span>

        </div>

    {% elif results.possible_count %}

        <div class="matcher-next-action-icon verify">
            !
        </div>

        <div>

            <strong>
                {{ results.possible_count }}
                service{{ results.possible_count|pluralize }}
                need{{ results.possible_count|pluralize:"s," }}
                verification
            </strong>

            <span>
                Review the missing eligibility information before advising the client.
            </span>

        </div>

    {% elif results.not_eligible_count %}

        <div class="matcher-next-action-icon rejected">
            ×
        </div>

        <div>

            <strong>
                No currently eligible service found
            </strong>

            <span>
                Review the conflicting eligibility criteria below.
            </span>

        </div>

    {% else %}

        <div class="matcher-next-action-icon neutral">
            ⌕
        </div>

        <div>

            <strong>
                No matching Toolkit service found
            </strong>

            <span>
                Try a broader search or edit the client details.
            </span>

        </div>

    {% endif %}

</div>


<!-- =====================================================
     RESULT SUMMARY
====================================================== -->

<div class="matcher-summary-grid">


    <div class="card matcher-summary-card high">

        <div class="stat-label">
            STRONG MATCH
        </div>

        <div class="search-kpi-value">
            {{ results.high_count }}
        </div>

        <div class="search-kpi-meta">
            Eligibility can be supported by Toolkit data
        </div>

    </div>


    <div class="card matcher-summary-card possible">

        <div class="stat-label">
            NEEDS VERIFICATION
        </div>

        <div class="search-kpi-value">
            {{ results.possible_count }}
        </div>

        <div class="search-kpi-meta">
            Relevant, but eligibility needs checking
        </div>

    </div>


    <div class="card matcher-summary-card rejected">

        <div class="stat-label">
            DOESN'T FIT
        </div>

        <div class="search-kpi-value">
            {{ results.not_eligible_count }}
        </div>

        <div class="search-kpi-meta">
            Current Toolkit criteria conflict
        </div>

    </div>

</div>



<!-- =====================================================
     RESULT TOOLS
====================================================== -->

{% if results.high_count or results.possible_count %}

<div class="matcher-result-toolbar">

    <div>

        <strong>
            Recommendations
        </strong>

        <span>
            Ranked using actual Toolkit information
        </span>

    </div>


    <label class="matcher-sort-control">

        <span>
            SORT
        </span>

        <select id="matcher-result-sort">

            <option value="best">
                Best Match
            </option>

            <option value="type">
                Service Type
            </option>

            <option value="verified">
                Recently Verified
            </option>

        </select>

    </label>

</div>

{% endif %}


<!-- =====================================================
     STRONG MATCHES
     EMPTY SECTIONS ARE NOT SHOWN
====================================================== -->

{% if results.high_count %}

<section class="matcher-result-section">

    <div class="matcher-result-heading">

        <div>

            <div class="matcher-result-title">
                Strong Matches
            </div>

            <div class="table-subtitle">
                Best services to discuss first
            </div>

        </div>


        <span class="matcher-count-badge high">
            {{ results.high_count }}
        </span>

    </div>


    <div class="matcher-results-grid {% if results.high_count == 1 %}single-result{% endif %}">

        {% for item in results.high %}

            <article
                class="card matcher-result-card high"
                data-kind="{{ item.service.get_service_kind_display }}"
                data-verified="{% if item.service.last_verified_at %}{{ item.service.last_verified_at|date:'YmdHis' }}{% else %}0{% endif %}">


                <div class="matcher-card-header">

                    <div>

                        <div class="toolkit-service-id">
                            {{ item.service.service_id }}
                        </div>

                        <a
                            href="{% url 'toolkit:service_detail' item.service.slug %}"
                            class="matcher-service-title">

                            {{ item.service.title }}

                        </a>

                    </div>


                    <span class="matcher-status high">
                        STRONG MATCH
                    </span>

                </div>



                <div class="matcher-service-meta">

                    <span>
                        {{ item.service.domain.name }}
                    </span>

                    <span>•</span>

                    <span>
                        {{ item.service.category.name }}
                    </span>

                    <span>•</span>

                    <span>
                        {{ item.service.get_service_kind_display }}
                    </span>

                </div>



                {% if item.service.bde_summary %}

                    <p class="matcher-summary-copy">
                        {{ item.service.bde_summary }}
                    </p>

                {% endif %}



                <div class="matcher-call-section">

                    <div class="matcher-call-label">
                        WHY WE'RE SHOWING THIS
                    </div>


                    {% if form.need_query.value %}

                        <div class="matcher-reason positive">
                            ✓ Relevant to your search: {{ form.need_query.value }}
                        </div>

                    {% endif %}


                    {% for reason in item.matches %}

                        <div class="matcher-reason positive">
                            ✓ {{ reason.reason }}
                        </div>

                    {% endfor %}

                </div>



                <div class="matcher-card-actions">


                    <form
                        method="post"
                        action="{% url 'toolkit:toggle_saved_service' item.service.pk %}"
                        class="matcher-save-form">

                        {% csrf_token %}

                        <button
                            type="submit"
                            class="matcher-save-button {% if item.service.pk in saved_service_ids %}saved{% endif %}">

                            {% if item.service.pk in saved_service_ids %}
                                <span class="bn-bookmark-icon is-saved" aria-hidden="true"></span> Saved
                            {% else %}
                                <span class="bn-bookmark-icon" aria-hidden="true"></span> Save
                            {% endif %}

                        </button>

                    </form>


                    <button
                        type="button"
                        class="secondary-button bde-quick-button"
                        data-service-quick="{{ item.service.pk }}">
                        Quick View
                    </button>

                    <a
                        href="{% url 'toolkit:service_detail' item.service.slug %}"
                        class="primary-button matcher-open-service">

                        Open Service Details →

                    </a>

                </div>

            </article>

        {% endfor %}

    </div>

</section>

{% endif %}



<!-- =====================================================
     NEEDS VERIFICATION
====================================================== -->

{% if results.possible_count %}

<section class="matcher-result-section">

    <div class="matcher-result-heading">

        <div>

            <div class="matcher-result-title">
                Needs Verification
            </div>

            <div class="table-subtitle">
                Relevant services where some eligibility information still needs checking
            </div>

        </div>


        <span class="matcher-count-badge possible">
            {{ results.possible_count }}
        </span>

    </div>



    <div class="matcher-results-grid {% if results.possible_count == 1 %}single-result{% endif %}">

        {% for item in results.possible %}

            <article
                class="card matcher-result-card possible"
                data-kind="{{ item.service.get_service_kind_display }}"
                data-verified="{% if item.service.last_verified_at %}{{ item.service.last_verified_at|date:'YmdHis' }}{% else %}0{% endif %}">


                <div class="matcher-card-header">

                    <div>

                        <div class="toolkit-service-id">
                            {{ item.service.service_id }}
                        </div>

                        <a
                            href="{% url 'toolkit:service_detail' item.service.slug %}"
                            class="matcher-service-title">

                            {{ item.service.title }}

                        </a>

                    </div>


                    <span class="matcher-status possible">
                        VERIFY
                    </span>

                </div>



                <div class="matcher-service-meta">

                    <span>
                        {{ item.service.domain.name }}
                    </span>

                    <span>•</span>

                    <span>
                        {{ item.service.category.name }}
                    </span>

                    <span>•</span>

                    <span>
                        {{ item.service.get_service_kind_display }}
                    </span>

                </div>



                {% if item.service.bde_summary %}

                    <p class="matcher-summary-copy">
                        {{ item.service.bde_summary }}
                    </p>

                {% endif %}



                <div class="matcher-call-section">

                    <div class="matcher-call-label">
                        WHY WE'RE SHOWING THIS
                    </div>


                    {% if form.need_query.value %}

                        <div class="matcher-reason positive">
                            ✓ Relevant to your search: {{ form.need_query.value }}
                        </div>

                    {% endif %}


                    {% for reason in item.matches %}

                        <div class="matcher-reason positive">
                            ✓ {{ reason.reason }}
                        </div>

                    {% endfor %}

                </div>



                <div class="matcher-call-section verify">

                    <div class="matcher-call-label">
                        WHAT TO VERIFY
                    </div>


                    {% for reason in item.unknowns %}

                        <div class="matcher-reason warning">
                            ! {{ reason.reason }}
                        </div>

                    {% empty %}

                        <div class="matcher-reason warning">
                            ! Ask the client a few eligibility questions before recommending this service.
                        </div>

                    {% endfor %}

                </div>



                <div class="matcher-card-actions">


                    <form
                        method="post"
                        action="{% url 'toolkit:toggle_saved_service' item.service.pk %}"
                        class="matcher-save-form">

                        {% csrf_token %}

                        <button
                            type="submit"
                            class="matcher-save-button {% if item.service.pk in saved_service_ids %}saved{% endif %}">

                            {% if item.service.pk in saved_service_ids %}
                                <span class="bn-bookmark-icon is-saved" aria-hidden="true"></span> Saved
                            {% else %}
                                <span class="bn-bookmark-icon" aria-hidden="true"></span> Save
                            {% endif %}

                        </button>

                    </form>


                    <button
                        type="button"
                        class="secondary-button bde-quick-button"
                        data-service-quick="{{ item.service.pk }}">
                        Quick View
                    </button>

                    <a
                        href="{% url 'toolkit:service_detail' item.service.slug %}"
                        class="primary-button matcher-open-service">

                        Check Eligibility & Details →

                    </a>

                </div>

            </article>

        {% endfor %}

    </div>

</section>

{% endif %}



<!-- =====================================================
     DOESN'T FIT
====================================================== -->

{% if results.not_eligible_count %}

<details class="matcher-rejected-section">

    <summary>

        <span>
            Doesn't fit current eligibility
        </span>

        <strong>
            {{ results.not_eligible_count }}
        </strong>

    </summary>


    <div class="matcher-results-grid rejected">

        {% for item in results.not_eligible %}

            <article class="card matcher-result-card rejected">


                <div class="matcher-card-header">

                    <div>

                        <div class="toolkit-service-id">
                            {{ item.service.service_id }}
                        </div>

                        <a
                            href="{% url 'toolkit:service_detail' item.service.slug %}"
                            class="matcher-service-title">

                            {{ item.service.title }}

                        </a>

                    </div>


                    <span class="matcher-status rejected">
                        DOESN'T FIT
                    </span>

                </div>



                <div class="matcher-call-section verify">

                    <div class="matcher-call-label">
                        WHY IT DOESN'T FIT
                    </div>


                    {% for reason in item.mismatches %}

                        <div class="matcher-reason negative">
                            × {{ reason.reason }}
                        </div>

                    {% endfor %}

                </div>



                <div class="matcher-card-actions">

                    <button
                        type="button"
                        class="secondary-button bde-quick-button"
                        data-service-quick="{{ item.service.pk }}">
                        Quick View
                    </button>

                    <a
                        href="{% url 'toolkit:service_detail' item.service.slug %}"
                        class="secondary-button">

                        Review Service

                    </a>

                </div>

            </article>

        {% endfor %}

    </div>

</details>

{% endif %}



<!-- =====================================================
     NOTHING RELEVANT
====================================================== -->

{% if not results.high_count and not results.possible_count and not results.not_eligible_count %}

<section class="card matcher-no-results">

    <div class="matcher-no-results-icon">
        ⌕
    </div>

    <h3>
        No matching Toolkit service found
    </h3>

    <p>
        Try a broader description or remove some company criteria. The system will not invent a scheme that does not exist in the Toolkit.
    </p>

    <a
        href="#client-match-form"
        class="secondary-button">

        Edit Search

    </a>

</section>

{% endif %}


{% endif %}


</div>


<script src="{% static 'js/matcher_saved.js' %}"></script>
<script src="{% static 'js/matcher_sort.js' %}"></script>

{% endblock %}


====================================================================================================
FILE: toolkit/templates/toolkit/saved_collection_detail.html
LINES: 391
SHA256: bfba934252dae67e3de5622b2cd23a5c973b95cfec72d004e430e77a10d4e920
====================================================================================================
{% extends "base.html" %}

{% block title %}
{{ collection.name }} | Saved Services
{% endblock %}

{% block content %}

<div class="bnx-collection-detail-page">

    <a
        href="{% url 'toolkit:saved_services' %}"
        class="bnx-back-link">

        ← Client Collections

    </a>


    <header class="bnx-collection-detail-hero">

        <div class="bnx-collection-detail-avatar">

            {{ collection.name|slice:":1"|upper }}

        </div>


        <div class="bnx-collection-detail-copy">

            <div class="bde-eyebrow">
                CLIENT COLLECTION
            </div>

            <h1>
                {{ collection.name }}
            </h1>

            {% if collection.note %}

                <p>
                    {{ collection.note }}
                </p>

            {% else %}

                <p>
                    Saved services for this client.
                </p>

            {% endif %}


            <div class="bnx-detail-meta">

                <span>
                    {{ service_count }}
                    saved service{{ service_count|pluralize }}
                </span>

                <span>
                    Updated
                    {{ collection.updated_at|timesince }}
                    ago
                </span>

            </div>

        </div>


        <div class="bnx-detail-actions">

            <button
                type="button"
                class="secondary-button"
                data-bnx-edit-collection>

                Edit

            </button>

            <a
                href="{% url 'toolkit:home' %}"
                class="primary-button">

                + Find services

            </a>

        </div>

    </header>


    <section class="bnx-collection-service-list">

        {% for item in items %}

            <article class="bnx-client-service-card">

                <div class="bnx-client-service-copy">

                    <div class="bnx-client-service-topline">

                        <span>
                            {{ item.saved_service.service.service_id }}
                        </span>

                        <span class="bnx-service-kind-pill">
                            {{ item.saved_service.service.get_service_kind_display }}
                        </span>

                    </div>


                    <h2>
                        {{ item.saved_service.service.title }}
                    </h2>


                    <p class="bnx-client-service-path">

                        {{ item.saved_service.service.domain.name }}
                        ·
                        {{ item.saved_service.service.category.name }}

                    </p>


                    {% if item.saved_service.service.bde_summary %}

                        <p class="bnx-client-service-summary">

                            {{ item.saved_service.service.bde_summary|truncatechars:210 }}

                        </p>

                    {% endif %}

                </div>


                <div class="bnx-client-service-actions">

                    <a
                        href="{% url 'toolkit:service_detail' item.saved_service.service.slug %}"
                        class="primary-button">

                        Details

                    </a>


                    <form
                        method="post"
                        action="{% url 'toolkit:saved_collection_remove_item' collection.pk item.pk %}">

                        {% csrf_token %}

                        <button
                            type="submit"
                            class="bnx-remove-from-collection">

                            Remove

                        </button>

                    </form>

                </div>

            </article>

        {% empty %}

            <div class="bnx-client-collection-empty">

                <div>⌑</div>

                <h3>
                    No services in this collection yet
                </h3>

                <p>
                    Search the Toolkit and use the bookmark
                    button to add relevant services for
                    {{ collection.name }}.
                </p>

                <a
                    href="{% url 'toolkit:home' %}"
                    class="primary-button">

                    Find services

                </a>

            </div>

        {% endfor %}

    </section>

</div>


<div
    class="bnx-create-collection-modal"
    data-bnx-edit-modal
    hidden>

    <div
        class="bnx-modal-backdrop"
        data-bnx-close-edit>
    </div>

    <div
        class="bnx-create-collection-dialog"
        role="dialog"
        aria-modal="true">

        <div class="bnx-modal-handle"></div>

        <header>

            <div>

                <small>
                    COLLECTION SETTINGS
                </small>

                <h2>
                    Edit {{ collection.name }}
                </h2>

            </div>

            <button
                type="button"
                class="bnx-modal-close"
                data-bnx-close-edit>

                ×

            </button>

        </header>


        <form
            method="post"
            action="{% url 'toolkit:saved_collection_rename' collection.pk %}">

            {% csrf_token %}

            <label>
                Collection name

                <input
                    type="text"
                    name="name"
                    value="{{ collection.name }}"
                    maxlength="150"
                    required>
            </label>


            <label>
                Note

                <input
                    type="text"
                    name="note"
                    value="{{ collection.note }}"
                    maxlength="500">
            </label>


            <div class="bnx-modal-actions">

                <button
                    type="button"
                    class="secondary-button"
                    data-bnx-close-edit>

                    Cancel

                </button>

                <button
                    type="submit"
                    class="primary-button">

                    Save changes

                </button>

            </div>

        </form>


        <form
            method="post"
            action="{% url 'toolkit:saved_collection_delete' collection.pk %}"
            class="bnx-delete-collection-form"
            onsubmit="return confirm('Delete this collection? Saved services themselves will remain available under Unsorted.');">

            {% csrf_token %}

            <button type="submit">
                Delete collection
            </button>

        </form>

    </div>

</div>


<script>
(function () {

    "use strict";

    const modal =
        document.querySelector(
            "[data-bnx-edit-modal]"
        );


    const open =
        document.querySelector(
            "[data-bnx-edit-collection]"
        );


    if (!modal || !open) {
        return;
    }


    function show() {

        modal.hidden = false;

        document.body.classList.add(
            "bnx-modal-open"
        );

    }


    function hide() {

        modal.hidden = true;

        document.body.classList.remove(
            "bnx-modal-open"
        );

    }


    open.addEventListener(
        "click",
        show
    );


    modal
        .querySelectorAll(
            "[data-bnx-close-edit]"
        )
        .forEach(
            function (button) {

                button.addEventListener(
                    "click",
                    hide
                );

            }
        );

})();
</script>

{% endblock %}


====================================================================================================
FILE: toolkit/templates/toolkit/saved_services.html
LINES: 473
SHA256: 0fc7508a96574a96a255d1cbf421902335e4a2c1ee50eff98d6b5b06aebc5705
====================================================================================================
{% extends "base.html" %}

{% block title %}
Saved Services | BharatNXT Wave
{% endblock %}

{% block content %}

<div class="bnx-collections-page">

    <header class="bnx-collections-hero">

        <div>

            <div class="bde-eyebrow">
                SAVED SERVICES
            </div>

            <h1>
                Client Collections
            </h1>

            <p>
                Organise saved schemes and services by client,
                just like collections in Instagram or Pinterest.
            </p>

        </div>


        <button
            type="button"
            class="primary-button"
            data-bnx-new-collection>

            + New collection

        </button>

    </header>


    <section class="bnx-collection-summary">

        <div>
            <strong>{{ collection_count }}</strong>
            <span>client collections</span>
        </div>

        <div>
            <strong>{{ unsorted_count }}</strong>
            <span>unsorted services</span>
        </div>

    </section>


    <section class="bnx-collections-section">

        <div class="bnx-collections-heading">

            <div>

                <h2>
                    Collections
                </h2>

                <p>
                    One collection can represent one client.
                </p>

            </div>

            <button
                type="button"
                class="bnx-text-action"
                data-bnx-new-collection>

                New collection

            </button>

        </div>


        <div class="bnx-collection-grid">

            {% for collection in collections %}

                <a
                    href="{% url 'toolkit:saved_collection_detail' collection.pk %}"
                    class="bnx-collection-card">

                    <div class="bnx-collection-cover">

                        <span>
                            {{ collection.name|slice:":1"|upper }}
                        </span>

                    </div>


                    <div class="bnx-collection-card-body">

                        <strong>
                            {{ collection.name }}
                        </strong>

                        {% if collection.note %}

                            <p>
                                {{ collection.note|truncatechars:65 }}
                            </p>

                        {% else %}

                            <p>
                                Client collection
                            </p>

                        {% endif %}


                        <div class="bnx-collection-card-meta">

                            <span>
                                {{ collection.service_count }}
                                service{{ collection.service_count|pluralize }}
                            </span>

                            <span>
                                Updated
                                {{ collection.updated_at|timesince }}
                                ago
                            </span>

                        </div>

                    </div>


                    <div class="bnx-collection-arrow">
                        →
                    </div>

                </a>

            {% empty %}

                <button
                    type="button"
                    class="bnx-collection-empty"
                    data-bnx-new-collection>

                    <span class="bnx-collection-empty-plus">
                        +
                    </span>

                    <strong>
                        Create your first client collection
                    </strong>

                    <small>
                        Example: Deepak · Agriculture business
                    </small>

                </button>

            {% endfor %}

        </div>

    </section>


    {% if unsorted_items %}

        <section class="bnx-unsorted-section">

            <div class="bnx-collections-heading">

                <div>

                    <h2>
                        Unsorted
                    </h2>

                    <p>
                        Existing bookmarks not yet assigned
                        to a client collection.
                    </p>

                </div>

                <span class="bnx-count-pill">
                    {{ unsorted_count }}
                </span>

            </div>


            <div class="bnx-unsorted-list">

                {% for item in unsorted_items %}

                    <article class="bnx-unsorted-service">

                        <div>

                            <span class="bnx-service-code">
                                {{ item.service.service_id }}
                            </span>

                            <h3>
                                {{ item.service.title }}
                            </h3>

                            <p>
                                {{ item.service.domain.name }}
                                ·
                                {{ item.service.category.name }}
                            </p>

                        </div>


                        <div class="bnx-unsorted-actions">

                            <a
                                href="{% url 'toolkit:service_detail' item.service.slug %}"
                                class="secondary-button">

                                Details

                            </a>

                            <button
                                type="button"
                                class="primary-button"
                                data-bnx-manage-saved
                                data-service-id="{{ item.service.pk }}">

                                Add to client

                            </button>

                        </div>

                    </article>

                {% endfor %}

            </div>

        </section>

    {% endif %}


</div>


<div
    class="bnx-create-collection-modal"
    data-bnx-create-collection-modal
    hidden>

    <div
        class="bnx-modal-backdrop"
        data-bnx-close-new-collection>
    </div>

    <div
        class="bnx-create-collection-dialog"
        role="dialog"
        aria-modal="true"
        aria-labelledby="bnx-create-title">

        <div class="bnx-modal-handle"></div>

        <header>

            <div>

                <small>
                    NEW COLLECTION
                </small>

                <h2 id="bnx-create-title">
                    Create client collection
                </h2>

            </div>

            <button
                type="button"
                class="bnx-modal-close"
                data-bnx-close-new-collection>

                ×

            </button>

        </header>


        <form
            method="post"
            action="{% url 'toolkit:saved_collection_create' %}">

            {% csrf_token %}

            <label>
                Client / collection name

                <input
                    type="text"
                    name="name"
                    maxlength="150"
                    placeholder="e.g. Deepak"
                    required>
            </label>


            <label>
                Note
                <span>Optional</span>

                <input
                    type="text"
                    name="note"
                    maxlength="500"
                    placeholder="e.g. Agriculture business">
            </label>


            <div class="bnx-modal-actions">

                <button
                    type="button"
                    class="secondary-button"
                    data-bnx-close-new-collection>

                    Cancel

                </button>

                <button
                    type="submit"
                    class="primary-button">

                    Create collection

                </button>

            </div>

        </form>

    </div>

</div>


<script>
(function () {

    "use strict";

    const modal =
        document.querySelector(
            "[data-bnx-create-collection-modal]"
        );


    if (!modal) {
        return;
    }


    function openModal() {

        modal.hidden = false;

        document.body.classList.add(
            "bnx-modal-open"
        );


        const input =
            modal.querySelector(
                'input[name="name"]'
            );


        if (input) {

            setTimeout(
                function () {
                    input.focus();
                },
                30
            );

        }

    }


    function closeModal() {

        modal.hidden = true;

        document.body.classList.remove(
            "bnx-modal-open"
        );

    }


    document
        .querySelectorAll(
            "[data-bnx-new-collection]"
        )
        .forEach(
            function (button) {

                button.addEventListener(
                    "click",
                    openModal
                );

            }
        );


    document
        .querySelectorAll(
            "[data-bnx-close-new-collection]"
        )
        .forEach(
            function (button) {

                button.addEventListener(
                    "click",
                    closeModal
                );

            }
        );


    document.addEventListener(
        "keydown",
        function (event) {

            if (
                event.key === "Escape"
                &&
                !modal.hidden
            ) {

                closeModal();

            }

        }
    );

})();
</script>

{% endblock %}


====================================================================================================
FILE: toolkit/templates/toolkit/service_detail.html
LINES: 3144
SHA256: 2c880eee1b4a875d2419f8b923be1494f3bdc541f5f15b07bdbc0063aa998f60
====================================================================================================
{% extends "base.html" %}

{% block title %}
{{ service.title }} | BharatNXT Wave
{% endblock %}


{% block content %}

<style>

/* ==========================================================
   BHARATNXT SERVICE CALL WORKSPACE V1
========================================================== */

.bnx-call-page{
    max-width:1420px;
    margin:0 auto;
    padding-bottom:110px;
}

.bnx-call-topbar{
    display:flex;
    justify-content:space-between;
    align-items:center;
    gap:20px;
    margin-bottom:22px;
}

.bnx-back{
    color:#475467;
    font-size:13px;
    font-weight:750;
    text-decoration:none;
}

.bnx-call-actions{
    display:flex;
    align-items:center;
    gap:9px;
    flex-wrap:wrap;
}

.bnx-call-hero{
    background:#fff;
    border:1px solid #e4e7ec;
    border-radius:22px;
    padding:28px;
    margin-bottom:14px;
}

.bnx-call-meta{
    display:flex;
    align-items:center;
    flex-wrap:wrap;
    gap:8px;
    margin-bottom:14px;
}

.bnx-call-code{
    font-size:11px;
    letter-spacing:.08em;
    font-weight:900;
    color:#667085;
}

.bnx-tag{
    background:#f2f4f7;
    border-radius:999px;
    padding:6px 9px;
    color:#344054;
    font-size:11px;
    font-weight:750;
}

.bnx-status{
    padding:6px 9px;
    border-radius:999px;
    font-size:11px;
    font-weight:850;
    background:#ecfdf3;
    color:#067647;
}

.bnx-call-hero h1{
    font-size:38px;
    line-height:1.08;
    letter-spacing:-.04em;
    margin:0;
    max-width:1050px;
}

.bnx-breadcrumb{
    color:#667085;
    font-size:12px;
    margin-top:10px;
}

.bnx-breadcrumb span{
    padding:0 5px;
    color:#d0d5dd;
}

.bnx-call-summary{
    margin:21px 0 0;
    font-size:17px;
    line-height:1.65;
    color:#344054;
    max-width:1050px;
}

.bnx-glance{
    display:grid;
    grid-template-columns:repeat(4,1fr);
    gap:10px;
    margin-bottom:14px;
}

.bnx-glance-card{
    background:#fff;
    border:1px solid #e4e7ec;
    border-radius:16px;
    padding:16px;
    min-height:108px;
}

.bnx-glance-card span{
    display:block;
    color:#667085;
    font-size:10px;
    font-weight:900;
    letter-spacing:.08em;
    margin-bottom:8px;
}

.bnx-glance-card strong{
    font-size:13px;
    line-height:1.5;
    color:#101828;
}

.bnx-layout{
    display:grid;
    grid-template-columns:minmax(0,1fr) 330px;
    gap:14px;
    align-items:start;
}

.bnx-main{
    display:flex;
    flex-direction:column;
    gap:12px;
}

.bnx-section{
    background:#fff;
    border:1px solid #e4e7ec;
    border-radius:18px;
    padding:22px;
    scroll-margin-top:20px;
}

.bnx-section.call-first{
    border-color:#c7d7fe;
    background:linear-gradient(
        180deg,
        #ffffff 0%,
        #fbfcff 100%
    );
}

.bnx-section-head{
    display:flex;
    gap:12px;
    align-items:flex-start;
    margin-bottom:17px;
}

.bnx-section-number{
    height:30px;
    min-width:30px;
    border-radius:9px;
    background:#f2f4f7;
    display:flex;
    align-items:center;
    justify-content:center;
    font-size:11px;
    font-weight:900;
    color:#475467;
}

.bnx-section-head small{
    display:block;
    color:#667085;
    font-size:9px;
    font-weight:900;
    letter-spacing:.1em;
    margin-bottom:3px;
}

.bnx-section-head h2{
    margin:0;
    font-size:19px;
    letter-spacing:-.02em;
}

.bnx-copy{
    color:#344054;
    font-size:14px;
    line-height:1.72;
}

.bnx-copy p:first-child{
    margin-top:0;
}

.bnx-copy p:last-child{
    margin-bottom:0;
}

.bnx-eligibility{
    display:grid;
    grid-template-columns:repeat(2,1fr);
    gap:8px;
    margin-top:15px;
}

.bnx-info{
    border:1px solid #eaecf0;
    background:#fafafa;
    border-radius:11px;
    padding:11px 12px;
}

.bnx-info span{
    display:block;
    color:#667085;
    font-size:10px;
    margin-bottom:4px;
}

.bnx-info strong{
    display:block;
    color:#101828;
    font-size:12px;
    line-height:1.45;
}

.bnx-rule-list{
    display:flex;
    flex-direction:column;
    gap:7px;
    margin-top:15px;
}

.bnx-rule{
    border-left:3px solid #d0d5dd;
    padding:8px 11px;
    background:#fafafa;
    border-radius:0 9px 9px 0;
}

.bnx-rule strong{
    display:block;
    font-size:12px;
    margin-bottom:3px;
}

.bnx-rule span{
    color:#475467;
    font-size:12px;
    line-height:1.5;
}

.bnx-funding-grid{
    display:grid;
    grid-template-columns:repeat(2,1fr);
    gap:8px;
    margin-top:14px;
}

.bnx-money{
    border-radius:12px;
    padding:13px;
    border:1px solid #eaecf0;
}

.bnx-money span{
    color:#667085;
    display:block;
    font-size:10px;
    margin-bottom:4px;
}

.bnx-money strong{
    font-size:13px;
}

.bnx-commercial-list{
    display:flex;
    flex-direction:column;
    gap:10px;
}

.bnx-commercial{
    border:1px solid #e4e7ec;
    border-radius:13px;
    overflow:hidden;
}

.bnx-commercial-head{
    background:#f9fafb;
    padding:11px 13px;
    font-size:12px;
    font-weight:850;
}

.bnx-commercial-grid{
    display:grid;
    grid-template-columns:repeat(2,1fr);
}

.bnx-commercial-item{
    padding:11px 13px;
    border-top:1px solid #f2f4f7;
}

.bnx-commercial-item:nth-child(odd){
    border-right:1px solid #f2f4f7;
}

.bnx-commercial-item span{
    display:block;
    color:#667085;
    font-size:10px;
    margin-bottom:4px;
}

.bnx-commercial-item strong{
    font-size:12px;
    line-height:1.4;
}

.bnx-commercial-note{
    padding:11px 13px;
    border-top:1px solid #f2f4f7;
    color:#475467;
    font-size:12px;
    line-height:1.55;
}

.bnx-checklist{
    display:flex;
    flex-direction:column;
    gap:8px;
}

.bnx-check{
    display:flex;
    gap:10px;
    align-items:flex-start;
    border:1px solid #eaecf0;
    border-radius:11px;
    padding:11px;
}

.bnx-check-icon{
    width:22px;
    height:22px;
    border-radius:7px;
    background:#ecfdf3;
    color:#067647;
    display:flex;
    justify-content:center;
    align-items:center;
    font-size:11px;
    font-weight:900;
    flex:none;
}

.bnx-check.optional .bnx-check-icon{
    background:#f2f4f7;
    color:#667085;
}

.bnx-check strong{
    display:block;
    font-size:12px;
}

.bnx-check small{
    display:block;
    color:#667085;
    margin-top:3px;
    line-height:1.4;
}

.bnx-process{
    position:relative;
    display:flex;
    flex-direction:column;
    gap:9px;
}

.bnx-process-item{
    display:grid;
    grid-template-columns:34px 1fr;
    gap:10px;
}

.bnx-process-number{
    height:30px;
    width:30px;
    border-radius:9px;
    background:#101828;
    color:white;
    display:flex;
    align-items:center;
    justify-content:center;
    font-size:11px;
    font-weight:900;
}

.bnx-process-copy{
    border:1px solid #eaecf0;
    border-radius:11px;
    padding:11px 12px;
}

.bnx-process-copy strong{
    font-size:12px;
}

.bnx-process-copy p{
    color:#667085;
    font-size:12px;
    margin:4px 0 0;
    line-height:1.5;
}

.bnx-process-copy small{
    color:#475467;
    display:block;
    margin-top:5px;
}

.bnx-extra{
    display:flex;
    flex-direction:column;
    gap:9px;
}

.bnx-extra details{
    border:1px solid #eaecf0;
    border-radius:11px;
    overflow:hidden;
}

.bnx-extra summary{
    cursor:pointer;
    padding:12px 13px;
    background:#fafafa;
    list-style:none;
}

.bnx-extra summary::-webkit-details-marker{
    display:none;
}

.bnx-extra-title{
    display:flex;
    justify-content:space-between;
    gap:12px;
}

.bnx-extra-title strong{
    font-size:12px;
}

.bnx-extra-title span{
    color:#667085;
    font-size:10px;
}

.bnx-extra-body{
    padding:13px;
    color:#344054;
    font-size:12px;
    line-height:1.65;
    border-top:1px solid #eaecf0;
}

.bnx-warning{
    border-color:#fedf89;
    background:#fffcf5;
}

.bnx-call-script{
    background:#f8faff;
    border-color:#c7d7fe;
}

.bnx-copy-button{
    margin-top:12px;
    border:1px solid #d0d5dd;
    background:#fff;
    border-radius:9px;
    padding:8px 11px;
    cursor:pointer;
    font-size:11px;
    font-weight:800;
}

.bnx-aside{
    position:sticky;
    top:18px;
    display:flex;
    flex-direction:column;
    gap:10px;
}

.bnx-side-card{
    background:#fff;
    border:1px solid #e4e7ec;
    border-radius:15px;
    padding:15px;
}

.bnx-side-kicker{
    color:#667085;
    font-size:9px;
    font-weight:900;
    letter-spacing:.1em;
    margin-bottom:11px;
}

.bnx-side-fact{
    display:flex;
    justify-content:space-between;
    gap:12px;
    padding:9px 0;
    border-bottom:1px solid #f2f4f7;
}

.bnx-side-fact:last-child{
    border-bottom:0;
}

.bnx-side-fact span{
    color:#667085;
    font-size:11px;
}

.bnx-side-fact strong{
    font-size:11px;
    text-align:right;
    max-width:170px;
}

.bnx-side-nav{
    display:flex;
    flex-direction:column;
    gap:3px;
}

.bnx-side-nav a{
    text-decoration:none;
    color:#475467;
    font-size:11px;
    padding:7px 8px;
    border-radius:8px;
}

.bnx-side-nav a:hover{
    background:#f2f4f7;
    color:#101828;
}

.bnx-source-list{
    display:flex;
    flex-direction:column;
    gap:6px;
}

.bnx-source{
    display:flex;
    justify-content:space-between;
    gap:8px;
    text-decoration:none;
    color:#101828;
    border:1px solid #eaecf0;
    border-radius:9px;
    padding:9px;
}

.bnx-source strong{
    display:block;
    font-size:11px;
}

.bnx-source span{
    display:block;
    color:#667085;
    font-size:9px;
    margin-top:3px;
}

.bnx-muted{
    color:#98a2b3;
    font-size:12px;
}

.bnx-sticky{
    position:fixed;
    bottom:14px;
    left:50%;
    transform:translateX(-50%);
    width:min(880px,calc(100% - 40px));
    z-index:50;
    background:rgba(255,255,255,.96);
    backdrop-filter:blur(10px);
    border:1px solid #d0d5dd;
    box-shadow:0 12px 32px rgba(16,24,40,.12);
    border-radius:16px;
    padding:9px 10px 9px 14px;
    display:flex;
    align-items:center;
    justify-content:space-between;
    gap:12px;
}

.bnx-sticky strong{
    display:block;
    font-size:11px;
}

.bnx-sticky span{
    color:#667085;
    font-size:10px;
}

.bnx-sticky-actions{
    display:flex;
    gap:7px;
}

@media(max-width:1050px){

    .bnx-layout{
        grid-template-columns:1fr;
    }

    .bnx-aside{
        position:static;
    }

}

@media(max-width:760px){

    .bnx-call-hero h1{
        font-size:29px;
    }

    .bnx-glance{
        grid-template-columns:repeat(2,1fr);
    }

    .bnx-eligibility,
    .bnx-funding-grid,
    .bnx-commercial-grid{
        grid-template-columns:1fr;
    }

    .bnx-commercial-item:nth-child(odd){
        border-right:0;
    }

    .bnx-call-topbar{
        align-items:flex-start;
        flex-direction:column;
    }

}

</style>


<div class="bnx-call-page">


    <!-- ====================================================
         TOP ACTIONS
    ===================================================== -->

    <div class="bnx-call-topbar">

        <a
            href="{% url 'toolkit:service_library' %}"
            class="bnx-back">
            ← Service Library
        </a>


        <div class="bnx-call-actions">

            <form
                method="post"
                action="{% url 'toolkit:toggle_saved_service' service.pk %}"
                class="bde-ajax-save">

                {% csrf_token %}

                <input
                    type="hidden"
                    name="next"
                    value="{{ request.path }}">

                <button
                    type="submit"
                    class="secondary-button bde-save-text {% if is_saved %}saved{% endif %}">

                    {% if is_saved %}
                        <span class="bn-bookmark-icon is-saved" aria-hidden="true"></span> Saved
                    {% else %}
                        <span class="bn-bookmark-icon" aria-hidden="true"></span> Save
                    {% endif %}

                </button>

            </form>


            {% if primary_source and primary_source.source_url %}

                <a
                    href="{{ primary_source.source_url }}"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="bde-external-button">

                    Open Scheme ↗

                </a>

            {% endif %}

        </div>

    </div>


    <!-- ====================================================
         HERO
    ===================================================== -->

    <header class="bnx-call-hero">

        <div class="bnx-call-meta">

            <span class="bnx-call-code">
                {{ service.service_id }}
            </span>

            <span class="bnx-tag">
                {{ service.get_service_kind_display }}
            </span>

            <span class="bnx-status">
                {{ service.pitch_state_label }}
            </span>

        </div>


        <h1>
            {{ service.title }}
        </h1>


        <div class="bnx-breadcrumb">

            {{ service.domain.name }}

            <span>›</span>

            {{ service.category.name }}

            {% for item in classification_categories %}

                {% if item.pk != service.category_id %}

                    <span>›</span>
                    {{ item.name }}

                {% endif %}

            {% endfor %}

        </div>


        {% if service.bde_summary %}

            <p class="bnx-call-summary">
                {{ service.bde_summary }}
            </p>

        {% elif service.overview %}

            <p class="bnx-call-summary">
                {{ service.overview|truncatechars:450 }}
            </p>

        {% endif %}

    </header>


    <!-- ====================================================
         CALL AT-A-GLANCE
    ===================================================== -->

    <section class="bnx-glance">

        <div class="bnx-glance-card">

            <span>CLIENT BENEFIT</span>

            <strong>

                {% if service.benefits %}
                    {{ service.benefits|truncatechars:125 }}
                {% else %}
                    Check detailed Toolkit information below
                {% endif %}

            </strong>

        </div>


        <div class="bnx-glance-card">

            <span>DEADLINE</span>

            <strong>
                {{ service.bde_deadline_label }}
            </strong>

        </div>


        <div class="bnx-glance-card">

            <span>APPLICABLE FOR</span>

            <strong>

                {% if service.applicable_for_raw %}

                    {{ service.applicable_for_raw|truncatechars:125 }}

                {% elif service.business_types %}

                    {{ service.business_types|join:", " }}

                {% else %}

                    Check eligibility below

                {% endif %}

            </strong>

        </div>


        <div class="bnx-glance-card">

            <span>ORGANISATION / TYPE</span>

            <strong>

                {% if service.funding_organisation %}

                    {{ service.funding_organisation }}

                {% elif service.funding_type %}

                    {{ service.funding_type }}

                {% else %}

                    {{ service.get_service_kind_display }}

                {% endif %}

            </strong>

        </div>

    </section>


    <div class="bnx-layout">


        <!-- =================================================
             MAIN CALL WORKSPACE
        ================================================== -->

        <main class="bnx-main">


            <!-- CALL SUMMARY -->

            <section
                class="bnx-section call-first"
                id="call-summary">

                <div class="bnx-section-head">

                    <span class="bnx-section-number">
                        01
                    </span>

                    <div>
                        <small>CALL FIRST</small>
                        <h2>What this does</h2>
                    </div>

                </div>


                <div class="bnx-copy">

                    {% if service.overview %}

                        {{ service.overview|linebreaks }}

                    {% elif service.bde_summary %}

                        {{ service.bde_summary|linebreaks }}

                    {% else %}

                        <p class="bnx-muted">
                            No separate overview is recorded.
                            Continue with the Toolkit information below.
                        </p>

                    {% endif %}

                </div>

            </section>


            <!-- ELIGIBILITY -->

            <section
                class="bnx-section"
                id="eligibility">

                <div class="bnx-section-head">

                    <span class="bnx-section-number">
                        02
                    </span>

                    <div>
                        <small>WHO CAN APPLY</small>
                        <h2>Eligibility</h2>
                    </div>

                </div>


                {% if service.eligibility_summary %}

                    <div class="bnx-copy">
                        {{ service.eligibility_summary|linebreaks }}
                    </div>

                {% endif %}


                <div class="bnx-eligibility">

                    {% if service.business_types %}

                        <div class="bnx-info">
                            <span>Entity type</span>
                            <strong>
                                {{ service.business_types|join:", " }}
                            </strong>
                        </div>

                    {% endif %}


                    {% if service.business_stages %}

                        <div class="bnx-info">
                            <span>Business stage</span>
                            <strong>
                                {{ service.business_stages|join:", " }}
                            </strong>
                        </div>

                    {% endif %}


                    {% if service.industries %}

                        <div class="bnx-info">
                            <span>Industry / Sector</span>
                            <strong>
                                {{ service.industries|join:", " }}
                            </strong>
                        </div>

                    {% endif %}


                    {% if service.applicable_states %}

                        <div class="bnx-info">
                            <span>Location</span>
                            <strong>
                                {{ service.applicable_states|join:", " }}
                            </strong>
                        </div>

                    {% endif %}


                    {% if service.founder_categories %}

                        <div class="bnx-info">
                            <span>Founder category</span>
                            <strong>
                                {{ service.founder_categories|join:", " }}
                            </strong>
                        </div>

                    {% endif %}


                    {% if service.applicable_for_raw %}

                        <div class="bnx-info">
                            <span>Toolkit wording</span>
                            <strong>
                                {{ service.applicable_for_raw }}
                            </strong>
                        </div>

                    {% endif %}


                    {% if service.min_business_age_months != None %}

                        <div class="bnx-info">
                            <span>Minimum business age</span>
                            <strong>
                                {{ service.min_business_age_months }}
                                months
                            </strong>
                        </div>

                    {% endif %}


                    {% if service.max_business_age_months != None %}

                        <div class="bnx-info">
                            <span>Maximum business age</span>
                            <strong>
                                {{ service.max_business_age_months }}
                                months
                            </strong>
                        </div>

                    {% endif %}


                    {% if service.min_turnover != None %}

                        <div class="bnx-info">
                            <span>Minimum turnover</span>
                            <strong>
                                ₹{{ service.min_turnover }}
                            </strong>
                        </div>

                    {% endif %}


                    {% if service.max_turnover != None %}

                        <div class="bnx-info">
                            <span>Maximum turnover</span>
                            <strong>
                                ₹{{ service.max_turnover }}
                            </strong>
                        </div>

                    {% endif %}

                </div>


                {% if eligibility_rules %}

                    <div class="bnx-rule-list">

                        {% for rule in eligibility_rules %}

                            <div class="bnx-rule">

                                <strong>
                                    {{ rule.title }}
                                </strong>

                                <span>
                                    {{ rule.description }}
                                </span>

                            </div>

                        {% endfor %}

                    </div>

                {% elif not service.eligibility_summary and not service.applicable_for_raw %}

                    <p class="bnx-muted">
                        No additional eligibility wording
                        is recorded for this Service.
                    </p>

                {% endif %}

            </section>


            <!-- BENEFITS & FUNDING -->

            <section
                class="bnx-section"
                id="benefits">

                <div class="bnx-section-head">

                    <span class="bnx-section-number">
                        03
                    </span>

                    <div>
                        <small>VALUE FOR CLIENT</small>
                        <h2>Benefits & funding</h2>
                    </div>

                </div>


                <div class="bnx-copy">

                    {% if service.benefits %}

                        {{ service.benefits|linebreaks }}

                    {% else %}

                        <p class="bnx-muted">
                            No separate benefits summary is recorded.
                        </p>

                    {% endif %}

                </div>


                {% if service.funding_type or service.funding_min != None or service.funding_max != None or service.interest_rate_min != None or service.interest_rate_max != None or service.tenure or service.subsidy_details %}

                <div class="bnx-funding-grid">


                    {% if service.funding_type %}

                        <div class="bnx-money">
                            <span>Funding type</span>
                            <strong>
                                {{ service.funding_type }}
                            </strong>
                        </div>

                    {% endif %}


                    {% if service.funding_min != None or service.funding_max != None %}

                        <div class="bnx-money">

                            <span>Funding range</span>

                            <strong>

                                {% if service.funding_min != None %}
                                    ₹{{ service.funding_min }}
                                {% endif %}

                                {% if service.funding_min != None and service.funding_max != None %}
                                    –
                                {% endif %}

                                {% if service.funding_max != None %}
                                    ₹{{ service.funding_max }}
                                {% endif %}

                            </strong>

                        </div>

                    {% endif %}


                    {% if service.interest_rate_min != None or service.interest_rate_max != None %}

                        <div class="bnx-money">

                            <span>Interest rate</span>

                            <strong>

                                {% if service.interest_rate_min != None %}
                                    {{ service.interest_rate_min }}%
                                {% endif %}

                                {% if service.interest_rate_min != None and service.interest_rate_max != None %}
                                    –
                                {% endif %}

                                {% if service.interest_rate_max != None %}
                                    {{ service.interest_rate_max }}%
                                {% endif %}

                            </strong>

                        </div>

                    {% endif %}


                    {% if service.tenure %}

                        <div class="bnx-money">
                            <span>Tenure</span>
                            <strong>{{ service.tenure }}</strong>
                        </div>

                    {% endif %}


                    {% if service.collateral_required != None %}

                        <div class="bnx-money">

                            <span>Collateral</span>

                            <strong>

                                {% if service.collateral_required %}
                                    Required
                                {% else %}
                                    Not required
                                {% endif %}

                            </strong>

                        </div>

                    {% endif %}

                </div>

                {% endif %}


                {% if service.subsidy_details %}

                    <div class="bnx-copy" style="margin-top:15px;">
                        {{ service.subsidy_details|linebreaks }}
                    </div>

                {% endif %}

            </section>


            <!-- COMMERCIAL -->

            {% if commercial_terms or service.government_fee != None or service.bharatnxt_fee != None or service.pricing_notes %}

            <section
                class="bnx-section"
                id="commercial">

                <div class="bnx-section-head">

                    <span class="bnx-section-number">
                        04
                    </span>

                    <div>
                        <small>COMMERCIAL CHECK</small>
                        <h2>Charges & BharatNXT commercial details</h2>
                    </div>

                </div>


                {% if service.government_fee != None or service.bharatnxt_fee != None %}

                    <div class="bnx-funding-grid"
                         style="margin-top:0;margin-bottom:12px;">

                        {% if service.government_fee != None %}

                            <div class="bnx-money">
                                <span>Government fee</span>
                                <strong>
                                    ₹{{ service.government_fee }}
                                </strong>
                            </div>

                        {% endif %}


                        {% if service.bharatnxt_fee != None %}

                            <div class="bnx-money">
                                <span>BharatNXT fee</span>
                                <strong>
                                    ₹{{ service.bharatnxt_fee }}
                                </strong>
                            </div>

                        {% endif %}

                    </div>

                {% endif %}


                {% if commercial_terms %}

                    <div class="bnx-commercial-list">

                        {% for item in commercial_terms %}

                            <div class="bnx-commercial">

                                <div class="bnx-commercial-head">

                                    {% if item.label %}
                                        {{ item.label }}
                                    {% else %}
                                        Commercial terms
                                    {% endif %}

                                </div>


                                <div class="bnx-commercial-grid">


                                    {% if item.minimum_charge_raw or item.minimum_charge != None %}

                                        <div class="bnx-commercial-item">

                                            <span>
                                                Minimum charge
                                            </span>

                                            <strong>

                                                {% if item.minimum_charge_raw %}
                                                    {{ item.minimum_charge_raw }}
                                                {% else %}
                                                    ₹{{ item.minimum_charge }}
                                                {% endif %}

                                            </strong>

                                        </div>

                                    {% endif %}


                                    {% if item.government_fee_raw or item.government_fee != None %}

                                        <div class="bnx-commercial-item">

                                            <span>
                                                Government fee
                                            </span>

                                            <strong>

                                                {% if item.government_fee_raw %}
                                                    {{ item.government_fee_raw }}
                                                {% else %}
                                                    ₹{{ item.government_fee }}
                                                {% endif %}

                                            </strong>

                                        </div>

                                    {% endif %}


                                    {% if item.vendor_cost_raw or item.vendor_cost != None %}

                                        <div class="bnx-commercial-item">

                                            <span>
                                                Vendor cost
                                            </span>

                                            <strong>

                                                {% if item.vendor_cost_raw %}
                                                    {{ item.vendor_cost_raw }}
                                                {% else %}
                                                    ₹{{ item.vendor_cost }}
                                                {% endif %}

                                            </strong>

                                        </div>

                                    {% endif %}


                                    {% if item.bdm_deduction_raw or item.bdm_deduction != None %}

                                        <div class="bnx-commercial-item">

                                            <span>
                                                BDM deduction
                                            </span>

                                            <strong>

                                                {% if item.bdm_deduction_raw %}
                                                    {{ item.bdm_deduction_raw }}
                                                {% else %}
                                                    ₹{{ item.bdm_deduction }}
                                                {% endif %}

                                            </strong>

                                        </div>

                                    {% endif %}

                                </div>


                                {% if item.remarks %}

                                    <div class="bnx-commercial-note">
                                        {{ item.remarks|linebreaksbr }}
                                    </div>

                                {% endif %}

                            </div>

                        {% endfor %}

                    </div>

                {% endif %}


                {% if service.pricing_notes %}

                    <div class="bnx-copy"
                         style="margin-top:13px;">

                        {{ service.pricing_notes|linebreaks }}

                    </div>

                {% endif %}

            </section>

            {% endif %}


            <!-- DOCUMENTS -->

            <section
                class="bnx-section"
                id="documents">

                <div class="bnx-section-head">

                    <span class="bnx-section-number">
                        05
                    </span>

                    <div>
                        <small>CLIENT CHECKLIST</small>
                        <h2>Documents required</h2>
                    </div>

                </div>


                {% if document_requirements %}

                    <div class="bnx-checklist">

                        {% for item in document_requirements %}

                            <div class="bnx-check {% if not item.is_mandatory %}optional{% endif %}">

                                <span class="bnx-check-icon">

                                    {% if item.is_mandatory %}
                                        ✓
                                    {% else %}
                                        ○
                                    {% endif %}

                                </span>


                                <div>

                                    <strong>
                                        {{ item.name }}
                                    </strong>

                                    {% if item.description %}

                                        <small>
                                            {{ item.description }}
                                        </small>

                                    {% endif %}

                                </div>

                            </div>

                        {% endfor %}

                    </div>

                {% else %}

                    <p class="bnx-muted">
                        No separate structured document checklist
                        is recorded. Check Additional Toolkit
                        Information below for document-related
                        source content.
                    </p>

                {% endif %}

            </section>


            <!-- PROCESS -->

            <section
                class="bnx-section"
                id="process">

                <div class="bnx-section-head">

                    <span class="bnx-section-number">
                        06
                    </span>

                    <div>
                        <small>HOW IT WORKS</small>
                        <h2>Process / next steps</h2>
                    </div>

                </div>


                {% if process_steps %}

                    <div class="bnx-process">

                        {% for item in process_steps %}

                            <div class="bnx-process-item">

                                <span class="bnx-process-number">
                                    {{ item.step_number }}
                                </span>

                                <div class="bnx-process-copy">

                                    <strong>
                                        {{ item.title }}
                                    </strong>

                                    {% if item.description %}

                                        <p>
                                            {{ item.description }}
                                        </p>

                                    {% endif %}


                                    {% if item.estimated_time %}

                                        <small>
                                            Estimated:
                                            {{ item.estimated_time }}
                                        </small>

                                    {% endif %}

                                </div>

                            </div>

                        {% endfor %}

                    </div>

                {% else %}

                    <p class="bnx-muted">
                        No separate structured process steps are
                        recorded. Check Additional Toolkit
                        Information below for process-related
                        source content.
                    </p>

                {% endif %}

            </section>


            <!-- SALES PITCH -->

            {% if service.sales_pitch %}

            <section
                class="bnx-section bnx-call-script"
                id="pitch">

                <div class="bnx-section-head">

                    <span class="bnx-section-number">
                        07
                    </span>

                    <div>
                        <small>CALL ASSIST</small>
                        <h2>How to explain this to the client</h2>
                    </div>

                </div>


                <div
                    class="bnx-copy"
                    id="bnx-sales-pitch">

                    {{ service.sales_pitch|linebreaks }}

                </div>


                <button
                    type="button"
                    class="bnx-copy-button"
                    id="bnx-copy-pitch">

                    Copy pitch

                </button>

            </section>

            {% endif %}


            <!-- IMPORTANT CONDITIONS -->

            {% if service.restrictions or service.important_notes or service.internal_notes or service.escalation_notes %}

            <section
                class="bnx-section bnx-warning"
                id="conditions">

                <div class="bnx-section-head">

                    <span class="bnx-section-number">
                        !
                    </span>

                    <div>
                        <small>CHECK BEFORE PROMISING</small>
                        <h2>Important conditions & internal notes</h2>
                    </div>

                </div>


                {% if service.restrictions %}

                    <div class="bnx-copy">
                        {{ service.restrictions|linebreaks }}
                    </div>

                {% endif %}


                {% if service.important_notes %}

                    <div class="bnx-copy">
                        {{ service.important_notes|linebreaks }}
                    </div>

                {% endif %}


                {% if service.internal_notes %}

                    <div class="bnx-copy">
                        {{ service.internal_notes|linebreaks }}
                    </div>

                {% endif %}


                {% if service.escalation_notes %}

                    <div class="bnx-copy">
                        {{ service.escalation_notes|linebreaks }}
                    </div>

                {% endif %}

            </section>

            {% endif %}


            <!-- ALL ADDITIONAL TOOLKIT CONTENT -->

            {% if content_sections or knowledge_sections %}

            <section
                class="bnx-section"
                id="toolkit-information">

                <div class="bnx-section-head">

                    <span class="bnx-section-number">
                        +
                    </span>

                    <div>
                        <small>COMPLETE TOOLKIT KNOWLEDGE</small>
                        <h2>Additional Toolkit information</h2>
                    </div>

                </div>


                <div class="bnx-extra">


                    {% for item in content_sections %}

                        <details>

                            <summary>

                                <div class="bnx-extra-title">

                                    <strong>

                                        {% if item.title %}
                                            {{ item.title }}
                                        {% else %}
                                            {{ item.get_section_type_display }}
                                        {% endif %}

                                    </strong>

                                    <span>
                                        {{ item.get_section_type_display }}
                                    </span>

                                </div>

                            </summary>


                            <div class="bnx-extra-body">
                                {{ item.content|linebreaks }}
                            </div>

                        </details>

                    {% endfor %}


                    {% for item in knowledge_sections %}

                        <details>

                            <summary>

                                <div class="bnx-extra-title">

                                    <strong>

                                        {% if item.title %}
                                            {{ item.title }}
                                        {% else %}
                                            {{ item.get_section_type_display }}
                                        {% endif %}

                                    </strong>

                                    <span>
                                        {{ item.document.title }}
                                    </span>

                                </div>

                            </summary>


                            <div class="bnx-extra-body">
                                {{ item.content|linebreaks }}
                            </div>

                        </details>

                    {% endfor %}

                </div>

            </section>

            {% endif %}


            <!-- COMPARISON DATA -->

            {% if comparison_entries %}

            <section
                class="bnx-section"
                id="comparison">

                <div class="bnx-section-head">

                    <span class="bnx-section-number">
                        ↔
                    </span>

                    <div>
                        <small>REFERENCE</small>
                        <h2>Comparison data</h2>
                    </div>

                </div>


                <div class="bnx-extra">

                    {% for item in comparison_entries %}

                        <div class="bnx-info">

                            <span>
                                {{ item.matrix.name }}
                                ·
                                {{ item.column_name }}
                            </span>

                            <strong>

                                {% if item.row_label %}
                                    {{ item.row_label }}:
                                {% endif %}

                                {{ item.value_raw }}

                            </strong>

                        </div>

                    {% endfor %}

                </div>

            </section>

            {% endif %}


        </main>


        <!-- =================================================
             CALL SIDEBAR
        ================================================== -->

        <aside class="bnx-aside">


            <section class="bnx-side-card">

                <div class="bnx-side-kicker">
                    DURING THE CALL
                </div>


                <nav class="bnx-side-nav">

                    <a href="#call-summary">
                        What it does
                    </a>

                    <a href="#eligibility">
                        Eligibility
                    </a>

                    <a href="#benefits">
                        Benefits & funding
                    </a>

                    {% if commercial_terms or service.government_fee != None or service.bharatnxt_fee != None %}

                        <a href="#commercial">
                            Charges
                        </a>

                    {% endif %}

                    <a href="#documents">
                        Documents
                    </a>

                    <a href="#process">
                        Process
                    </a>

                    {% if content_sections or knowledge_sections %}

                        <a href="#toolkit-information">
                            More Toolkit info
                        </a>

                    {% endif %}

                </nav>

            </section>


            <section class="bnx-side-card">

                <div class="bnx-side-kicker">
                    DATES & STATUS
                </div>


                <div class="bnx-side-fact">

                    <span>
                        Deadline
                    </span>

                    <strong>
                        {{ service.bde_deadline_label }}
                    </strong>

                </div>


                {% if service.effective_from %}

                    <div class="bnx-side-fact">

                        <span>
                            Pitch from
                        </span>

                        <strong>
                            {{ service.effective_from|date:"d M Y" }}
                        </strong>

                    </div>

                {% endif %}


                {% if service.pitch_until %}

                    <div class="bnx-side-fact">

                        <span>
                            Pitch until
                        </span>

                        <strong>
                            {{ service.pitch_until|date:"d M Y" }}
                        </strong>

                    </div>

                {% endif %}


                {% if service.estimated_processing_time %}

                    <div class="bnx-side-fact">

                        <span>
                            Processing
                        </span>

                        <strong>
                            {{ service.estimated_processing_time }}
                        </strong>

                    </div>

                {% endif %}

            </section>


            <section
                class="bnx-side-card"
                id="scheme-links">

                <div class="bnx-side-kicker">
                    OFFICIAL / SOURCE LINKS
                </div>


                {% if sources %}

                    <div class="bnx-source-list">

                        {% for source in sources %}

                            {% if source.source_url %}

                                <a
                                    href="{{ source.source_url }}"
                                    target="_blank"
                                    rel="noopener noreferrer"
                                    class="bnx-source">

                                    <div>

                                        <strong>

                                            {% if source.source_kind == "APPLICATION" %}

                                                Apply / Open Portal

                                            {% elif source.source_kind == "OFFICIAL_PORTAL" %}

                                                Official Scheme Page

                                            {% elif source.source_kind == "FLYER" %}

                                                Guidelines / Flyer

                                            {% elif source.source_kind == "REFERENCE" %}

                                                Reference

                                            {% else %}

                                                External Link

                                            {% endif %}

                                        </strong>

                                        <span>
                                            {{ source.source_name }}
                                        </span>

                                    </div>

                                    <b>↗</b>

                                </a>

                            {% endif %}

                        {% endfor %}

                    </div>

                {% else %}

                    <p class="bnx-muted">
                        No external scheme link is recorded.
                    </p>

                {% endif %}

            </section>


            <section class="bnx-side-card">

                <div class="bnx-side-kicker">
                    TOOLKIT RECORD
                </div>


                <div class="bnx-side-fact">

                    <span>Last updated</span>

                    <strong>
                        {{ service.updated_at|date:"d M Y" }}
                    </strong>

                </div>


                <div class="bnx-side-fact">

                    <span>Last verified</span>

                    <strong>

                        {% if service.last_verified_at %}
                            {{ service.last_verified_at|date:"d M Y" }}
                        {% else %}
                            Not recorded
                        {% endif %}

                    </strong>

                </div>

            </section>


        </aside>

    </div>


    <!-- ====================================================
         STICKY CALL ACTION
    ===================================================== -->

    <div class="bnx-sticky">

        <div>

            <strong>
                {{ service.title|truncatechars:60 }}
            </strong>

            <span>
                {{ service.bde_deadline_label }}
            </span>

        </div>


        <div class="bnx-sticky-actions">

            {% if primary_source and primary_source.source_url %}

                <a
                    href="{{ primary_source.source_url }}"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="bde-external-button">

                    Scheme ↗

                </a>

            {% endif %}


            <a
                href="{% url 'toolkit:client_matcher' %}?service={{ service.pk }}"
                class="primary-button">

                Find for Client

            </a>

        </div>

    </div>

</div>


<script>
(function(){

    const copyButton =
        document.getElementById(
            "bnx-copy-pitch"
        );

    const pitch =
        document.getElementById(
            "bnx-sales-pitch"
        );


    if(
        copyButton
        && pitch
        && navigator.clipboard
    ){

        copyButton.addEventListener(
            "click",
            async function(){

                try{

                    await navigator.clipboard.writeText(
                        pitch.innerText.trim()
                    );

                    copyButton.textContent =
                        "Copied ✓";


                    setTimeout(
                        function(){

                            copyButton.textContent =
                                "Copy pitch";

                        },
                        1600
                    );

                }catch(error){

                    copyButton.textContent =
                        "Select text to copy";

                }

            }
        );

    }

})();
</script>



<!-- ========================================================
     PREMIUM TOOLKIT ACCORDION V1.1
========================================================= -->

<style id="premium-toolkit-accordion-v11">

.bnx-extra details{

    transition:
        border-color .15s ease,
        box-shadow .15s ease;
}


.bnx-extra details:hover{

    border-color:
        #cbd5e1;

    box-shadow:
        0 4px 14px
        rgba(16,24,40,.04);
}


.bnx-extra details[open]{

    border-color:
        #b9ceff;

    box-shadow:
        0 5px 18px
        rgba(23,105,255,.06);
}


.bnx-extra details > summary{

    display:flex;

    align-items:center;

    gap:16px;

    min-height:54px;

    cursor:pointer;

    user-select:none;
}


.bnx-extra-title{

    flex:1;
}


.bnx-expand-control{

    flex:none;

    display:inline-flex;

    align-items:center;

    gap:7px;

    padding:
        6px 8px 6px 10px;

    border-radius:9px;

    background:#f2f4f7;

    color:#475467;

    font-size:10px;
    font-weight:800;

    transition:
        background .15s ease,
        color .15s ease;
}


.bnx-extra summary:hover
.bnx-expand-control{

    color:#1769ff;

    background:#eef3ff;
}


.bnx-expand-control-arrow{

    width:22px;
    height:22px;

    display:flex;

    align-items:center;
    justify-content:center;

    border-radius:6px;

    background:#ffffff;

    border:1px solid #d0d5dd;

    font-size:15px;

    line-height:1;

    transition:
        transform .18s ease;
}


.bnx-extra details[open]
.bnx-expand-control-arrow{

    transform:
        rotate(180deg);
}


.bnx-extra details[open]
.bnx-expand-control{

    color:#1769ff;

    background:#eef3ff;
}


/* Expanded content presentation */

.bnx-extra details[open]
.bnx-extra-body{

    background:#ffffff;

    padding:17px;

    font-size:13px;

    line-height:1.7;
}


@media(max-width:720px){

    .bnx-expand-control-label{
        display:none;
    }

}

</style>


<script id="premium-toolkit-accordion-script-v11">

(function(){

    document
        .querySelectorAll(
            ".bnx-extra details"
        )
        .forEach(
            function(item){

                const summary =
                    item.querySelector(
                        ":scope > summary"
                    );


                if(!summary){
                    return;
                }


                /*
                 * Additional Toolkit sections
                 * start compact.
                 */
                item.open = false;


                let control =
                    summary.querySelector(
                        ".bnx-expand-control"
                    );


                if(!control){

                    control =
                        document.createElement(
                            "span"
                        );


                    control.className =
                        "bnx-expand-control";


                    control.innerHTML =
                        '<span '
                        + 'class="bnx-expand-control-label">'
                        + 'Click to open'
                        + '</span>'
                        + '<span '
                        + 'class="bnx-expand-control-arrow" '
                        + 'aria-hidden="true">'
                        + '⌄'
                        + '</span>';


                    summary.appendChild(
                        control
                    );
                }


                const label =
                    control.querySelector(
                        ".bnx-expand-control-label"
                    );


                function refresh(){

                    const opened =
                        item.open;


                    summary.setAttribute(
                        "aria-expanded",
                        opened
                            ? "true"
                            : "false"
                    );


                    if(label){

                        label.textContent =
                            opened
                                ? "Close"
                                : "Click to open";

                    }

                }


                item.addEventListener(
                    "toggle",
                    refresh
                );


                refresh();

            }
        );

})();

</script>



<!-- =========================================================
     BNX_SERVICE_ACCORDION_CLEANUP_V1
     Display-only cleanup:
     - Fields.X -> X
     - single Open / Close pill
========================================================== -->

<style id="BNX_SERVICE_ACCORDION_CLEANUP_V1">

/* =========================================================
   ADDITIONAL TOOLKIT ACCORDION
========================================================= */

.bnx-extra details {

    overflow:
        hidden !important;

    border:
        1px solid
        #d9e2ef !important;

    border-radius:
        12px !important;

    background:
        #ffffff !important;

    transition:
        border-color .16s ease,
        box-shadow .16s ease,
        background .16s ease !important;
}


.bnx-extra details:hover {

    border-color:
        #bfd0ef !important;
}


.bnx-extra details[open] {

    border-color:
        #9fbcff !important;

    box-shadow:
        0 6px 20px
        rgba(23,105,255,.06) !important;
}


/* =========================================================
   SUMMARY ROW
========================================================= */

.bnx-extra details > summary {

    min-height:
        66px !important;

    padding:
        0 18px !important;

    display:
        flex !important;

    align-items:
        center !important;

    gap:
        16px !important;

    cursor:
        pointer !important;

    list-style:
        none !important;

    user-select:
        none !important;

    transition:
        background .15s ease !important;
}


.bnx-extra details > summary:hover {

    background:
        #fafcff !important;
}


.bnx-extra details[open] > summary {

    background:
        #fbfcff !important;

    border-bottom:
        1px solid
        #e7ebf2 !important;
}


.bnx-extra details > summary::-webkit-details-marker {

    display:
        none !important;
}


/* =========================================================
   OLD CONTROL:
   Keep its DOM for compatibility with existing JS,
   but hide its old children and render one clean pill.
========================================================= */

.bnx-extra
.bnx-expand-control {

    flex:
        0 0 auto !important;

    min-width:
        84px !important;

    height:
        34px !important;

    margin-left:
        4px !important;

    padding:
        0 11px !important;

    display:
        inline-flex !important;

    align-items:
        center !important;

    justify-content:
        center !important;

    gap:
        7px !important;

    border:
        1px solid
        #d7e2f5 !important;

    border-radius:
        9px !important;

    background:
        #f6f9ff !important;

    color:
        #1769ff !important;

    font-size:
        0 !important;

    font-weight:
        700 !important;

    line-height:
        1 !important;

    box-shadow:
        none !important;

    transform:
        none !important;

    transition:
        background .15s ease,
        border-color .15s ease !important;
}


.bnx-extra
.bnx-expand-control:hover {

    background:
        #edf4ff !important;

    border-color:
        #bfd2ff !important;
}


/*
Hide the old "Click to open", "Close",
and separate square-arrow presentation.

The elements remain in the DOM so the
existing V1.1 JavaScript does not break.
*/

.bnx-extra
.bnx-expand-control > * {

    display:
        none !important;
}


/* CLEAN TEXT */

.bnx-extra
.bnx-expand-control::before {

    content:
        "Open";

    display:
        inline-block;

    font-size:
        12px !important;

    font-weight:
        700 !important;

    line-height:
        1 !important;

    letter-spacing:
        0 !important;
}


/* CLEAN ARROW — NO SEPARATE BOX */

.bnx-extra
.bnx-expand-control::after {

    content:
        "↓";

    display:
        inline-block;

    position:
        static !important;

    width:
        auto !important;

    height:
        auto !important;

    padding:
        0 !important;

    margin:
        0 !important;

    border:
        0 !important;

    border-radius:
        0 !important;

    background:
        transparent !important;

    color:
        currentColor !important;

    font-size:
        14px !important;

    font-weight:
        600 !important;

    line-height:
        1 !important;

    transform:
        none !important;

    box-shadow:
        none !important;
}


/* OPEN STATE */

.bnx-extra
details[open]
.bnx-expand-control {

    background:
        #eef4ff !important;

    border-color:
        #cbdcff !important;
}


.bnx-extra
details[open]
.bnx-expand-control::before {

    content:
        "Close";
}


.bnx-extra
details[open]
.bnx-expand-control::after {

    content:
        "↑";

    transform:
        none !important;
}


/* =========================================================
   CONTENT AREA
========================================================= */

.bnx-extra
details[open] > :not(summary) {

    animation:
        bnxAccordionContentIn
        .16s ease-out;
}


@keyframes bnxAccordionContentIn {

    from {
        opacity: 0;
        transform:
            translateY(-3px);
    }

    to {
        opacity: 1;
        transform:
            translateY(0);
    }
}


/* =========================================================
   MOBILE
========================================================= */

@media(max-width:760px) {

    .bnx-extra
    details > summary {

        min-height:
            58px !important;

        padding:
            0 13px !important;

        gap:
            9px !important;
    }


    .bnx-extra
    .bnx-expand-control {

        min-width:
            70px !important;

        height:
            32px !important;

        padding:
            0 9px !important;
    }

}

</style>


<script id="bnx-service-accordion-cleanup-v1">

(function () {

    "use strict";


    /*
     * Display cleanup only.
     *
     * Imported Toolkit section names may contain:
     *
     * Fields.Focus Sectors
     * Fields.Deadline
     * Fields.Funding Organisation
     * Fields.Applicable For
     *
     * Keep stored/source data unchanged.
     * Only remove "Fields." from what the BDE sees.
     */


    function cleanSummaryLabel(summary) {

        if (!summary) {
            return;
        }


        const walker =
            document.createTreeWalker(
                summary,
                NodeFilter.SHOW_TEXT
            );


        const nodes = [];


        while (
            walker.nextNode()
        ) {

            nodes.push(
                walker.currentNode
            );
        }


        nodes.forEach(
            function (node) {

                const parent =
                    node.parentElement;


                /*
                 * Never change the existing
                 * accordion control text.
                 */
                if (
                    parent
                    &&
                    parent.closest(
                        ".bnx-expand-control"
                    )
                ) {

                    return;
                }


                const value =
                    node.nodeValue;


                if (!value) {
                    return;
                }


                /*
                 * Only remove a leading
                 * Fields. / Fields:
                 *
                 * Do NOT change normal text.
                 */
                const cleaned =
                    value.replace(
                        /^(\s*)Fields[\.:]\s*/i,
                        "$1"
                    );


                if (
                    cleaned
                    !== value
                ) {

                    node.nodeValue =
                        cleaned;
                }

            }
        );

    }


    function prepareAccordion() {

        const detailsItems =
            document.querySelectorAll(
                ".bnx-extra details"
            );


        detailsItems.forEach(
            function (details) {

                const summary =
                    details.querySelector(
                        ":scope > summary"
                    );


                if (!summary) {
                    return;
                }


                cleanSummaryLabel(
                    summary
                );


                /*
                 * Accessibility:
                 * summary already owns the interaction.
                 * The visual Open/Close pill is decorative.
                 */
                const control =
                    summary.querySelector(
                        ".bnx-expand-control"
                    );


                if (control) {

                    control.setAttribute(
                        "aria-hidden",
                        "true"
                    );

                }

            }
        );

    }


    if (
        document.readyState
        === "loading"
    ) {

        document.addEventListener(
            "DOMContentLoaded",
            prepareAccordion
        );

    }

    else {

        prepareAccordion();

    }

})();

</script>


{% endblock %}


====================================================================================================
FILE: toolkit/templates/toolkit/service_library.html
LINES: 527
SHA256: d85ffae3047f2f579629d3754c1e4102052e9c59feadc21652b01aab8239cfe4
====================================================================================================
{% extends "base.html" %}
{% load static %}
{% block title %}Service Library | BharatNXT Wave{% endblock %}

{% block content %}

<style id="BNX_LIBRARY_COUNT_ALIGNMENT_FINAL">

/* =========================================================
   SERVICE LIBRARY — PUBLISHED SERVICES COUNT
   Optical centering only.
   Card dimensions and page layout remain unchanged.
========================================================= */

.bnx-library-total-card {

    box-sizing:
        border-box !important;

    display:
        flex !important;

    flex-direction:
        column !important;

    align-items:
        center !important;

    justify-content:
        center !important;

    text-align:
        center !important;

    gap:
        8px !important;

}


/* NUMBER */

.bnx-library-total-card strong {

    display:
        block !important;

    width:
        100% !important;

    margin:
        0 !important;

    padding:
        0 !important;

    text-align:
        center !important;

    align-self:
        center !important;

    line-height:
        1 !important;

}


/* "published services" */

.bnx-library-total-card span {

    display:
        block !important;

    width:
        100% !important;

    margin:
        0 !important;

    padding:
        0 !important;

    text-align:
        center !important;

    align-self:
        center !important;

    line-height:
        1.35 !important;

}


/*
Remove inherited horizontal offsets
that caused the visual misalignment.
*/

.bnx-library-total-card > * {

    margin-left:
        auto !important;

    margin-right:
        auto !important;

}

</style>


<div class="bde-page bde-library-page">

    <header class="bde-page-hero bde-library-hero">
        <div>
            <div class="bde-eyebrow">SERVICE LIBRARY</div>
            <h1>Every service. <span>Organised for a live call.</span></h1>
            <p>Browse the complete BDE catalog by category, or search instantly without losing your place.</p>
        </div>
        <div class="bde-library-total bnx-library-total-card">
            <strong>{{ total_service_count }}</strong>
            <span>published services</span>
        </div>
    </header>

    <section class="bde-library-toolbar" aria-label="Service Library tools">
        
        <form method="get"
              action="{% url 'toolkit:service_library' %}"
              class="bde-page-search bde-library-page-search bde-feedback-form"
              data-search-context="library"
              data-feedback-message="Searching Service Library…"
              autocomplete="off">

            {% if selected_category %}
                <input type="hidden"
                       name="category"
                       value="{{ selected_category }}">
            {% endif %}

            {% if selected_kind %}
                <input type="hidden"
                       name="kind"
                       value="{{ selected_kind }}">
            {% endif %}

            <span class="bde-page-search-icon"
                  aria-hidden="true">
                ⌕
            </span>

            <input
                type="search"
                name="q"
                value="{{ query }}"
                placeholder="Search all {{ total_service_count }} published services..."
                autocomplete="off"
                aria-label="Search Service Library">

            <span class="bde-page-search-shortcut">
                Press Enter
            </span>

            <div class="bde-page-suggestions"
                 hidden></div>

        </form>


        <form method="get"
              class="bde-library-filter-form bde-feedback-form"
              data-feedback-message="Updating Service Library…">
            {% if query %}<input type="hidden" name="q" value="{{ query }}">{% endif %}
            {% if selected_category %}<input type="hidden" name="category" value="{{ selected_category }}">{% endif %}

            <label>
                <span>Service type</span>
                <select name="kind"
                        class="bde-auto-submit"
                        aria-label="Filter by service type">
                    <option value="">All service types</option>
                    {% for value, label in service_kinds %}
                        <option value="{{ value }}"
                                {% if selected_kind == value %}selected{% endif %}>
                            {{ label }}
                        </option>
                    {% endfor %}
                </select>
            </label>

            {% if query or selected_category or selected_kind %}
                <a href="{% url 'toolkit:service_library' %}"
                   class="bde-clear-library">
                    Clear filters
                </a>
            {% endif %}
        </form>
    </section>

    {% if query %}
        <div class="bde-active-search-line" role="status">
            <span>Search</span>
            <strong>“{{ query }}”</strong>
            <b>{{ result_count }} match{{ result_count|pluralize:"es" }}</b>
        </div>
    {% endif %}

    <div class="bde-library-grid">

        <aside class="bde-library-categories">
            <div class="bde-library-pane-head">
                <span>CATEGORIES</span>
                <strong>Browse</strong>
            </div>

            <a href="{% url 'toolkit:service_library' %}{% if query or selected_kind %}?{% if query %}q={{ query|urlencode }}{% endif %}{% if query and selected_kind %}&{% endif %}{% if selected_kind %}kind={{ selected_kind|urlencode }}{% endif %}{% endif %}"
               class="bde-category-link {% if not selected_category %}active{% endif %}">
                <span>All Services</span>
                <strong>{{ total_service_count }}</strong>
            </a>

            <div class="bde-category-scroll">
                {% for item in categories %}
                    {% if item.bde_count %}
                        {% ifchanged item.domain.name %}
                            <div class="bde-category-group-label">
                                {{ item.domain.name }}
                            </div>
                        {% endifchanged %}

                        <a href="?category={{ item.pk }}{% if query %}&q={{ query|urlencode }}{% endif %}{% if selected_kind %}&kind={{ selected_kind|urlencode }}{% endif %}"
                           class="bde-category-link {% if selected_category == item.pk|stringformat:'s' %}active{% endif %}">
                            <span>{{ item.name }}</span>
                            <strong>{{ item.bde_count }}</strong>
                        </a>
                    {% endif %}
                {% endfor %}
            </div>
        </aside>

        <section class="bde-library-results">
            <div class="bde-library-pane-head">
                <span>SERVICES</span>
                <strong>{{ result_count }} shown</strong>
            </div>

            <div class="bde-library-list">
                {% for service in services %}
                    <button type="button"
                            class="bde-library-row {% if selected_service and selected_service.pk == service.pk %}active{% endif %}"
                            data-service-quick="{{ service.pk }}"
                            data-quick-mode="library">
                        <div>
                            <span class="bde-service-code">{{ service.service_id }}</span>
                            <strong>{{ service.title }}</strong>
                            <small>
                                {{ service.get_service_kind_display }}
                                ·
                                {{ service.category.name }}
                            </small>
                        </div>

                        <div class="bde-library-row-side">
                            <span>{{ service.bde_deadline_label|truncatechars:24 }}</span>
                            <i>›</i>
                        </div>
                    </button>
                {% empty %}
                    <div class="bde-empty-state compact">
                        <h3>No services in this view</h3>
                        <p>Remove a filter or search another phrase.</p>
                    </div>
                {% endfor %}
            </div>
        </section>

        <aside id="bde-library-preview"
               class="bde-library-preview">

            {% if selected_service %}

                <div class="bde-preview-kicker">
                    CALL QUICK VIEW
                </div>

                <div class="bde-preview-code">
                    {{ selected_service.service_id }}
                </div>


                <h2>
                    {{ selected_service.title }}
                </h2>


                <div class="bde-preview-tags">

                    <span>
                        {{ selected_service.get_service_kind_display }}
                    </span>

                    <span>
                        {{ selected_service.category.name }}
                    </span>

                </div>


                <div class="bde-preview-fact-grid">

                    <div>
                        <span>Deadline</span>
                        <strong>
                            {{ selected_service.bde_deadline_label }}
                        </strong>
                    </div>

                    <div>
                        <span>Pitch status</span>
                        <strong>
                            {{ selected_service.pitch_state_label }}
                        </strong>
                    </div>

                </div>


                {% if selected_service.bde_summary or selected_service.overview %}

                    <section class="bde-preview-section">

                        <span>
                            WHAT TO SAY FIRST
                        </span>

                        <p>

                            {% if selected_service.bde_summary %}

                                {{ selected_service.bde_summary|truncatechars:330 }}

                            {% else %}

                                {{ selected_service.overview|truncatechars:330 }}

                            {% endif %}

                        </p>

                    </section>

                {% endif %}


                {% if selected_service.benefits %}

                    <section class="bde-preview-section">

                        <span>
                            CLIENT BENEFIT
                        </span>

                        <p>
                            {{ selected_service.benefits|truncatechars:260 }}
                        </p>

                    </section>

                {% endif %}


                {% if selected_service.eligibility_summary or selected_service.applicable_for_raw %}

                    <section class="bde-preview-section">

                        <span>
                            WHO CAN APPLY
                        </span>

                        <p>

                            {% if selected_service.eligibility_summary %}

                                {{ selected_service.eligibility_summary|truncatechars:300 }}

                            {% else %}

                                {{ selected_service.applicable_for_raw|truncatechars:300 }}

                            {% endif %}

                        </p>

                    </section>

                {% endif %}


                {% if selected_service.funding_type or selected_service.funding_min != None or selected_service.funding_max != None or selected_service.government_fee != None or selected_service.bharatnxt_fee != None %}

                    <section class="bde-preview-section">

                        <span>
                            FUNDING / CHARGES
                        </span>

                        <p>

                            {% if selected_service.funding_type %}

                                {{ selected_service.funding_type }}

                            {% endif %}


                            {% if selected_service.funding_min != None or selected_service.funding_max != None %}

                                {% if selected_service.funding_type %}
                                    ·
                                {% endif %}

                                {% if selected_service.funding_min != None %}
                                    ₹{{ selected_service.funding_min }}
                                {% endif %}

                                {% if selected_service.funding_min != None and selected_service.funding_max != None %}
                                    –
                                {% endif %}

                                {% if selected_service.funding_max != None %}
                                    ₹{{ selected_service.funding_max }}
                                {% endif %}

                            {% endif %}


                            {% if selected_service.government_fee != None %}

                                · Govt fee ₹{{ selected_service.government_fee }}

                            {% endif %}


                            {% if selected_service.bharatnxt_fee != None %}

                                · BharatNXT ₹{{ selected_service.bharatnxt_fee }}

                            {% endif %}

                        </p>

                    </section>

                {% endif %}


                {% if selected_service.important_notes %}

                    <section class="bde-preview-section">

                        <span>
                            CHECK BEFORE PROMISING
                        </span>

                        <p>
                            {{ selected_service.important_notes|truncatechars:220 }}
                        </p>

                    </section>

                {% endif %}


                <div class="bde-preview-actions">

                    {% if selected_service.primary_source %}

                        <a
                            href="{{ selected_service.primary_source.source_url }}"
                            target="_blank"
                            rel="noopener noreferrer"
                            class="bde-external-button">

                            Scheme ↗

                        </a>

                    {% endif %}


                    <a
                        href="{% url 'toolkit:service_detail' selected_service.slug %}"
                        class="primary-button">

                        Open Call Workspace

                    </a>

                </div>


            {% else %}

                <div class="bde-preview-empty">

                    <span>▦</span>

                    <h3>
                        Select a service
                    </h3>

                    <p>
                        Its call summary, eligibility,
                        benefit and key facts will appear here.
                    </p>

                </div>

            {% endif %}

        </aside>
    </div>
</div>
{% endblock %}


====================================================================================================
FILE: toolkit/templates/toolkit/toolkit_home.html
LINES: 708
SHA256: 38931f1b351fc5ef0c93f4060daedc8ec06aeb5d1ea1307058ae8190ae02559e
====================================================================================================
{% extends "base.html" %}
{% load static %}
{% block title %}Business Toolkit | BharatNXT Wave{% endblock %}

{% block content %}
<div class="bde-page bde-search-page">

    <header class="bde-page-hero">
        <div>
            <div class="bde-eyebrow">BUSINESS TOOLKIT</div>
            <h1>Find the right service <span>without leaving the call.</span></h1>
            <p>Search naturally by scheme, sector, funding type or client need. Suggestions appear while you type.</p>
        </div>
        <a href="{% url 'toolkit:service_library' %}"
           class="bde-hero-link">
            Browse complete Service Library →
        </a>
    </header>

    
    <section class="bde-primary-search-shell">

        <form method="get"
              action="{% url 'toolkit:home' %}"
              class="bde-page-search bde-feedback-form"
              data-search-context="toolkit"
              data-feedback-message="Searching services…"
              autocomplete="off"
 data-bnx-search-v41="1">

            <span class="bde-page-search-icon" aria-hidden="true">
                ⌕
            </span>

            <input
                type="search"
                name="q"
                value="{{ query }}"
                placeholder="Search schemes, grants, loans, sectors or client needs..."
                autocomplete="off"
                aria-label="Search BharatNXT services">

            <button
    type="submit"
    class="search-shortcut bnx-full-search-button-v41"
    aria-label="See all search results">
    See all results
    <span aria-hidden="true">↵</span>
</button>

            <div class="bde-page-suggestions"
                 hidden></div>

        </form>

<div
    class="bnx-search-help-v41"
    id="bnx-search-help-v41"
    role="status"
    aria-live="polite">

    <span
        class="bnx-search-help-icon-v41"
        aria-hidden="true">
        i
    </span>

    <span id="bnx-search-help-text-v41">
        Suggestions appear instantly while you type.
        Press Enter or click <strong>See all results</strong>
        to view the complete list of matching services.
    </span>

</div>


        <div class="bde-search-examples">
            <span>QUICK SEARCHES</span>
            <a href="?q=grant">Grants</a>
            <a href="?q=women">Women</a>
            <a href="?q=agriculture">Agriculture</a>
            <a href="?q=technology">Technology</a>
            <a href="?q=loan">Loans</a>
        </div>

    </section>


    <details class="bde-filter-panel" {% if selected_sector or selected_category or selected_kind or selected_deadline %}open{% endif %} data-bnx-filters-always-open-v1="1">
        <summary aria-expanded="true" aria-disabled="true" tabindex="-1">
            <span>Filters</span>
            <small>Sector, category, type and deadline</small>
        </summary>

        <form method="get"
              class="bde-filter-grid bde-feedback-form"
              data-feedback-message="Applying filters…">
            {% if query %}
                <input type="hidden" name="q" value="{{ query }}">
            {% endif %}

            <label>
                <span>Sector</span>
                <select name="sector">
                    <option value="">All sectors</option>
                    {% for item in sector_options %}
                        <option value="{{ item.name }}"
                                {% if selected_sector == item.name %}selected{% endif %}>
                            {{ item.name }}
                        </option>
                    {% endfor %}
                </select>
            </label>

            <label>
                <span>Category</span>
                <select name="category">
                    <option value="">All categories</option>
                    {% for item in categories %}
                        <option value="{{ item.pk }}"
                                {% if selected_category == item.pk|stringformat:"s" %}selected{% endif %}>
                            {{ item.name }}
                        </option>
                    {% endfor %}
                </select>
            </label>

            <label>
                <span>Service type</span>
                <select name="kind">
                    <option value="">All types</option>
                    {% for value, label in service_kinds %}
                        <option value="{{ value }}"
                                {% if selected_kind == value %}selected{% endif %}>
                            {{ label }}
                        </option>
                    {% endfor %}
                </select>
            </label>

            <label>
                <span>Deadline</span>
                <select name="deadline">
                    <option value="">Any deadline</option>
                    <option value="urgent" {% if selected_deadline == "urgent" %}selected{% endif %}>Within 7 days</option>
                    <option value="closing" {% if selected_deadline == "closing" %}selected{% endif %}>Within 30 days</option>
                    <option value="no_deadline" {% if selected_deadline == "no_deadline" %}selected{% endif %}>No fixed date</option>
                </select>
            </label>

            <button type="submit" class="secondary-button">Apply filters</button>
        </form>
    </details>

    <section class="bde-results-section">
        <div class="bde-results-head">
            <div>
                <div class="bde-eyebrow">
                    {% if query %}SEARCH RESULTS{% else %}ALL AVAILABLE SERVICES{% endif %}
                </div>
                <h2>
                    {% if query %}
                        {{ result_count }} result{{ result_count|pluralize }} for “{{ query }}”
                    {% else %}
                        {{ result_count }} services ready to browse
                    {% endif %}
                </h2>
            </div>

            <a href="{% url 'toolkit:service_library' %}"
               class="secondary-button">
                Open Service Library
            </a>
        </div>

        <div class="bde-service-list">
            {% for service in services %}
                <article class="bde-service-row">
                    <div class="bde-service-row-main">
                        <div class="bde-service-row-topline">
                            <span class="bde-service-code">{{ service.service_id }}</span>
                            <span class="bde-pill">{{ service.get_service_kind_display }}</span>
                            <span class="bde-pitch-state {{ service.pitch_state_code|lower }}">
                                {{ service.pitch_state_label }}
                            </span>
                        </div>

                        <a href="{% url 'toolkit:service_detail' service.slug %}"
                           class="bde-service-row-title">
                            {{ service.title }}
                        </a>

                        <div class="bde-service-row-path">
                            {{ service.domain.name }} · {{ service.category.name }}
                        </div>

                        {% if service.bde_summary %}
                            <p>{{ service.bde_summary|truncatechars:170 }}</p>
                        {% elif service.overview %}
                            <p>{{ service.overview|truncatechars:170 }}</p>
                        {% elif service.benefits %}
                            <p>{{ service.benefits|truncatechars:170 }}</p>
                        {% endif %}
                    </div>

                    <div class="bde-service-row-facts">
                        <div>
                            <span>Deadline</span>
                            <strong>{{ service.bde_deadline_label|truncatechars:42 }}</strong>
                        </div>
                        {% if service.funding_organisation %}
                            <div>
                                <span>Run by</span>
                                <strong>{{ service.funding_organisation|truncatechars:48 }}</strong>
                            </div>
                        {% endif %}
                    </div>

                    <div class="bde-service-row-actions">
                        <button type="button"
                                class="secondary-button bde-quick-button"
                                data-service-quick="{{ service.pk }}">
                            Quick View
                        </button>

                        {% if service.primary_source %}
                            <a href="{{ service.primary_source.source_url }}"
                               target="_blank"
                               rel="noopener noreferrer"
                               class="bde-external-button">
                                Open Scheme ↗
                            </a>
                        {% endif %}

                        <a href="{% url 'toolkit:service_detail' service.slug %}"
                           class="primary-button">
                            Details
                        </a>

                        <form method="post"
                              action="{% url 'toolkit:toggle_saved_service' service.pk %}"
                              class="bde-ajax-save">
                            {% csrf_token %}
                            <input type="hidden" name="next" value="{{ request.get_full_path }}">
                            <button type="submit"
                                    class="bde-save-icon {% if service.pk in saved_service_ids %}saved{% endif %}"
                                    aria-label="Save service">
                                {% if service.pk in saved_service_ids %}<span class="bn-bookmark-icon is-saved" aria-hidden="true"></span>{% else %}<span class="bn-bookmark-icon" aria-hidden="true"></span>{% endif %}
                            </button>
                        </form>
                    </div>
                </article>
            {% empty %}
                <div class="bde-empty-state">
                    <span>⌕</span>
                    <h3>No matching service found</h3>
                    <p>Try a broader term or remove a filter. The Toolkit only shows real published records.</p>
                </div>
            {% endfor %}
        </div>
    </section>
</div>


<!-- BNX_SEARCH_UX_V41 -->
<style id="BNX_SEARCH_UX_V41">

[data-bnx-search-v41="1"]
.bnx-full-search-button-v41 {

    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    gap: 7px !important;

    min-height: 42px !important;

    padding:
        0 16px !important;

    border:
        1px solid #1769ff !important;

    border-radius:
        11px !important;

    background:
        #1769ff !important;

    color:
        #ffffff !important;

    font-size:
        13px !important;

    font-weight:
        700 !important;

    line-height:
        1 !important;

    white-space:
        nowrap !important;

    cursor:
        pointer !important;

    pointer-events:
        auto !important;

    box-shadow:
        0 5px 16px
        rgba(23, 105, 255, .18) !important;

    transition:
        background .15s ease,
        border-color .15s ease,
        box-shadow .15s ease,
        transform .15s ease !important;
}


[data-bnx-search-v41="1"]
.bnx-full-search-button-v41:hover {

    background:
        #0f5ce8 !important;

    border-color:
        #0f5ce8 !important;

    box-shadow:
        0 7px 20px
        rgba(23, 105, 255, .23) !important;

    transform:
        translateY(-1px) !important;
}


[data-bnx-search-v41="1"]
.bnx-full-search-button-v41:active {

    transform:
        translateY(0) !important;
}


[data-bnx-search-v41="1"]
.bnx-full-search-button-v41:focus-visible {

    outline:
        3px solid
        rgba(23, 105, 255, .22) !important;

    outline-offset:
        2px !important;
}


.bnx-search-help-v41 {

    display:
        flex;

    align-items:
        flex-start;

    gap:
        8px;

    width:
        fit-content;

    max-width:
        100%;

    margin:
        9px 0 0;

    padding:
        7px 10px;

    border:
        1px solid
        rgba(23, 105, 255, .13);

    border-radius:
        9px;

    background:
        rgba(23, 105, 255, .04);

    color:
        #667085;

    font-size:
        12.5px;

    font-weight:
        500;

    line-height:
        1.4;
}


.bnx-search-help-v41 strong {

    color:
        #344054;

    font-weight:
        700;
}


.bnx-search-help-icon-v41 {

    display:
        inline-flex;

    align-items:
        center;

    justify-content:
        center;

    flex:
        0 0 16px;

    width:
        16px;

    height:
        16px;

    margin-top:
        1px;

    border-radius:
        50%;

    background:
        #eaf2ff;

    color:
        #1769ff;

    font-size:
        10px;

    font-weight:
        800;
}


.bnx-search-help-v41.is-searching
.bnx-search-help-icon-v41 {

    animation:
        bnxSearchPulseV41
        .75s ease-in-out infinite;
}


@keyframes bnxSearchPulseV41 {

    0%,
    100% {
        opacity: .4;
    }

    50% {
        opacity: 1;
    }

}

</style>


<script id="BNX_SEARCH_UX_V41_SCRIPT">

(function () {

    "use strict";


    const form =
        document.querySelector(
            '[data-bnx-search-v41="1"]'
        );


    if (!form) {
        return;
    }


    const input =
        form.querySelector(
            'input[name="q"]'
        );


    const button =
        form.querySelector(
            ".bnx-full-search-button-v41"
        );


    const help =
        document.getElementById(
            "bnx-search-help-v41"
        );


    const helpText =
        document.getElementById(
            "bnx-search-help-text-v41"
        );


    if (
        !input
        ||
        !help
        ||
        !helpText
    ) {
        return;
    }


    function defaultMessage() {

        help.classList.remove(
            "is-searching"
        );


        helpText.innerHTML =
            'Suggestions appear instantly while you type. '
            + 'Press Enter or click '
            + '<strong>See all results</strong> '
            + 'to view the complete list of matching services.';

    }


    function typingMessage() {

        const query =
            input.value.trim();


        if (!query) {

            defaultMessage();

            return;

        }


        help.classList.remove(
            "is-searching"
        );


        helpText.innerHTML =
            'Showing instant suggestions for <strong></strong>. '
            + 'Press Enter or click '
            + '<strong>See all results</strong> '
            + 'to view every matching service.';


        const firstStrong =
            helpText.querySelector(
                "strong"
            );


        if (firstStrong) {

            firstStrong.textContent =
                "“" + query + "”";

        }

    }


    input.addEventListener(
        "input",
        typingMessage
    );


    form.addEventListener(
        "submit",
        function () {

            help.classList.add(
                "is-searching"
            );


            helpText.textContent =
                "Opening complete search results…";


            if (button) {

                button.disabled =
                    true;

                button.innerHTML =
                    "Searching…";

            }

        }
    );


    typingMessage();

})();

</script>



<!-- BNX_FILTERS_ALWAYS_OPEN_V1 -->
<style id="BNX_FILTERS_ALWAYS_OPEN_V1_STYLE">

details[data-bnx-filters-always-open-v1="1"]
> summary {
    cursor: default !important;
}

details[data-bnx-filters-always-open-v1="1"]
> summary::marker {
    display: none;
}

</style>

<script id="BNX_FILTERS_ALWAYS_OPEN_V1_SCRIPT">

(function () {

    "use strict";

    const filters =
        document.querySelector(
            'details[data-bnx-filters-always-open-v1="1"]'
        );

    if (!filters) {
        return;
    }

    filters.open = true;


    const summary =
        filters.querySelector(
            ":scope > summary"
        );


    if (summary) {

        summary.addEventListener(
            "click",
            function (event) {

                event.preventDefault();

                filters.open = true;

            }
        );

    }


    filters.addEventListener(
        "toggle",
        function () {

            if (!filters.open) {

                filters.open = true;

            }

        }
    );

})();

</script>

{% endblock %}


====================================================================================================
FILE: toolkit/urls.py
LINES: 167
SHA256: 25c3f54e20b5593518f0fd0e3220edc7fcd2c41224d97296aeb94bee8db5e690
====================================================================================================
from django.urls import path

from . import admin_views, import_views, matcher_views, reconciliation_views, views, database_map_views

app_name = "toolkit"

urlpatterns = [
    path(
        "toolkit/",
        views.toolkit_home,
        name="home"
    ),
    path(
        "toolkit/search/suggestions/",
        views.search_suggestions,
        name="search_suggestions"
    ),
    path(
        "toolkit/library/",
        views.service_library,
        name="service_library"
    ),
    path(
        "toolkit/library/quick/<int:service_id>/",
        views.service_quick_view,
        name="service_quick_view"
    ),
    path(
        "toolkit/recent/",
        views.recent_services,
        name="recent_services"
    ),
    path(
        "toolkit/service/<slug:slug>/",
        views.service_detail,
        name="service_detail"
    ),
    path(
        "toolkit/saved/",
        views.saved_services,
        name="saved_services"
    ),

    # BNX_SAVED_COLLECTIONS_V1
    path(
        "toolkit/saved/collection/create/",
        views.saved_collection_create,
        name="saved_collection_create",
    ),
    path(
        "toolkit/saved/collection/<int:collection_id>/",
        views.saved_collection_detail,
        name="saved_collection_detail",
    ),
    path(
        "toolkit/saved/collection/<int:collection_id>/rename/",
        views.saved_collection_rename,
        name="saved_collection_rename",
    ),
    path(
        "toolkit/saved/collection/<int:collection_id>/delete/",
        views.saved_collection_delete,
        name="saved_collection_delete",
    ),
    path(
        "toolkit/saved/collection/<int:collection_id>/item/<int:item_id>/remove/",
        views.saved_collection_remove_item,
        name="saved_collection_remove_item",
    ),
    path(
        "toolkit/service/<int:service_id>/collections/",
        views.saved_service_collection_state,
        name="saved_service_collection_state",
    ),
    path(
        "toolkit/service/<int:service_id>/collections/action/",
        views.saved_service_collection_action,
        name="saved_service_collection_action",
    ),

    path(
        "toolkit/matcher/",
        matcher_views.client_matcher,
        name="client_matcher"
    ),
    path(
        "toolkit/service/<int:service_id>/save/",
        views.toggle_saved_service,
        name="toggle_saved_service"
    ),

    path(
        "admin-center/toolkit/",
        admin_views.service_management_list,
        name="admin_service_list"
    ),
    path(
        "admin-center/toolkit/pitch-windows/",
        admin_views.pitch_window_list,
        name="pitch_windows"
    ),
    path(
        "admin-center/toolkit/add/",
        admin_views.service_create,
        name="admin_service_create"
    ),
    path(
        "admin-center/toolkit/<int:service_id>/edit/",
        admin_views.service_edit,
        name="admin_service_edit"
    ),
    path(
        "admin-center/toolkit/<int:service_id>/verify/",
        admin_views.service_verify,
        name="admin_service_verify"
    ),

    path(
        "admin-center/import/",
        import_views.import_center,
        name="import_center"
    ),
    path(
        "admin-center/import/<int:batch_id>/review/",
        import_views.import_extraction_review,
        name="import_extraction_review",
    ),

    path(
        "admin-center/import/<int:batch_id>/review/<int:row_id>/decision/",
        import_views.import_extraction_row_decision,
        name="import_extraction_row_decision",
    ),

    # ADMIN_IMPORT_UX_V2
    path(
        "admin-center/import/<int:batch_id>/review/approve-safe/",
        import_views.import_bulk_safe_approve,
        name="import_bulk_safe_approve"
    ),

    path(
        "admin-center/import/<int:batch_id>/finalize/",
        reconciliation_views.reconciliation_finalize,
        name="reconciliation_finalize"
    ),
    path(
        "admin-center/import/history/",
        import_views.import_history,
        name="import_history"
    ),
    path(
        "admin-center/import/history/<int:batch_id>/",
        import_views.import_history_detail,
        name="import_history_detail"
    ),
    path(
        "admin-center/import/history/<int:batch_id>/delete/",
        import_views.import_source_delete,
        name="import_source_delete",
    ),
    path(
        "admin-center/database-map/",
        database_map_views.database_map,
        name="database_map"
    ),
]


====================================================================================================
FILE: toolkit/views.py
LINES: 4035
SHA256: 66593b1858fd9184853560230df1616dd1d77183d946d421134bf3d41c02b0e1
====================================================================================================
from toolkit.visibility import visible_service_statuses
from django.contrib import messages
from django.contrib.auth.decorators import login_required
from django.db.models import Q
from django.http import JsonResponse
from django.shortcuts import (
    get_object_or_404,
    redirect,
    render,
)
from django.utils.http import (
    url_has_allowed_host_and_scheme,
)
from django.views.decorators.http import require_POST

from accounts.activity import log_activity

from .models import (
    Category,
    SavedCollection,
    SavedCollectionItem,
    SavedService,
    SearchEvent,
    Service,
    ServiceDomain,
)

from .pitching import (
    VISIBLE_STATUSES,
    apply_pitch_state,
    apply_pitch_states,
    available_sectors,
    closing_soon_queryset,
    pitchable_queryset,
    sector_match_ids,
)


SOURCE_PRIORITY = {
    "APPLICATION": 0,
    "OFFICIAL_PORTAL": 1,
    "REFERENCE": 2,
    "FLYER": 3,
    "OTHER": 4,
}

SOURCE_LABELS = {
    "APPLICATION": "Apply / Open Portal",
    "OFFICIAL_PORTAL": "Official Scheme Page",
    "REFERENCE": "Reference",
    "FLYER": "Guidelines / Flyer",
    "OTHER": "External Link",
}


def visible_services():
    """
    Services that are currently pitchable according to the pitch-window engine.
    Kept for pitch-window-specific workflows.
    """
    return pitchable_queryset(
        Service.objects.select_related(
            "domain",
            "category",
            "verified_by",
        )
    )


def all_bde_services():
    """
    Complete BDE catalog.

    Published/approved statuses are visible even when a pitch-window warning
    exists. The UI shows the pitch state instead of silently hiding the record.
    """
    return (
        Service.objects
        .select_related(
            "domain",
            "category",
            "verified_by",
        )
        .prefetch_related(
            "sources",
            "classifications",
            "eligibility_rules",
            "document_requirements",
            "process_steps",
        )
        .filter(
            status__in=VISIBLE_STATUSES
        )
    )



def business_knowledge_match_ids(query):
    """
    Return Service IDs matched through BDE-visible
    supporting business knowledge.

    Raw imports, hashes, audit data and technical metadata
    are deliberately excluded.
    """

    from django.apps import apps

    from django.db import (
        models as django_models,
    )

    from django.db.models import (
        Q as KnowledgeQ,
    )


    query = str(
        query or ""
    ).strip()


    if not query:

        return set()


    # Explicit allow-list.
    # Technical models such as ImportBatch, ImportRow,
    # ImportChange and audit/security models are absent.
    business_models = (
        "ServiceContentSection",
        "ServiceCommercial",
        "ServiceSource",
        "KnowledgeSection",
        "EligibilityRule",
        "DocumentRequirement",
        "ProcessStep",
        "ReferenceItem",
        "CommunicationTemplate",
    )


    blocked_fields = {
        "visibility",
        "status",
        "section_type",
        "content_type",
        "kind",
        "source_kind",
        "source_type",
        "source_reference",
        "source_sheet",
        "source_row_number",
        "row_hash",
        "raw_data",
        "metadata",
        "before_snapshot",
        "after_snapshot",
        "object_model",
        "object_pk",
        "engine_version",
        "ip_address",
        "user_agent",
    }


    blocked_fragments = (
        "internal",
        "technical",
        "audit",
        "hash",
        "import_",
        "created_by",
        "updated_by",
    )


    service_ids = set()


    for model_name in business_models:

        try:

            Model = apps.get_model(
                "toolkit",
                model_name,
            )

        except LookupError:

            continue


        # ----------------------------------------------------
        # REQUIRE DIRECT SERVICE RELATION
        # ----------------------------------------------------

        service_field = None


        for field in Model._meta.fields:

            remote = getattr(
                field,
                "remote_field",
                None,
            )

            remote_model = getattr(
                remote,
                "model",
                None,
            )


            if remote_model is None:

                continue


            if (
                getattr(
                    remote_model._meta,
                    "label_lower",
                    "",
                )
                == "toolkit.service"
            ):

                service_field = field
                break


        # Do not invent relationships between unrelated
        # business records and Services.
        if service_field is None:

            continue


        # ----------------------------------------------------
        # SAFE BUSINESS TEXT FIELDS
        # ----------------------------------------------------

        searchable_fields = []


        for field in Model._meta.fields:

            if not isinstance(
                field,
                (
                    django_models.CharField,
                    django_models.TextField,
                ),
            ):

                continue


            name = field.name
            lowered = name.lower()


            if name in blocked_fields:

                continue


            if any(
                fragment in lowered
                for fragment
                in blocked_fragments
            ):

                continue


            searchable_fields.append(
                name
            )


        if not searchable_fields:

            continue


        condition = KnowledgeQ()


        for field_name in searchable_fields:

            condition |= _safe_text_condition(
                KnowledgeQ,
                field_name,
                query,
            )


        queryset = Model.objects.filter(
            condition
        )


        model_fields = {
            field.name
            for field in Model._meta.fields
        }


        # ----------------------------------------------------
        # BDE VISIBILITY
        # ----------------------------------------------------

        if "visibility" in model_fields:

            visibility_field = (
                Model._meta.get_field(
                    "visibility"
                )
            )


            valid_choices = {
                str(value)
                for value, label
                in (
                    visibility_field.choices
                    or []
                )
            }


            permitted = []


            if "BDE" in valid_choices:

                permitted.append(
                    "BDE"
                )


            if "BDE_ALLOWED" in valid_choices:

                permitted.append(
                    "BDE_ALLOWED"
                )


            # If a visibility-controlled model does not expose
            # a known BDE permission, do not search it.
            if not permitted:

                continue


            queryset = queryset.filter(
                visibility__in=permitted
            )


        if "is_active" in model_fields:

            queryset = queryset.filter(
                is_active=True
            )


        if "is_published" in model_fields:

            queryset = queryset.filter(
                is_published=True
            )


        ids = queryset.values_list(
            service_field.attname,
            flat=True,
        )


        service_ids.update(
            service_id
            for service_id
            in ids
            if service_id
        )


    return service_ids





# ============================================================
# SHORT_TECH_KEYWORD_SEARCH_V11
# ============================================================

_SHORT_TECH_PATTERNS = {

    "ai": (
        r"(^|[^A-Za-z0-9])"
        r"ai"
        r"([^A-Za-z0-9]|$)"
    ),

    "iot": (
        r"(^|[^A-Za-z0-9])"
        r"iot"
        r"([^A-Za-z0-9]|$)"
    ),
}


def _short_tech_pattern(
    query,
):

    normalized = str(
        query or ""
    ).strip().casefold()


    return (
        _SHORT_TECH_PATTERNS
        .get(
            normalized
        )
    )


def _short_tech_terms_in_query(
    query,
):
    """
    Detect explicit AI / IoT concepts inside
    longer natural-language searches.
    """

    import re


    text = str(
        query or ""
    ).casefold()


    result = []


    if (
        re.search(
            r"(^|[^a-z0-9])ai([^a-z0-9]|$)",
            text,
        )
        or
        "artificial intelligence"
        in text
    ):

        result.append(
            "ai"
        )


    if (
        re.search(
            r"(^|[^a-z0-9])iot([^a-z0-9]|$)",
            text,
        )
        or
        "internet of things"
        in text
    ):

        result.append(
            "iot"
        )


    return result


def _safe_text_condition(
    q_class,
    field_name,
    query,
):
    """
    AI and IoT use word/concept boundaries.

    Every other query keeps the existing
    icontains behaviour.
    """

    pattern = (
        _short_tech_pattern(
            query
        )
    )


    if pattern:

        return q_class(
            **{
                f"{field_name}__iregex":
                    pattern
            }
        )


    return q_class(
        **{
            f"{field_name}__icontains":
                query
        }
    )


def _exact_short_tech_ids(
    base_services,
    query,
):

    import re


    pattern_text = (
        _short_tech_pattern(
            query
        )
    )


    if not pattern_text:

        return None


    # --------------------------------------------------------
    # CORE SERVICE TEXT
    # --------------------------------------------------------

    result = set(
        base_services
        .filter(
            search_text_filter(
                query
            )
        )
        .distinct()
        .values_list(
            "pk",
            flat=True,
        )
    )


    # --------------------------------------------------------
    # BDE BUSINESS KNOWLEDGE
    # --------------------------------------------------------

    result.update(
        business_knowledge_match_ids(
            query
        )
    )


    # --------------------------------------------------------
    # STRUCTURED INDUSTRIES
    #
    # IMPORTANT:
    # all_bde_services() has select_related().
    # Clear it before only() so related fields
    # are not deferred/traversed simultaneously.
    # --------------------------------------------------------

    regex = re.compile(
        pattern_text,
        flags=re.I,
    )


    industry_queryset = (
        base_services
        .select_related(
            None
        )
        .only(
            "pk",
            "industries",
        )
    )


    for service in industry_queryset:

        industries = (
            service.industries
            or []
        )


        if isinstance(
            industries,
            (
                list,
                tuple,
                set,
            ),
        ):

            text = " ".join(
                str(value)
                for value in industries
                if value
            )

        else:

            text = str(
                industries
            )


        if regex.search(
            text
        ):

            result.add(
                service.pk
            )


    return result


def _guard_short_tech_results(
    base_services,
    query,
    service_ids,
):
    """
    When AI or IoT is explicitly requested,
    require genuine AI/IoT evidence.
    """

    result = set(
        service_ids
    )


    for term in (
        _short_tech_terms_in_query(
            query
        )
    ):

        exact_ids = (
            _exact_short_tech_ids(
                base_services,
                term,
            )
        )


        if exact_ids is not None:

            result &= set(
                exact_ids
            )


    return result


def search_text_filter(query):
    return (
        _safe_text_condition(
            Q,
            "sales_pitch",
            query,
        )
        | _safe_text_condition(
            Q,
            "subsidy_details",
            query,
        )
        | _safe_text_condition(
            Q,
            "funding_type",
            query,
        )
        | _safe_text_condition(
            Q,
            "service_kind",
            query,
        )
        | _safe_text_condition(
            Q,
            "classifications__name",
            query,
        )
        | _safe_text_condition(
            Q,
            "category__name",
            query,
        )
        | _safe_text_condition(
            Q,
            "domain__name",
            query,
        )
        | _safe_text_condition(
            Q,
            "application_deadline_raw",
            query,
        )
        | _safe_text_condition(
            Q,
            "funding_organisation",
            query,
        )
        | _safe_text_condition(
            Q,
            "applicable_for_raw",
            query,
        )
        | _safe_text_condition(
            Q,
            "eligibility_summary",
            query,
        )
        | _safe_text_condition(
            Q,
            "benefits",
            query,
        )
        | _safe_text_condition(
            Q,
            "overview",
            query,
        )
        | _safe_text_condition(
            Q,
            "bde_summary",
            query,
        )
        | _safe_text_condition(
            Q,
            "title",
            query,
        )
        | _safe_text_condition(
            Q,
            "service_id",
            query,
        )
    )



def _natural_variant_match_ids(
    base_services,
    variants,
):
    """
    Search one natural-language concept against the same
    trusted V1 data surfaces.
    """

    condition = Q()


    for variant in variants:

        condition |= search_text_filter(
            variant
        )


    service_ids = set(
        base_services
        .filter(
            condition
        )
        .distinct()
        .values_list(
            "pk",
            flat=True,
        )
    )


    for variant in variants:

        service_ids.update(
            sector_match_ids(
                base_services,
                variant,
            )
        )


        service_ids.update(
            business_knowledge_match_ids(
                variant
            )
        )


    return service_ids


def natural_language_match_ids(
    base_services,
    query,
):
    """
    Deterministic English/Hinglish search.

    Existing concept matching remains unchanged.

    AI / IoT additionally require real
    keyword evidence to avoid substring matches.
    """

    from .search_intelligence import (
        query_groups,
    )


    groups = query_groups(
        query
    )


    if not groups:

        return set()


    matched_groups = [
        _natural_variant_match_ids(
            base_services,
            variants,
        )
        for variants in groups
    ]


    if len(
        matched_groups
    ) == 1:

        return _guard_short_tech_results(
            base_services,
            query,
            matched_groups[
                0
            ],
        )


    if all(
        matched_groups
    ):

        intersection = set.intersection(
            *matched_groups
        )


        if intersection:

            return _guard_short_tech_results(
                base_services,
                query,
                intersection,
            )


    scores = {}


    for matched_ids in matched_groups:

        for service_id in matched_ids:

            scores[
                service_id
            ] = (
                scores.get(
                    service_id,
                    0,
                )
                + 1
            )


    group_count = len(
        matched_groups
    )


    if group_count <= 2:

        threshold = group_count

    elif group_count == 3:

        threshold = 2

    else:

        threshold = max(
            2,
            group_count - 1,
        )


    result = {
        service_id
        for service_id, score
        in scores.items()
        if score >= threshold
    }


    return _guard_short_tech_results(
        base_services,
        query,
        result,
    )




def _search_money_amounts(value):
    """
    Extract monetary values and normalize them to lakh units.

    5 lakh  -> 5
    25 lakh -> 25
    1 crore -> 100

    This prevents substring mistakes such as
    5 lakh matching 25 lakh.
    """

    import re

    from decimal import Decimal


    text = str(
        value or ""
    ).casefold()


    pattern = re.compile(
        r"(?<![0-9])"
        r"([0-9]+(?:\.[0-9]+)?)"
        r"\s*"
        r"(lakh|lakhs|lac|lacs|crore|crores|cr)"
        r"\b"
    )


    amounts = set()


    for number, unit in pattern.findall(
        text
    ):

        amount = Decimal(
            number
        )


        if unit in {
            "crore",
            "crores",
            "cr",
        }:

            amount *= Decimal(
                "100"
            )


        amounts.add(
            amount
        )


    return amounts


def natural_language_rank_map(
    base_services,
    query,
):
    """
    Rank existing V2A.1 candidates.

    This DOES NOT change search recall.
    It only determines which relevant Services appear first.

    Scores are internal only.
    """

    from .search_intelligence import (
        normalize_query,
        query_groups,
    )


    groups = query_groups(
        query
    )


    if not groups:

        return {}


    matched_groups = [
        _natural_variant_match_ids(
            base_services,
            variants,
        )
        for variants in groups
    ]


    candidate_ids = set()


    for matched_ids in matched_groups:

        candidate_ids.update(
            matched_ids
        )


    if not candidate_ids:

        return {}


    services = list(
        base_services
        .filter(
            pk__in=candidate_ids
        )
        .select_related(
            "domain",
            "category",
        )
        .distinct()
    )


    query_amounts = (
        _search_money_amounts(
            query
        )
    )


    scores = {}


    for service in services:

        values = (
            service.title,
            service.service_id,
            service.bde_summary,
            service.overview,
            service.benefits,
            service.eligibility_summary,
            service.applicable_for_raw,
            service.funding_organisation,
            service.funding_type,
            service.subsidy_details,
            service.sales_pitch,
            service.restrictions,
            service.important_notes,
        )


        raw_text = " ".join(
            str(value)
            for value in values
            if value
        )


        core_text = normalize_query(
            raw_text
        )


        service_amounts = (
            _search_money_amounts(
                raw_text
            )
        )


        score = 0
        true_matches = 0


        for variants, matched_ids in zip(
            groups,
            matched_groups,
        ):

            group_amounts = set()


            for variant in variants:

                group_amounts.update(
                    _search_money_amounts(
                        variant
                    )
                )


            # =================================================
            # NUMERIC CONCEPT
            # =================================================

            if group_amounts:

                if (
                    query_amounts
                    & service_amounts
                ):

                    true_matches += 1

                    # Exact amount evidence.
                    score += 2500


                elif (
                    query_amounts
                    and service_amounts
                    and any(
                        available
                        >= requested
                        for available
                        in service_amounts
                        for requested
                        in query_amounts
                    )
                ):

                    # Can potentially provide at least
                    # requested amount, but isn't exact.
                    score += 120


                continue


            # =================================================
            # NORMAL BUSINESS CONCEPT
            # =================================================

            if service.pk not in matched_ids:

                continue


            true_matches += 1
            score += 450


            title = normalize_query(
                service.title
            )


            category = normalize_query(
                service.category.name
                if service.category
                else ""
            )


            domain = normalize_query(
                service.domain.name
                if service.domain
                else ""
            )


            normalized_variants = [
                normalize_query(
                    variant
                )
                for variant in variants
                if variant
            ]


            if any(
                variant
                and variant in title
                for variant
                in normalized_variants
            ):

                score += 350


            if any(
                variant
                and variant in core_text
                for variant
                in normalized_variants
            ):

                score += 220


            if any(
                variant
                and (
                    variant in category
                    or variant in domain
                )
                for variant
                in normalized_variants
            ):

                score += 100


        # =====================================================
        # FULL-INTENT BONUS
        # =====================================================

        if true_matches == len(
            groups
        ):

            score += 1500


        elif (
            len(groups) >= 3
            and true_matches
            == len(groups) - 1
        ):

            score += 200


        scores[
            service.pk
        ] = score


    return scores


def search_relevance_sort_key(
    service,
    relevance_scores,
    query,
    domain_tiebreak=False,
):
    """
    Shared relevance order for:

    - Business Toolkit
    - live search suggestions
    - Service Library
    """

    from .search_intelligence import (
        normalize_query,
    )


    score = int(
        relevance_scores.get(
            service.pk,
            0,
        )
    )


    query_text = normalize_query(
        query
    )


    title = normalize_query(
        service.title
    )


    # Known Service name searches stay strongest.
    if (
        query_text
        and title == query_text
    ):

        score += 5000


    elif (
        query_text
        and title.startswith(
            query_text
        )
    ):

        score += 1800


    elif (
        query_text
        and query_text in title
    ):

        score += 900


    domain_order = 0


    if domain_tiebreak:

        domain_order = getattr(
            service.domain,
            "display_order",
            9999,
        )


    return (
        -score,
        domain_order,
        title,
    )




def _source_sort_key(source):
    return (
        SOURCE_PRIORITY.get(
            source.source_kind,
            99,
        ),
        0 if source.is_official else 1,
        source.pk or 0,
    )


def _deadline_label(service):
    if service.application_deadline:
        return service.application_deadline.strftime(
            "%d %b %Y"
        )

    raw = (
        service.application_deadline_raw
        or ""
    ).strip()

    if raw:
        return raw

    if service.deadline_status not in {
        "",
        "UNKNOWN",
    }:
        return service.get_deadline_status_display()

    return "Not recorded"


def _attach_bde_ui(service):
    apply_pitch_state(service)

    sources = [
        source
        for source
        in service.sources.all()
        if source.source_url
    ]

    sources.sort(
        key=_source_sort_key
    )

    service.bde_sources = sources
    service.primary_source = (
        sources[0]
        if sources
        else None
    )

    service.bde_deadline_label = (
        _deadline_label(service)
    )

    return service


def _attach_bde_ui_many(services):
    for service in services:
        _attach_bde_ui(service)

    return services


def _remember_recent(request, service):
    recent = list(
        request.session.get(
            "bde_recent_service_ids",
            [],
        )
    )

    service_id = int(service.pk)

    recent = [
        item
        for item
        in recent
        if int(item) != service_id
    ]

    recent.insert(
        0,
        service_id,
    )

    request.session[
        "bde_recent_service_ids"
    ] = recent[:12]


def _serialize_service(
    request,
    service,
):
    _attach_bde_ui(service)
    _remember_recent(
        request,
        service,
    )

    summary = (
        service.bde_summary
        or service.overview
        or service.benefits
        or ""
    ).strip()

    sources = [
        {
            "name": (
                source.source_name
                or SOURCE_LABELS.get(
                    source.source_kind,
                    "External Link",
                )
            ),
            "label": SOURCE_LABELS.get(
                source.source_kind,
                "External Link",
            ),
            "kind": source.source_kind,
            "url": source.source_url,
            "official": source.is_official,
        }
        for source
        in service.bde_sources
    ]

    return {
        "id": service.pk,
        "service_id": service.service_id,
        "title": service.title,
        "domain": service.domain.name,
        "category": service.category.name,
        "kind": (
            service.get_service_kind_display()
        ),
        "summary": summary,
        "benefits": service.benefits or "",
        "eligibility": (
            service.eligibility_summary
            or ""
        ),
        "applicable_for": (
            service.applicable_for_raw
            or ""
        ),
        "funding_organisation": (
            service.funding_organisation
            or ""
        ),
        "deadline": (
            service.bde_deadline_label
        ),
        "pitch_state": (
            service.pitch_state_label
        ),
        "pitch_state_code": (
            service.pitch_state_code
        ),
        "primary_url": (
            service.primary_source.source_url
            if service.primary_source
            else ""
        ),
        "detail_url": (
            f"/toolkit/service/{service.slug}/"
        ),
        "sources": sources,
        "requirements": [
            {
                "name": item.name,
                "description": (
                    item.description
                    or ""
                ),
                "mandatory": (
                    item.is_mandatory
                ),
            }
            for item
            in service.document_requirements.all()
        ],
        "process": [
            {
                "number": item.step_number,
                "title": item.title,
                "description": (
                    item.description
                    or ""
                ),
            }
            for item
            in service.process_steps.all()
        ],
        "saved": SavedService.objects.filter(
            user=request.user,
            service=service,
        ).exists(),
    }


@login_required
def toolkit_home(request):
    # BDE search is now a complete published catalog search.
    # Pitch windows are displayed as state, not used to hide valid records.
    base_services = all_bde_services()

    query = request.GET.get(
        "q",
        ""
    ).strip()

    sector = request.GET.get(
        "sector",
        ""
    ).strip()

    domain_slug = request.GET.get(
        "domain",
        ""
    ).strip()

    category_id = request.GET.get(
        "category",
        ""
    ).strip()

    service_kind = request.GET.get(
        "kind",
        ""
    ).strip()

    deadline_filter = request.GET.get(
        "deadline",
        ""
    ).strip()


    services = base_services


    if query:
        sector_ids = sector_match_ids(
            base_services,
            query
        )

        services = services.filter(
            search_text_filter(query)
            | Q(pk__in=sector_ids)
            | Q(
                pk__in=business_knowledge_match_ids(
                    query
                )
            )
            | Q(
                pk__in=natural_language_match_ids(
                    base_services,
                    query
                )
            )
        ).distinct()


    if sector:
        sector_ids = sector_match_ids(
            base_services,
            sector
        )

        services = services.filter(
            pk__in=sector_ids
        )


    if domain_slug:
        services = services.filter(
            domain__slug=domain_slug
        )


    if category_id.isdigit():
        category_pk = int(
            category_id
        )

        services = services.filter(
            Q(category_id=category_pk)
            | Q(
                classifications__id=(
                    category_pk
                )
            )
        ).distinct()


    valid_kinds = {
        value
        for value, label
        in Service.SERVICE_KIND_CHOICES
    }

    if service_kind in valid_kinds:
        services = services.filter(
            service_kind=service_kind
        )


    if deadline_filter == "closing":
        services = closing_soon_queryset(
            services,
            days=30
        )

    elif deadline_filter == "urgent":
        services = closing_soon_queryset(
            services,
            days=7
        )

    elif deadline_filter == "no_deadline":
        services = services.filter(
            application_deadline__isnull=True,
        )


    result_count = services.count()


    filters = {
        "sector": sector,
        "domain": domain_slug,
        "category": category_id,
        "service_kind": service_kind,
        "deadline": deadline_filter,
    }

    filters_used = any(
        filters.values()
    )


    if query or filters_used:
        SearchEvent.objects.create(
            user=request.user,
            event_type=(
                "SEARCH"
                if query
                else "FILTER"
            ),
            query=query,
            filters=filters,
            result_count=result_count,
        )


    if query:
        log_activity(
            request,
            "SEARCH",
            f'Toolkit search: "{query}"',
            metadata={
                "query": query,
                "result_count": result_count,
            },
        )


    if filters_used:
        log_activity(
            request,
            "FILTER",
            "Toolkit filters applied.",
            metadata={
                **filters,
                "result_count": result_count,
            },
        )


    domains = (
        ServiceDomain.objects
        .filter(is_active=True)
        .order_by(
            "display_order",
            "name"
        )
    )


    categories = (
        Category.objects
        .filter(is_active=True)
        .select_related("domain")
        .order_by(
            "domain__display_order",
            "display_order",
            "name"
        )
    )


    sector_options = available_sectors(
        base_services
    )


    if query:

        relevance_scores = (
            natural_language_rank_map(
                base_services,
                query,
            )
        )

        service_list = list(
            services.select_related(
                "domain",
                "category",
            )
        )

        service_list.sort(
            key=lambda service:
                search_relevance_sort_key(
                    service,
                    relevance_scores,
                    query,
                    domain_tiebreak=True,
                )
        )

        service_list = (
            service_list[:250]
        )

    else:

        service_list = list(
            services.order_by(
                "domain__display_order",
                "title"
            )[:250]
        )

    _attach_bde_ui_many(
        service_list
    )


    saved_service_ids = set(
        SavedService.objects
        .filter(user=request.user)
        .values_list(
            "service_id",
            flat=True
        )
    )


    context = {
        "services": service_list,
        "result_count": result_count,
        "domains": domains,
        "categories": categories,
        "sector_options": sector_options,
        "service_kinds": (
            Service.SERVICE_KIND_CHOICES
        ),
        "query": query,
        "selected_sector": sector,
        "selected_domain": domain_slug,
        "selected_category": category_id,
        "selected_kind": service_kind,
        "selected_deadline": deadline_filter,
        "saved_service_ids": (
            saved_service_ids
        ),
    }


    return render(
        request,
        "toolkit/toolkit_home.html",
        context
    )


@login_required
def search_suggestions(request):
    """
    BharatNXT Smart Instant Typeahead V3.

    Purpose:
    - instant dropdown while the BDE types
    - include BDE-visible supporting Toolkit knowledge
    - understand common business wording variations
    - preserve token-safe AI / IoT behaviour

    This is NOT the authoritative full Search V2.

    Pressing Enter still runs toolkit_home() and the complete
    Search V2 intelligence.
    """

    from time import monotonic

    from .models import (
        ServiceContentSection,
    )


    query = request.GET.get(
        "q",
        ""
    ).strip()


    if len(query) < 2:

        return JsonResponse(
            {
                "results": []
            }
        )


    # =====================================================
    # NORMALISE TEXT
    # =====================================================

    def normalize(value):

        text = str(
            value or ""
        ).casefold()


        chars = []

        previous_space = False


        for char in text:

            if char.isalnum():

                chars.append(
                    char
                )

                previous_space = False

            else:

                if not previous_space:

                    chars.append(
                        " "
                    )

                    previous_space = True


        return " ".join(
            "".join(
                chars
            ).split()
        )


    # =====================================================
    # SEARCH LANGUAGE NORMALISATION
    #
    # Phrase aliases handle abbreviations.
    # Word aliases handle safe singular/plural/common variants.
    #
    # We deliberately DO NOT use aggressive stemming because
    # that can create false positives in business searches.
    # =====================================================

    phrase_aliases = (
        (
            "artificial intelligence",
            "ai",
        ),
        (
            "internet of things",
            "iot",
        ),
        (
            "fin tech",
            "fintech",
        ),
        (
            "financial technology",
            "fintech",
        ),
        (
            "women led",
            "woman led",
        ),
        (
            "female led",
            "woman led",
        ),
        (
            "female entrepreneur",
            "woman entrepreneur",
        ),
        (
            "female entrepreneurs",
            "woman entrepreneur",
        ),
    )


    word_aliases = {
        "women":
            "woman",

        "womens":
            "woman",

        "female":
            "woman",
        "mahila":
            "woman",

        "mahilaon":
            "woman",

        "females":
            "woman",

        "entrepreneurs":
            "entrepreneur",

        "startups":
            "startup",

        "schemes":
            "scheme",

        "grants":
            "grant",

        "loans":
            "loan",

        "founders":
            "founder",

        "companies":
            "company",

        "businesses":
            "business",

        "industries":
            "industry",

        "technologies":
            "technology",

        "agricultural":
            "agriculture",
    }


    def canonicalize(value):

        text = normalize(
            value
        )


        for source, replacement in (
            phrase_aliases
        ):

            text = text.replace(
                source,
                replacement,
            )


        tokens = []


        for token in text.split():

            tokens.append(
                word_aliases.get(
                    token,
                    token,
                )
            )


        return " ".join(
            tokens
        )


    query_canonical = canonicalize(
        query
    )


    query_terms = (
        query_canonical.split()
    )


    if not query_terms:

        return JsonResponse(
            {
                "results": []
            }
        )


    # =====================================================
    # FAST CACHE
    #
    # Query 1:
    # Service catalogue
    #
    # Query 2:
    # All BDE-visible ServiceContentSection knowledge
    #
    # Warm requests:
    # 0 SQL
    #
    # No per-service queries.
    # =====================================================

    now = monotonic()


    cache = getattr(
        search_suggestions,
        "_bnx_fast_catalog_cache_v3",
        None,
    )


    if (
        not cache
        or
        now - cache[
            "created_at"
        ] > 10.0
    ):

        catalogue = list(
            Service.objects
            .filter(
                status__in=VISIBLE_STATUSES
            )
            .values(
                "id",
                "title",
                "service_id",
                "slug",
                "service_kind",
                "industries",

                "domain__name",
                "category__name",

                "bde_summary",
                "overview",
                "benefits",
                "eligibility_summary",
                "applicable_for_raw",

                "funding_organisation",
                "funding_type",
                "subsidy_details",
                "sales_pitch",
                "application_deadline_raw",
            )
        )


        by_service_id = {
            row["id"]: row
            for row in catalogue
        }


        for row in catalogue:

            row[
                "_bde_content"
            ] = []


        # -------------------------------------------------
        # BDE business knowledge only.
        #
        # ADMIN_ONLY content is deliberately excluded.
        # -------------------------------------------------

        content_rows = (
            ServiceContentSection.objects
            .filter(
                service_id__in=(
                    by_service_id.keys()
                ),
                visibility="BDE",
            )
            .values_list(
                "service_id",
                "title",
                "content",
            )
        )


        for (
            service_id,
            section_title,
            section_content,
        ) in content_rows:

            target = (
                by_service_id.get(
                    service_id
                )
            )


            if target is None:

                continue


            if section_title:

                target[
                    "_bde_content"
                ].append(
                    str(
                        section_title
                    )
                )


            if section_content:

                target[
                    "_bde_content"
                ].append(
                    str(
                        section_content
                    )
                )


        # Pre-canonicalise once.
        #
        # Search requests after this point only scan
        # small in-memory strings.

        kind_labels = dict(
            Service.SERVICE_KIND_CHOICES
        )


        prepared = []


        for row in catalogue:

            title = str(
                row.get(
                    "title"
                )
                or ""
            )

            service_id = str(
                row.get(
                    "service_id"
                )
                or ""
            )

            domain = str(
                row.get(
                    "domain__name"
                )
                or ""
            )

            category = str(
                row.get(
                    "category__name"
                )
                or ""
            )

            kind = str(
                kind_labels.get(
                    row.get(
                        "service_kind"
                    ),
                    row.get(
                        "service_kind"
                    )
                    or "",
                )
            )


            industries_value = (
                row.get(
                    "industries"
                )
            )


            if isinstance(
                industries_value,
                (
                    list,
                    tuple,
                    set,
                ),
            ):

                industries = " ".join(
                    str(
                        item or ""
                    )
                    for item
                    in industries_value
                )

            else:

                industries = str(
                    industries_value
                    or ""
                )


            direct_business_text = " ".join(
                str(
                    row.get(
                        field
                    )
                    or ""
                )

                for field in (
                    "bde_summary",
                    "overview",
                    "benefits",
                    "eligibility_summary",
                    "applicable_for_raw",
                    "funding_organisation",
                    "funding_type",
                    "subsidy_details",
                    "sales_pitch",
                    "application_deadline_raw",
                )
            )


            bde_content = " ".join(
                row.get(
                    "_bde_content",
                    []
                )
            )


            title_c = canonicalize(
                title
            )

            service_id_c = canonicalize(
                service_id
            )

            domain_c = canonicalize(
                domain
            )

            category_c = canonicalize(
                category
            )

            kind_c = canonicalize(
                kind
            )

            industries_c = canonicalize(
                industries
            )

            direct_c = canonicalize(
                direct_business_text
            )

            bde_content_c = canonicalize(
                bde_content
            )


            combined_c = " ".join(
                value

                for value in (
                    title_c,
                    service_id_c,
                    domain_c,
                    category_c,
                    kind_c,
                    industries_c,
                    direct_c,
                    bde_content_c,
                )

                if value
            )


            prepared.append(
                {
                    "id":
                        row["id"],

                    "title":
                        title,

                    "service_id":
                        service_id,

                    "slug":
                        str(
                            row.get(
                                "slug"
                            )
                            or ""
                        ),

                    "domain":
                        domain,

                    "category":
                        category,

                    "kind":
                        kind,

                    "title_c":
                        title_c,

                    "service_id_c":
                        service_id_c,

                    "domain_c":
                        domain_c,

                    "category_c":
                        category_c,

                    "kind_c":
                        kind_c,

                    "industries_c":
                        industries_c,

                    "direct_c":
                        direct_c,

                    "bde_content_c":
                        bde_content_c,

                    "combined_c":
                        combined_c,
                }
            )


        cache = {
            "created_at":
                now,

            "catalogue":
                prepared,
        }


        setattr(
            search_suggestions,
            "_bnx_fast_catalog_cache_v3",
            cache,
        )


    catalogue = cache[
        "catalogue"
    ]


    # =====================================================
    # SAFE MATCHING
    # =====================================================

    def matches_terms(
        text
    ):

        if not text:

            return False


        tokens = set(
            text.split()
        )


        for term in query_terms:

            # ---------------------------------------------
            # Short technology terms MUST be token exact.
            #
            # AI must not match "paid".
            # IoT must not match letters inside a word.
            # ---------------------------------------------

            if len(term) <= 3:

                if term not in tokens:

                    return False

            else:

                if term not in text:

                    return False


        return True


    matches = []


    # =====================================================
    # RANK
    # =====================================================

    for row in catalogue:

        if not matches_terms(
            row[
                "combined_c"
            ]
        ):

            continue


        title_c = row[
            "title_c"
        ]


        # Exact / title matches first.

        if (
            title_c
            == query_canonical
        ):

            rank = 0


        elif (
            len(
                query_canonical
            ) > 3
            and
            title_c.startswith(
                query_canonical
            )
        ):

            rank = 1


        elif matches_terms(
            title_c
        ):

            rank = 2


        elif (
            matches_terms(
                row[
                    "category_c"
                ]
            )
            or
            matches_terms(
                row[
                    "domain_c"
                ]
            )
        ):

            rank = 3


        elif matches_terms(
            row[
                "industries_c"
            ]
        ):

            rank = 4


        elif matches_terms(
            row[
                "direct_c"
            ]
        ):

            rank = 5


        elif matches_terms(
            row[
                "bde_content_c"
            ]
        ):

            rank = 6


        else:

            rank = 7


        matches.append(
            (
                rank,

                row[
                    "title"
                ].casefold(),

                {
                    "title":
                        row[
                            "title"
                        ],

                    "service_id":
                        row[
                            "service_id"
                        ],

                    "domain":
                        row[
                            "domain"
                        ],

                    "category":
                        row[
                            "category"
                        ],

                    "kind":
                        row[
                            "kind"
                        ],

                    "url":
                        (
                            "/toolkit/service/"
                            + row[
                                "slug"
                            ]
                            + "/"
                        ),
                },
            )
        )


    matches.sort(
        key=lambda item: (
            item[0],
            item[1],
        )
    )


    results = [
        item[2]
        for item
        in matches[:8]
    ]


    return JsonResponse(
        {
            "results":
                results
        }
    )






@login_required
def service_library(request):
    base_services = all_bde_services()

    query = request.GET.get(
        "q",
        ""
    ).strip()

    category_id = request.GET.get(
        "category",
        ""
    ).strip()

    service_kind = request.GET.get(
        "kind",
        ""
    ).strip()

    selected_service_id = (
        request.GET.get(
            "service",
            ""
        ).strip()
    )


    # Build category counts from the complete BDE catalog.
    catalog_services = list(
        base_services.order_by(
            "title"
        )
    )

    category_counts = {}

    for service in catalog_services:
        category_ids = {
            service.category_id
        }

        category_ids.update(
            category.pk
            for category
            in service.classifications.all()
        )

        for item_id in category_ids:
            category_counts[
                item_id
            ] = (
                category_counts.get(
                    item_id,
                    0
                )
                + 1
            )


    category_items = list(
        Category.objects
        .filter(is_active=True)
        .select_related("domain")
        .order_by(
            "domain__display_order",
            "domain__name",
            "display_order",
            "name",
        )
    )

    for item in category_items:
        item.bde_count = (
            category_counts.get(
                item.pk,
                0
            )
        )


    services = base_services

    if query:
        sector_ids = sector_match_ids(
            base_services,
            query
        )

        services = services.filter(
            search_text_filter(query)
            | Q(pk__in=sector_ids)
            | Q(
                pk__in=business_knowledge_match_ids(
                    query
                )
            )
            | Q(
                pk__in=natural_language_match_ids(
                    base_services,
                    query
                )
            )
        ).distinct()


    if category_id.isdigit():
        category_pk = int(
            category_id
        )

        services = services.filter(
            Q(category_id=category_pk)
            | Q(
                classifications__id=(
                    category_pk
                )
            )
        ).distinct()


    valid_kinds = {
        value
        for value, label
        in Service.SERVICE_KIND_CHOICES
    }

    if service_kind in valid_kinds:
        services = services.filter(
            service_kind=service_kind
        )


    if query:

        relevance_scores = (
            natural_language_rank_map(
                base_services,
                query,
            )
        )

        service_list = list(
            services.select_related(
                "domain",
                "category",
            )
        )

        service_list.sort(
            key=lambda service:
                search_relevance_sort_key(
                    service,
                    relevance_scores,
                    query,
                )
        )

        service_list = (
            service_list[:300]
        )

    else:

        service_list = list(
            services.order_by(
                "title"
            )[:300]
        )

    _attach_bde_ui_many(
        service_list
    )


    selected_service = None

    if selected_service_id.isdigit():
        selected_pk = int(
            selected_service_id
        )

        selected_service = next(
            (
                item
                for item
                in service_list
                if item.pk == selected_pk
            ),
            None,
        )

        if selected_service is None:
            selected_service = (
                all_bde_services()
                .filter(
                    pk=selected_pk
                )
                .first()
            )

            if selected_service:
                _attach_bde_ui(
                    selected_service
                )


    if (
        selected_service is None
        and service_list
    ):
        selected_service = (
            service_list[0]
        )


    saved_service_ids = set(
        SavedService.objects
        .filter(user=request.user)
        .values_list(
            "service_id",
            flat=True
        )
    )


    return render(
        request,
        "toolkit/service_library.html",
        {
            "services": service_list,
            "result_count": len(
                service_list
            ),
            "total_service_count": len(
                catalog_services
            ),
            "categories": category_items,
            "selected_category": (
                category_id
            ),
            "selected_kind": (
                service_kind
            ),
            "service_kinds": (
                Service.SERVICE_KIND_CHOICES
            ),
            "query": query,
            "selected_service": (
                selected_service
            ),
            "saved_service_ids": (
                saved_service_ids
            ),
        }
    )


@login_required
def service_quick_view(
    request,
    service_id,
):
    service = get_object_or_404(
        all_bde_services(),
        pk=service_id,
    )

    return JsonResponse(
        _serialize_service(
            request,
            service,
        )
    )


@login_required
def service_detail(request, slug):
    service = get_object_or_404(
        all_bde_services(),
        slug=slug
    )

    _attach_bde_ui(
        service
    )

    _remember_recent(
        request,
        service,
    )


    log_activity(
        request,
        "SERVICE_VIEW",
        f"Viewed service: {service.title}.",
        target_type="service",
        target_id=service.pk,
        metadata={
            "service_id": service.service_id,
            "domain": service.domain.name,
            "category": service.category.name,
            "pitch_state": (
                service.pitch_state_code
            ),
        },
    )


    is_saved = SavedService.objects.filter(
        user=request.user,
        service=service,
    ).exists()


    eligibility_rules = list(
        service.eligibility_rules
        .all()
        .order_by(
            "display_order",
            "id",
        )
    )


    document_requirements = list(
        service.document_requirements
        .all()
        .order_by(
            "display_order",
            "name",
        )
    )


    process_steps = list(
        service.process_steps
        .all()
        .order_by(
            "step_number",
            "id",
        )
    )


    content_sections = list(
        service.content_sections
        .filter(
            visibility="BDE"
        )
        .order_by(
            "display_order",
            "id",
        )
    )


    commercial_terms = list(
        service.commercial_terms
        .filter(
            visibility="BDE_ALLOWED",
            is_active=True,
        )
        .order_by(
            "id"
        )
    )


    knowledge_sections = list(
        service.knowledge_sections
        .filter(
            visibility="BDE"
        )
        .select_related(
            "document"
        )
        .order_by(
            "document__title",
            "display_order",
            "id",
        )
    )


    comparison_entries = list(
        service.comparison_entries
        .select_related(
            "matrix"
        )
        .order_by(
            "matrix__name",
            "row_number",
            "id",
        )
    )


    return render(
        request,
        "toolkit/service_detail.html",
        {
            "service":
                service,

            "is_saved":
                is_saved,

            "sources":
                service.bde_sources,

            "primary_source":
                service.primary_source,

            "classification_categories":
                list(
                    service.classifications.all()
                ),

            "eligibility_rules":
                eligibility_rules,

            "document_requirements":
                document_requirements,

            "process_steps":
                process_steps,

            "content_sections":
                content_sections,

            "commercial_terms":
                commercial_terms,

            "knowledge_sections":
                knowledge_sections,

            "comparison_entries":
                comparison_entries,
        }
    )


@login_required
def saved_services(request):
    """
    Collection-first Saved Services workspace.

    Existing SavedService rows with no collection membership
    automatically appear under Unsorted.
    """

    collections = list(
        SavedCollection.objects
        .filter(
            user=request.user
        )
        .prefetch_related(
            "items__saved_service__service"
        )
        .order_by(
            "-updated_at",
            "name",
        )
    )


    for collection in collections:

        collection.service_count = (
            len(
                collection.items.all()
            )
        )


    unsorted_items = list(
        SavedService.objects
        .filter(
            user=request.user,
            collection_items__isnull=True,
        )
        .select_related(
            "service",
            "service__domain",
            "service__category",
        )
        .order_by(
            "-created_at"
        )
    )


    return render(
        request,
        "toolkit/saved_services.html",
        {
            "collections":
                collections,

            "collection_count":
                len(collections),

            "unsorted_items":
                unsorted_items,

            "unsorted_count":
                len(unsorted_items),
        },
    )




@login_required
def recent_services(request):
    recent_ids = [
        int(item)
        for item
        in request.session.get(
            "bde_recent_service_ids",
            [],
        )
        if str(item).isdigit()
    ]

    services_by_id = {
        service.pk: service
        for service
        in all_bde_services().filter(
            pk__in=recent_ids
        )
    }

    services = [
        services_by_id[
            service_id
        ]
        for service_id
        in recent_ids
        if service_id
        in services_by_id
    ]

    _attach_bde_ui_many(
        services
    )

    return render(
        request,
        "toolkit/recent_services.html",
        {
            "services": services,
        }
    )


@login_required
@require_POST
def toggle_saved_service(
    request,
    service_id
):
    service = get_object_or_404(
        Service,
        pk=service_id,
        status__in=visible_service_statuses(request.user),
    )


    saved_item = SavedService.objects.filter(
        user=request.user,
        service=service,
    ).first()


    if saved_item:
        saved_item.delete()

        saved = False

        if (
            request.headers.get(
                "X-Requested-With"
            )
            != "XMLHttpRequest"
        ):
            messages.success(
                request,
                f'Removed "{service.title}" from Saved Services.'
            )

        log_activity(
            request,
            "SERVICE_UNSAVE",
            (
                "Removed saved service: "
                f"{service.title}."
            ),
            target_type="service",
            target_id=service.pk,
            metadata={
                "service_id": (
                    service.service_id
                ),
            },
        )

    else:
        SavedService.objects.create(
            user=request.user,
            service=service,
        )

        saved = True

        if (
            request.headers.get(
                "X-Requested-With"
            )
            != "XMLHttpRequest"
        ):
            messages.success(
                request,
                f'Saved "{service.title}" to Saved Services.'
            )

        log_activity(
            request,
            "SERVICE_SAVE",
            (
                "Saved service: "
                f"{service.title}."
            ),
            target_type="service",
            target_id=service.pk,
            metadata={
                "service_id": (
                    service.service_id
                ),
            },
        )


    if (
        request.headers.get(
            "X-Requested-With"
        )
        == "XMLHttpRequest"
    ):
        return JsonResponse(
            {
                "saved": saved,
                "service_id": service.pk,
            }
        )


    next_url = request.POST.get(
        "next",
        ""
    )

    if (
        next_url
        and url_has_allowed_host_and_scheme(
            url=next_url,
            allowed_hosts={
                request.get_host()
            },
            require_https=request.is_secure(),
        )
    ):
        return redirect(
            next_url
        )


    return redirect(
        "toolkit:service_detail",
        slug=service.slug
    )



# ============================================================
# BNX_SAVED_COLLECTIONS_V1
# ============================================================

@login_required
def saved_collection_detail(
    request,
    collection_id,
):

    collection = get_object_or_404(
        SavedCollection,
        pk=collection_id,
        user=request.user,
    )


    items = list(
        SavedCollectionItem.objects
        .filter(
            collection=collection
        )
        .select_related(
            "saved_service",
            "saved_service__service",
            "saved_service__service__domain",
            "saved_service__service__category",
        )
        .order_by(
            "-created_at"
        )
    )


    for item in items:

        apply_pitch_state(
            item.saved_service.service
        )


    return render(
        request,
        "toolkit/saved_collection_detail.html",
        {
            "collection":
                collection,

            "items":
                items,

            "service_count":
                len(items),
        },
    )


@login_required
@require_POST
def saved_collection_create(
    request,
):

    name = request.POST.get(
        "name",
        ""
    ).strip()


    note = request.POST.get(
        "note",
        ""
    ).strip()


    if not name:

        messages.error(
            request,
            "Enter a client or collection name.",
        )

        return redirect(
            "toolkit:saved_services"
        )


    existing = (
        SavedCollection.objects
        .filter(
            user=request.user,
            name__iexact=name,
        )
        .first()
    )


    if existing:

        messages.info(
            request,
            f'"{existing.name}" already exists.',
        )

        return redirect(
            "toolkit:saved_collection_detail",
            collection_id=existing.pk,
        )


    collection = (
        SavedCollection.objects.create(
            user=request.user,
            name=name[:150],
            note=note[:500],
        )
    )


    messages.success(
        request,
        f'Created client collection "{collection.name}".',
    )


    return redirect(
        "toolkit:saved_collection_detail",
        collection_id=collection.pk,
    )


@login_required
@require_POST
def saved_collection_rename(
    request,
    collection_id,
):

    collection = get_object_or_404(
        SavedCollection,
        pk=collection_id,
        user=request.user,
    )


    name = request.POST.get(
        "name",
        ""
    ).strip()


    note = request.POST.get(
        "note",
        collection.note,
    ).strip()


    if not name:

        messages.error(
            request,
            "Collection name cannot be empty.",
        )

        return redirect(
            "toolkit:saved_collection_detail",
            collection_id=collection.pk,
        )


    conflict = (
        SavedCollection.objects
        .filter(
            user=request.user,
            name__iexact=name,
        )
        .exclude(
            pk=collection.pk
        )
        .exists()
    )


    if conflict:

        messages.error(
            request,
            "You already have a collection with that name.",
        )

        return redirect(
            "toolkit:saved_collection_detail",
            collection_id=collection.pk,
        )


    collection.name = name[:150]
    collection.note = note[:500]

    collection.save(
        update_fields=[
            "name",
            "note",
            "updated_at",
        ]
    )


    messages.success(
        request,
        "Collection updated.",
    )


    return redirect(
        "toolkit:saved_collection_detail",
        collection_id=collection.pk,
    )


@login_required
@require_POST
def saved_collection_delete(
    request,
    collection_id,
):

    collection = get_object_or_404(
        SavedCollection,
        pk=collection_id,
        user=request.user,
    )


    name = collection.name

    collection.delete()


    # SavedService rows themselves are intentionally retained.
    # They therefore fall back into Unsorted if they no longer
    # belong to another collection.


    messages.success(
        request,
        (
            f'Deleted collection "{name}". '
            "Its saved services were not deleted."
        ),
    )


    return redirect(
        "toolkit:saved_services"
    )


@login_required
def saved_service_collection_state(
    request,
    service_id,
):

    service = get_object_or_404(
        Service,
        pk=service_id,
        status__in=(
            visible_service_statuses(
                request.user
            )
        ),
    )


    saved_service = (
        SavedService.objects
        .filter(
            user=request.user,
            service=service,
        )
        .first()
    )


    selected_ids = set()


    if saved_service:

        selected_ids = set(
            SavedCollectionItem.objects
            .filter(
                saved_service=saved_service,
                collection__user=request.user,
            )
            .values_list(
                "collection_id",
                flat=True,
            )
        )


    collections = list(
        SavedCollection.objects
        .filter(
            user=request.user
        )
        .order_by(
            "-updated_at",
            "name",
        )
    )


    return JsonResponse(
        {
            "service": {
                "id":
                    service.pk,

                "title":
                    service.title,

                "service_id":
                    service.service_id,
            },

            "saved":
                bool(
                    saved_service
                ),

            "collections": [
                {
                    "id":
                        collection.pk,

                    "name":
                        collection.name,

                    "note":
                        collection.note,

                    "selected":
                        (
                            collection.pk
                            in selected_ids
                        ),
                }

                for collection
                in collections
            ],
        }
    )


@login_required
@require_POST
def saved_service_collection_action(
    request,
    service_id,
):

    service = get_object_or_404(
        Service,
        pk=service_id,
        status__in=(
            visible_service_statuses(
                request.user
            )
        ),
    )


    action = request.POST.get(
        "action",
        ""
    ).strip()


    # --------------------------------------------------------
    # CREATE COLLECTION + ADD SERVICE
    # --------------------------------------------------------

    if action == "create_and_add":

        name = request.POST.get(
            "name",
            ""
        ).strip()


        note = request.POST.get(
            "note",
            ""
        ).strip()


        if not name:

            return JsonResponse(
                {
                    "ok": False,
                    "error": (
                        "Enter a client or collection name."
                    ),
                },
                status=400,
            )


        collection = (
            SavedCollection.objects
            .filter(
                user=request.user,
                name__iexact=name,
            )
            .first()
        )


        if collection is None:

            collection = (
                SavedCollection.objects.create(
                    user=request.user,
                    name=name[:150],
                    note=note[:500],
                )
            )


        saved_service, _ = (
            SavedService.objects.get_or_create(
                user=request.user,
                service=service,
            )
        )


        SavedCollectionItem.objects.get_or_create(
            collection=collection,
            saved_service=saved_service,
        )


        collection.save(
            update_fields=[
                "updated_at"
            ]
        )


        return JsonResponse(
            {
                "ok": True,
                "saved": True,
                "collection_id":
                    collection.pk,

                "collection_name":
                    collection.name,
            }
        )


    # --------------------------------------------------------
    # TOGGLE SERVICE INSIDE AN EXISTING COLLECTION
    # --------------------------------------------------------

    if action == "toggle_collection":

        collection_id = request.POST.get(
            "collection_id",
            ""
        )


        if not str(
            collection_id
        ).isdigit():

            return JsonResponse(
                {
                    "ok": False,
                    "error": "Invalid collection.",
                },
                status=400,
            )


        collection = get_object_or_404(
            SavedCollection,
            pk=int(
                collection_id
            ),
            user=request.user,
        )


        saved_service, _ = (
            SavedService.objects.get_or_create(
                user=request.user,
                service=service,
            )
        )


        existing = (
            SavedCollectionItem.objects
            .filter(
                collection=collection,
                saved_service=saved_service,
            )
            .first()
        )


        if existing:

            existing.delete()
            selected = False

        else:

            SavedCollectionItem.objects.create(
                collection=collection,
                saved_service=saved_service,
            )

            selected = True


        collection.save(
            update_fields=[
                "updated_at"
            ]
        )


        return JsonResponse(
            {
                "ok": True,
                "saved": True,
                "selected":
                    selected,

                "collection_id":
                    collection.pk,
            }
        )


    # --------------------------------------------------------
    # REMOVE SERVICE FROM ALL SAVED SERVICES
    # --------------------------------------------------------

    if action == "remove_saved":

        saved_service = (
            SavedService.objects
            .filter(
                user=request.user,
                service=service,
            )
            .first()
        )


        if saved_service:

            saved_service.delete()


        return JsonResponse(
            {
                "ok": True,
                "saved": False,
            }
        )


    return JsonResponse(
        {
            "ok": False,
            "error": "Unsupported action.",
        },
        status=400,
    )


@login_required
@require_POST
def saved_collection_remove_item(
    request,
    collection_id,
    item_id,
):

    collection = get_object_or_404(
        SavedCollection,
        pk=collection_id,
        user=request.user,
    )


    item = get_object_or_404(
        SavedCollectionItem,
        pk=item_id,
        collection=collection,
    )


    service_title = (
        item.saved_service.service.title
    )


    item.delete()


    collection.save(
        update_fields=[
            "updated_at"
        ]
    )


    messages.success(
        request,
        (
            f'Removed "{service_title}" '
            f'from "{collection.name}".'
        ),
    )


    return redirect(
        "toolkit:saved_collection_detail",
        collection_id=collection.pk,
    )

