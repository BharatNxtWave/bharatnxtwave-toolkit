===== TOOLKIT URLS =====
from django.urls import path

from . import admin_views, import_views, matcher_views, views

app_name = "toolkit"

urlpatterns = [
    path(
        "toolkit/",
        views.toolkit_home,
        name="home"
    ),
    path(
        "toolkit/search/suggestions/",
        views.search_suggestions,
        name="search_suggestions"
    ),
    path(
        "toolkit/service/<slug:slug>/",
        views.service_detail,
        name="service_detail"
    ),

    path(
        "toolkit/saved/",
        views.saved_services,
        name="saved_services"
    ),

    path(
        "toolkit/matcher/",
        matcher_views.client_matcher,
        name="client_matcher"
    ),
    path(
        "toolkit/service/<int:service_id>/save/",
        views.toggle_saved_service,
        name="toggle_saved_service"
    ),

    path(
        "admin-center/toolkit/",
        admin_views.service_management_list,
        name="admin_service_list"
    ),
    path(
        "admin-center/toolkit/pitch-windows/",
        admin_views.pitch_window_list,
        name="pitch_windows"
    ),
    path(
        "admin-center/toolkit/add/",
        admin_views.service_create,
        name="admin_service_create"
    ),
    path(
        "admin-center/toolkit/<int:service_id>/edit/",
        admin_views.service_edit,
        name="admin_service_edit"
    ),
    path(
        "admin-center/toolkit/<int:service_id>/verify/",
        admin_views.service_verify,
        name="admin_service_verify"
    ),

    path(
        "admin-center/import/",
        import_views.import_center,
        name="import_center"
    ),

    path(
        "admin-center/import/history/",
        import_views.import_history,
        name="import_history"
    ),

    path(
        "admin-center/import/history/<int:batch_id>/",
        import_views.import_history_detail,
        name="import_history_detail"
    ),


    path(
        "admin-center/import/history/<int:batch_id>/delete/",
        import_views.import_source_delete,
        name="import_source_delete",
    ),
]

===== TOOLKIT VIEWS =====
from toolkit.visibility import visible_service_statuses
from django.contrib import messages
from django.contrib.auth.decorators import login_required
from django.db.models import Q
from django.http import JsonResponse
from django.shortcuts import (
    get_object_or_404,
    redirect,
    render,
)
from django.utils.http import (
    url_has_allowed_host_and_scheme,
)
from django.views.decorators.http import require_POST

from accounts.activity import log_activity

from .models import (
    Category,
    SavedService,
    SearchEvent,
    Service,
    ServiceDomain,
)

from .pitching import (
    VISIBLE_STATUSES,
    apply_pitch_state,
    apply_pitch_states,
    available_sectors,
    closing_soon_queryset,
    pitchable_queryset,
    sector_match_ids,
)


def visible_services():
    return pitchable_queryset(
        Service.objects.select_related(
            "domain",
            "category",
            "verified_by",
        )
    )


def all_bde_services():
    return (
        Service.objects
        .select_related(
            "domain",
            "category",
            "verified_by",
        )
        .filter(
            status__in=VISIBLE_STATUSES
        )
    )


def search_text_filter(query):
    return (
        Q(title__icontains=query)
        | Q(service_id__icontains=query)
        | Q(bde_summary__icontains=query)
        | Q(overview__icontains=query)
        | Q(benefits__icontains=query)
        | Q(eligibility_summary__icontains=query)
        | Q(domain__name__icontains=query)
        | Q(category__name__icontains=query)
        | Q(service_kind__icontains=query)
        | Q(funding_type__icontains=query)
        | Q(subsidy_details__icontains=query)
        | Q(sales_pitch__icontains=query)
    )


@login_required
def toolkit_home(request):
    base_services = visible_services()

    query = request.GET.get(
        "q",
        ""
    ).strip()

    sector = request.GET.get(
        "sector",
        ""
    ).strip()

    domain_slug = request.GET.get(
        "domain",
        ""
    ).strip()

    category_id = request.GET.get(
        "category",
        ""
    ).strip()

    service_kind = request.GET.get(
        "kind",
        ""
    ).strip()

    deadline_filter = request.GET.get(
        "deadline",
        ""
    ).strip()


    services = base_services


    # =====================================================
    # SCHEME NAME + NATURAL TEXT + SECTOR SEARCH
    # =====================================================

    if query:
        sector_ids = sector_match_ids(
            base_services,
            query
        )

        services = services.filter(
            search_text_filter(query)
            | Q(pk__in=sector_ids)
        ).distinct()


    # =====================================================
    # DIRECT SECTOR FILTER
    # =====================================================

    if sector:
        sector_ids = sector_match_ids(
            base_services,
            sector
        )

        services = services.filter(
            pk__in=sector_ids
        )


    if domain_slug:
        services = services.filter(
            domain__slug=domain_slug
        )


    if category_id.isdigit():
        services = services.filter(
            category_id=int(
                category_id
            )
        )


    valid_kinds = {
        value
        for value, label
        in Service.SERVICE_KIND_CHOICES
    }

    if service_kind in valid_kinds:
        services = services.filter(
            service_kind=service_kind
        )


    # =====================================================
    # DEADLINE FILTERS
    # =====================================================

    if deadline_filter == "closing":
        services = closing_soon_queryset(
            services,
            days=30
        )

    elif deadline_filter == "urgent":
        services = closing_soon_queryset(
            services,
            days=7
        )

    elif deadline_filter == "no_deadline":
        services = services.filter(
            pitch_until__isnull=True,
            application_deadline__isnull=True,
        )


    result_count = services.count()


    filters = {
        "sector": sector,
        "domain": domain_slug,
        "category": category_id,
        "service_kind": service_kind,
        "deadline": deadline_filter,
    }

    filters_used = any(
        filters.values()
    )


    # =====================================================
    # REAL SEARCH ANALYTICS
    # =====================================================

    if query or filters_used:

        SearchEvent.objects.create(
            user=request.user,

            event_type=(
                "SEARCH"
                if query
                else "FILTER"
            ),

            query=query,

            filters=filters,

            result_count=result_count,
        )


    if query:
        log_activity(
            request,
            "SEARCH",
            f'Toolkit search: "{query}"',
            metadata={
                "query": query,
                "result_count": result_count,
            },
        )


    if filters_used:
        log_activity(
            request,
            "FILTER",
            "Toolkit filters applied.",
            metadata={
                **filters,
                "result_count": result_count,
            },
        )


    domains = (
        ServiceDomain.objects
        .filter(is_active=True)
        .order_by(
            "display_order",
            "name"
        )
    )


    categories = (
        Category.objects
        .filter(is_active=True)
        .select_related("domain")
        .order_by(
            "domain__display_order",
            "display_order",
            "name"
        )
    )


    sector_options = available_sectors(
        base_services
    )


    service_list = list(
        services.order_by(
            "domain__display_order",
            "title"
        )[:100]
    )

    apply_pitch_states(
        service_list
    )


    saved_service_ids = set(
        SavedService.objects
        .filter(user=request.user)
        .values_list(
            "service_id",
            flat=True
        )
    )


    context = {
        "services": service_list,
        "result_count": result_count,

        "domains": domains,
        "categories": categories,
        "sector_options": sector_options,

        "service_kinds": (
            Service.SERVICE_KIND_CHOICES
        ),

        "query": query,
        "selected_sector": sector,
        "selected_domain": domain_slug,
        "selected_category": category_id,
        "selected_kind": service_kind,
        "selected_deadline": deadline_filter,

        "saved_service_ids": (
            saved_service_ids
        ),
    }


    return render(
        request,
        "toolkit/toolkit_home.html",
        context
    )


@login_required
def search_suggestions(request):
    query = request.GET.get(
        "q",
        ""
    ).strip()

    if len(query) < 2:
        return JsonResponse(
            {"results": []}
        )


    base_services = visible_services()

    sector_ids = sector_match_ids(
        base_services,
        query
    )


    services = (
        base_services
        .filter(
            search_text_filter(query)
            | Q(pk__in=sector_ids)
        )
        .distinct()
        .order_by("title")[:8]
    )


    results = []

    for service in services:
        apply_pitch_state(
            service
        )

        results.append(
            {
                "title": service.title,

                "service_id": (
                    service.service_id
                ),

                "domain": (
                    service.domain.name
                ),

                "category": (
                    service.category.name
                ),

                "kind": (
                    service.get_service_kind_display()
                ),

                "url": (
                    f"/toolkit/service/"
                    f"{service.slug}/"
                ),
            }
        )


    return JsonResponse(
        {"results": results}
    )


@login_required
def service_detail(request, slug):
    service = get_object_or_404(
        all_bde_services(),
        slug=slug
    )

    apply_pitch_state(
        service
    )


    log_activity(
        request,
        "SERVICE_VIEW",
        f"Viewed service: {service.title}.",
        target_type="service",
        target_id=service.pk,
        metadata={
            "service_id": service.service_id,
            "domain": service.domain.name,
            "category": service.category.name,
            "pitch_state": (
                service.pitch_state_code
            ),
        },
    )


    is_saved = SavedService.objects.filter(
        user=request.user,
        service=service,
    ).exists()


    return render(
        request,
        "toolkit/service_detail.html",
        {
            "service": service,
            "is_saved": is_saved,
        }
    )


@login_required
def saved_services(request):
    saved_items = list(
        SavedService.objects
        .filter(user=request.user)
        .select_related(
            "service",
            "service__domain",
            "service__category",
        )
        .order_by("-created_at")
    )

    for item in saved_items:
        apply_pitch_state(
            item.service
        )


    return render(
        request,
        "toolkit/saved_services.html",
        {
            "saved_items": saved_items,
        }
    )


@login_required
@require_POST
def toggle_saved_service(
    request,
    service_id
):
    service = get_object_or_404(
        Service,
        pk=service_id,
        status__in=visible_service_statuses(request.user),
    )


    saved_item = SavedService.objects.filter(
        user=request.user,
        service=service,
    ).first()


    if saved_item:
        saved_item.delete()

        saved = False

        if (
            request.headers.get(
                "X-Requested-With"
            )
            != "XMLHttpRequest"
        ):
            messages.success(
                request,
                f'Removed "{service.title}" from Saved Services.'
            )

        log_activity(
            request,
            "SERVICE_UNSAVE",
            (
                "Removed saved service: "
                f"{service.title}."
            ),
            target_type="service",
            target_id=service.pk,
            metadata={
                "service_id": (
                    service.service_id
                ),
            },
        )

    else:
        SavedService.objects.create(
            user=request.user,
            service=service,
        )

        saved = True

        if (
            request.headers.get(
                "X-Requested-With"
            )
            != "XMLHttpRequest"
        ):
            messages.success(
                request,
                f'Saved "{service.title}" to Saved Services.'
            )

        log_activity(
            request,
            "SERVICE_SAVE",
            (
                "Saved service: "
                f"{service.title}."
            ),
            target_type="service",
            target_id=service.pk,
            metadata={
                "service_id": (
                    service.service_id
                ),
            },
        )


    if (
        request.headers.get(
            "X-Requested-With"
        )
        == "XMLHttpRequest"
    ):
        return JsonResponse(
            {
                "saved": saved,
                "service_id": service.pk,
            }
        )


    next_url = request.POST.get(
        "next",
        ""
    )

    if (
        next_url
        and url_has_allowed_host_and_scheme(
            url=next_url,
            allowed_hosts={
                request.get_host()
            },
            require_https=request.is_secure(),
        )
    ):
        return redirect(
            next_url
        )


    return redirect(
        "toolkit:service_detail",
        slug=service.slug
    )

===== TOOLKIT TEMPLATES =====

===== FILE: toolkit/templates/toolkit/client_matcher.html =====
{% extends "base.html" %}
{% load static %}

{% block title %}Client Service Checker | BharatNXT Wave{% endblock %}

{% block content %}

<div class="client-matcher-page">


<!-- =====================================================
     PAGE HEADER
====================================================== -->

<div class="page-eyebrow">
    CLIENT SERVICE FINDER
</div>

<h1 class="page-title">
    Find Services for a Client
</h1>

<p class="page-description matcher-v2-description">
    Enter what the client is looking for and the company details you know. The Toolkit will show services worth discussing.
</p>


<div class="matcher-v2-guide">

    <div>
        <span>1</span>
        <strong>Enter client requirement</strong>
    </div>

    <div>
        <span>2</span>
        <strong>Add company details</strong>
    </div>

    <div>
        <span>3</span>
        <strong>Review suitable services</strong>
    </div>

</div>



<!-- =====================================================
     CLIENT MATCH FORM
====================================================== -->

<section
    class="card matcher-v2-card"
    id="client-match-form">

    <form
        method="post"
        autocomplete="off">

        {% csrf_token %}


        <!-- STEP 1 -->

        <div class="matcher-v2-step">

            <div class="matcher-v2-step-number">
                1
            </div>


            <div class="matcher-v2-step-content">

                <h2>
                    What is the client looking for?
                </h2>

                <p>
                    Type naturally. The client does not need to know the exact scheme or service name.
                </p>


                <div class="matcher-v2-main-search">
                    {{ form.need_query }}
                </div>


                <div class="matcher-v2-examples">

                    <span class="matcher-example-label">
                        Examples
                    </span>

                    <span>Technology schemes</span>
                    <span>Startup funding</span>
                    <span>MSME loan</span>
                    <span>Startup India</span>

                </div>

            </div>

        </div>


        <div class="matcher-v2-divider"></div>


        <!-- STEP 2 -->

        <div class="matcher-v2-step">

            <div class="matcher-v2-step-number">
                2
            </div>


            <div class="matcher-v2-step-content">

                <h2>
                    What do you know about the company?
                </h2>

                <p>
                    Fill only what the client tells you. More information can improve eligibility checking.
                </p>


                <div class="matcher-v2-basic-grid">


                    <div class="matcher-v2-field">

                        <label>
                            Industry / Sector
                        </label>

                        {{ form.industry }}

                    </div>


                    <div class="matcher-v2-field">

                        <label>
                            Business Stage
                        </label>

                        {{ form.business_stage }}

                    </div>


                    <div class="matcher-v2-field">

                        <label>
                            State
                        </label>

                        {{ form.state }}

                    </div>


                    <div class="matcher-v2-field">

                        <label>
                            Business Type
                        </label>

                        {{ form.business_type }}

                    </div>

                </div>



                <!-- OPTIONAL ELIGIBILITY -->

                <details class="matcher-v2-advanced">

                    <summary>

                        <span>
                            + Add more eligibility details
                        </span>

                        <small>
                            Optional
                        </small>

                    </summary>


                    <div class="matcher-v2-advanced-content">


                        <div class="matcher-v2-field">

                            <label>
                                Founder Category
                            </label>

                            {{ form.founder_category }}

                        </div>


                        <div class="matcher-v2-field">

                            <label>
                                Business Age
                            </label>


                            <div class="matcher-v2-input-suffix">

                                {{ form.business_age_years }}

                                <span>
                                    Years
                                </span>

                            </div>

                        </div>


                        <div class="matcher-v2-field">

                            <label>
                                Annual Turnover
                            </label>


                            <div class="matcher-v2-turnover">

                                {{ form.turnover_amount }}

                                {{ form.turnover_unit }}

                            </div>

                        </div>


                        <div class="matcher-v2-service-types">

                            <label>
                                Limit results to service types
                            </label>

                            <div class="matcher-v2-kind-grid">
                                {{ form.service_kinds }}
                            </div>

                        </div>

                    </div>

                </details>

            </div>

        </div>



        {% if form.errors %}

            <div class="matcher-v2-error">
                Please check the information above and try again.
            </div>

        {% endif %}



        <!-- ACTION -->

        <div class="matcher-v2-action">

            <div class="matcher-v2-privacy">

                <span>◇</span>

                <div>

                    <strong>
                        Client profile isn't saved
                    </strong>

                    <small>
                        The supplied details are used only for this matching request.
                    </small>

                </div>

            </div>


            <button
                type="submit"
                class="primary-button matcher-v2-submit">

                Find Matching Services →

            </button>

        </div>

    </form>

</section>



{% if submitted %}


<!-- =====================================================
     RESULT CONTEXT
====================================================== -->

<section
    class="matcher-result-context"
    id="match-results">

    <div>

        <div class="page-eyebrow">
            MATCH RESULTS
        </div>


        <h2>
            Services worth discussing with the client
        </h2>


        <div class="matcher-context-query">

            <span>
                MATCHING FOR
            </span>

            <strong class="matcher-query-chip">

                {% if form.need_query.value %}

                    {{ form.need_query.value }}

                {% else %}

                    Client company profile

                {% endif %}

            </strong>

        </div>


        <div class="matcher-context-details">

            {% if form.industry.value %}
                <span>
                    Industry: {{ form.industry.value }}
                </span>
            {% endif %}

            {% if form.business_stage.value %}
                <span>
                    Stage: {{ form.business_stage.value }}
                </span>
            {% endif %}

            {% if form.state.value %}
                <span>
                    State: {{ form.state.value }}
                </span>
            {% endif %}

            {% if form.business_type.value %}
                <span>
                    Type: {{ form.business_type.value }}
                </span>
            {% endif %}

        </div>

    </div>


    <div class="matcher-result-actions">

        <a
            href="#client-match-form"
            class="secondary-button">

            Edit Client Details

        </a>


        <a
            href="{% url 'toolkit:client_matcher' %}"
            class="primary-button">

            New Client Service Check

        </a>

    </div>

</section>



<!-- =====================================================
     NEXT ACTION
====================================================== -->

<div class="matcher-next-action">

    {% if results.high_count %}

        <div class="matcher-next-action-icon strong">
            ✓
        </div>

        <div>

            <strong>
                {{ results.high_count }}
                strong match{{ results.high_count|pluralize:"es" }}
                found
            </strong>

            <span>
                Start with these services during the client conversation.
            </span>

        </div>

    {% elif results.possible_count %}

        <div class="matcher-next-action-icon verify">
            !
        </div>

        <div>

            <strong>
                {{ results.possible_count }}
                service{{ results.possible_count|pluralize }}
                need{{ results.possible_count|pluralize:"s," }}
                verification
            </strong>

            <span>
                Review the missing eligibility information before advising the client.
            </span>

        </div>

    {% elif results.not_eligible_count %}

        <div class="matcher-next-action-icon rejected">
            ×
        </div>

        <div>

            <strong>
                No currently eligible service found
            </strong>

            <span>
                Review the conflicting eligibility criteria below.
            </span>

        </div>

    {% else %}

        <div class="matcher-next-action-icon neutral">
            ⌕
        </div>

        <div>

            <strong>
                No matching Toolkit service found
            </strong>

            <span>
                Try a broader search or edit the client details.
            </span>

        </div>

    {% endif %}

</div>


<!-- =====================================================
     RESULT SUMMARY
====================================================== -->

<div class="matcher-summary-grid">


    <div class="card matcher-summary-card high">

        <div class="stat-label">
            STRONG MATCH
        </div>

        <div class="search-kpi-value">
            {{ results.high_count }}
        </div>

        <div class="search-kpi-meta">
            Eligibility can be supported by Toolkit data
        </div>

    </div>


    <div class="card matcher-summary-card possible">

        <div class="stat-label">
            NEEDS VERIFICATION
        </div>

        <div class="search-kpi-value">
            {{ results.possible_count }}
        </div>

        <div class="search-kpi-meta">
            Relevant, but eligibility needs checking
        </div>

    </div>


    <div class="card matcher-summary-card rejected">

        <div class="stat-label">
            DOESN'T FIT
        </div>

        <div class="search-kpi-value">
            {{ results.not_eligible_count }}
        </div>

        <div class="search-kpi-meta">
            Current Toolkit criteria conflict
        </div>

    </div>

</div>



<!-- =====================================================
     RESULT TOOLS
====================================================== -->

{% if results.high_count or results.possible_count %}

<div class="matcher-result-toolbar">

    <div>

        <strong>
            Recommendations
        </strong>

        <span>
            Ranked using actual Toolkit information
        </span>

    </div>


    <label class="matcher-sort-control">

        <span>
            SORT
        </span>

        <select id="matcher-result-sort">

            <option value="best">
                Best Match
            </option>

            <option value="type">
                Service Type
            </option>

            <option value="verified">
                Recently Verified
            </option>

        </select>

    </label>

</div>

{% endif %}


<!-- =====================================================
     STRONG MATCHES
     EMPTY SECTIONS ARE NOT SHOWN
====================================================== -->

{% if results.high_count %}

<section class="matcher-result-section">

    <div class="matcher-result-heading">

        <div>

            <div class="matcher-result-title">
                Strong Matches
            </div>

            <div class="table-subtitle">
                Best services to discuss first
            </div>

        </div>


        <span class="matcher-count-badge high">
            {{ results.high_count }}
        </span>

    </div>


    <div class="matcher-results-grid {% if results.high_count == 1 %}single-result{% endif %}">

        {% for item in results.high %}

            <article
                class="card matcher-result-card high"
                data-kind="{{ item.service.get_service_kind_display }}"
                data-verified="{% if item.service.last_verified_at %}{{ item.service.last_verified_at|date:'YmdHis' }}{% else %}0{% endif %}">


                <div class="matcher-card-header">

                    <div>

                        <div class="toolkit-service-id">
                            {{ item.service.service_id }}
                        </div>

                        <a
                            href="{% url 'toolkit:service_detail' item.service.slug %}"
                            class="matcher-service-title">

                            {{ item.service.title }}

                        </a>

                    </div>


                    <span class="matcher-status high">
                        STRONG MATCH
                    </span>

                </div>



                <div class="matcher-service-meta">

                    <span>
                        {{ item.service.domain.name }}
                    </span>

                    <span>•</span>

                    <span>
                        {{ item.service.category.name }}
                    </span>

                    <span>•</span>

                    <span>
                        {{ item.service.get_service_kind_display }}
                    </span>

                </div>



                {% if item.service.bde_summary %}

                    <p class="matcher-summary-copy">
                        {{ item.service.bde_summary }}
                    </p>

                {% endif %}



                <div class="matcher-call-section">

                    <div class="matcher-call-label">
                        WHY WE'RE SHOWING THIS
                    </div>


                    {% if form.need_query.value %}

                        <div class="matcher-reason positive">
                            ✓ Relevant to your search: {{ form.need_query.value }}
                        </div>

                    {% endif %}


                    {% for reason in item.matches %}

                        <div class="matcher-reason positive">
                            ✓ {{ reason.reason }}
                        </div>

                    {% endfor %}

                </div>



                <div class="matcher-card-actions">


                    <form
                        method="post"
                        action="{% url 'toolkit:toggle_saved_service' item.service.pk %}"
                        class="matcher-save-form">

                        {% csrf_token %}

                        <button
                            type="submit"
                            class="matcher-save-button {% if item.service.pk in saved_service_ids %}saved{% endif %}">

                            {% if item.service.pk in saved_service_ids %}
                                ★ Saved
                            {% else %}
                                ☆ Save
                            {% endif %}

                        </button>

                    </form>


                    <a
                        href="{% url 'toolkit:service_detail' item.service.slug %}"
                        class="primary-button matcher-open-service">

                        Open Service Details →

                    </a>

                </div>

            </article>

        {% endfor %}

    </div>

</section>

{% endif %}



<!-- =====================================================
     NEEDS VERIFICATION
====================================================== -->

{% if results.possible_count %}

<section class="matcher-result-section">

    <div class="matcher-result-heading">

        <div>

            <div class="matcher-result-title">
                Needs Verification
            </div>

            <div class="table-subtitle">
                Relevant services where some eligibility information still needs checking
            </div>

        </div>


        <span class="matcher-count-badge possible">
            {{ results.possible_count }}
        </span>

    </div>



    <div class="matcher-results-grid {% if results.possible_count == 1 %}single-result{% endif %}">

        {% for item in results.possible %}

            <article
                class="card matcher-result-card possible"
                data-kind="{{ item.service.get_service_kind_display }}"
                data-verified="{% if item.service.last_verified_at %}{{ item.service.last_verified_at|date:'YmdHis' }}{% else %}0{% endif %}">


                <div class="matcher-card-header">

                    <div>

                        <div class="toolkit-service-id">
                            {{ item.service.service_id }}
                        </div>

                        <a
                            href="{% url 'toolkit:service_detail' item.service.slug %}"
                            class="matcher-service-title">

                            {{ item.service.title }}

                        </a>

                    </div>


                    <span class="matcher-status possible">
                        VERIFY
                    </span>

                </div>



                <div class="matcher-service-meta">

                    <span>
                        {{ item.service.domain.name }}
                    </span>

                    <span>•</span>

                    <span>
                        {{ item.service.category.name }}
                    </span>

                    <span>•</span>

                    <span>
                        {{ item.service.get_service_kind_display }}
                    </span>

                </div>



                {% if item.service.bde_summary %}

                    <p class="matcher-summary-copy">
                        {{ item.service.bde_summary }}
                    </p>

                {% endif %}



                <div class="matcher-call-section">

                    <div class="matcher-call-label">
                        WHY WE'RE SHOWING THIS
                    </div>


                    {% if form.need_query.value %}

                        <div class="matcher-reason positive">
                            ✓ Relevant to your search: {{ form.need_query.value }}
                        </div>

                    {% endif %}


                    {% for reason in item.matches %}

                        <div class="matcher-reason positive">
                            ✓ {{ reason.reason }}
                        </div>

                    {% endfor %}

                </div>



                <div class="matcher-call-section verify">

                    <div class="matcher-call-label">
                        WHAT TO VERIFY
                    </div>


                    {% for reason in item.unknowns %}

                        <div class="matcher-reason warning">
                            ! {{ reason.reason }}
                        </div>

                    {% empty %}

                        <div class="matcher-reason warning">
                            ! Structured eligibility criteria have not yet been recorded for this service in the Toolkit.
                        </div>

                    {% endfor %}

                </div>



                <div class="matcher-card-actions">


                    <form
                        method="post"
                        action="{% url 'toolkit:toggle_saved_service' item.service.pk %}"
                        class="matcher-save-form">

                        {% csrf_token %}

                        <button
                            type="submit"
                            class="matcher-save-button {% if item.service.pk in saved_service_ids %}saved{% endif %}">

                            {% if item.service.pk in saved_service_ids %}
                                ★ Saved
                            {% else %}
                                ☆ Save
                            {% endif %}

                        </button>

                    </form>


                    <a
                        href="{% url 'toolkit:service_detail' item.service.slug %}"
                        class="primary-button matcher-open-service">

                        Check Eligibility & Details →

                    </a>

                </div>

            </article>

        {% endfor %}

    </div>

</section>

{% endif %}



<!-- =====================================================
     DOESN'T FIT
====================================================== -->

{% if results.not_eligible_count %}

<details class="matcher-rejected-section">

    <summary>

        <span>
            Doesn't fit current eligibility
        </span>

        <strong>
            {{ results.not_eligible_count }}
        </strong>

    </summary>


    <div class="matcher-results-grid rejected">

        {% for item in results.not_eligible %}

            <article class="card matcher-result-card rejected">


                <div class="matcher-card-header">

                    <div>

                        <div class="toolkit-service-id">
                            {{ item.service.service_id }}
                        </div>

                        <a
                            href="{% url 'toolkit:service_detail' item.service.slug %}"
                            class="matcher-service-title">

                            {{ item.service.title }}

                        </a>

                    </div>


                    <span class="matcher-status rejected">
                        DOESN'T FIT
                    </span>

                </div>



                <div class="matcher-call-section verify">

                    <div class="matcher-call-label">
                        WHY IT DOESN'T FIT
                    </div>


                    {% for reason in item.mismatches %}

                        <div class="matcher-reason negative">
                            × {{ reason.reason }}
                        </div>

                    {% endfor %}

                </div>



                <div class="matcher-card-actions">

                    <a
                        href="{% url 'toolkit:service_detail' item.service.slug %}"
                        class="secondary-button">

                        Review Service

                    </a>

                </div>

            </article>

        {% endfor %}

    </div>

</details>

{% endif %}



<!-- =====================================================
     NOTHING RELEVANT
====================================================== -->

{% if not results.high_count and not results.possible_count and not results.not_eligible_count %}

<section class="card matcher-no-results">

    <div class="matcher-no-results-icon">
        ⌕
    </div>

    <h3>
        No matching Toolkit service found
    </h3>

    <p>
        Try a broader description or remove some company criteria. The system will not invent a scheme that does not exist in the Toolkit.
    </p>

    <a
        href="#client-match-form"
        class="secondary-button">

        Edit Search

    </a>

</section>

{% endif %}


{% endif %}


</div>


<script src="{% static 'js/matcher_saved.js' %}"></script>
<script src="{% static 'js/matcher_sort.js' %}"></script>

{% endblock %}

===== FILE: toolkit/templates/toolkit/saved_services.html =====
{% extends "base.html" %}

{% block title %}Saved Services | BharatNXT Wave{% endblock %}

{% block content %}

<div class="page-eyebrow">
    BDE WORKSPACE
</div>

<h1 class="page-title">
    Saved Services
</h1>

<p class="page-description">
    Services and schemes you starred for quick access during future client calls.
</p>


<div class="saved-services-grid">

    {% for item in saved_items %}

        <article class="toolkit-service-card saved-service-card">

            <div class="toolkit-card-top">

                <span class="toolkit-service-id">
                    {{ item.service.service_id }}
                </span>

                <form
                    method="post"
                    action="{% url 'toolkit:toggle_saved_service' item.service.pk %}"
                    class="save-service-form">

                    {% csrf_token %}

                    <input
                        type="hidden"
                        name="next"
                        value="{{ request.path }}">

                    <button
                        type="submit"
                        class="save-service-button saved"
                        title="Remove from saved services">
                        ★
                    </button>

                </form>

            </div>


            <a
                href="{% url 'toolkit:service_detail' item.service.slug %}"
                class="saved-service-link">

                <h2 class="toolkit-service-title">
                    {{ item.service.title }}
                </h2>

                <div class="toolkit-service-path">
                    {{ item.service.domain.name }}
                    <span>›</span>
                    {{ item.service.category.name }}
                </div>

                {% if item.service.bde_summary %}

                    <p class="toolkit-service-summary">
                        {{ item.service.bde_summary|truncatechars:180 }}
                    </p>

                {% endif %}

            </a>


            <div class="toolkit-card-footer">

                <span>
                    {{ item.service.get_service_kind_display }}
                </span>

                <span>
                    Saved {{ item.created_at|date:"d M Y" }}
                </span>

            </div>

        </article>

    {% empty %}

        <div class="card toolkit-empty-state">

            <div class="toolkit-empty-title">
                No saved services yet
            </div>

            <div class="toolkit-empty-copy">
                Star useful schemes and services to keep them here.
            </div>

            <a
                href="{% url 'toolkit:home' %}"
                class="primary-button saved-empty-button">
                Browse Business Toolkit
            </a>

        </div>

    {% endfor %}

</div>

{% endblock %}

===== FILE: toolkit/templates/toolkit/service_detail.html =====
{% extends "base.html" %}

{% block title %}{{ service.title }} | BharatNXT Wave{% endblock %}

{% block content %}
<!-- SERVICE_PITCH_WINDOW_STATUS -->

<div class="service-pitch-banner {{ service.pitch_state_code|lower }}">

    <div>

        <span class="service-pitch-banner-label">
            CAN THIS BE PITCHED NOW?
        </span>

        <strong>
            {{ service.pitch_state_label }}
        </strong>

    </div>


    <div class="service-pitch-banner-dates">

        {% if service.effective_from %}

            <span>
                Start Pitching From:
                <strong>
                    {{ service.effective_from|date:"d M Y" }}
                </strong>
            </span>

        {% endif %}


        {% if service.pitch_until %}

            <span>
                Stop Pitching After:
                <strong>
                    {{ service.pitch_until|date:"d M Y" }}
                </strong>
            </span>

        {% endif %}


        {% if service.application_deadline %}

            <span>
                Client Application Deadline:
                <strong>
                    {{ service.application_deadline|date:"d M Y" }}
                </strong>
            </span>

        {% endif %}


        {% if service.pitch_days_remaining != None and service.is_pitchable_now %}

            <span>
                <strong>
                    {{ service.pitch_days_remaining }}
                    day{{ service.pitch_days_remaining|pluralize }}
                    remaining
                </strong>
            </span>

        {% endif %}

    </div>

</div>


<div class="service-detail-header">

    <a
        href="{% url 'toolkit:home' %}"
        class="service-back-link">
        ← Back to Business Toolkit
    </a>


    <div class="page-eyebrow">
        {{ service.service_id }}
    </div>

    <div class="service-title-row">

        <div>

            <h1 class="page-title">
                {{ service.title }}
            </h1>

            <div class="service-breadcrumb">
                {{ service.domain.name }}
                <span>›</span>
                {{ service.category.name }}
                <span>›</span>
                {{ service.get_service_kind_display }}
            </div>

        </div>

        <div class="service-header-actions">

            <form
                method="post"
                action="{% url 'toolkit:toggle_saved_service' service.pk %}"
                class="save-service-form">

                {% csrf_token %}

                <input
                    type="hidden"
                    name="next"
                    value="{{ request.path }}">

                <button
                    type="submit"
                    class="save-service-button {% if is_saved %}saved{% endif %}">

                    {% if is_saved %}
                        ★ Saved
                    {% else %}
                        ☆ Save
                    {% endif %}

                </button>

            </form>

            <span class="status-badge active">
                {{ service.get_status_display }}
            </span>

        </div>

    </div>

</div>


{% if service.bde_summary %}

<div class="card service-call-card">

    <div class="service-section-label">
        QUICK CALL SUMMARY
    </div>

    <div class="service-call-summary">
        {{ service.bde_summary|linebreaksbr }}
    </div>

</div>

{% endif %}


<div class="service-detail-grid">


    <div class="service-main-column">


        {% if service.overview %}

        <section class="card service-section">

            <div class="service-section-label">
                OVERVIEW
            </div>

            <div class="service-copy">
                {{ service.overview|linebreaks }}
            </div>

        </section>

        {% endif %}


        {% if service.benefits %}

        <section class="card service-section">

            <div class="service-section-label">
                BENEFITS
            </div>

            <div class="service-copy">
                {{ service.benefits|linebreaks }}
            </div>

        </section>

        {% endif %}


        {% if service.eligibility_summary or service.business_types or service.business_stages or service.industries or service.applicable_states or service.founder_categories %}

        <section class="card service-section">

            <div class="service-section-label">
                ELIGIBILITY
            </div>

            {% if service.eligibility_summary %}
                <div class="service-copy">
                    {{ service.eligibility_summary|linebreaks }}
                </div>
            {% endif %}


            <div class="service-info-grid">

                {% if service.business_types %}
                    <div class="service-info-item">
                        <span>Business Types</span>
                        <strong>{{ service.business_types|join:", " }}</strong>
                    </div>
                {% endif %}

                {% if service.business_stages %}
                    <div class="service-info-item">
                        <span>Business Stages</span>
                        <strong>{{ service.business_stages|join:", " }}</strong>
                    </div>
                {% endif %}

                {% if service.industries %}
                    <div class="service-info-item">
                        <span>Industries</span>
                        <strong>{{ service.industries|join:", " }}</strong>
                    </div>
                {% endif %}

                {% if service.applicable_states %}
                    <div class="service-info-item">
                        <span>Applicable States</span>
                        <strong>{{ service.applicable_states|join:", " }}</strong>
                    </div>
                {% endif %}

                {% if service.founder_categories %}
                    <div class="service-info-item">
                        <span>Founder Categories</span>
                        <strong>{{ service.founder_categories|join:", " }}</strong>
                    </div>
                {% endif %}

            </div>

        </section>

        {% endif %}


        {% if service.restrictions %}

        <section class="card service-section">

            <div class="service-section-label">
                RESTRICTIONS
            </div>

            <div class="service-copy">
                {{ service.restrictions|linebreaks }}
            </div>

        </section>

        {% endif %}


        {% if service.important_notes %}

        <section class="card service-section">

            <div class="service-section-label">
                IMPORTANT NOTES
            </div>

            <div class="service-copy">
                {{ service.important_notes|linebreaks }}
            </div>

        </section>

        {% endif %}


        {% if service.sales_pitch %}

        <section class="card service-section">

            <div class="service-section-label">
                BDE SALES PITCH
            </div>

            <div class="service-copy">
                {{ service.sales_pitch|linebreaks }}
            </div>

        </section>

        {% endif %}


        {% if service.internal_notes %}

        <section class="card service-section">

            <div class="service-section-label">
                INTERNAL NOTES
            </div>

            <div class="service-copy">
                {{ service.internal_notes|linebreaks }}
            </div>

        </section>

        {% endif %}


    </div>


    <aside class="service-side-column">


        <div class="card service-facts-card">

            <div class="service-section-label">
                QUICK FACTS
            </div>


            {% if service.estimated_processing_time %}

            <div class="service-fact">
                <span>Processing Time</span>
                <strong>{{ service.estimated_processing_time }}</strong>
            </div>

            {% endif %}


            {% if service.application_deadline %}

            <div class="service-fact">
                <span>Deadline</span>
                <strong>{{ service.application_deadline|date:"d M Y" }}</strong>
            </div>

            {% endif %}


            {% if service.government_fee != None %}

            <div class="service-fact">
                <span>Government Fee</span>
                <strong>₹{{ service.government_fee }}</strong>
            </div>

            {% endif %}


            {% if service.bharatnxt_fee != None %}

            <div class="service-fact">
                <span>BharatNXT Fee</span>
                <strong>₹{{ service.bharatnxt_fee }}</strong>
            </div>

            {% endif %}


            {% if service.funding_min != None or service.funding_max != None %}

            <div class="service-fact">
                <span>Funding Range</span>

                <strong>
                    {% if service.funding_min != None %}
                        ₹{{ service.funding_min }}
                    {% endif %}

                    {% if service.funding_min != None and service.funding_max != None %}
                        –
                    {% endif %}

                    {% if service.funding_max != None %}
                        ₹{{ service.funding_max }}
                    {% endif %}
                </strong>
            </div>

            {% endif %}


            {% if service.funding_type %}

            <div class="service-fact">
                <span>Funding Type</span>
                <strong>{{ service.funding_type }}</strong>
            </div>

            {% endif %}


            {% if service.tenure %}

            <div class="service-fact">
                <span>Tenure</span>
                <strong>{{ service.tenure }}</strong>
            </div>

            {% endif %}


            {% if service.collateral_required != None %}

            <div class="service-fact">
                <span>Collateral</span>

                <strong>
                    {% if service.collateral_required %}
                        Required
                    {% else %}
                        Not Required
                    {% endif %}
                </strong>
            </div>

            {% endif %}

        </div>


        <div class="card service-facts-card">

            <div class="service-section-label">
                DATA CONFIDENCE
            </div>

            <div class="service-fact">
                <span>Version</span>
                <strong>{{ service.version }}</strong>
            </div>


            <div class="service-fact">
                <span>Last Updated</span>
                <strong>{{ service.updated_at|date:"d M Y" }}</strong>
            </div>


            <div class="service-fact">
                <span>Last Verified</span>

                <strong>
                    {% if service.last_verified_at %}
                        {{ service.last_verified_at|date:"d M Y" }}
                    {% else %}
                        Not recorded
                    {% endif %}
                </strong>

            </div>

        </div>


        {% if service.pricing_notes %}

        <div class="card service-facts-card">

            <div class="service-section-label">
                PRICING NOTES
            </div>

            <div class="service-copy">
                {{ service.pricing_notes|linebreaks }}
            </div>

        </div>

        {% endif %}


        {% if service.escalation_notes %}

        <div class="card service-facts-card">

            <div class="service-section-label">
                ESCALATION
            </div>

            <div class="service-copy">
                {{ service.escalation_notes|linebreaks }}
            </div>

        </div>

        {% endif %}


    </aside>

</div>

{% endblock %}

===== FILE: toolkit/templates/toolkit/toolkit_home.html =====
{% extends "base.html" %}
{% load static %}

{% block title %}Business Toolkit | BharatNXT Wave{% endblock %}

{% block content %}

<div class="toolkit-v3-page">


<div class="page-eyebrow">
    BDE WORKSPACE
</div>

<h1 class="page-title">
    Search BharatNXT Wave Toolkit
</h1>

<p class="page-description">
    Search by scheme name, service, sector or client requirement. Only currently pitchable Toolkit records are shown.
</p>



<!-- =====================================================
     MAIN SEARCH
====================================================== -->

<section class="toolkit-v3-search-card">

    <form
        method="get"
        action="{% url 'toolkit:home' %}"
        class="toolkit-v3-search-form">


        <div class="toolkit-search-wrapper">

            <input
                id="toolkit-search-input"
                class="toolkit-search-input toolkit-v3-search-input"
                type="search"
                name="q"
                value="{{ query }}"
                placeholder="Search scheme name or sector — e.g. Startup India, Technology, Agriculture..."
                autocomplete="off"
                autofocus
            >


            <div
                id="toolkit-suggestions"
                class="toolkit-suggestions"
                hidden>
            </div>

        </div>


        <button
            type="submit"
            class="primary-button toolkit-v3-search-button">
            Search Toolkit
        </button>

    </form>


    <div class="toolkit-v3-search-help">
        Search by exact scheme/service name or simply type a sector such as Technology, Manufacturing or Agriculture.
    </div>

</section>



<!-- =====================================================
     SECTOR DISCOVERY
====================================================== -->

{% if sector_options %}

<section class="toolkit-sector-section">

    <div class="toolkit-section-header">

        <div>

            <div class="table-title">
                Browse by Sector
            </div>

            <div class="table-subtitle">
                Select a sector to see all currently pitchable matching services
            </div>

        </div>

    </div>


    <div class="toolkit-sector-chips">

        {% for item in sector_options %}

            <a
                href="?sector={{ item.name|urlencode }}"
                class="toolkit-sector-chip {% if selected_sector == item.name %}active{% endif %}">

                <span>
                    {{ item.name }}
                </span>

                <strong>
                    {{ item.count }}
                </strong>

            </a>

        {% endfor %}

    </div>

</section>

{% endif %}



<!-- =====================================================
     FILTERS
====================================================== -->

<section class="card toolkit-v3-filter-card">

    <form
        method="get"
        class="toolkit-v3-filters">


        {% if query %}
            <input
                type="hidden"
                name="q"
                value="{{ query }}">
        {% endif %}


        <div class="toolkit-v3-filter">

            <label>
                SECTOR
            </label>

            <select name="sector">

                <option value="">
                    All Sectors
                </option>

                {% for item in sector_options %}

                    <option
                        value="{{ item.name }}"
                        {% if selected_sector == item.name %}selected{% endif %}>

                        {{ item.name }}

                    </option>

                {% endfor %}

            </select>

        </div>


        <div class="toolkit-v3-filter">

            <label>
                SERVICE TYPE
            </label>

            <select name="kind">

                <option value="">
                    All Types
                </option>

                {% for value, label in service_kinds %}

                    <option
                        value="{{ value }}"
                        {% if selected_kind == value %}selected{% endif %}>

                        {{ label }}

                    </option>

                {% endfor %}

            </select>

        </div>


        <div class="toolkit-v3-filter">

            <label>
                DEADLINE
            </label>

            <select name="deadline">

                <option
                    value=""
                    {% if not selected_deadline %}selected{% endif %}>
                    All Active
                </option>

                <option
                    value="urgent"
                    {% if selected_deadline == 'urgent' %}selected{% endif %}>
                    Closing in 7 Days
                </option>

                <option
                    value="closing"
                    {% if selected_deadline == 'closing' %}selected{% endif %}>
                    Closing in 30 Days
                </option>

                <option
                    value="no_deadline"
                    {% if selected_deadline == 'no_deadline' %}selected{% endif %}>
                    No Deadline
                </option>

            </select>

        </div>


        <div class="toolkit-v3-filter">

            <label>
                DOMAIN
            </label>

            <select name="domain">

                <option value="">
                    All Domains
                </option>

                {% for domain in domains %}

                    <option
                        value="{{ domain.slug }}"
                        {% if selected_domain == domain.slug %}selected{% endif %}>

                        {{ domain.name }}

                    </option>

                {% endfor %}

            </select>

        </div>


        <button
            type="submit"
            class="secondary-button">
            Apply Filters
        </button>


        {% if query or selected_sector or selected_kind or selected_domain or selected_deadline %}

            <a
                href="{% url 'toolkit:home' %}"
                class="toolkit-clear-filters">
                Clear
            </a>

        {% endif %}

    </form>

</section>



<!-- =====================================================
     RESULTS HEADER
====================================================== -->

<div class="toolkit-results-header">

    <div>

        <div class="table-title">

            {% if selected_sector %}

                {{ selected_sector }} Services

            {% elif query %}

                Results for “{{ query }}”

            {% else %}

                Available Services

            {% endif %}

        </div>


        <div class="table-subtitle">
            {{ result_count }}
            currently pitchable service{{ result_count|pluralize }}
        </div>

    </div>


    {% if selected_sector %}

        <div class="toolkit-active-sector">
            Sector: {{ selected_sector }}
        </div>

    {% endif %}

</div>



<!-- =====================================================
     RESULT CARDS
====================================================== -->

<div class="toolkit-v3-results">

    {% for service in services %}

        <article class="card toolkit-v3-service-card">


            <div class="toolkit-v3-card-top">

                <div>

                    <div class="toolkit-service-id">
                        {{ service.service_id }}
                    </div>

                    <a
                        href="{% url 'toolkit:service_detail' service.slug %}"
                        class="toolkit-v3-service-title">

                        {{ service.title }}

                    </a>

                </div>


                <div class="toolkit-pitch-badge {{ service.pitch_state_code|lower }}">

                    {{ service.pitch_state_label }}

                </div>

            </div>



            <div class="toolkit-v3-meta">

                <span>
                    {{ service.domain.name }}
                </span>

                <span>•</span>

                <span>
                    {{ service.category.name }}
                </span>

                <span>•</span>

                <span>
                    {{ service.get_service_kind_display }}
                </span>

            </div>



            {% if service.industries %}

                <div class="toolkit-sector-tags">

                    {% for industry in service.industries|slice:":5" %}

                        <span>
                            {{ industry }}
                        </span>

                    {% endfor %}

                </div>

            {% endif %}



            {% if service.bde_summary %}

                <p class="toolkit-v3-summary">
                    {{ service.bde_summary|truncatechars:220 }}
                </p>

            {% elif service.overview %}

                <p class="toolkit-v3-summary">
                    {{ service.overview|truncatechars:220 }}
                </p>

            {% endif %}



            <!-- PITCH WINDOW -->

            <div class="toolkit-pitch-info">


                {% if service.effective_from %}

                    <div>

                        <span>
                            EFFECTIVE FROM
                        </span>

                        <strong>
                            {{ service.effective_from|date:"d M Y" }}
                        </strong>

                    </div>

                {% endif %}


                {% if service.pitch_end_date %}

                    <div>

                        <span>
                            {{ service.pitch_end_label|upper }}
                        </span>

                        <strong>
                            {{ service.pitch_end_date|date:"d M Y" }}
                        </strong>

                    </div>


                    <div>

                        <span>
                            TIME REMAINING
                        </span>

                        <strong class="{% if service.pitch_days_remaining <= 7 %}danger-text{% elif service.pitch_days_remaining <= 30 %}warning-text{% endif %}">

                            {{ service.pitch_days_remaining }}
                            day{{ service.pitch_days_remaining|pluralize }}

                        </strong>

                    </div>

                {% else %}

                    <div>

                        <span>
                            PITCH WINDOW
                        </span>

                        <strong>
                            No fixed deadline
                        </strong>

                    </div>

                {% endif %}

            </div>



            <div class="toolkit-v3-actions">


                <form
                    method="post"
                    action="{% url 'toolkit:toggle_saved_service' service.pk %}">

                    {% csrf_token %}

                    <input
                        type="hidden"
                        name="next"
                        value="{{ request.get_full_path }}">

                    <button
                        type="submit"
                        class="matcher-save-button {% if service.pk in saved_service_ids %}saved{% endif %}">

                        {% if service.pk in saved_service_ids %}
                            ★ Saved
                        {% else %}
                            ☆ Save
                        {% endif %}

                    </button>

                </form>


                <a
                    href="{% url 'toolkit:service_detail' service.slug %}"
                    class="primary-button">

                    View Details →

                </a>

            </div>

        </article>


    {% empty %}

        <div class="card toolkit-v3-empty">

            <div class="toolkit-v3-empty-icon">
                ⌕
            </div>

            <h3>
                No matching active Toolkit service
            </h3>

            <p>
                Try another scheme name, sector or remove some filters. The Toolkit will not create or invent unavailable schemes.
            </p>

        </div>

    {% endfor %}

</div>


</div>


<script src="{% static 'js/toolkit_search.js' %}"></script>

{% endblock %}

===== DASHBOARD TEMPLATES =====

===== FILE: dashboard/templates/dashboard/admin_overview.html =====
{% extends "admin_base.html" %}

{% block title %}Admin Overview | BharatNXT Wave{% endblock %}

{% block content %}

<div class="page-eyebrow">
    ADMIN CENTER
</div>

<h1 class="page-title">
    Overview
</h1>

<p class="page-description">
    Current status of employees, toolkit data and internal platform activity.
</p>


<div class="admin-overview-grid">

    <div class="card stat-card">
        <div class="stat-label">
            TOTAL EMPLOYEES
        </div>

        <div class="stat-value">
            {{ total_employees }}
        </div>

        <div class="overview-stat-meta">
            {{ active_employees }} active
        </div>
    </div>


    <div class="card stat-card">
        <div class="stat-label">
            TOOLKIT SERVICES
        </div>

        <div class="stat-value">
            {{ total_services }}
        </div>

        <div class="overview-stat-meta">
            {{ published_services }} available to BDEs
        </div>
    </div>


    <div class="card stat-card">
        <div class="stat-label">
            ACTIVE SESSIONS
        </div>

        <div class="stat-value">
            {{ active_sessions }}
        </div>

        <div class="overview-stat-meta">
            Current recorded sessions
        </div>
    </div>

</div>


<div class="card overview-activity-card">

    <div class="table-header">

        <div>

            <div class="table-title">
                Recent Activity
            </div>

            <div class="table-subtitle">
                Latest important platform events
            </div>

        </div>


        <a
            href="{% url 'accounts:activity_logs' %}"
            class="secondary-button">
            View All Activity
        </a>

    </div>


    <div class="table-wrapper">

        <table class="employee-table">

            <thead>
                <tr>
                    <th>Time</th>
                    <th>Employee</th>
                    <th>Activity</th>
                    <th>Description</th>
                </tr>
            </thead>

            <tbody>

                {% for log in recent_activity %}

                <tr>

                    <td>
                        {{ log.created_at|date:"d M Y, h:i A" }}
                    </td>

                    <td>
                        {% if log.user %}
                            {{ log.user.get_full_name|default:log.user.username }}
                        {% else %}
                            Unknown
                        {% endif %}
                    </td>

                    <td>
                        <span class="activity-type">
                            {{ log.get_action_display }}
                        </span>
                    </td>

                    <td>
                        {{ log.description }}
                    </td>

                </tr>

                {% empty %}

                <tr>
                    <td colspan="4">
                        No activity has been recorded yet.
                    </td>
                </tr>

                {% endfor %}

            </tbody>

        </table>

    </div>

</div>

{% endblock %}

===== FILE: dashboard/templates/dashboard/bde_analytics.html =====
{% extends "admin_base.html" %}
{% load static %}

{% block title %}BDE Analytics | BharatNXT Wave{% endblock %}

{% block content %}

<div class="page-eyebrow">
    ADMIN CENTER
</div>

<div class="dashboard-title-row">

    <div>

        <h1 class="page-title">
            BDE Analytics
        </h1>

        <p class="page-description">
            Analyse real toolkit searches, service usage and employee activity.
        </p>

    </div>

    <div class="live-data-badge">
        ● LIVE DATABASE
    </div>

</div>


{% if not selected_bde %}

    <div class="card admin-no-bde">

        <div class="table-title">
            No BDE accounts available
        </div>

        <p class="page-description">
            Analytics will become available after BDE employee accounts are created.
        </p>

    </div>

{% else %}


<!-- =====================================================
     EMPLOYEE SELECTOR
====================================================== -->

<section class="card analytics-control-bar">

    <form method="get"
          class="analytics-employee-form">

        <div>

            <label class="analytics-control-label">
                BDE EMPLOYEE
            </label>

            <select
                name="employee"
                onchange="this.form.submit()">

                {% for bde in bdes %}

                    <option
                        value="{{ bde.pk }}"
                        {% if bde.pk == selected_bde.pk %}selected{% endif %}>

                        {% if bde.get_full_name %}
                            {{ bde.get_full_name }}
                        {% else %}
                            {{ bde.username }}
                        {% endif %}

                        {% if bde.employee_id %}
                            — {{ bde.employee_id }}
                        {% endif %}

                    </option>

                {% endfor %}

            </select>

        </div>

        <input
            type="hidden"
            name="start"
            value="{{ selected_start|date:'Y-m-d' }}">

        <input
            type="hidden"
            name="end"
            value="{{ selected_end|date:'Y-m-d' }}">

    </form>


    <div class="selected-bde-status">

        <span class="status-dot
            {% if selected_bde.is_account_active %}active{% endif %}">
        </span>

        {% if selected_bde.is_account_active %}
            Active account
        {% else %}
            Inactive account
        {% endif %}

    </div>

</section>


<!-- =====================================================
     KPI CARDS
====================================================== -->

<div class="admin-analytics-kpi-grid">

    <div class="card search-kpi-card">
        <div class="stat-label">SEARCHES</div>
        <div class="search-kpi-value">{{ searches }}</div>
        <div class="search-kpi-meta">Selected period</div>
    </div>

    <div class="card search-kpi-card">
        <div class="stat-label">SERVICES VIEWED</div>
        <div class="search-kpi-value">{{ service_views }}</div>
        <div class="search-kpi-meta">Service detail opens</div>
    </div>

    <div class="card search-kpi-card">
        <div class="stat-label">UNIQUE QUERIES</div>
        <div class="search-kpi-value">{{ unique_queries }}</div>
        <div class="search-kpi-meta">Distinct searches</div>
    </div>

    <div class="card search-kpi-card">
        <div class="stat-label">ZERO RESULTS</div>
        <div class="search-kpi-value">{{ zero_results }}</div>
        <div class="search-kpi-meta">Possible toolkit gaps</div>
    </div>

    <div class="card search-kpi-card">
        <div class="stat-label">SAVED SERVICES</div>
        <div class="search-kpi-value">{{ current_saved }}</div>
        <div class="search-kpi-meta">Currently starred</div>
    </div>

    <div class="card search-kpi-card">
        <div class="stat-label">LOGINS</div>
        <div class="search-kpi-value">{{ login_count }}</div>
        <div class="search-kpi-meta">Selected period</div>
    </div>

</div>


<!-- =====================================================
     CALENDAR
====================================================== -->

<section class="card admin-calendar-section">

    <div class="calendar-header">

        <div>

            <div class="table-title">
                Employee Activity Calendar
            </div>

            <div class="table-subtitle">
                Search counts are shown directly on each date
            </div>

        </div>


        <div class="calendar-month-controls">

            <a
                href="?employee={{ selected_bde.pk }}&month={{ previous_month }}&start={{ selected_start|date:'Y-m-d' }}&end={{ selected_end|date:'Y-m-d' }}"
                class="calendar-arrow">
                ‹
            </a>

            <strong>
                {{ calendar_title }}
            </strong>

            <a
                href="?employee={{ selected_bde.pk }}&month={{ next_month }}&start={{ selected_start|date:'Y-m-d' }}&end={{ selected_end|date:'Y-m-d' }}"
                class="calendar-arrow">
                ›
            </a>

        </div>

    </div>


    <div class="calendar-presets">

        <a
            href="?employee={{ selected_bde.pk }}&preset=today"
            class="calendar-preset {% if preset == 'today' %}active{% endif %}">
            Today
        </a>

        <a
            href="?employee={{ selected_bde.pk }}&preset=7d"
            class="calendar-preset {% if preset == '7d' or not preset %}active{% endif %}">
            Last 7 Days
        </a>

        <a
            href="?employee={{ selected_bde.pk }}&preset=30d"
            class="calendar-preset {% if preset == '30d' %}active{% endif %}">
            Last 30 Days
        </a>

    </div>


    <form method="get"
          id="calendar-range-form">

        <input
            type="hidden"
            name="employee"
            value="{{ selected_bde.pk }}">

        <input
            type="hidden"
            name="month"
            value="{{ calendar_month_value }}">

        <input
            type="hidden"
            id="calendar-start"
            name="start"
            value="{{ selected_start|date:'Y-m-d' }}">

        <input
            type="hidden"
            id="calendar-end"
            name="end"
            value="{{ selected_end|date:'Y-m-d' }}">


        <div class="calendar-weekdays">

            <span>MON</span>
            <span>TUE</span>
            <span>WED</span>
            <span>THU</span>
            <span>FRI</span>
            <span>SAT</span>
            <span>SUN</span>

        </div>


        <div class="analytics-calendar">

            {% for week in calendar_weeks %}

                {% for day in week %}

                    <button
                        type="button"
                        class="calendar-day
                        {% if not day.in_month %}outside{% endif %}
                        {% if day.is_today %}today{% endif %}
                        {% if day.selected %}selected{% endif %}"
                        data-date="{{ day.date_value }}">

                        <span class="calendar-day-number">
                            {{ day.day }}
                        </span>

                        {% if day.search_count %}

                            <span class="calendar-search-count">
                                {{ day.search_count }}
                                search{{ day.search_count|pluralize:"es" }}
                            </span>

                        {% endif %}

                    </button>

                {% endfor %}

            {% endfor %}

        </div>


        <div class="calendar-selection-footer">

            <div>

                <span class="calendar-selection-label">
                    ANALYSING
                </span>

                <strong id="calendar-range-label">
                    {{ selected_start|date:"d M Y" }}
                    —
                    {{ selected_end|date:"d M Y" }}
                </strong>

            </div>

            <button
                type="submit"
                class="primary-button">
                Analyse Range
            </button>

        </div>

    </form>

</section>


<!-- =====================================================
     INTERACTIVE ACTIVITY CHART
====================================================== -->

<section class="card dashboard-chart-large">

    <div class="dashboard-card-heading">

        <div>

            <div class="table-title">
                Searches vs Service Views
            </div>

            <div class="table-subtitle">
                Click a date to inspect that day's activity
            </div>

        </div>


        <div class="analytics-chart-legend">

            <span>
                <i class="legend-search"></i>
                Searches
            </span>

            <span>
                <i class="legend-view"></i>
                Service Views
            </span>

        </div>

    </div>


    <div class="dual-bar-chart">

        {% for day in chart %}

            <a
                href="?employee={{ selected_bde.pk }}&start={{ day.date_value }}&end={{ day.date_value }}&month={{ day.date|date:'Y-m' }}"
                class="dual-bar-column">

                <div class="dual-values">
                    {{ day.searches }} / {{ day.views }}
                </div>

                <div class="dual-bars">

                    <div class="dual-bar-track">
                        <div
                            class="dual-bar search-series"
                            style="height: {{ day.search_height }}%;">
                        </div>
                    </div>

                    <div class="dual-bar-track">
                        <div
                            class="dual-bar view-series"
                            style="height: {{ day.view_height }}%;">
                        </div>
                    </div>

                </div>

                <div class="search-bar-day">
                    {{ day.label }}
                </div>

            </a>

        {% endfor %}

    </div>

</section>


<!-- =====================================================
     SEARCH INTELLIGENCE
====================================================== -->

<div class="admin-analytics-grid">


    <section class="card dashboard-list-card">

        <div class="dashboard-card-heading">

            <div>
                <div class="table-title">
                    Top Searches
                </div>

                <div class="table-subtitle">
                    Most frequent queries
                </div>
            </div>

        </div>


        <div class="top-query-list">

            {% for item in top_queries %}

                <div class="top-query-item">

                    <span class="top-query-name">
                        {{ item.query }}
                    </span>

                    <span class="top-query-count">
                        {{ item.total }}
                    </span>

                </div>

            {% empty %}

                <div class="dashboard-empty">
                    No searches in this period.
                </div>

            {% endfor %}

        </div>

    </section>


    <section class="card dashboard-list-card">

        <div class="dashboard-card-heading">

            <div>
                <div class="table-title">
                    Zero-Result Searches
                </div>

                <div class="table-subtitle">
                    Searches where the toolkit returned nothing
                </div>
            </div>

        </div>


        <div class="top-query-list">

            {% for item in zero_result_queries %}

                <div class="top-query-item zero-query">

                    <span class="top-query-name">
                        {{ item.query }}
                    </span>

                    <span class="top-query-count">
                        {{ item.total }}
                    </span>

                </div>

            {% empty %}

                <div class="dashboard-empty">
                    No zero-result searches.
                </div>

            {% endfor %}

        </div>

    </section>


    <section class="card dashboard-list-card">

        <div class="dashboard-card-heading">

            <div>
                <div class="table-title">
                    Most Viewed Services
                </div>

                <div class="table-subtitle">
                    Services this BDE opened most
                </div>
            </div>

        </div>


        <div class="top-query-list">

            {% for item in top_services %}

                <div class="top-query-item">

                    <span class="top-query-name">
                        {{ item.service.title }}
                    </span>

                    <span class="top-query-count">
                        {{ item.total }}
                    </span>

                </div>

            {% empty %}

                <div class="dashboard-empty">
                    No service views recorded.
                </div>

            {% endfor %}

        </div>

    </section>

</div>


<!-- =====================================================
     REAL SEARCH LOG
====================================================== -->

<section class="card admin-search-log">

    <div class="dashboard-card-heading">

        <div>

            <div class="table-title">
                Search Event Log
            </div>

            <div class="table-subtitle">
                Actual searches submitted by this BDE
            </div>

        </div>

    </div>


    <div class="admin-analytics-table-wrap">

        <table class="admin-analytics-table">

            <thead>
                <tr>
                    <th>Date / Time</th>
                    <th>Query</th>
                    <th>Results</th>
                    <th>Type</th>
                </tr>
            </thead>

            <tbody>

                {% for event in recent_search_events %}

                    <tr>

                        <td>
                            {{ event.created_at|date:"d M Y, h:i A" }}
                        </td>

                        <td>
                            {{ event.query|default:"—" }}
                        </td>

                        <td>
                            {{ event.result_count }}
                        </td>

                        <td>
                            {{ event.get_event_type_display }}
                        </td>

                    </tr>

                {% empty %}

                    <tr>
                        <td colspan="4">
                            No recorded searches.
                        </td>
                    </tr>

                {% endfor %}

            </tbody>

        </table>

    </div>

</section>


<script src="{% static 'js/dashboard_calendar.js' %}"></script>
<script src="{% static 'js/dashboard_live.js' %}"></script>

{% endif %}

{% endblock %}

===== FILE: dashboard/templates/dashboard/home.html =====
{% extends "base.html" %}
{% load static %}

{% block title %}
Dashboard | BharatNXT Wave
{% endblock %}


{% block content %}

<div class="bn-home-v2">


<!-- =====================================================
     DASHBOARD HEADER
====================================================== -->

<header class="bn-home-header">

    <div>

        <h1>
            Dashboard
        </h1>

        <p>
            Your BharatNXT Wave Toolkit activity and important services at a glance.
        </p>

    </div>


    <!-- DATE RANGE POPOVER -->

    <details class="bn-date-picker">

        <summary>

            <span class="bn-date-icon">
                ◷
            </span>

            <span>

                {{ selected_start|date:"d M" }}
                –
                {{ selected_end|date:"d M Y" }}

            </span>

            <span class="bn-date-chevron">
                ▾
            </span>

        </summary>


        <div class="bn-date-popover">


            <div class="bn-date-popover-title">

                <strong>
                    Select Date Range
                </strong>

                <span>
                    Change dashboard reporting period
                </span>

            </div>


            <div class="bn-date-presets">

                <a href="?preset=today">
                    Today
                </a>

                <a href="?preset=7d">
                    Last 7 Days
                </a>

                <a href="?preset=30d">
                    Last 30 Days
                </a>

            </div>


            <form method="get">

                <label>

                    <span>
                        From
                    </span>

                    <input
                        type="date"
                        name="start"
                        value="{{ selected_start|date:'Y-m-d' }}">

                </label>


                <label>

                    <span>
                        To
                    </span>

                    <input
                        type="date"
                        name="end"
                        value="{{ selected_end|date:'Y-m-d' }}">

                </label>


                <button
                    type="submit"
                    class="primary-button">

                    Apply Range

                </button>

            </form>

        </div>

    </details>

</header>



<!-- =====================================================
     KPI STRIP
====================================================== -->

<section class="bn-home-kpi-strip">


    <a
        href="{% url 'toolkit:home' %}"
        class="bn-home-kpi">

        <div class="bn-kpi-icon">
            ⌕
        </div>

        <div>

            <span>
                Toolkit Searches
            </span>

            <strong>
                {{ search_count }}
            </strong>

        </div>

    </a>


    <a
        href="{% url 'toolkit:client_matcher' %}"
        class="bn-home-kpi">

        <div class="bn-kpi-icon">
            ◇
        </div>

        <div>

            <span>
                Client Service Checks
            </span>

            <strong>
                {{ client_check_count }}
            </strong>

        </div>

    </a>


    <div class="bn-home-kpi">

        <div class="bn-kpi-icon">
            ◉
        </div>

        <div>

            <span>
                Services Viewed
            </span>

            <strong>
                {{ service_view_count }}
            </strong>

        </div>

    </div>


    <a
        href="{% url 'toolkit:saved_services' %}"
        class="bn-home-kpi">

        <div class="bn-kpi-icon">
            ★
        </div>

        <div>

            <span>
                Saved Services
            </span>

            <strong>
                {{ saved_service_count }}
            </strong>

        </div>

    </a>


    <div class="bn-home-kpi">

        <div class="bn-kpi-icon warning">
            !
        </div>

        <div>

            <span>
                Searches With No Result
            </span>

            <strong>
                {{ zero_result_count }}
            </strong>

        </div>

    </div>

</section>



<!-- =====================================================
     MAIN ROW
====================================================== -->

<div class="bn-home-main-grid">



<!-- TOOLKIT ACTIVITY -->

<section class="bn-dashboard-panel bn-activity-panel">


    <div class="bn-panel-header">

        <div>

            <h2>
                Toolkit Activity
            </h2>

            <p>
                How you used the Toolkit during the selected period
            </p>

        </div>


        <span class="bn-activity-period">

            {{ selected_start|date:"d M" }}
            –
            {{ selected_end|date:"d M Y" }}

        </span>

    </div>



    <div
        id="bn-activity-pie"
        class="bn-activity-pie-layout"

        data-searches="{{ search_count }}"
        data-client-checks="{{ client_check_count }}">


        <!-- =================================================
             DONUT
        ================================================== -->

        <div class="bn-pie-visual">


            <svg
                class="bn-pie-svg"
                viewBox="0 0 220 220"
                role="img"
                aria-label="Toolkit activity composition">

                <circle
                    class="bn-pie-track"
                    cx="110"
                    cy="110"
                    r="82">
                </circle>


                <circle
                    id="bn-pie-search"
                    class="bn-pie-segment search"
                    cx="110"
                    cy="110"
                    r="82">
                </circle>


                <circle
                    id="bn-pie-client"
                    class="bn-pie-segment client"
                    cx="110"
                    cy="110"
                    r="82">
                </circle>

            </svg>



            <div class="bn-pie-center">

                <span id="bn-pie-center-label">
                    TOTAL ACTIONS
                </span>

                <strong id="bn-pie-center-value">
                    0
                </strong>

                <small id="bn-pie-center-detail">
                    Selected period
                </small>

            </div>

        </div>



        <!-- =================================================
             EASY-TO-READ ACTIVITY SUMMARY
        ================================================== -->

        <div class="bn-pie-summary">


            <div class="bn-pie-summary-heading">

                <h3>
                    Activity Breakdown
                </h3>

                <p>
                    See which Toolkit feature you used more.
                </p>

            </div>



            <button
                type="button"
                class="bn-pie-legend-item search"
                data-pie-series="search">


                <span class="bn-pie-legend-mark">
                </span>


                <div class="bn-pie-legend-copy">

                    <strong>
                        Toolkit Searches
                    </strong>

                    <span>
                        Searches made in the Business Toolkit
                    </span>

                </div>


                <div class="bn-pie-legend-number">

                    <strong>
                        {{ search_count }}
                    </strong>

                    <span id="bn-search-percent">
                        0%
                    </span>

                </div>

            </button>



            <button
                type="button"
                class="bn-pie-legend-item client"
                data-pie-series="client">


                <span class="bn-pie-legend-mark">
                </span>


                <div class="bn-pie-legend-copy">

                    <strong>
                        Client Service Checks
                    </strong>

                    <span>
                        Times you checked which services fit a client
                    </span>

                </div>


                <div class="bn-pie-legend-number">

                    <strong>
                        {{ client_check_count }}
                    </strong>

                    <span id="bn-client-percent">
                        0%
                    </span>

                </div>

            </button>



            <div
                id="bn-pie-empty"
                class="bn-pie-empty"
                hidden>

                No Toolkit searches or client service checks were
                recorded during this period.

            </div>



            <div class="bn-pie-actions">

                <a
                    href="{% url 'toolkit:home' %}"
                    class="bn-pie-action-link">

                    Search Toolkit →

                </a>


                <a
                    href="{% url 'toolkit:client_matcher' %}"
                    class="bn-pie-action-link">

                    Find Services for Client →

                </a>

            </div>


        </div>

    </div>

</section>



<!-- CLOSING SOON -->

<aside class="bn-dashboard-panel bn-closing-panel">


    <div class="bn-panel-header">

        <div>

            <h2>
                Closing Soon
            </h2>

            <p>
                Services to prioritise
            </p>

        </div>


        {% if request.user.is_superuser %}

            <a
                href="{% url 'toolkit:pitch_windows' %}"
                class="bn-panel-link">

                View All →

            </a>

        {% endif %}

    </div>


    <div class="bn-closing-list">


        {% for service in closing_soon_services %}

            <a
                href="{% url 'toolkit:service_detail' service.slug %}"
                class="bn-closing-item">

                <div>

                    <strong>
                        {{ service.title }}
                    </strong>

                    <span>

                        {{ service.domain.name }}

                        {% if service.industries %}
                            · {{ service.industries.0 }}
                        {% endif %}

                    </span>

                </div>


                <div class="bn-days-left {% if service.pitch_days_remaining <= 7 %}urgent{% endif %}">

                    <strong>
                        {{ service.pitch_days_remaining }}
                    </strong>

                    <span>
                        days
                    </span>

                </div>

            </a>


        {% empty %}

            <div class="bn-panel-empty">

                <strong>
                    Nothing closing soon
                </strong>

                <span>
                    No currently pitchable service closes within 30 days.
                </span>

            </div>

        {% endfor %}


    </div>

</aside>


</div>



<!-- =====================================================
     SECOND ROW
====================================================== -->

<div class="bn-home-secondary-grid">


<!-- RECENT ACTIVITY -->

<section class="bn-dashboard-panel">


    <div class="bn-panel-header">

        <div>

            <h2>
                Recent Toolkit Activity
            </h2>

            <p>
                Your latest searches and client service checks
            </p>

        </div>

    </div>


    <div class="bn-recent-list">


        {% for event in recent_events %}

            <div class="bn-recent-item">


                <time>
                    {{ event.created_at|date:"h:i A" }}
                </time>


                <div class="bn-recent-type">


                    {% if event.event_type == "SEARCH" %}

                        <span class="search">
                            Search
                        </span>


                    {% elif event.event_type == "CLIENT_MATCH" %}

                        <span class="client">
                            Client Service Check
                        </span>


                    {% elif event.event_type == "FILTER" %}

                        <span>
                            Filter
                        </span>

                    {% endif %}

                </div>


                <div class="bn-recent-detail">


                    {% if event.event_type == "SEARCH" %}

                        <strong>

                            {% if event.query %}
                                {{ event.query }}
                            {% else %}
                                Toolkit Search
                            {% endif %}

                        </strong>


                    {% elif event.event_type == "CLIENT_MATCH" %}

                        <strong>
                            Checked suitable services for a client
                        </strong>


                    {% else %}

                        <strong>
                            Toolkit filters applied
                        </strong>

                    {% endif %}


                    <span>

                        {{ event.result_count }}
                        result{{ event.result_count|pluralize }}
                        shown

                    </span>

                </div>


                
                <a
                    href="{% url 'dashboard:search_activity_detail' %}?date={{ event.created_at|date:'Y-m-d' }}"
                    class="bn-row-action"
                    aria-label="View activity details">

                    <span>
                        View
                    </span>

                    <span
                        class="bn-action-chevron"
                        aria-hidden="true">

                        ›

                    </span>

                </a>


            </div>


        {% empty %}

            <div class="bn-panel-empty">

                <strong>
                    No activity yet
                </strong>

                <span>
                    Searches and client service checks will appear here.
                </span>

            </div>

        {% endfor %}


    </div>

</section>



<!-- MOST SEARCHED -->

<section class="bn-dashboard-panel">


    <div class="bn-panel-header">

        <div>

            <h2>
                Most Searched
            </h2>

            <p>
                What you searched for most often
            </p>

        </div>

    </div>


    <div class="bn-top-search-list">


        {% for item in top_queries %}

            <a
                href="{% url 'toolkit:home' %}?q={{ item.query|urlencode }}">

                <div>

                    <span class="bn-search-rank">
                        {{ forloop.counter }}
                    </span>

                    <strong>
                        {{ item.query }}
                    </strong>

                </div>


                <span>
                    {{ item.total }}
                    search{{ item.total|pluralize }}
                </span>

            </a>


        {% empty %}

            <div class="bn-panel-empty">

                <strong>
                    No search trend yet
                </strong>

                <span>
                    Your most frequent Toolkit searches will appear here.
                </span>

            </div>

        {% endfor %}


    </div>

</section>


</div>



<!-- =====================================================
     SEARCH GAPS
====================================================== -->

<section class="bn-dashboard-panel bn-search-gap-panel">


    <div class="bn-panel-header">

        <div>

            <h2>
                Searches With No Result
            </h2>

            <p>
                These searches may indicate missing Toolkit information
            </p>

        </div>


        <span class="bn-gap-total">
            {{ zero_result_count }}
        </span>

    </div>


    {% if zero_queries %}

        <div class="bn-gap-list">


            {% for item in zero_queries %}

                <div class="bn-gap-item">

                    <div>

                        <span>
                            SEARCH
                        </span>

                        <strong>
                            {{ item.query }}
                        </strong>

                    </div>


                    <div>

                        {{ item.total }}
                        time{{ item.total|pluralize }}

                    </div>

                </div>

            {% endfor %}


        </div>


    {% else %}

        <div class="bn-gap-good">

            <span>
                ✓
            </span>

            <div>

                <strong>
                    No search gaps in this period
                </strong>

                <p>
                    Every recorded Toolkit search returned at least one result.
                </p>

            </div>

        </div>

    {% endif %}


</section>


</div>


<script src="{% static 'js/dashboard_home_v2.js' %}"></script>

{% endblock %}

===== FILE: dashboard/templates/dashboard/search_activity_detail.html =====
{% extends "base.html" %}
{% load static %}

{% block title %}
BDE Activity | BharatNXT Wave
{% endblock %}


{% block content %}

<div class="search-console-page">


<!-- =====================================================
     HEADER
====================================================== -->

<header class="search-console-header">

    <div>

        <div class="page-eyebrow">
            DAILY BDE ACTIVITY
        </div>

        <h1>
            BDE Activity
        </h1>

        <p>
            {{ selected_date|date:"l, d M Y" }}
            ·
            See what the BDE did in the Toolkit on this day
        </p>

    </div>


    <a
        href="{% url 'dashboard:home' %}"
        class="secondary-button">

        ← Dashboard

    </a>

</header>



<!-- =====================================================
     DAY NAVIGATION
====================================================== -->

<div class="search-console-daynav">

    <a
        href="?date={{ previous_date|date:'Y-m-d' }}">

        ← {{ previous_date|date:"d M" }}

    </a>


    <div>

        <span>
            SELECTED DAY
        </span>

        <strong>
            {{ selected_date|date:"d M Y" }}
        </strong>

    </div>


    {% if next_date <= today %}

        <a
            href="?date={{ next_date|date:'Y-m-d' }}">

            {{ next_date|date:"d M" }} →

        </a>

    {% else %}

        <span class="disabled">
            Next →
        </span>

    {% endif %}

</div>



<!-- =====================================================
     COMPACT DATA STRIP
====================================================== -->

<div class="search-console-strip">

    <div>

        <strong>
            {{ total_events }}
        </strong>

        <span>
            total actions
        </span>

    </div>


    <div>

        <strong>
            {{ total_searches }}
        </strong>

        <span>
            Toolkit searches
        </span>

    </div>


    <div>

        <strong>
            {{ matcher_count }}
        </strong>

        <span>
            client service checks
        </span>

    </div>


    <div>

        <strong class="{% if zero_result_searches %}danger{% endif %}">
            {{ zero_result_searches }}
        </strong>

        <span>
            searches with no result
        </span>

    </div>


    <div>

        <strong>
            {{ total_results }}
        </strong>

        <span>
            results shown
        </span>

    </div>

</div>



<!-- =====================================================
     DAY TIMELINE
====================================================== -->

<section class="search-console-timeline">

    <div class="search-console-timeline-heading">

        <strong>
            When the Toolkit Was Used
        </strong>

        <span>
            See what time the BDE used the Toolkit during the day
        </span>

    </div>


    <div class="search-time-scale">

        <span>00:00</span>
        <span>06:00</span>
        <span>12:00</span>
        <span>18:00</span>
        <span>24:00</span>

    </div>


    <div class="search-time-line">

        <div class="search-time-grid q1"></div>
        <div class="search-time-grid q2"></div>
        <div class="search-time-grid q3"></div>


        {% for event in events %}

            <button
                type="button"

                class="search-time-event event-{{ event.event_type|lower }}"

                data-left="{{ event.timeline_pct }}"

                title="{{ event.console_time }} · {{ event.get_event_type_display }}">

                <span>
                </span>

            </button>

        {% endfor %}

    </div>


    {% if not events %}

        <div class="search-console-no-events">
            No activity recorded for this date.
        </div>

    {% endif %}

</section>



<!-- =====================================================
     INVESTIGATION BODY
====================================================== -->

<div class="search-console-grid">


<!-- ACTIVITY STREAM -->

<main class="search-event-stream">

    <div class="search-stream-heading">

        <div>

            <strong>
                What the BDE Did
            </strong>

            <span>
                Actions are shown in the order they happened
            </span>

        </div>


        <span class="search-real-data">
            ● REAL DATA
        </span>

    </div>


    {% for event in events %}

        <article class="search-stream-event">


            <div class="search-stream-time">

                {{ event.console_time }}

            </div>


            <div class="search-stream-marker event-{{ event.event_type|lower }}">
            </div>


            <div class="search-stream-content">


                <div class="search-stream-event-head">


                    {% if event.event_type == "SEARCH" %}

                        <span class="search-stream-type search">
                            TOOLKIT SEARCH
                        </span>


                    {% elif event.event_type == "CLIENT_MATCH" %}

                        <span class="search-stream-type matcher">
                            CLIENT SERVICE CHECK
                        </span>


                    {% elif event.event_type == "FILTER" %}

                        <span class="search-stream-type filter">
                            FILTER
                        </span>


                    {% elif event.event_type == "SUGGESTION_CLICK" %}

                        <span class="search-stream-type suggestion">
                            SUGGESTION
                        </span>


                    {% else %}

                        <span class="search-stream-type">
                            {{ event.get_event_type_display }}
                        </span>

                    {% endif %}


                    <strong>
                        {{ event.result_count }}
                        result{{ event.result_count|pluralize }}
                    </strong>

                </div>



                {% if event.event_type == "SEARCH" %}

                    <h3>

                        {% if event.query %}

                            “{{ event.query }}”

                        {% else %}

                            Toolkit search

                        {% endif %}

                    </h3>


                    {% if event.result_count > 0 %}

                        <div class="search-stream-outcome success">

                            Results found successfully

                        </div>

                    {% else %}

                        <div class="search-stream-outcome gap">

                            Search gap — no Toolkit result found

                        </div>

                    {% endif %}



                {% elif event.event_type == "CLIENT_MATCH" %}

                    <h3>
                        Checked services for a client
                    </h3>


                    <div class="search-stream-metrics">


                        {% if event.filters.criteria_count != None %}

                            <span>

                                <strong>
                                    {{ event.filters.criteria_count }}
                                </strong>

                                company details used

                            </span>

                        {% endif %}


                        {% if event.filters.high_count != None %}

                            <span>

                                <strong>
                                    {{ event.filters.high_count }}
                                </strong>

                                good match

                            </span>

                        {% endif %}


                        {% if event.filters.possible_count != None %}

                            <span>

                                <strong>
                                    {{ event.filters.possible_count }}
                                </strong>

                                needs checking

                            </span>

                        {% endif %}


                        {% if event.filters.not_eligible_count != None %}

                            <span>

                                <strong>
                                    {{ event.filters.not_eligible_count }}
                                </strong>

                                not suitable

                            </span>

                        {% endif %}

                    </div>


                    <div class="search-stream-privacy">

                        Client-identifying profile data is not retained
                        in the activity log.

                    </div>



                {% elif event.event_type == "FILTER" %}

                    <h3>
                        Toolkit filters applied
                    </h3>


                    <div class="search-stream-filter-list">

                        {% if event.filters.sector %}

                            <span>
                                Sector · {{ event.filters.sector }}
                            </span>

                        {% endif %}


                        {% if event.filters.service_kind %}

                            <span>
                                Type · {{ event.filters.service_kind }}
                            </span>

                        {% endif %}


                        {% if event.filters.deadline %}

                            <span>
                                Deadline · {{ event.filters.deadline }}
                            </span>

                        {% endif %}

                    </div>


                {% else %}

                    <h3>
                        {{ event.get_event_type_display }}
                    </h3>

                {% endif %}



                {% if event.selected_service %}

                    <div class="search-stream-selected">

                        <span>
                            SERVICE
                        </span>

                        <a
                            href="{% url 'toolkit:service_detail' event.selected_service.slug %}">

                            {{ event.selected_service.title }}
                            →

                        </a>

                    </div>

                {% endif %}


            </div>

        </article>


    {% empty %}

        <div class="search-stream-empty">

            <strong>
                No recorded activity
            </strong>

            <p>
                There are no Toolkit Toolkit searches, client service checks,
                filters or suggestion events on this date.
            </p>

        </div>

    {% endfor %}

</main>



<!-- DAY INTELLIGENCE -->

<aside class="search-day-intelligence">

    <div class="search-intelligence-title">

        <strong>
            Day Summary
        </strong>

        <span>
            Quick summary of today's Toolkit usage
        </span>

    </div>


    <div class="search-intelligence-primary">

        <span>
            TOTAL ACTIONS
        </span>

        <strong>
            {{ total_events }}
        </strong>

    </div>


    <div class="search-intelligence-row">

        <span>
            Searches
        </span>

        <strong>
            {{ total_searches }}
        </strong>

    </div>


    <div class="search-intelligence-row">

        <span>
            Client service checks
        </span>

        <strong>
            {{ matcher_count }}
        </strong>

    </div>


    <div class="search-intelligence-row">

        <span>
            Filter actions
        </span>

        <strong>
            {{ filter_count }}
        </strong>

    </div>


    <div class="search-intelligence-row">

        <span>
            Searches with no result
        </span>

        <strong class="{% if zero_result_searches %}danger{% endif %}">

            {{ zero_result_searches }}

        </strong>

    </div>


    <div class="search-intelligence-section">

        <span>
            BUSIEST TIME
        </span>

        <strong>
            {{ peak_period }}
        </strong>

    </div>


    <div class="search-intelligence-section">

        <span>
            MOST USED TOOL
        </span>

        <strong>
            {{ most_used_feature }}
        </strong>

    </div>


    <div class="search-intelligence-section">

        <span>
            SEARCHES THAT FOUND RESULTS
        </span>


        {% if total_searches %}

            <strong>

                {{ successful_searches }}
                /
                {{ total_searches }}

            </strong>

        {% else %}

            <strong>
                —
            </strong>

        {% endif %}

    </div>


    <div class="search-intelligence-note">

        This view intentionally uses privacy-safe activity
        metadata only.

    </div>

</aside>


</div>


</div>


<script src="{% static 'js/search_activity_console.js' %}"></script>

{% endblock %}

===== GLOBAL TEMPLATES =====

===== FILE: templates/admin_base.html =====
{% load static %}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>{% block title %}BharatNXT Wave Internal{% endblock %}</title>

    <link rel="stylesheet" href="{% static 'css/app.css' %}">
<link rel="stylesheet" href="{% static 'css/ui_stability.css' %}">
</head>

<body>

<div class="app-shell">

    <!-- SIDEBAR -->
    <aside class="sidebar">

        <!-- BRAND -->
        <div class="brand">

            <div class="brand-mark">
                BN
            </div>

            <div class="brand-text">
                <div class="brand-name">
                    BharatNXT Wave
                </div>

                <div class="brand-subtitle">
                    ADMIN CENTER
                </div>
            </div>

        </div>


        <!-- ADMIN CENTER NAVIGATION -->
        <nav class="navigation">

            <div class="nav-section-label">
                ADMIN CENTER
            </div>

            <a href="{% url 'dashboard:admin_overview' %}"
               class="nav-item {% if request.path == '/admin-center/' %}active{% endif %}">
                <span class="nav-icon">⌂</span>
                <span>Overview</span>
            </a>

            {% if request.user.is_superuser or request.user.role == "SUPER_ADMIN" or request.user.role == "IT_ADMIN" or request.user.role == "SECURITY_ADMIN" %}

                <a href="{% url 'dashboard:bde_analytics' %}"
                   class="nav-item {% if '/bde-analytics/' in request.path %}active{% endif %}">

                    <span class="nav-icon">⌁</span>
                    <span>BDE Analytics</span>

                </a>

            {% endif %}



            {% if request.user.is_superuser or request.user.role == "SUPER_ADMIN" or request.user.role == "IT_ADMIN" %}

            <a href="{% url 'accounts:employee_list' %}"
               class="nav-item {% if '/employees/' in request.path %}active{% endif %}">
                <span class="nav-icon">♙</span>
                <span>Employees</span>
            </a>

            {% endif %}


            {% if request.user.is_superuser or request.user.role == "SUPER_ADMIN" or request.user.role == "DATA_ADMIN" %}

            <a href="{% url 'toolkit:admin_service_list' %}"
               class="nav-item {% if '/admin-center/toolkit/' in request.path %}active{% endif %}">
                <span class="nav-icon">◫</span>
                <span>Toolkit Management</span>
            </a>

                {% if request.user.is_superuser or request.user.role == "SUPER_ADMIN" or request.user.role == "DATA_ADMIN" or request.user.role == "IT_ADMIN" %}

                    <a
                        href="{% url 'toolkit:pitch_windows' %}"
                        class="nav-item {% if '/pitch-windows/' in request.path %}active{% endif %}">

                        <span class="nav-icon">◷</span>
                        <span>Pitch Windows</span>

                    </a>

                {% endif %}


            <a href="{% url 'toolkit:import_center' %}"
               class="nav-item {% if '/admin-center/import/' in request.path %}active{% endif %}">
                <span class="nav-icon">⇧</span>
                <span>Import Data</span>
            </a>

                <a
                    href="{% url 'toolkit:import_history' %}"
                    class="nav-item {% if '/import/history/' in request.path %}active{% endif %}">

                    <span class="nav-icon">↻</span>
                    <span>Source History</span>

                </a>


            {% endif %}


            {% if request.user.is_superuser or request.user.role == "SUPER_ADMIN" or request.user.role == "IT_ADMIN" or request.user.role == "SECURITY_ADMIN" %}

            <a href="{% url 'accounts:activity_logs' %}"
               class="nav-item {% if '/activity-logs/' in request.path %}active{% endif %}">
                <span class="nav-icon">◉</span>
                <span>Activity Logs</span>
            </a>

            {% endif %}

        </nav>


        <!-- SIDEBAR BOTTOM -->
        <div class="sidebar-bottom">

            <div class="nav-section-label">
                SYSTEM
            </div>

            {% if request.user.is_superuser %}

            <a href="/admin/" class="nav-item">
                <span class="nav-icon">⚙</span>
                <span>Developer Admin</span>
            </a>

            {% endif %}

        </div>

    
    <!-- SIDEBAR ACCOUNT ACTIONS -->
    <div class="sidebar-account-actions">

        <form
            method="post"
            action="{% url 'accounts:logout' %}"
            class="sidebar-logout-form">

            {% csrf_token %}

            <button
                type="submit"
                class="sidebar-logout-button">

                <span
                    class="sidebar-logout-icon"
                    aria-hidden="true">

                    <svg
                        viewBox="0 0 24 24"
                        fill="none">

                        <path
                            d="M10 5H6.5C5.7 5 5 5.7 5 6.5V17.5C5 18.3 5.7 19 6.5 19H10">
                        </path>

                        <path
                            d="M14 8L18 12L14 16">
                        </path>

                        <path
                            d="M18 12H9">
                        </path>

                    </svg>

                </span>

                <span>
                    Sign Out
                </span>

            </button>

        </form>

    </div>

</aside>


    <!-- MAIN APPLICATION AREA -->
    <main class="main-area">


        <!-- TOP NAVIGATION BAR -->
        <header class="topbar">


            <!-- TOP RIGHT -->
            <div class="topbar-actions">


                <!-- SECURITY STATUS -->
                <div class="security-status">

                    <span class="security-dot"></span>

                    Secure Session

                </div>


                <!-- NOTIFICATIONS -->
                <button
                    class="notification-button"
                    type="button"
                    aria-label="Notifications"
                >
                    <svg
                        viewBox="0 0 24 24"
                        width="18"
                        height="18"
                        fill="none"
                        aria-hidden="true">

                        <path
                            d="M6.5 9.5C6.5 6.3 8.7 4 12 4C15.3 4 17.5 6.3 17.5 9.5V14L19.5 17H4.5L6.5 14V9.5Z"
                            stroke="currentColor"
                            stroke-width="1.7"
                            stroke-linejoin="round">
                        </path>

                        <path
                            d="M10 19C10.5 19.7 11.1 20 12 20C12.9 20 13.5 19.7 14 19"
                            stroke="currentColor"
                            stroke-width="1.7"
                            stroke-linecap="round">
                        </path>

                    </svg>
</button>


                <!-- USER PROFILE -->
                <div class="user-profile">

                    <div class="user-avatar">

                        {{ request.user.username|slice:":1"|upper }}

                    </div>


                    <div class="user-info">

                        <div class="user-name">

                            {{ request.user.username }}

                        </div>

                        <div class="user-role">

                            {{ request.user.get_role_display }}

                        </div>

                    </div>

                </div>

            </div>

        
            

</header>


        <!-- MAIN PAGE CONTENT -->
        
        <!-- =================================================
             GLOBAL HUMAN FEEDBACK
        ================================================== -->

        {% if messages %}

            <div
                class="bn-feedback-stack"
                aria-live="polite"
                aria-atomic="true">

                {% for message in messages %}

                    <div
                        class="bn-feedback-toast {{ message.tags|default:'info' }}"
                        data-feedback-type="{{ message.tags|default:'info' }}"
                        role="status">


                        <div
                            class="bn-feedback-icon"
                            aria-hidden="true">

                            {% if "success" in message.tags %}
                                ✓
                            {% elif "error" in message.tags %}
                                !
                            {% elif "warning" in message.tags %}
                                !
                            {% else %}
                                i
                            {% endif %}

                        </div>


                        <div class="bn-feedback-copy">

                            {% if "success" in message.tags %}

                                <strong>
                                    Done
                                </strong>

                            {% elif "error" in message.tags %}

                                <strong>
                                    Something went wrong
                                </strong>

                            {% elif "warning" in message.tags %}

                                <strong>
                                    Please check
                                </strong>

                            {% else %}

                                <strong>
                                    Update
                                </strong>

                            {% endif %}


                            <span>
                                {{ message }}
                            </span>

                        </div>


                        <button
                            type="button"
                            class="bn-feedback-close"
                            aria-label="Dismiss notification">

                            ×

                        </button>

                    </div>

                {% endfor %}

            </div>

        {% endif %}


<section class="content">

            {% block content %}

            {% endblock %}

        </section>


    </main>

</div>

    <script src="{% static 'js/session_location.js' %}"></script>


    <script src="{% static 'js/ui_feedback.js' %}"></script>

</body>

</html>
===== FILE: templates/base.html =====
{% load static %}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>{% block title %}BharatNXT Wave Internal{% endblock %}</title>

    <link rel="stylesheet" href="{% static 'css/app.css' %}">
<link rel="stylesheet" href="{% static 'css/ui_stability.css' %}">
</head>

<body>

<div class="app-shell">

    <!-- SIDEBAR -->
    <aside class="sidebar">

        <!-- BRAND -->
        <div class="brand">

            <div class="brand-mark">
                BN
            </div>

            <div class="brand-text">
                <div class="brand-name">
                    BharatNXT Wave
                </div>

                <div class="brand-subtitle">
                    INTERNAL
                </div>
            </div>

        </div>


        <!-- MAIN NAVIGATION -->
        <nav class="navigation">

            <div class="nav-section-label">
                WORKSPACE
            </div>

            <a href="{% url 'dashboard:home' %}"
               class="nav-item {% if request.path == '/' %}active{% endif %}">
                <span class="nav-icon">⌂</span>
                <span>Home</span>
            </a>

            <a href="{% url 'toolkit:home' %}"
               class="nav-item {% if request.path == '/toolkit/' or '/toolkit/service/' in request.path %}active{% endif %}">
                <span class="nav-icon">◫</span>
                <span>Business Toolkit</span>
            </a>

            <a href="{% url 'toolkit:saved_services' %}"
               class="nav-item {% if '/toolkit/saved/' in request.path %}active{% endif %}">
                <span class="nav-icon">★</span>
                <span>Saved Services</span>
            </a>

            <a
                href="{% url 'toolkit:client_matcher' %}"
                class="nav-item {% if '/toolkit/matcher/' in request.path %}active{% endif %}">

                
                <span
                    class="nav-icon bn-nav-svg-wrap"
                    aria-hidden="true">

                    <svg
                        class="bn-nav-svg"
                        viewBox="0 0 24 24"
                        fill="none">

                        <circle
                            cx="8"
                            cy="7"
                            r="3">
                        </circle>

                        <path
                            d="M3.5 16.5C4.1 13.8 5.7 12.5 8 12.5C9.2 12.5 10.2 12.8 11 13.4">
                        </path>

                        <circle
                            cx="16.5"
                            cy="15.5"
                            r="4">
                        </circle>

                        <path
                            d="M19.5 18.5L22 21">
                        </path>

                    </svg>

                </span>

                <span>
                    Find Services for Client
                </span>


            </a>


        </nav>

    
    <!-- SIDEBAR ACCOUNT ACTIONS -->
    <div class="sidebar-account-actions">

        <form
            method="post"
            action="{% url 'accounts:logout' %}"
            class="sidebar-logout-form">

            {% csrf_token %}

            <button
                type="submit"
                class="sidebar-logout-button">

                <span
                    class="sidebar-logout-icon"
                    aria-hidden="true">

                    <svg
                        viewBox="0 0 24 24"
                        fill="none">

                        <path
                            d="M10 5H6.5C5.7 5 5 5.7 5 6.5V17.5C5 18.3 5.7 19 6.5 19H10">
                        </path>

                        <path
                            d="M14 8L18 12L14 16">
                        </path>

                        <path
                            d="M18 12H9">
                        </path>

                    </svg>

                </span>

                <span>
                    Sign Out
                </span>

            </button>

        </form>

    </div>

</aside>


    <!-- MAIN APPLICATION AREA -->
    <main class="main-area">


        <!-- TOP NAVIGATION BAR -->
        <header class="topbar">


            <!-- GLOBAL SEARCH -->

            <form method="get"
                  action="{% url 'toolkit:home' %}"
                  class="global-search">

                <span
                    class="search-icon bn-search-icon"
                    aria-hidden="true">

                    <svg
                        viewBox="0 0 24 24"
                        fill="none">

                        <circle
                            cx="11"
                            cy="11"
                            r="6.5">
                        </circle>

                        <path
                            d="M16 16L21 21">
                        </path>

                    </svg>

                </span>

                <input
                    type="search"
                    name="q"
                    placeholder="Search BharatNXT Wave Toolkit..."
                    autocomplete="off"
                >

                <span class="search-shortcut">
                    Enter
                </span>

            </form>

            <!-- TOP RIGHT -->
            <div class="topbar-actions">


                <!-- SECURITY STATUS -->
                <div class="security-status">

                    <span class="security-dot"></span>

                    Secure Session

                </div>


                <!-- NOTIFICATIONS -->
                <button
                    class="notification-button"
                    type="button"
                    aria-label="Notifications"
                >
                    <svg
                        viewBox="0 0 24 24"
                        width="18"
                        height="18"
                        fill="none"
                        aria-hidden="true">

                        <path
                            d="M6.5 9.5C6.5 6.3 8.7 4 12 4C15.3 4 17.5 6.3 17.5 9.5V14L19.5 17H4.5L6.5 14V9.5Z"
                            stroke="currentColor"
                            stroke-width="1.7"
                            stroke-linejoin="round">
                        </path>

                        <path
                            d="M10 19C10.5 19.7 11.1 20 12 20C12.9 20 13.5 19.7 14 19"
                            stroke="currentColor"
                            stroke-width="1.7"
                            stroke-linecap="round">
                        </path>

                    </svg>
</button>


                <!-- USER PROFILE -->
                <div class="user-profile">

                    <div class="user-avatar">

                        {{ request.user.username|slice:":1"|upper }}

                    </div>


                    <div class="user-info">

                        <div class="user-name">

                            {{ request.user.username }}

                        </div>

                        <div class="user-role">

                            {{ request.user.get_role_display }}

                        </div>

                    </div>

                </div>

            </div>

        
            

</header>


        <!-- MAIN PAGE CONTENT -->
        
        <!-- =================================================
             GLOBAL HUMAN FEEDBACK
        ================================================== -->

        {% if messages %}

            <div
                class="bn-feedback-stack"
                aria-live="polite"
                aria-atomic="true">

                {% for message in messages %}

                    <div
                        class="bn-feedback-toast {{ message.tags|default:'info' }}"
                        data-feedback-type="{{ message.tags|default:'info' }}"
                        role="status">


                        <div
                            class="bn-feedback-icon"
                            aria-hidden="true">

                            {% if "success" in message.tags %}
                                ✓
                            {% elif "error" in message.tags %}
                                !
                            {% elif "warning" in message.tags %}
                                !
                            {% else %}
                                i
                            {% endif %}

                        </div>


                        <div class="bn-feedback-copy">

                            {% if "success" in message.tags %}

                                <strong>
                                    Done
                                </strong>

                            {% elif "error" in message.tags %}

                                <strong>
                                    Something went wrong
                                </strong>

                            {% elif "warning" in message.tags %}

                                <strong>
                                    Please check
                                </strong>

                            {% else %}

                                <strong>
                                    Update
                                </strong>

                            {% endif %}


                            <span>
                                {{ message }}
                            </span>

                        </div>


                        <button
                            type="button"
                            class="bn-feedback-close"
                            aria-label="Dismiss notification">

                            ×

                        </button>

                    </div>

                {% endfor %}

            </div>

        {% endif %}


<section class="content">

            {% block content %}

            {% endblock %}

        </section>


    </main>

</div>

    <script src="{% static 'js/session_location.js' %}"></script>


    <script src="{% static 'js/ui_feedback.js' %}"></script>

</body>

</html>
===== TOOLKIT STATIC =====

===== FILE: static/css/app.css =====
:root {
    --bg: #F7F7F5;
    --surface: #FFFFFF;
    --text: #17191C;
    --text-secondary: #62666D;
    --border: #E3E5E8;

    --blue: #1769FF;
    --blue-soft: #EEF3FF;

    --yellow: #F6C945;

    --success: #168A5B;
    --warning: #B7791F;
    --danger: #C83A3A;

    --sidebar-width: 240px;
    --topbar-height: 64px;
}


* {
    box-sizing: border-box;
}


html,
body {
    margin: 0;
    padding: 0;
    min-height: 100%;
}


body {
    font-family:
        Inter,
        -apple-system,
        BlinkMacSystemFont,
        "Segoe UI",
        sans-serif;

    background: var(--bg);
    color: var(--text);
    font-size: 14px;
}


/* APP LAYOUT */

.app-shell {
    min-height: 100vh;
}


/* SIDEBAR */

.sidebar {
    position: fixed;
    top: 0;
    left: 0;
    bottom: 0;

    width: var(--sidebar-width);

    background: var(--surface);
    border-right: 1px solid var(--border);

    display: flex;
    flex-direction: column;

    padding: 20px 14px;
}


/* BRAND */

.brand {
    display: flex;
    align-items: center;
    gap: 12px;

    padding: 4px 8px 24px;
}


.brand-mark {
    width: 38px;
    height: 38px;

    border-radius: 8px;

    display: flex;
    align-items: center;
    justify-content: center;

    background: var(--blue);
    color: white;

    font-size: 13px;
    font-weight: 700;
}


.brand-name {
    font-size: 14px;
    font-weight: 650;
}


.brand-subtitle {
    margin-top: 2px;

    font-size: 10px;
    letter-spacing: 1.4px;

    color: var(--text-secondary);
}


/* NAVIGATION */

.navigation {
    display: flex;
    flex-direction: column;
    gap: 3px;
}


.admin-navigation {
    margin-top: 26px;
}


.nav-section-label {
    padding: 0 12px;
    margin-bottom: 8px;

    font-size: 10px;
    font-weight: 600;
    letter-spacing: 1px;

    color: #92969C;
}


.nav-item {
    height: 38px;

    display: flex;
    align-items: center;
    gap: 11px;

    padding: 0 12px;

    border-radius: 6px;

    text-decoration: none;

    color: var(--text-secondary);

    font-size: 13px;
    font-weight: 500;

    transition: 120ms ease;
}


.nav-item:hover {
    background: #F4F5F6;
    color: var(--text);
}


.nav-item.active {
    background: var(--blue-soft);
    color: var(--blue);
}


.nav-icon {
    width: 18px;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    font-size: 17px;
}


/* SIDEBAR BOTTOM */

.sidebar-bottom {
    margin-top: auto;
}


/* MAIN AREA */

.main-area {
    margin-left: var(--sidebar-width);
    min-height: 100vh;
}


/* TOP BAR */

.topbar {
    position: sticky;
    top: 0;
    z-index: 50;

    height: var(--topbar-height);

    display: flex;
    align-items: center;
    justify-content: space-between;

    gap: 24px;

    padding: 0 28px;

    background: rgba(255, 255, 255, 0.96);

    border-bottom: 1px solid var(--border);
}


/* SEARCH */

.global-search {
    width: min(620px, 55vw);
    height: 40px;

    display: flex;
    align-items: center;

    border: 1px solid var(--border);
    border-radius: 7px;

    background: #FAFAFA;

    transition: 120ms ease;
}


.global-search:focus-within {
    background: white;
    border-color: var(--blue);

    box-shadow:
        0 0 0 3px rgba(23, 105, 255, 0.08);
}


.search-icon {
    margin-left: 13px;

    color: #858A91;

    font-size: 18px;
}


.global-search input {
    flex: 1;

    border: none;
    outline: none;

    background: transparent;

    padding: 0 12px;

    color: var(--text);

    font-size: 13px;
}


.global-search input::placeholder {
    color: #969BA2;
}


.keyboard-shortcut {
    margin-right: 8px;

    padding: 4px 7px;

    border: 1px solid var(--border);
    border-radius: 5px;

    background: white;

    color: #8A8F96;

    font-size: 10px;
}


/* TOP BAR RIGHT */

.topbar-actions {
    display: flex;
    align-items: center;
    gap: 16px;
}


.security-status {
    display: flex;
    align-items: center;
    gap: 7px;

    color: var(--text-secondary);

    font-size: 11px;
}


.security-dot {
    width: 7px;
    height: 7px;

    border-radius: 50%;

    background: var(--success);
}


.notification-button {
    width: 36px;
    height: 36px;

    display: flex;
    align-items: center;
    justify-content: center;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: white;

    cursor: pointer;

    color: var(--text-secondary);

    font-size: 17px;
}


.notification-button:hover {
    background: #F7F7F7;
}


/* USER PROFILE */

.user-profile {
    display: flex;
    align-items: center;
    gap: 9px;
}


.user-avatar {
    width: 34px;
    height: 34px;

    display: flex;
    align-items: center;
    justify-content: center;

    border-radius: 50%;

    background: var(--blue-soft);
    color: var(--blue);

    font-weight: 700;
}


.user-name {
    max-width: 150px;

    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;

    font-size: 12px;
    font-weight: 600;
}


.user-role {
    margin-top: 1px;

    color: var(--text-secondary);

    font-size: 10px;
}


/* MAIN CONTENT */

.content {
    padding: 34px 38px 48px;
}


/* COMMON ELEMENTS */

.page-eyebrow {
    margin-bottom: 7px;

    color: var(--blue);

    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.1px;
}


.page-title {
    margin: 0;

    font-size: 26px;
    line-height: 1.2;

    font-weight: 650;
}


.page-description {
    max-width: 650px;

    margin-top: 8px;

    color: var(--text-secondary);

    line-height: 1.6;
}


/* CARD */

.card {
    background: var(--surface);

    border: 1px solid var(--border);
    border-radius: 8px;
}


/* RESPONSIVE */

@media (max-width: 900px) {

    .sidebar {
        width: 70px;
    }

    .brand-text,
    .nav-item span:last-child,
    .nav-section-label,
    .user-info {
        display: none;
    }

    .brand {
        justify-content: center;
    }

    .nav-item {
        justify-content: center;
    }

    .main-area {
        margin-left: 70px;
    }

    .security-status {
        display: none;
    }

    .global-search {
        width: 100%;
    }
}

/* =========================================================
   EMPLOYEE MANAGEMENT
========================================================= */

.employee-stats {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 16px;
    margin-top: 28px;
    margin-bottom: 22px;
}

.stat-card {
    padding: 20px 22px;
}

.stat-label {
    color: var(--text-secondary);
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.8px;
}

.stat-value {
    margin-top: 10px;
    font-size: 28px;
    font-weight: 650;
    line-height: 1;
}

.employee-table-card {
    overflow: hidden;
}

.table-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    padding: 20px 22px;
    border-bottom: 1px solid var(--border);
}

.table-title {
    font-size: 15px;
    font-weight: 650;
}

.table-subtitle {
    margin-top: 4px;
    color: var(--text-secondary);
    font-size: 12px;
}

.primary-button {
    min-height: 36px;
    padding: 0 14px;
    border: none;
    border-radius: 6px;
    background: var(--blue);
    color: white;
    font-size: 12px;
    font-weight: 600;
}

.primary-button:disabled {
    opacity: 0.45;
    cursor: not-allowed;
}

.table-wrapper {
    width: 100%;
    overflow-x: auto;
}

.employee-table {
    width: 100%;
    border-collapse: collapse;
}

.employee-table th {
    padding: 12px 18px;
    text-align: left;
    background: #FAFAFA;
    border-bottom: 1px solid var(--border);
    color: var(--text-secondary);
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.5px;
    white-space: nowrap;
}

.employee-table td {
    padding: 15px 18px;
    border-bottom: 1px solid var(--border);
    font-size: 12px;
    vertical-align: middle;
}

.employee-table tbody tr:last-child td {
    border-bottom: none;
}

.employee-table tbody tr:hover {
    background: #FAFAFA;
}

.employee-cell {
    display: flex;
    align-items: center;
    gap: 11px;
}

.employee-avatar {
    width: 34px;
    height: 34px;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    background: var(--blue-soft);
    color: var(--blue);
    font-weight: 700;
}

.employee-name {
    font-size: 12px;
    font-weight: 600;
}

.employee-email {
    margin-top: 3px;
    color: var(--text-secondary);
    font-size: 11px;
}

.status-badge {
    display: inline-flex;
    align-items: center;
    padding: 4px 8px;
    border-radius: 999px;
    font-size: 10px;
    font-weight: 650;
}

.status-badge.active {
    background: #EAF7F1;
    color: var(--success);
}

.status-badge.inactive {
    background: #F5EEEE;
    color: var(--danger);
}

@media (max-width: 900px) {
    .employee-stats {
        grid-template-columns: 1fr;
    }
}


/* =========================================================
   ADD EMPLOYEE FORM
========================================================= */

.employee-form-card {
    max-width: 780px;
    margin-top: 28px;
    padding: 26px 28px;
}

.employee-form-card form > p {
    margin: 0 0 20px;
}

.employee-form-card label {
    display: block;
    margin-bottom: 7px;

    color: var(--text);
    font-size: 12px;
    font-weight: 600;
}

.employee-form-card input[type="text"],
.employee-form-card input[type="email"],
.employee-form-card input[type="password"],
.employee-form-card select {
    width: 100%;
    height: 40px;

    padding: 0 11px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;
    color: var(--text);

    font: inherit;

    outline: none;

    transition: 120ms ease;
}

.employee-form-card input:focus,
.employee-form-card select:focus {
    border-color: var(--blue);

    box-shadow:
        0 0 0 3px rgba(23, 105, 255, 0.08);
}

.employee-form-card input[type="checkbox"] {
    width: 16px;
    height: 16px;

    margin-right: 7px;

    vertical-align: middle;
}

.employee-form-card .helptext {
    display: block;

    margin-top: 6px;

    color: var(--text-secondary);

    font-size: 10px;
    line-height: 1.5;
}

.employee-form-card ul {
    margin: 7px 0 0;
    padding-left: 18px;

    color: var(--text-secondary);

    font-size: 10px;
    line-height: 1.6;
}

.employee-form-card .errorlist {
    margin: 0 0 7px;
    padding: 9px 11px;

    border-radius: 6px;

    background: #FFF0F0;
    color: var(--danger);

    list-style: none;
}

.primary-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;

    text-decoration: none;
    cursor: pointer;
}



/* =========================================================
   EMPLOYEE ACTIONS
========================================================= */

.employee-actions {
    display: flex;
    align-items: center;
    gap: 7px;
    white-space: nowrap;
}

.inline-action-form {
    margin: 0;
}

.table-action-button {
    min-height: 30px;
    display: inline-flex;
    align-items: center;
    justify-content: center;

    padding: 0 10px;

    border: 1px solid var(--border);
    border-radius: 5px;

    background: #FFFFFF;
    color: var(--text);

    font: inherit;
    font-size: 11px;
    font-weight: 600;

    text-decoration: none;
    cursor: pointer;
}

.table-action-button:hover {
    background: #F7F7F7;
}

.danger-action {
    color: var(--danger);
}

.success-action {
    color: var(--success);
}

.muted-action {
    color: var(--text-secondary);
    font-size: 11px;
}

.form-actions {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-top: 24px;
}

.secondary-button {
    min-height: 36px;
    display: inline-flex;
    align-items: center;
    justify-content: center;

    padding: 0 14px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;
    color: var(--text);

    font-size: 12px;
    font-weight: 600;

    text-decoration: none;
}

.secondary-button:hover {
    background: #F7F7F7;
}

.employee-form-heading {
    margin-bottom: 3px;
    font-size: 15px;
    font-weight: 650;
}

.employee-form-meta {
    margin-bottom: 24px;
    color: var(--text-secondary);
    font-size: 11px;
}


/* =========================================================
   AUTHENTICATION
========================================================= */

.auth-page {
    min-height: 100vh;
    margin: 0;
    background: var(--bg);
}

.auth-shell {
    min-height: 100vh;
    display: grid;
    grid-template-columns: 380px 1fr;
}

.auth-brand-panel {
    padding: 48px;
    display: flex;
    flex-direction: column;

    background: #17191C;
    color: #FFFFFF;
}

.auth-brand-mark {
    width: 44px;
    height: 44px;

    display: flex;
    align-items: center;
    justify-content: center;

    border-radius: 9px;

    background: var(--blue);
    color: #FFFFFF;

    font-size: 13px;
    font-weight: 750;
}

.auth-brand-name {
    margin-top: 20px;

    font-size: 19px;
    font-weight: 650;
}

.auth-brand-label {
    margin-top: 5px;

    color: #AEB3BB;

    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1px;
}

.auth-brand-copy {
    max-width: 240px;
    margin-top: auto;

    color: #B7BBC2;

    font-size: 12px;
    line-height: 1.6;
}

.auth-form-panel {
    padding: 48px;

    display: flex;
    align-items: center;
    justify-content: center;
}

.auth-form-container {
    width: 100%;
    max-width: 390px;
}

.auth-title {
    margin: 8px 0 0;

    color: var(--text);

    font-size: 30px;
    font-weight: 650;
}

.auth-description {
    margin: 8px 0 28px;

    color: var(--text-secondary);

    font-size: 13px;
}

.auth-form {
    display: flex;
    flex-direction: column;
    gap: 18px;
}

.auth-field label {
    display: block;

    margin-bottom: 7px;

    color: var(--text);

    font-size: 12px;
    font-weight: 600;
}

.auth-field input {
    width: 100%;
    height: 42px;

    padding: 0 12px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;
    color: var(--text);

    font: inherit;

    outline: none;
}

.auth-field input:focus {
    border-color: var(--blue);

    box-shadow:
        0 0 0 3px rgba(23, 105, 255, 0.08);
}

.auth-submit {
    width: 100%;
    margin-top: 4px;
}

.auth-error {
    margin-bottom: 18px;
    padding: 11px 12px;

    border-radius: 6px;

    background: #FFF0F0;
    color: var(--danger);

    font-size: 11px;
    line-height: 1.5;
}

.auth-security-note {
    margin-top: 24px;

    color: var(--text-secondary);

    font-size: 10px;
    line-height: 1.5;
}

@media (max-width: 760px) {

    .auth-shell {
        grid-template-columns: 1fr;
    }

    .auth-brand-panel {
        min-height: 180px;
        padding: 28px;
    }

    .auth-brand-copy {
        display: none;
    }

    .auth-form-panel {
        padding: 32px 24px;
    }
}


/* =========================================================
   LOGOUT
========================================================= */

.topbar-logout-form {
    margin: 0 0 0 10px;
}

.topbar-logout-button {
    min-height: 32px;
    padding: 0 11px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;
    color: var(--text-secondary);

    font: inherit;
    font-size: 11px;
    font-weight: 600;

    cursor: pointer;
}

.topbar-logout-button:hover {
    color: var(--danger);
    background: #FFF7F7;
    border-color: #E8CACA;
}



/* =========================================================
   ACTIVITY LOGS
========================================================= */

.activity-filter-card {
    margin-top: 28px;
    margin-bottom: 18px;
    padding: 16px 18px;
}

.activity-filters {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
}

.activity-search {
    width: min(360px, 100%);
    height: 36px;
    padding: 0 11px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;
    color: var(--text);

    font: inherit;
    font-size: 12px;

    outline: none;
}

.activity-select {
    height: 36px;
    min-width: 180px;

    padding: 0 10px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;
    color: var(--text);

    font: inherit;
    font-size: 12px;
}

.activity-search:focus,
.activity-select:focus {
    border-color: var(--blue);

    box-shadow:
        0 0 0 3px rgba(23, 105, 255, 0.08);
}

.activity-type {
    display: inline-flex;
    align-items: center;

    padding: 4px 8px;

    border-radius: 999px;

    background: var(--blue-soft);
    color: var(--blue);

    font-size: 10px;
    font-weight: 650;

    white-space: nowrap;
}


/* =========================================================
   BUSINESS TOOLKIT
========================================================= */

.toolkit-search-section {
    margin-top: 28px;
}

.toolkit-search-wrapper {
    position: relative;
    max-width: 760px;
}

.toolkit-search-input {
    width: 100%;
    height: 52px;

    padding: 0 18px;

    border: 1px solid var(--border);
    border-radius: 8px;

    background: #FFFFFF;
    color: var(--text);

    font: inherit;
    font-size: 14px;

    outline: none;

    box-shadow:
        0 1px 2px rgba(0, 0, 0, 0.03);
}

.toolkit-search-input:focus {
    border-color: var(--blue);

    box-shadow:
        0 0 0 3px rgba(23, 105, 255, 0.08);
}

.toolkit-suggestions {
    position: absolute;
    z-index: 50;

    top: calc(100% + 6px);
    left: 0;
    right: 0;

    overflow: hidden;

    border: 1px solid var(--border);
    border-radius: 8px;

    background: #FFFFFF;

    box-shadow:
        0 14px 32px rgba(0, 0, 0, 0.10);
}

.toolkit-suggestion {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;

    padding: 13px 15px;

    border-bottom: 1px solid var(--border);

    color: var(--text);
    text-decoration: none;
}

.toolkit-suggestion:last-child {
    border-bottom: none;
}

.toolkit-suggestion:hover {
    background: #F8F9FA;
}

.toolkit-suggestion-title {
    font-size: 12px;
    font-weight: 650;
}

.toolkit-suggestion-path {
    margin-top: 4px;

    color: var(--text-secondary);

    font-size: 10px;
}

.toolkit-suggestion-meta {
    flex-shrink: 0;

    color: var(--text-secondary);

    font-size: 10px;
}

.toolkit-suggestion-empty {
    padding: 15px;

    color: var(--text-secondary);

    font-size: 11px;
}

.toolkit-filter-row {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;

    margin-top: 14px;
}

.toolkit-filter {
    height: 36px;
    min-width: 170px;

    padding: 0 10px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;
    color: var(--text);

    font: inherit;
    font-size: 11px;

    outline: none;
}

.toolkit-filter:focus {
    border-color: var(--blue);
}

.toolkit-result-header {
    display: flex;
    align-items: center;
    justify-content: space-between;

    margin-top: 30px;
    margin-bottom: 14px;
}

.toolkit-grid {
    display: grid;
    grid-template-columns:
        repeat(3, minmax(0, 1fr));

    gap: 14px;
}

.toolkit-service-card {
    min-height: 210px;

    display: flex;
    flex-direction: column;

    padding: 18px;

    border: 1px solid var(--border);
    border-radius: 8px;

    background: #FFFFFF;
    color: var(--text);

    text-decoration: none;

    transition:
        transform 120ms ease,
        border-color 120ms ease,
        box-shadow 120ms ease;
}

.toolkit-service-card:hover {
    transform: translateY(-1px);

    border-color: #CDD2D9;

    box-shadow:
        0 8px 20px rgba(0, 0, 0, 0.05);
}

.toolkit-card-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
}

.toolkit-service-id {
    color: var(--text-secondary);

    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.6px;
}

.toolkit-service-title {
    margin: 17px 0 0;

    font-size: 16px;
    font-weight: 650;
    line-height: 1.3;
}

.toolkit-service-path {
    margin-top: 8px;

    color: var(--blue);

    font-size: 10px;
    line-height: 1.4;
}

.toolkit-service-path span {
    margin: 0 4px;
    color: var(--text-secondary);
}

.toolkit-service-summary {
    margin: 13px 0 0;

    color: var(--text-secondary);

    font-size: 11px;
    line-height: 1.55;
}

.toolkit-card-footer {
    display: flex;
    justify-content: space-between;
    gap: 10px;

    margin-top: auto;
    padding-top: 18px;

    color: var(--text-secondary);

    font-size: 9px;
}

.toolkit-empty-state {
    grid-column: 1 / -1;
    padding: 40px;
    text-align: center;
}

.toolkit-empty-title {
    font-size: 14px;
    font-weight: 650;
}

.toolkit-empty-copy {
    margin-top: 6px;

    color: var(--text-secondary);

    font-size: 11px;
}


/* =========================================================
   SERVICE DETAIL
========================================================= */

.service-detail-header {
    margin-bottom: 26px;
}

.service-back-link {
    display: inline-block;

    margin-bottom: 22px;

    color: var(--text-secondary);

    font-size: 11px;
    font-weight: 600;

    text-decoration: none;
}

.service-back-link:hover {
    color: var(--blue);
}

.service-title-row {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 24px;
}

.service-breadcrumb {
    margin-top: 8px;

    color: var(--text-secondary);

    font-size: 11px;
}

.service-breadcrumb span {
    margin: 0 5px;
}

.service-call-card {
    margin-bottom: 16px;
    padding: 22px 24px;

    border-left: 3px solid var(--blue);
}

.service-section-label {
    margin-bottom: 11px;

    color: var(--text-secondary);

    font-size: 9px;
    font-weight: 750;
    letter-spacing: 0.8px;
}

.service-call-summary {
    max-width: 900px;

    font-size: 15px;
    font-weight: 500;
    line-height: 1.65;
}

.service-detail-grid {
    display: grid;
    grid-template-columns:
        minmax(0, 1fr) 300px;

    gap: 16px;
    align-items: start;
}

.service-main-column,
.service-side-column {
    display: flex;
    flex-direction: column;
    gap: 16px;
}

.service-section,
.service-facts-card {
    padding: 22px 24px;
}

.service-copy {
    color: var(--text);
    font-size: 12px;
    line-height: 1.7;
}

.service-copy p {
    margin-top: 0;
}

.service-info-grid {
    display: grid;
    grid-template-columns:
        repeat(2, minmax(0, 1fr));

    gap: 12px;

    margin-top: 18px;
}

.service-info-item {
    padding: 12px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FAFAFA;
}

.service-info-item span,
.service-fact span {
    display: block;

    color: var(--text-secondary);

    font-size: 9px;
    font-weight: 600;
}

.service-info-item strong {
    display: block;

    margin-top: 5px;

    font-size: 11px;
    line-height: 1.5;
}

.service-fact {
    padding: 11px 0;

    border-bottom: 1px solid var(--border);
}

.service-fact:last-child {
    padding-bottom: 0;
    border-bottom: none;
}

.service-fact strong {
    display: block;

    margin-top: 4px;

    font-size: 11px;
    line-height: 1.4;
}


@media (max-width: 1100px) {

    .toolkit-grid {
        grid-template-columns:
            repeat(2, minmax(0, 1fr));
    }

}


@media (max-width: 850px) {

    .toolkit-grid {
        grid-template-columns: 1fr;
    }

    .service-detail-grid {
        grid-template-columns: 1fr;
    }

    .service-info-grid {
        grid-template-columns: 1fr;
    }

}



/* =========================================================
   TOOLKIT ADMIN MANAGEMENT
========================================================= */

.toolkit-admin-form {
    max-width: 980px;

    display: flex;
    flex-direction: column;
    gap: 16px;

    margin-top: 28px;
}

.toolkit-form-section {
    padding: 22px 24px;
}

.toolkit-form-section label {
    display: block;

    margin-top: 15px;
    margin-bottom: 6px;

    color: var(--text);

    font-size: 11px;
    font-weight: 600;
}

.toolkit-form-section
.service-section-label + label {
    margin-top: 0;
}

.toolkit-form-section input,
.toolkit-form-section select,
.toolkit-form-section textarea {
    width: 100%;

    padding: 9px 10px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;
    color: var(--text);

    font: inherit;
    font-size: 12px;

    outline: none;
}

.toolkit-form-section input,
.toolkit-form-section select {
    min-height: 38px;
}

.toolkit-form-section textarea {
    resize: vertical;
    line-height: 1.55;
}

.toolkit-form-section input:focus,
.toolkit-form-section select:focus,
.toolkit-form-section textarea:focus {
    border-color: var(--blue);

    box-shadow:
        0 0 0 3px rgba(23, 105, 255, 0.08);
}

.admin-form-grid {
    display: grid;
    grid-template-columns:
        repeat(2, minmax(0, 1fr));

    gap: 4px 18px;
}

.toolkit-form-section .errorlist {
    margin: 5px 0 0;
    padding: 0;

    color: var(--danger);

    font-size: 10px;

    list-style: none;
}

@media (max-width: 800px) {

    .admin-form-grid {
        grid-template-columns: 1fr;
    }

}



/* =========================================================
   IMPORT CENTER
========================================================= */

.import-layout {
    display: grid;
    grid-template-columns:
        minmax(0, 1.4fr)
        minmax(260px, 0.8fr);

    gap: 16px;

    margin-top: 28px;
}

.import-upload-card,
.import-google-card {
    padding: 22px 24px;
}

.import-section-title {
    margin: 0;

    font-size: 15px;
    font-weight: 650;
}

.import-section-copy {
    max-width: 680px;

    margin: 7px 0 0;

    color: var(--text-secondary);

    font-size: 11px;
    line-height: 1.6;
}

.import-form {
    display: flex;
    align-items: flex-end;
    gap: 12px;
    flex-wrap: wrap;

    margin-top: 22px;
}

.import-file-field {
    min-width: 340px;
    flex: 1;
}

.import-file-field label {
    display: block;

    margin-bottom: 7px;

    font-size: 11px;
    font-weight: 600;
}

.import-file-field input[type="file"] {
    width: 100%;

    padding: 9px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;

    font-size: 11px;
}

.import-file-field .helptext {
    margin-top: 6px;

    color: var(--text-secondary);

    font-size: 9px;
}

.import-file-field .errorlist {
    margin: 6px 0 0;
    padding: 0;

    color: var(--danger);

    font-size: 10px;

    list-style: none;
}

.import-status-note {
    display: inline-flex;

    margin-top: 18px;
    padding: 6px 9px;

    border-radius: 999px;

    background: #F4F4F2;
    color: var(--text-secondary);

    font-size: 9px;
    font-weight: 650;
}

.import-preview-heading {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;

    margin-top: 30px;
    margin-bottom: 12px;
}

.import-sheet-card {
    overflow: hidden;

    margin-bottom: 14px;
}

.import-preview-table {
    min-width: 900px;
}

.import-preview-table td {
    max-width: 260px;

    overflow: hidden;

    text-overflow: ellipsis;
    white-space: nowrap;
}

.import-empty-sheet {
    padding: 30px;

    color: var(--text-secondary);

    font-size: 11px;
}

.import-mapping-blocked {
    margin-top: 16px;
    padding: 22px 24px;
}

@media (max-width: 900px) {

    .import-layout {
        grid-template-columns: 1fr;
    }

    .import-file-field {
        min-width: 100%;
    }

}



/* =========================================================
   ADMIN OVERVIEW
========================================================= */

.admin-overview-grid {
    display: grid;
    grid-template-columns:
        repeat(3, minmax(0, 1fr));

    gap: 16px;

    margin-top: 28px;
    margin-bottom: 18px;
}

.overview-stat-meta {
    margin-top: 9px;

    color: var(--text-secondary);

    font-size: 10px;
}

.overview-activity-card {
    overflow: hidden;
}

@media (max-width: 900px) {

    .admin-overview-grid {
        grid-template-columns: 1fr;
    }

}



/* =========================================================
   BDE HOME
========================================================= */

.bde-home-search-card {
    margin-top: 28px;
    padding: 26px;

    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 28px;

    border: 1px solid var(--border);
    border-radius: 8px;

    background: #FFFFFF;
}

.bde-home-search-label {
    color: var(--blue);

    font-size: 9px;
    font-weight: 750;
    letter-spacing: 0.8px;
}

.bde-home-search-title {
    margin-top: 7px;

    font-size: 19px;
    font-weight: 650;
}

.bde-home-search-copy {
    max-width: 650px;

    margin-top: 7px;

    color: var(--text-secondary);

    font-size: 11px;
    line-height: 1.6;
}

.bde-home-stats {
    display: grid;
    grid-template-columns:
        repeat(2, minmax(0, 1fr));

    gap: 16px;

    margin-top: 16px;
    margin-bottom: 16px;
}

.bde-recent-card {
    overflow: hidden;
}

.bde-recent-list {
    display: flex;
    flex-direction: column;
}

.bde-recent-item {
    padding: 14px 20px;

    border-bottom: 1px solid var(--border);
}

.bde-recent-item:last-child {
    border-bottom: none;
}

.bde-recent-empty {
    padding: 24px 20px;

    color: var(--text-secondary);

    font-size: 11px;
}

@media (max-width: 760px) {

    .bde-home-search-card {
        align-items: flex-start;
        flex-direction: column;
    }

    .bde-home-stats {
        grid-template-columns: 1fr;
    }

}



/* =========================================================
   BDE PRIMARY SEARCH
========================================================= */

.bde-primary-search {
    max-width: 900px;
    margin-top: 30px;
    margin-bottom: 22px;
}

.bde-primary-search .toolkit-search-wrapper {
    max-width: 900px;
}

.bde-primary-search .toolkit-search-input {
    height: 58px;
    padding-left: 18px;

    font-size: 15px;
}

.bde-search-hint {
    margin-top: 8px;

    color: var(--text-secondary);

    font-size: 10px;
}


/* =========================================================
   BDE SEARCH INTELLIGENCE DASHBOARD
========================================================= */

.bde-dashboard-search {
    margin-top: 26px;
    padding: 24px 26px;

    border: 1px solid var(--border);
    border-radius: 9px;

    background: #FFFFFF;
}

.bde-search-heading {
    font-size: 17px;
    font-weight: 650;
}

.bde-search-subheading {
    margin-top: 5px;

    color: var(--text-secondary);

    font-size: 11px;
}

.bde-dashboard-search .toolkit-search-wrapper {
    max-width: 900px;

    margin-top: 18px;
}

.bde-dashboard-search-input {
    height: 56px;

    font-size: 14px;
}

.bde-search-helper {
    margin-top: 8px;

    color: var(--text-secondary);

    font-size: 9px;
}


.search-kpi-grid {
    display: grid;

    grid-template-columns:
        repeat(4, minmax(0, 1fr));

    gap: 14px;

    margin-top: 16px;
}

.search-kpi-card {
    padding: 18px 20px;
}

.search-kpi-value {
    margin-top: 8px;

    font-size: 26px;
    font-weight: 650;
}

.search-kpi-meta {
    margin-top: 7px;

    color: var(--text-secondary);

    font-size: 9px;
}


.search-dashboard-main-grid {
    display: grid;

    grid-template-columns:
        minmax(0, 1.7fr)
        minmax(260px, 0.8fr);

    gap: 14px;

    margin-top: 14px;
}


.search-activity-card,
.top-search-card,
.dashboard-list-card {
    overflow: hidden;
}


.dashboard-card-heading {
    min-height: 62px;

    display: flex;
    align-items: center;
    justify-content: space-between;

    padding: 16px 18px;

    border-bottom: 1px solid var(--border);
}


.search-bar-chart {
    height: 230px;

    display: flex;
    align-items: flex-end;
    gap: 14px;

    padding: 24px 20px 18px;
}

.search-bar-column {
    height: 100%;

    flex: 1;

    display: flex;
    flex-direction: column;
    align-items: center;
}

.search-bar-value {
    height: 20px;

    color: var(--text-secondary);

    font-size: 9px;
}

.search-bar-track {
    position: relative;

    width: 100%;
    max-width: 42px;
    flex: 1;

    display: flex;
    align-items: flex-end;

    border-radius: 5px 5px 2px 2px;

    background: #F1F3F5;

    overflow: hidden;
}

.search-bar-fill {
    width: 100%;

    min-height: 4px;

    border-radius: 5px 5px 0 0;

    background: var(--blue);
}

.search-bar-day {
    margin-top: 8px;

    font-size: 9px;
    font-weight: 650;
}

.search-bar-date {
    margin-top: 3px;

    color: var(--text-secondary);

    font-size: 8px;
}


.top-query-list,
.dashboard-activity-list {
    display: flex;
    flex-direction: column;
}

.top-query-item,
.dashboard-activity-item {
    min-height: 54px;

    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;

    padding: 11px 16px;

    border-bottom: 1px solid var(--border);

    color: var(--text);

    text-decoration: none;
}

.top-query-item:last-child,
.dashboard-activity-item:last-child {
    border-bottom: none;
}

.top-query-item:hover,
.dashboard-activity-item:hover {
    background: #FAFAFA;
}

.top-query-name,
.dashboard-activity-title {
    font-size: 11px;
    font-weight: 600;
}

.top-query-count {
    min-width: 26px;
    height: 24px;

    display: flex;
    align-items: center;
    justify-content: center;

    border-radius: 999px;

    background: var(--blue-soft);
    color: var(--blue);

    font-size: 9px;
    font-weight: 700;
}

.dashboard-activity-time {
    margin-top: 4px;

    color: var(--text-secondary);

    font-size: 9px;
}

.dashboard-open-arrow {
    color: var(--text-secondary);

    font-size: 13px;
}

.dashboard-empty {
    padding: 26px 18px;

    color: var(--text-secondary);

    font-size: 10px;
    line-height: 1.5;
}


.search-dashboard-secondary-grid {
    display: grid;

    grid-template-columns:
        repeat(2, minmax(0, 1fr));

    gap: 14px;

    margin-top: 14px;
}


.domain-explorer-section {
    margin-top: 24px;
}

.domain-explorer-heading {
    display: flex;
    align-items: center;
    justify-content: space-between;

    margin-bottom: 12px;
}

.domain-explorer-grid {
    display: grid;

    grid-template-columns:
        repeat(4, minmax(0, 1fr));

    gap: 12px;
}

.domain-explorer-card {
    min-height: 100px;

    display: flex;
    flex-direction: column;
    justify-content: space-between;

    padding: 16px;

    border: 1px solid var(--border);
    border-radius: 7px;

    background: #FFFFFF;
    color: var(--text);

    text-decoration: none;
}

.domain-explorer-card:hover {
    border-color: #C8CDD4;

    box-shadow:
        0 5px 14px rgba(0, 0, 0, 0.04);
}

.domain-explorer-name {
    font-size: 11px;
    font-weight: 650;
    line-height: 1.4;
}

.domain-explorer-footer {
    display: flex;
    justify-content: space-between;

    margin-top: 18px;

    color: var(--text-secondary);

    font-size: 9px;
}


@media (max-width: 1100px) {

    .search-kpi-grid {
        grid-template-columns:
            repeat(2, minmax(0, 1fr));
    }

    .domain-explorer-grid {
        grid-template-columns:
            repeat(2, minmax(0, 1fr));
    }

}


@media (max-width: 800px) {

    .search-dashboard-main-grid,
    .search-dashboard-secondary-grid {
        grid-template-columns: 1fr;
    }

}


@media (max-width: 600px) {

    .search-kpi-grid,
    .domain-explorer-grid {
        grid-template-columns: 1fr;
    }

}


/* =========================================================
   SAVED SERVICES
========================================================= */

.saved-services-grid {
    display: grid;
    grid-template-columns:
        repeat(3, minmax(0, 1fr));

    gap: 14px;

    margin-top: 28px;
}

.saved-service-card {
    min-height: 210px;
}

.saved-service-link {
    display: block;

    color: var(--text);
    text-decoration: none;
}

.save-service-form {
    margin: 0;
}

.save-service-button {
    min-height: 32px;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    padding: 0 10px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;
    color: var(--text-secondary);

    font: inherit;
    font-size: 11px;
    font-weight: 650;

    cursor: pointer;
}

.save-service-button:hover {
    border-color: #D3B329;

    color: #A37B00;

    background: #FFFBE8;
}

.save-service-button.saved {
    border-color: #E7CB59;

    background: #FFF8D6;
    color: #8A6900;
}

.service-header-actions {
    display: flex;
    align-items: center;
    gap: 10px;
}

.saved-empty-button {
    margin-top: 18px;
}

@media (max-width: 1000px) {

    .saved-services-grid {
        grid-template-columns:
            repeat(2, minmax(0, 1fr));
    }

}

@media (max-width: 650px) {

    .saved-services-grid {
        grid-template-columns: 1fr;
    }

}


/* =========================================================
   ANALYTICS CALENDAR
========================================================= */

.dashboard-title-row {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 24px;
}

.dashboard-date-layout {
    display: grid;
    grid-template-columns:
        minmax(0, 1.6fr)
        minmax(270px, 0.65fr);

    gap: 14px;

    margin-top: 16px;
}

.analytics-calendar-card {
    overflow: hidden;
}

.calendar-header {
    min-height: 68px;

    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;

    padding: 16px 18px;

    border-bottom: 1px solid var(--border);
}

.calendar-month-controls {
    display: flex;
    align-items: center;
    gap: 12px;

    font-size: 12px;
}

.calendar-arrow {
    width: 30px;
    height: 30px;

    display: flex;
    align-items: center;
    justify-content: center;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;
    color: var(--text);

    font-size: 18px;

    text-decoration: none;
}

.calendar-arrow:hover {
    background: #F7F7F7;
}

.calendar-presets {
    display: flex;
    gap: 7px;

    padding: 12px 18px;

    border-bottom: 1px solid var(--border);
}

.calendar-preset {
    min-height: 30px;

    display: inline-flex;
    align-items: center;

    padding: 0 10px;

    border: 1px solid var(--border);
    border-radius: 999px;

    background: #FFFFFF;
    color: var(--text-secondary);

    font-size: 9px;
    font-weight: 650;

    text-decoration: none;
}

.calendar-preset:hover,
.calendar-preset.active {
    border-color: #BFD1FF;

    background: var(--blue-soft);
    color: var(--blue);
}

.calendar-weekdays,
.analytics-calendar {
    display: grid;
    grid-template-columns:
        repeat(7, minmax(0, 1fr));
}

.calendar-weekdays {
    padding: 0 12px;

    border-bottom: 1px solid var(--border);

    background: #FAFAFA;
}

.calendar-weekdays span {
    padding: 9px 5px;

    color: var(--text-secondary);

    font-size: 8px;
    font-weight: 700;

    text-align: center;
}

.analytics-calendar {
    padding: 10px 12px;
}

.calendar-day {
    position: relative;

    min-height: 74px;

    display: flex;
    flex-direction: column;
    align-items: flex-start;

    padding: 8px;

    border: 1px solid transparent;
    border-radius: 6px;

    background: transparent;
    color: var(--text);

    font: inherit;

    cursor: pointer;
}

.calendar-day:hover {
    border-color: var(--border);

    background: #FAFAFA;
}

.calendar-day.outside {
    opacity: 0.32;
}

.calendar-day.today {
    border-color: #BFD1FF;
}

.calendar-day.selected,
.calendar-day.range-preview {
    background: #EDF3FF;
}

.calendar-day.today
.calendar-day-number {
    width: 25px;
    height: 25px;

    display: flex;
    align-items: center;
    justify-content: center;

    border-radius: 50%;

    background: var(--blue);
    color: #FFFFFF;
}

.calendar-day-number {
    font-size: 10px;
    font-weight: 650;
}

.calendar-search-count {
    margin-top: auto;

    padding: 3px 5px;

    border-radius: 4px;

    background: #EEF3FF;
    color: var(--blue);

    font-size: 7px;
    font-weight: 700;

    white-space: nowrap;
}

.calendar-selection-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;

    padding: 14px 18px;

    border-top: 1px solid var(--border);
}

.calendar-selection-footer strong {
    display: block;

    margin-top: 4px;

    font-size: 11px;
}

.calendar-selection-label {
    color: var(--text-secondary);

    font-size: 8px;
    font-weight: 700;
    letter-spacing: 0.6px;
}


.calendar-kpi-panel {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.date-range-summary {
    padding: 18px;
}

.date-range-title {
    margin-top: 8px;

    font-size: 13px;
    font-weight: 650;
}

.calendar-mini-kpis {
    display: grid;
    grid-template-columns:
        repeat(2, minmax(0, 1fr));

    gap: 12px;
}

.dashboard-chart-large {
    margin-top: 14px;
}

.live-data-badge {
    color: var(--success);

    font-size: 8px;
    font-weight: 750;
    letter-spacing: 0.6px;
}

.search-bar-column {
    color: var(--text);

    text-decoration: none;
}

.search-bar-column:hover
.search-bar-fill {
    opacity: 0.75;
}

.dashboard-summary-card {
    overflow: hidden;
}

.dashboard-summary-content {
    padding: 6px 18px;
}

.summary-metric {
    display: flex;
    align-items: center;
    justify-content: space-between;

    padding: 15px 0;

    border-bottom: 1px solid var(--border);
}

.summary-metric:last-child {
    border-bottom: none;
}

.summary-metric span {
    color: var(--text-secondary);

    font-size: 10px;
}

.summary-metric strong {
    font-size: 18px;
}


@media (max-width: 1050px) {

    .dashboard-date-layout {
        grid-template-columns: 1fr;
    }

}


@media (max-width: 700px) {

    .analytics-calendar {
        overflow-x: auto;
    }

    .calendar-day {
        min-height: 60px;
        padding: 5px;
    }

    .calendar-search-count {
        display: none;
    }

    .calendar-mini-kpis {
        grid-template-columns: 1fr;
    }

    .calendar-selection-footer {
        align-items: flex-start;
        flex-direction: column;
    }

}


/* =========================================================
   ADMIN BDE ANALYTICS
========================================================= */

.admin-no-bde {
    margin-top: 28px;
    padding: 28px;
}

.analytics-control-bar {
    display: flex;
    align-items: end;
    justify-content: space-between;

    margin-top: 24px;
    padding: 16px 18px;
}

.analytics-employee-form select {
    min-width: 280px;
    height: 38px;

    margin-top: 6px;
    padding: 0 10px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;

    font: inherit;
    font-size: 11px;
}

.analytics-control-label {
    display: block;

    color: var(--text-secondary);

    font-size: 8px;
    font-weight: 700;
    letter-spacing: .7px;
}

.selected-bde-status {
    display: flex;
    align-items: center;
    gap: 7px;

    color: var(--text-secondary);

    font-size: 9px;
}

.status-dot {
    width: 7px;
    height: 7px;

    border-radius: 50%;

    background: #B8BDC5;
}

.status-dot.active {
    background: var(--success);
}

.admin-analytics-kpi-grid {
    display: grid;
    grid-template-columns:
        repeat(6, minmax(0, 1fr));

    gap: 12px;

    margin-top: 14px;
}

.admin-calendar-section {
    margin-top: 14px;

    overflow: hidden;
}

.analytics-chart-legend {
    display: flex;
    gap: 16px;

    color: var(--text-secondary);

    font-size: 9px;
}

.analytics-chart-legend span {
    display: flex;
    align-items: center;
    gap: 5px;
}

.analytics-chart-legend i {
    width: 8px;
    height: 8px;

    border-radius: 2px;
}

.legend-search,
.search-series {
    background: var(--blue);
}

.legend-view,
.view-series {
    background: var(--yellow);
}

.dual-bar-chart {
    height: 260px;

    display: flex;
    align-items: flex-end;
    gap: 8px;

    padding: 26px 20px 18px;

    overflow-x: auto;
}

.dual-bar-column {
    min-width: 44px;
    height: 100%;

    flex: 1;

    display: flex;
    flex-direction: column;
    align-items: center;

    color: var(--text);
    text-decoration: none;
}

.dual-values {
    min-height: 20px;

    color: var(--text-secondary);

    font-size: 8px;
}

.dual-bars {
    width: 100%;
    max-width: 45px;
    flex: 1;

    display: flex;
    align-items: flex-end;
    gap: 3px;
}

.dual-bar-track {
    height: 100%;

    flex: 1;

    display: flex;
    align-items: flex-end;

    border-radius: 4px 4px 0 0;

    background: #F1F3F5;

    overflow: hidden;
}

.dual-bar {
    width: 100%;

    min-height: 3px;

    border-radius: 4px 4px 0 0;
}

.dual-bar-column:hover {
    opacity: .75;
}

.admin-analytics-grid {
    display: grid;
    grid-template-columns:
        repeat(3, minmax(0, 1fr));

    gap: 14px;

    margin-top: 14px;
}

.zero-query .top-query-count {
    background: #FFF0F0;
    color: var(--danger);
}

.admin-search-log {
    margin-top: 14px;

    overflow: hidden;
}

.admin-analytics-table-wrap {
    overflow-x: auto;
}

.admin-analytics-table {
    width: 100%;

    border-collapse: collapse;
}

.admin-analytics-table th,
.admin-analytics-table td {
    padding: 11px 16px;

    border-bottom: 1px solid var(--border);

    font-size: 9px;

    text-align: left;
}

.admin-analytics-table th {
    color: var(--text-secondary);

    background: #FAFAFA;

    font-size: 8px;
    font-weight: 700;
    letter-spacing: .4px;
}

.admin-analytics-table tbody tr:hover {
    background: #FAFAFA;
}


@media (max-width: 1200px) {

    .admin-analytics-kpi-grid {
        grid-template-columns:
            repeat(3, minmax(0, 1fr));
    }

}


@media (max-width: 900px) {

    .admin-analytics-grid {
        grid-template-columns: 1fr;
    }

}


@media (max-width: 650px) {

    .admin-analytics-kpi-grid {
        grid-template-columns:
            repeat(2, minmax(0, 1fr));
    }

    .analytics-control-bar {
        align-items: flex-start;
        flex-direction: column;

        gap: 14px;
    }

}


/* =========================================================
   IMPORT INTELLIGENCE / SOURCE HISTORY
========================================================= */

.import-history-shortcut {
    display: flex;
    justify-content: flex-end;

    margin-bottom: 14px;
}

.import-current-event {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 18px;

    margin: 14px 0;
    padding: 14px 16px;
}

.import-current-event strong {
    display: block;

    margin-top: 5px;

    font-size: 12px;
}

.import-history-kpis {
    display: grid;

    grid-template-columns:
        repeat(3, minmax(0, 1fr));

    gap: 14px;

    margin-top: 24px;
}

.import-history-filter-card {
    margin-top: 14px;
    padding: 15px 18px;
}

.import-history-filters {
    display: grid;

    grid-template-columns:
        minmax(220px, 1fr)
        180px
        180px
        auto;

    align-items: end;

    gap: 12px;
}

.import-history-filters label {
    display: block;

    margin-bottom: 6px;

    color: var(--text-secondary);

    font-size: 8px;
    font-weight: 700;
    letter-spacing: .5px;
}

.import-history-filters input,
.import-history-filters select {
    width: 100%;
    height: 38px;

    padding: 0 10px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;

    font: inherit;
    font-size: 10px;
}

.import-history-table-card {
    margin-top: 14px;

    overflow: hidden;
}

.import-source-name {
    max-width: 230px;

    overflow: hidden;

    font-weight: 650;

    text-overflow: ellipsis;
    white-space: nowrap;
}

.import-source-type,
.muted-text {
    margin-top: 3px;

    color: var(--text-secondary);

    font-size: 8px;
}

.import-version-badge,
.import-status-badge {
    display: inline-flex;
    align-items: center;

    min-height: 23px;

    padding: 0 7px;

    border-radius: 999px;

    font-size: 8px;
    font-weight: 700;
}

.import-version-badge.first {
    background: #EEF7F2;
    color: var(--success);
}

.import-version-badge.changed {
    background: #FFF7E5;
    color: var(--warning);
}

.import-version-badge.duplicate {
    background: #F0F2F5;
    color: var(--text-secondary);
}

.import-status-badge {
    background: var(--blue-soft);
    color: var(--blue);
}

.table-link {
    color: var(--blue);

    font-size: 9px;
    font-weight: 650;

    text-decoration: none;
}

.table-link:hover {
    text-decoration: underline;
}


.import-detail-grid {
    display: grid;

    grid-template-columns:
        repeat(2, minmax(0, 1fr));

    gap: 14px;

    margin-top: 24px;
}

.import-detail-card {
    overflow: hidden;
}

.import-detail-list {
    padding: 4px 18px;
}

.import-detail-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;

    padding: 14px 0;

    border-bottom: 1px solid var(--border);
}

.import-detail-row:last-child {
    border-bottom: none;
}

.import-detail-row span {
    color: var(--text-secondary);

    font-size: 9px;
}

.import-detail-row strong {
    max-width: 65%;

    overflow-wrap: anywhere;

    font-size: 10px;
    font-weight: 650;

    text-align: right;
}

.import-integrity-card,
.import-sheet-card {
    margin-top: 14px;

    overflow: hidden;
}

.import-hash-block {
    margin: 18px;
    padding: 14px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FAFAFA;
}

.import-hash-block span {
    display: block;

    margin-bottom: 7px;

    color: var(--text-secondary);

    font-size: 8px;
    font-weight: 700;
}

.import-hash-block code {
    display: block;

    overflow-wrap: anywhere;

    font-size: 10px;
}

.import-version-summary {
    padding: 0 18px 18px;
}

.import-version-message {
    display: flex;
    flex-direction: column;
    gap: 5px;

    padding: 14px;

    border: 1px solid var(--border);
    border-radius: 6px;
}

.import-version-message strong {
    font-size: 11px;
}

.import-version-message span {
    color: var(--text-secondary);

    font-size: 9px;
}

.import-version-message a {
    margin-top: 4px;

    color: var(--blue);

    font-size: 9px;
    font-weight: 650;

    text-decoration: none;
}

.import-version-message.duplicate {
    background: #FAFAFA;
}

.import-version-message.changed {
    background: #FFFCF3;
}

.import-version-message.first {
    background: #F6FBF8;
}


@media (max-width: 1000px) {

    .import-history-filters {
        grid-template-columns:
            repeat(2, minmax(0, 1fr));
    }

}


@media (max-width: 750px) {

    .import-history-kpis,
    .import-detail-grid {
        grid-template-columns: 1fr;
    }

}


@media (max-width: 600px) {

    .import-history-filters {
        grid-template-columns: 1fr;
    }

    .import-current-event {
        align-items: flex-start;
        flex-direction: column;
    }

}


/* =========================================================
   CLIENT COMPANY → SERVICE MATCHER
========================================================= */

.matcher-title-row {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 24px;
}

.matcher-privacy-badge {
    padding: 8px 11px;

    border: 1px solid #DDE8E2;
    border-radius: 999px;

    background: #F6FBF8;
    color: var(--success);

    font-size: 9px;
    font-weight: 650;

    white-space: nowrap;
}

.matcher-form-card {
    margin-top: 24px;
    padding: 24px;
}

.matcher-need-field label,
.matcher-field label {
    display: block;

    margin-bottom: 7px;

    color: var(--text-secondary);

    font-size: 8px;
    font-weight: 750;
    letter-spacing: .6px;
}

.matcher-need-field input {
    width: 100%;
    height: 54px;

    padding: 0 16px;

    border: 1px solid var(--border);
    border-radius: 7px;

    background: #FFFFFF;

    font: inherit;
    font-size: 13px;
}

.matcher-need-field input:focus,
.matcher-field input:focus {
    border-color: #AFC8FF;

    outline: none;

    box-shadow:
        0 0 0 3px
        rgba(23, 105, 255, .07);
}

.matcher-field-help {
    margin-top: 7px;

    color: var(--text-secondary);

    font-size: 8px;
}

.matcher-section-title {
    margin-top: 24px;
    margin-bottom: 12px;

    font-size: 11px;
    font-weight: 700;
}

.matcher-form-grid {
    display: grid;

    grid-template-columns:
        repeat(3, minmax(0, 1fr));

    gap: 14px;
}

.matcher-field input {
    width: 100%;
    height: 40px;

    padding: 0 11px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;

    font: inherit;
    font-size: 10px;
}

.matcher-service-kind-section {
    margin-top: 6px;
}

.matcher-kind-options ul {
    display: flex;
    flex-wrap: wrap;
    gap: 7px;

    margin: 0;
    padding: 0;

    list-style: none;
}

.matcher-kind-options label {
    min-height: 30px;

    display: inline-flex;
    align-items: center;
    gap: 6px;

    padding: 0 9px;

    border: 1px solid var(--border);
    border-radius: 999px;

    background: #FFFFFF;

    font-size: 9px;

    cursor: pointer;
}

.matcher-kind-options input {
    accent-color: var(--blue);
}

.matcher-form-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;

    margin-top: 24px;
    padding-top: 18px;

    border-top: 1px solid var(--border);
}

.matcher-privacy-copy {
    max-width: 620px;

    color: var(--text-secondary);

    font-size: 9px;
    line-height: 1.5;
}

.matcher-submit {
    min-width: 180px;
}

.matcher-form-error {
    margin-top: 16px;

    color: var(--danger);

    font-size: 9px;
}


.matcher-summary-grid {
    display: grid;

    grid-template-columns:
        repeat(3, minmax(0, 1fr));

    gap: 14px;

    margin-top: 18px;
}

.matcher-summary-card {
    position: relative;

    padding: 18px 20px;

    overflow: hidden;
}

.matcher-summary-card::before {
    content: "";

    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;

    width: 3px;
}

.matcher-summary-card.high::before {
    background: var(--success);
}

.matcher-summary-card.possible::before {
    background: var(--warning);
}

.matcher-summary-card.rejected::before {
    background: var(--danger);
}


.matcher-result-section {
    margin-top: 26px;
}

.matcher-result-heading {
    display: flex;
    align-items: center;
    justify-content: space-between;

    margin-bottom: 11px;
}

.matcher-result-title {
    font-size: 14px;
    font-weight: 700;
}

.matcher-count-badge {
    min-width: 30px;
    height: 27px;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    border-radius: 999px;

    font-size: 9px;
    font-weight: 750;
}

.matcher-count-badge.high {
    background: #EEF8F3;
    color: var(--success);
}

.matcher-count-badge.possible {
    background: #FFF7E6;
    color: var(--warning);
}

.matcher-results-grid {
    display: grid;

    grid-template-columns:
        repeat(2, minmax(0, 1fr));

    gap: 14px;
}

.matcher-result-card {
    padding: 18px;
}

.matcher-result-card.high {
    border-top: 2px solid var(--success);
}

.matcher-result-card.possible {
    border-top: 2px solid #E5B53B;
}

.matcher-result-card.rejected {
    border-top: 2px solid #E5A2A2;
}

.matcher-card-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 16px;
}

.matcher-service-title {
    display: block;

    margin-top: 6px;

    color: var(--text);

    font-size: 14px;
    font-weight: 700;
    line-height: 1.35;

    text-decoration: none;
}

.matcher-service-title:hover {
    color: var(--blue);
}

.matcher-status {
    flex-shrink: 0;

    min-height: 24px;

    display: inline-flex;
    align-items: center;

    padding: 0 8px;

    border-radius: 999px;

    font-size: 7px;
    font-weight: 800;
    letter-spacing: .4px;
}

.matcher-status.high {
    background: #EEF8F3;
    color: var(--success);
}

.matcher-status.possible {
    background: #FFF6DF;
    color: var(--warning);
}

.matcher-status.rejected {
    background: #FFF0F0;
    color: var(--danger);
}

.matcher-service-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;

    margin-top: 10px;

    color: var(--text-secondary);

    font-size: 8px;
}

.matcher-summary-copy {
    margin: 14px 0;

    color: var(--text-secondary);

    font-size: 10px;
    line-height: 1.55;
}

.matcher-reason-block {
    margin-top: 14px;
}

.matcher-reason-title {
    margin-bottom: 7px;

    font-size: 9px;
    font-weight: 700;
}

.matcher-reason {
    margin-top: 5px;

    padding: 7px 9px;

    border-radius: 5px;

    font-size: 8px;
    line-height: 1.45;
}

.matcher-reason.positive {
    background: #F4FAF6;
    color: #176B49;
}

.matcher-reason.warning {
    background: #FFFAED;
    color: #8B6711;
}

.matcher-reason.negative {
    background: #FFF4F4;
    color: #A62E2E;
}

.matcher-reason.neutral {
    background: #F6F7F8;
    color: var(--text-secondary);
}

.matcher-card-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;

    margin-top: 16px;
    padding-top: 12px;

    border-top: 1px solid var(--border);

    color: var(--text-secondary);

    font-size: 8px;
}

.matcher-empty-card {
    grid-column: 1 / -1;

    padding: 24px;

    color: var(--text-secondary);

    font-size: 10px;
}


.matcher-rejected-section {
    margin-top: 28px;

    border: 1px solid var(--border);
    border-radius: 7px;

    background: #FFFFFF;
}

.matcher-rejected-section > summary {
    display: flex;
    align-items: center;
    justify-content: space-between;

    padding: 16px 18px;

    font-size: 10px;
    font-weight: 700;

    cursor: pointer;

    list-style: none;
}

.matcher-rejected-section > summary::-webkit-details-marker {
    display: none;
}

.matcher-rejected-section
.matcher-results-grid {
    padding: 0 14px 14px;
}


@media (max-width: 1000px) {

    .matcher-form-grid {
        grid-template-columns:
            repeat(2, minmax(0, 1fr));
    }

}


@media (max-width: 750px) {

    .matcher-summary-grid,
    .matcher-results-grid {
        grid-template-columns: 1fr;
    }

    .matcher-title-row,
    .matcher-form-footer {
        align-items: flex-start;
        flex-direction: column;
    }

}


@media (max-width: 600px) {

    .matcher-form-grid {
        grid-template-columns: 1fr;
    }

}


/* =========================================================
   CLIENT MATCHER V2 — BDE FRIENDLY
========================================================= */

.client-matcher-page {
    padding-top: 24px;
    padding-bottom: 50px;
}

.matcher-v2-description {
    max-width: 760px;
}


.matcher-v2-guide {
    display: flex;
    align-items: center;

    margin-top: 24px;

    border: 1px solid var(--border);
    border-radius: 8px;

    background: #FFFFFF;

    overflow: hidden;
}

.matcher-v2-guide > div {
    flex: 1;

    display: flex;
    align-items: center;
    gap: 9px;

    padding: 13px 16px;

    border-right: 1px solid var(--border);

    font-size: 9px;
}

.matcher-v2-guide > div:last-child {
    border-right: none;
}

.matcher-v2-guide span {
    width: 22px;
    height: 22px;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    border-radius: 50%;

    background: var(--blue-soft);
    color: var(--blue);

    font-size: 8px;
    font-weight: 800;
}

.matcher-v2-guide strong {
    font-size: 9px;
    font-weight: 650;
}


.matcher-v2-card {
    max-width: 1100px;

    margin-top: 14px;

    overflow: hidden;
}


.matcher-v2-step {
    display: grid;

    grid-template-columns:
        42px
        minmax(0, 1fr);

    gap: 14px;

    padding: 24px 26px;
}

.matcher-v2-step-number {
    width: 32px;
    height: 32px;

    display: flex;
    align-items: center;
    justify-content: center;

    border-radius: 8px;

    background: #17191C;
    color: #FFFFFF;

    font-size: 11px;
    font-weight: 750;
}

.matcher-v2-step-content h2 {
    margin: 1px 0 4px;

    font-size: 15px;
    font-weight: 700;
}

.matcher-v2-step-content > p {
    margin: 0;

    color: var(--text-secondary);

    font-size: 9px;
}


.matcher-v2-divider {
    height: 1px;

    margin-left: 82px;

    background: var(--border);
}


.matcher-v2-main-search {
    margin-top: 16px;
}

.matcher-v2-main-search input {
    width: 100%;
    height: 58px;

    padding: 0 17px;

    border: 1px solid #C9D1DC;
    border-radius: 8px;

    background: #FFFFFF;

    font: inherit;
    font-size: 13px;
}

.matcher-v2-main-search input:focus {
    border-color: var(--blue);

    outline: none;

    box-shadow:
        0 0 0 3px
        rgba(23, 105, 255, .08);
}

.matcher-v2-examples {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 6px;

    margin-top: 9px;

    color: var(--text-secondary);

    font-size: 8px;
}

.matcher-v2-examples span {
    padding: 4px 7px;

    border-radius: 999px;

    background: #F4F5F6;

    color: var(--text-secondary);
}


.matcher-v2-basic-grid {
    display: grid;

    grid-template-columns:
        repeat(2, minmax(0, 1fr));

    gap: 14px;

    margin-top: 18px;
}

.matcher-v2-field label,
.matcher-v2-service-types > label {
    display: block;

    margin-bottom: 6px;

    color: var(--text);

    font-size: 9px;
    font-weight: 650;
}

.matcher-v2-field input,
.matcher-v2-field select {
    width: 100%;
    height: 42px;

    padding: 0 11px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;

    font: inherit;
    font-size: 10px;
}

.matcher-v2-field input:focus,
.matcher-v2-field select:focus {
    border-color: #AFC8FF;

    outline: none;
}


.matcher-v2-advanced {
    margin-top: 18px;

    border: 1px solid var(--border);
    border-radius: 7px;

    background: #FAFAFA;
}

.matcher-v2-advanced > summary {
    display: flex;
    align-items: center;
    justify-content: space-between;

    padding: 13px 15px;

    list-style: none;

    cursor: pointer;
}

.matcher-v2-advanced > summary::-webkit-details-marker {
    display: none;
}

.matcher-v2-advanced > summary span {
    font-size: 9px;
    font-weight: 650;
}

.matcher-v2-advanced > summary small {
    padding: 3px 7px;

    border-radius: 999px;

    background: #ECEEF1;
    color: var(--text-secondary);

    font-size: 7px;
    font-weight: 650;
}

.matcher-v2-advanced[open] > summary {
    border-bottom: 1px solid var(--border);
}

.matcher-v2-advanced-content {
    display: grid;

    grid-template-columns:
        repeat(3, minmax(0, 1fr));

    gap: 14px;

    padding: 16px;
}


.matcher-v2-input-suffix {
    display: grid;

    grid-template-columns:
        minmax(0, 1fr)
        auto;
}

.matcher-v2-input-suffix input {
    border-radius: 6px 0 0 6px;
}

.matcher-v2-input-suffix span {
    min-width: 55px;

    display: flex;
    align-items: center;
    justify-content: center;

    border: 1px solid var(--border);
    border-left: 0;
    border-radius: 0 6px 6px 0;

    background: #F6F7F8;
    color: var(--text-secondary);

    font-size: 8px;
}


.matcher-v2-turnover {
    display: grid;

    grid-template-columns:
        minmax(0, 1fr)
        95px;
}

.matcher-v2-turnover input {
    border-radius: 6px 0 0 6px;
}

.matcher-v2-turnover select {
    border-left: none;

    border-radius: 0 6px 6px 0;
}


.matcher-v2-service-types {
    grid-column: 1 / -1;

    margin-top: 4px;
}

.matcher-v2-kind-grid #id_service_kinds {
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
}

.matcher-v2-kind-grid
#id_service_kinds > div {
    margin: 0;
}

.matcher-v2-kind-grid label {
    min-height: 30px;

    display: inline-flex;
    align-items: center;
    gap: 6px;

    padding: 0 9px;

    border: 1px solid var(--border);
    border-radius: 999px;

    background: #FFFFFF;

    font-size: 8px;

    cursor: pointer;
}

.matcher-v2-kind-grid input {
    width: auto;
    height: auto;

    accent-color: var(--blue);
}


.matcher-v2-action {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;

    padding: 18px 26px;

    border-top: 1px solid var(--border);

    background: #FAFAFA;
}

.matcher-v2-privacy {
    display: flex;
    align-items: center;
    gap: 9px;

    color: var(--success);
}

.matcher-v2-privacy > span {
    font-size: 15px;
}

.matcher-v2-privacy strong,
.matcher-v2-privacy small {
    display: block;
}

.matcher-v2-privacy strong {
    font-size: 9px;
}

.matcher-v2-privacy small {
    margin-top: 2px;

    color: var(--text-secondary);

    font-size: 7px;
}

.matcher-v2-submit {
    min-width: 210px;
    min-height: 42px;
}

.matcher-v2-error {
    margin: 0 26px 18px;
    padding: 10px;

    border-radius: 6px;

    background: #FFF2F2;
    color: var(--danger);

    font-size: 9px;
}


.matcher-v2-results-heading {
    display: flex;
    justify-content: space-between;

    margin-top: 32px;
    margin-bottom: 12px;
}

.matcher-v2-results-heading h2 {
    margin: 3px 0 0;

    font-size: 18px;
}


@media (max-width: 850px) {

    .matcher-v2-basic-grid,
    .matcher-v2-advanced-content {
        grid-template-columns: 1fr;
    }

    .matcher-v2-guide {
        align-items: stretch;
        flex-direction: column;
    }

    .matcher-v2-guide > div {
        border-right: none;
        border-bottom: 1px solid var(--border);
    }

    .matcher-v2-guide > div:last-child {
        border-bottom: none;
    }

}


@media (max-width: 650px) {

    .matcher-v2-step {
        grid-template-columns: 1fr;

        padding: 19px;
    }

    .matcher-v2-divider {
        margin-left: 19px;
        margin-right: 19px;
    }

    .matcher-v2-action {
        align-items: flex-start;
        flex-direction: column;
    }

    .matcher-v2-submit {
        width: 100%;
    }

}


/* =========================================================
   GLOBAL STICKY HEADER / CONTENT SPACING FIX
========================================================= */

html {
    scroll-padding-top:
        calc(var(--topbar-height) + 28px);
}

.main-area > .content {
    padding-top: 30px !important;
}

.main-area > .content
[id] {
    scroll-margin-top:
        calc(var(--topbar-height) + 28px);
}


/* =========================================================
   CLIENT MATCHER RESULT UX
========================================================= */

.matcher-example-label {
    background: transparent !important;

    padding-left: 0 !important;

    font-weight: 700;
}


.matcher-result-context {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 30px;

    margin-top: 32px;
    padding: 20px 22px;

    border: 1px solid var(--border);
    border-radius: 8px;

    background: #FFFFFF;
}

.matcher-result-context h2 {
    margin: 3px 0 14px;

    font-size: 18px;
}


.matcher-context-query {
    display: flex;
    align-items: center;
    gap: 9px;
}

.matcher-context-query > span {
    color: var(--text-secondary);

    font-size: 7px;
    font-weight: 750;
    letter-spacing: .6px;
}

.matcher-context-query strong {
    font-size: 12px;
}


.matcher-context-details {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;

    margin-top: 10px;
}

.matcher-context-details span {
    padding: 4px 7px;

    border-radius: 999px;

    background: #F2F4F6;
    color: var(--text-secondary);

    font-size: 8px;
}


.matcher-result-actions {
    display: flex;
    gap: 8px;

    flex-shrink: 0;
}


.matcher-call-section {
    margin-top: 16px;
}

.matcher-call-section.verify {
    margin-top: 12px;
}

.matcher-call-label {
    margin-bottom: 7px;

    color: var(--text-secondary);

    font-size: 7px;
    font-weight: 800;
    letter-spacing: .7px;
}


.matcher-card-actions {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;

    margin-top: 17px;
    padding-top: 14px;

    border-top: 1px solid var(--border);
}


.matcher-save-button {
    min-height: 36px;

    padding: 0 12px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;
    color: var(--text-secondary);

    font: inherit;
    font-size: 9px;
    font-weight: 700;

    cursor: pointer;
}

.matcher-save-button:hover {
    border-color: #E2C64E;

    background: #FFFBEA;
    color: #856800;
}

.matcher-save-button.saved {
    border-color: #E5CE68;

    background: #FFF8D9;
    color: #7A6100;
}

.matcher-save-button:disabled {
    opacity: .6;

    cursor: wait;
}


.matcher-open-service {
    min-height: 36px;

    display: inline-flex;
    align-items: center;
}


.matcher-no-results {
    margin-top: 18px;
    padding: 34px;

    text-align: center;
}

.matcher-no-results-icon {
    width: 40px;
    height: 40px;

    display: flex;
    align-items: center;
    justify-content: center;

    margin: 0 auto 12px;

    border-radius: 50%;

    background: #F1F3F5;
    color: var(--text-secondary);

    font-size: 16px;
}

.matcher-no-results h3 {
    margin: 0;

    font-size: 14px;
}

.matcher-no-results p {
    max-width: 620px;

    margin: 8px auto 17px;

    color: var(--text-secondary);

    font-size: 9px;
    line-height: 1.55;
}


@media (max-width: 800px) {

    .matcher-result-context {
        flex-direction: column;
    }

    .matcher-result-actions {
        width: 100%;
    }

}


@media (max-width: 600px) {

    .matcher-result-actions,
    .matcher-card-actions {
        align-items: stretch;
        flex-direction: column;
    }

}


/* =========================================================
   MATCHER FINAL VISUAL POLISH
========================================================= */

.matcher-query-chip {
    display: inline-flex;
    align-items: center;

    min-height: 29px;

    padding: 0 10px;

    border: 1px solid #D8E3FF;
    border-radius: 999px;

    background: #F2F6FF;
    color: var(--blue);

    font-size: 10px;
    font-weight: 700;

    text-transform: none;
}


/* NEXT ACTION */

.matcher-next-action {
    display: flex;
    align-items: center;
    gap: 12px;

    margin-top: 14px;
    padding: 13px 16px;

    border: 1px solid var(--border);
    border-radius: 8px;

    background: #FFFFFF;
}

.matcher-next-action-icon {
    width: 30px;
    height: 30px;

    flex-shrink: 0;

    display: flex;
    align-items: center;
    justify-content: center;

    border-radius: 8px;

    font-size: 12px;
    font-weight: 800;
}

.matcher-next-action-icon.strong {
    background: #EDF8F2;
    color: var(--success);
}

.matcher-next-action-icon.verify {
    background: #FFF6E0;
    color: var(--warning);
}

.matcher-next-action-icon.rejected {
    background: #FFF0F0;
    color: var(--danger);
}

.matcher-next-action-icon.neutral {
    background: #F1F3F5;
    color: var(--text-secondary);
}

.matcher-next-action strong,
.matcher-next-action span {
    display: block;
}

.matcher-next-action strong {
    font-size: 11px;
}

.matcher-next-action span {
    margin-top: 3px;

    color: var(--text-secondary);

    font-size: 8px;
}


/* COMPACT RESULT KPIs */

.matcher-summary-grid {
    gap: 10px !important;

    margin-top: 12px !important;
}

.matcher-summary-card {
    min-height: 96px;

    padding: 13px 16px !important;
}

.matcher-summary-card
.search-kpi-value {
    margin-top: 5px;

    font-size: 21px;
}

.matcher-summary-card
.search-kpi-meta {
    margin-top: 4px;
}


/* RESULT TOOLBAR */

.matcher-result-toolbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;

    margin-top: 24px;
    margin-bottom: -4px;
}

.matcher-result-toolbar > div strong,
.matcher-result-toolbar > div span {
    display: block;
}

.matcher-result-toolbar > div strong {
    font-size: 11px;
}

.matcher-result-toolbar > div span {
    margin-top: 3px;

    color: var(--text-secondary);

    font-size: 8px;
}


.matcher-sort-control {
    display: flex;
    align-items: center;
    gap: 8px;
}

.matcher-sort-control > span {
    color: var(--text-secondary);

    font-size: 7px;
    font-weight: 750;
    letter-spacing: .5px;
}

.matcher-sort-control select {
    height: 34px;

    padding: 0 28px 0 9px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;
    color: var(--text);

    font: inherit;
    font-size: 9px;

    cursor: pointer;
}


/* RESULT WIDTH */

.matcher-results-grid.single-result {
    grid-template-columns:
        minmax(520px, 74%);
}


/* Give recommendation cards stronger hierarchy */

.matcher-result-card {
    min-height: 270px;
}

.matcher-result-card
.matcher-service-title {
    font-size: 15px;
}

.matcher-result-card
.matcher-summary-copy {
    max-width: 760px;
}


/* Keep action buttons visually balanced */

.matcher-card-actions {
    margin-top: 20px;
}

.matcher-open-service {
    padding-left: 15px;
    padding-right: 15px;
}


@media (max-width: 1000px) {

    .matcher-results-grid.single-result {
        grid-template-columns: 1fr;
    }

}


@media (max-width: 700px) {

    .matcher-result-toolbar {
        align-items: flex-start;
        flex-direction: column;
    }

}


/* =========================================================
   BUSINESS TOOLKIT V3 — SECTOR + PITCH WINDOWS
========================================================= */

.toolkit-v3-page {
    padding-bottom: 48px;
}

.toolkit-v3-search-card {
    margin-top: 24px;
    padding: 20px;

    border: 1px solid var(--border);
    border-radius: 8px;

    background: #FFFFFF;
}

.toolkit-v3-search-form {
    display: grid;

    grid-template-columns:
        minmax(0, 1fr)
        auto;

    gap: 10px;

    max-width: 1050px;
}

.toolkit-v3-search-input {
    height: 54px;

    font-size: 13px;
}

.toolkit-v3-search-button {
    min-width: 135px;
}

.toolkit-v3-search-help {
    margin-top: 8px;

    color: var(--text-secondary);

    font-size: 8px;
}


/* SECTORS */

.toolkit-sector-section {
    margin-top: 22px;
}

.toolkit-section-header {
    margin-bottom: 10px;
}

.toolkit-sector-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
}

.toolkit-sector-chip {
    min-height: 34px;

    display: inline-flex;
    align-items: center;
    gap: 8px;

    padding: 0 11px;

    border: 1px solid var(--border);
    border-radius: 999px;

    background: #FFFFFF;
    color: var(--text);

    font-size: 9px;
    font-weight: 600;

    text-decoration: none;
}

.toolkit-sector-chip strong {
    min-width: 19px;
    height: 19px;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    border-radius: 999px;

    background: #F1F3F5;
    color: var(--text-secondary);

    font-size: 7px;
}

.toolkit-sector-chip:hover,
.toolkit-sector-chip.active {
    border-color: #B9CDFF;

    background: #F2F6FF;
    color: var(--blue);
}


/* FILTERS */

.toolkit-v3-filter-card {
    margin-top: 18px;
    padding: 14px 16px;
}

.toolkit-v3-filters {
    display: flex;
    align-items: end;
    flex-wrap: wrap;
    gap: 10px;
}

.toolkit-v3-filter {
    min-width: 150px;
    flex: 1;
}

.toolkit-v3-filter label {
    display: block;

    margin-bottom: 5px;

    color: var(--text-secondary);

    font-size: 7px;
    font-weight: 750;
    letter-spacing: .5px;
}

.toolkit-v3-filter select {
    width: 100%;
    height: 36px;

    padding: 0 8px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;

    font: inherit;
    font-size: 9px;
}

.toolkit-clear-filters {
    padding: 9px 3px;

    color: var(--text-secondary);

    font-size: 9px;

    text-decoration: none;
}


/* RESULTS */

.toolkit-results-header {
    display: flex;
    align-items: center;
    justify-content: space-between;

    margin-top: 24px;
    margin-bottom: 10px;
}

.toolkit-active-sector {
    padding: 6px 9px;

    border-radius: 999px;

    background: var(--blue-soft);
    color: var(--blue);

    font-size: 8px;
    font-weight: 650;
}

.toolkit-v3-results {
    display: grid;

    grid-template-columns:
        repeat(2, minmax(0, 1fr));

    gap: 14px;
}

.toolkit-v3-service-card {
    padding: 18px;
}

.toolkit-v3-card-top {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 14px;
}

.toolkit-v3-service-title {
    display: block;

    margin-top: 5px;

    color: var(--text);

    font-size: 14px;
    font-weight: 700;
    line-height: 1.35;

    text-decoration: none;
}

.toolkit-v3-service-title:hover {
    color: var(--blue);
}

.toolkit-v3-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 5px;

    margin-top: 8px;

    color: var(--text-secondary);

    font-size: 8px;
}

.toolkit-sector-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 5px;

    margin-top: 11px;
}

.toolkit-sector-tags span {
    padding: 4px 7px;

    border-radius: 999px;

    background: #F3F4F5;
    color: var(--text-secondary);

    font-size: 7px;
}

.toolkit-v3-summary {
    margin: 13px 0 0;

    color: var(--text-secondary);

    font-size: 9px;
    line-height: 1.55;
}


/* PITCH STATUS */

.toolkit-pitch-badge {
    flex-shrink: 0;

    min-height: 24px;

    display: inline-flex;
    align-items: center;

    padding: 0 8px;

    border-radius: 999px;

    background: #EEF8F3;
    color: var(--success);

    font-size: 7px;
    font-weight: 800;

    white-space: nowrap;
}

.toolkit-pitch-badge.closing {
    background: #FFF6DF;
    color: var(--warning);
}

.toolkit-pitch-badge.urgent {
    background: #FFF0F0;
    color: var(--danger);
}

.toolkit-pitch-badge.closed {
    background: #F0F1F3;
    color: #747980;
}

.toolkit-pitch-badge.upcoming {
    background: #EEF3FF;
    color: var(--blue);
}


.toolkit-pitch-info {
    display: flex;
    flex-wrap: wrap;
    gap: 18px;

    margin-top: 15px;
    padding: 12px;

    border-radius: 6px;

    background: #FAFAFA;
}

.toolkit-pitch-info > div {
    min-width: 100px;
}

.toolkit-pitch-info span,
.toolkit-pitch-info strong {
    display: block;
}

.toolkit-pitch-info span {
    color: var(--text-secondary);

    font-size: 6px;
    font-weight: 750;
    letter-spacing: .5px;
}

.toolkit-pitch-info strong {
    margin-top: 4px;

    font-size: 9px;
}

.danger-text {
    color: var(--danger);
}

.warning-text {
    color: var(--warning);
}


.toolkit-v3-actions {
    display: flex;
    align-items: center;
    justify-content: space-between;

    margin-top: 15px;
    padding-top: 13px;

    border-top: 1px solid var(--border);
}

.toolkit-v3-empty {
    grid-column: 1 / -1;

    padding: 38px;

    text-align: center;
}

.toolkit-v3-empty-icon {
    margin-bottom: 10px;

    font-size: 20px;
}

.toolkit-v3-empty h3 {
    margin: 0;

    font-size: 14px;
}

.toolkit-v3-empty p {
    max-width: 560px;

    margin: 7px auto 0;

    color: var(--text-secondary);

    font-size: 9px;
}


/* =========================================================
   SERVICE DETAIL PITCH BANNER
========================================================= */

.service-pitch-banner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;

    margin-bottom: 18px;
    padding: 13px 15px;

    border: 1px solid #DDE9E2;
    border-left: 3px solid var(--success);
    border-radius: 7px;

    background: #F6FBF8;
}

.service-pitch-banner.closing {
    border-color: #F1DFC0;
    border-left-color: var(--warning);

    background: #FFFAF1;
}

.service-pitch-banner.urgent,
.service-pitch-banner.closed {
    border-color: #F0CCCC;
    border-left-color: var(--danger);

    background: #FFF7F7;
}

.service-pitch-banner.upcoming {
    border-color: #D8E3FF;
    border-left-color: var(--blue);

    background: #F5F8FF;
}

.service-pitch-banner-label {
    display: block;

    margin-bottom: 3px;

    color: var(--text-secondary);

    font-size: 6px;
    font-weight: 800;
    letter-spacing: .6px;
}

.service-pitch-banner > div > strong {
    font-size: 12px;
}

.service-pitch-banner-dates {
    display: flex;
    flex-wrap: wrap;
    gap: 15px;

    font-size: 8px;
}


/* =========================================================
   BDE CLOSING SOON
========================================================= */

.closing-soon-section {
    margin-top: 24px;
}

.closing-soon-grid {
    display: grid;

    grid-template-columns:
        repeat(3, minmax(0, 1fr));

    gap: 10px;
}

.closing-soon-card {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 15px;

    min-height: 85px;

    padding: 14px;

    border: 1px solid var(--border);
    border-radius: 7px;

    background: #FFFFFF;
    color: var(--text);

    text-decoration: none;
}

.closing-soon-card:hover {
    border-color: #D2B35A;
}

.closing-soon-title {
    font-size: 10px;
    font-weight: 700;
}

.closing-soon-meta {
    margin-top: 5px;

    color: var(--text-secondary);

    font-size: 7px;
}

.closing-soon-days {
    min-width: 52px;

    padding: 7px;

    border-radius: 7px;

    background: #FFF6E0;
    color: var(--warning);

    text-align: center;
}

.closing-soon-days.urgent {
    background: #FFF0F0;
    color: var(--danger);
}

.closing-soon-days strong,
.closing-soon-days span {
    display: block;
}

.closing-soon-days strong {
    font-size: 16px;
}

.closing-soon-days span {
    margin-top: 2px;

    font-size: 6px;
}


/* =========================================================
   ADMIN PITCH WINDOWS
========================================================= */

.pitch-admin-kpis {
    display: grid;

    grid-template-columns:
        repeat(5, minmax(0, 1fr));

    gap: 10px;

    margin-top: 22px;
}

.pitch-admin-kpi {
    padding: 14px;

    color: var(--text);

    text-decoration: none;
}

.pitch-admin-kpi span,
.pitch-admin-kpi strong {
    display: block;
}

.pitch-admin-kpi span {
    color: var(--text-secondary);

    font-size: 7px;
    font-weight: 750;
}

.pitch-admin-kpi strong {
    margin-top: 8px;

    font-size: 22px;
}

.pitch-admin-kpi.closing {
    border-top: 2px solid var(--warning);
}

.pitch-admin-kpi.urgent,
.pitch-admin-kpi.closed {
    border-top: 2px solid var(--danger);
}

.pitch-admin-filter {
    margin-top: 12px;
    padding: 13px;
}

.pitch-admin-filter form {
    display: grid;

    grid-template-columns:
        minmax(220px, 1fr)
        190px
        auto;

    gap: 9px;
}

.pitch-admin-filter input,
.pitch-admin-filter select {
    height: 36px;

    padding: 0 9px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;

    font: inherit;
    font-size: 9px;
}

.pitch-admin-table-card {
    margin-top: 12px;

    overflow: hidden;
}


@media (max-width: 1000px) {

    .toolkit-v3-results {
        grid-template-columns: 1fr;
    }

    .closing-soon-grid {
        grid-template-columns:
            repeat(2, minmax(0, 1fr));
    }

    .pitch-admin-kpis {
        grid-template-columns:
            repeat(3, minmax(0, 1fr));
    }

}


@media (max-width: 700px) {

    .toolkit-v3-search-form {
        grid-template-columns: 1fr;
    }

    .closing-soon-grid,
    .pitch-admin-kpis {
        grid-template-columns: 1fr;
    }

    .service-pitch-banner {
        align-items: flex-start;
        flex-direction: column;
    }

    .pitch-admin-filter form {
        grid-template-columns: 1fr;
    }

}


/* =========================================================
   SEARCH ACTIVITY DRILLDOWN
========================================================= */

.search-drilldown-page {
    padding-bottom: 50px;
}


.search-drilldown-top {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 24px;
}


.search-drilldown-datebar {
    display: grid;

    grid-template-columns:
        130px
        minmax(0, 1fr)
        130px;

    align-items: center;

    margin-top: 22px;
    padding: 11px 14px;

    border: 1px solid var(--border);
    border-radius: 8px;

    background: #FFFFFF;
}


.search-date-nav {
    color: var(--blue);

    font-size: 9px;
    font-weight: 650;

    text-decoration: none;
}


.search-date-nav:last-child {
    text-align: right;
}


.search-date-nav.disabled {
    color: #B7BBC1;

    pointer-events: none;
}


.search-selected-date {
    text-align: center;
}


.search-selected-date span,
.search-selected-date strong {
    display: block;
}


.search-selected-date span {
    color: var(--text-secondary);

    font-size: 6px;
    font-weight: 800;
    letter-spacing: .7px;
}


.search-selected-date strong {
    margin-top: 4px;

    font-size: 11px;
}



/* KPI */

.search-drilldown-kpis {
    display: grid;

    grid-template-columns:
        repeat(5, minmax(0, 1fr));

    gap: 10px;

    margin-top: 14px;
}


.search-drilldown-kpi {
    min-height: 105px;

    padding: 14px 16px;
}


.search-drilldown-kpi span,
.search-drilldown-kpi strong,
.search-drilldown-kpi small {
    display: block;
}


.search-drilldown-kpi span {
    color: var(--text-secondary);

    font-size: 7px;
    font-weight: 750;
    letter-spacing: .45px;
}


.search-drilldown-kpi strong {
    margin-top: 7px;

    font-size: 22px;
}


.search-drilldown-kpi small {
    margin-top: 5px;

    color: var(--text-secondary);

    font-size: 7px;
}


.search-drilldown-kpi.successful {
    border-top: 2px solid var(--success);
}


.search-drilldown-kpi.zero {
    border-top: 2px solid var(--danger);
}



/* TABLE */

.search-drilldown-table-card {
    margin-top: 14px;

    overflow: hidden;
}


.search-drilldown-table-heading {
    display: flex;
    align-items: center;
    justify-content: space-between;

    padding: 17px 18px;

    border-bottom: 1px solid var(--border);
}


.real-data-label {
    color: var(--success);

    font-size: 7px;
    font-weight: 800;
    letter-spacing: .5px;
}


.search-drilldown-table-wrap {
    overflow-x: auto;
}


.search-drilldown-table {
    width: 100%;

    border-collapse: collapse;
}


.search-drilldown-table th {
    padding: 10px 14px;

    border-bottom: 1px solid var(--border);

    background: #FAFAFA;
    color: var(--text-secondary);

    font-size: 7px;
    font-weight: 750;

    text-align: left;
}


.search-drilldown-table td {
    padding: 13px 14px;

    border-bottom: 1px solid #ECEDEF;

    font-size: 9px;

    vertical-align: top;
}


.search-drilldown-table tbody tr:hover {
    background: #FBFCFF;
}


.search-activity-time {
    white-space: nowrap;

    color: var(--text-secondary);
}


.search-event-badge {
    display: inline-flex;
    align-items: center;

    min-height: 22px;

    padding: 0 7px;

    border-radius: 999px;

    background: #F1F3F5;
    color: var(--text-secondary);

    font-size: 6px;
    font-weight: 800;
}


.search-event-badge.search {
    background: #EEF4FF;
    color: var(--blue);
}


.search-event-badge.filter {
    background: #F3F0FF;
    color: #6555B5;
}


.search-event-badge.matcher {
    background: #FFF5DF;
    color: var(--warning);
}


.search-event-badge.suggestion {
    background: #EFF8F4;
    color: var(--success);
}


.search-event-query {
    font-size: 9px;
}


.search-event-meta,
.search-event-selected {
    margin-top: 4px;

    color: var(--text-secondary);

    font-size: 7px;
}


.search-event-selected a {
    color: var(--blue);

    text-decoration: none;
}


.search-result-status {
    display: inline-flex;

    padding: 4px 7px;

    border-radius: 999px;

    font-size: 6px;
    font-weight: 750;
}


.search-result-status.success {
    background: #EDF8F2;
    color: var(--success);
}


.search-result-status.zero {
    background: #FFF0F0;
    color: var(--danger);
}


.search-result-status.info {
    background: #FFF6E0;
    color: var(--warning);
}


.search-result-status.neutral {
    background: #F1F3F5;
    color: var(--text-secondary);
}



/* EMPTY STATE */

.search-drilldown-empty {
    padding: 55px 24px;

    text-align: center;
}


.search-drilldown-empty-icon {
    width: 42px;
    height: 42px;

    display: flex;
    align-items: center;
    justify-content: center;

    margin: 0 auto 12px;

    border-radius: 50%;

    background: #F1F3F5;

    font-size: 16px;
}


.search-drilldown-empty h3 {
    margin: 0;

    font-size: 14px;
}


.search-drilldown-empty p {
    max-width: 520px;

    margin: 8px auto 17px;

    color: var(--text-secondary);

    font-size: 9px;
}


@media (max-width: 1000px) {

    .search-drilldown-kpis {
        grid-template-columns:
            repeat(3, minmax(0, 1fr));
    }

}


@media (max-width: 700px) {

    .search-drilldown-top {
        flex-direction: column;
    }

    .search-drilldown-datebar {
        grid-template-columns:
            90px
            minmax(0, 1fr)
            90px;
    }

    .search-drilldown-kpis {
        grid-template-columns:
            repeat(2, minmax(0, 1fr));
    }

}


/* =========================================================
   SEARCH CHART — ZERO ACTIVITY DAYS
========================================================= */

.search-bar-column.zero-activity {
    opacity: .55;

    cursor: default;
}

.search-bar-column.zero-activity
.search-bar-fill {
    height: 0 !important;
    min-height: 0 !important;

    background: transparent !important;
}

a[aria-disabled="true"] {
    cursor: default;
}


/* =========================================================
   TOOLKIT ACTIVITY CHART
   SEARCH + CLIENT MATCHER
========================================================= */

.activity-bar-values {
    min-height: 20px;

    display: flex;
    align-items: center;
    justify-content: center;
    gap: 5px;

    margin-bottom: 5px;
}


.activity-bar-values span {
    min-width: 28px;

    padding: 3px 5px;

    border-radius: 999px;

    font-size: 6px;
    font-weight: 750;

    text-align: center;
}


.activity-search-value {
    background: #EEF4FF;
    color: var(--blue);
}


.activity-matcher-value {
    background: #FFF5DF;
    color: var(--warning);
}



/* Two actual series inside same day track */

.search-bar-track {
    position: relative;
}


.activity-series-bars {
    width: 100%;
    height: 100%;

    display: flex;
    align-items: flex-end;
    justify-content: center;

    gap: 5px;

    padding: 0 8px;
}


.activity-series-bars
.search-bar-fill {
    position: static !important;

    width: 13px !important;
    min-width: 13px;

    border-radius:
        4px 4px 2px 2px;

    transition:
        height .2s ease,
        opacity .2s ease;
}


.activity-search-bar {
    background:
        var(--blue) !important;
}


.activity-matcher-bar {
    background:
        #D79A20 !important;
}



/* Date + total activity */

.activity-total-label {
    min-height: 15px;

    margin-top: 4px;

    color: var(--text-secondary);

    font-size: 6px;
    font-weight: 600;

    text-align: center;
}



/* Active days */

.search-bar-column[
    data-total
] {
    cursor: pointer;
}


/* Zero activity days */

.search-bar-column.zero-activity {
    opacity: .48;

    cursor: default;
}


.search-bar-column.zero-activity
.search-bar-fill {
    height: 0 !important;

    background:
        transparent !important;
}


.search-bar-column.zero-activity
.activity-bar-values span {
    background: #F3F4F5;

    color: #9A9EA5;
}


.search-bar-column:not(
    .zero-activity
):hover
.search-bar-track {
    outline:
        1px solid #D5DCEB;

    outline-offset: 2px;
}


a[aria-disabled="true"] {
    cursor: default;

    pointer-events: none;
}



/* Small visual key */

.activity-chart-key {
    display: flex;
    align-items: center;
    gap: 12px;
}


@media (max-width: 700px) {

    .activity-bar-values {
        gap: 2px;
    }

    .activity-bar-values span {
        min-width: 22px;

        padding-left: 3px;
        padding-right: 3px;
    }

    .activity-series-bars {
        gap: 3px;

        padding-left: 4px;
        padding-right: 4px;
    }

}


/* =========================================================
   BHARATNXT OPERATIONS CONSOLE DESIGN SYSTEM
========================================================= */

.pitch-ops-page,
.search-console-page {
    padding-bottom: 56px;
}


/* =========================================================
   PITCH OPERATIONS
========================================================= */

.pitch-ops-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 30px;
}

.pitch-ops-header h1,
.search-console-header h1 {
    margin: 3px 0 5px;

    font-size: 28px;
    line-height: 1.15;
    letter-spacing: -.6px;
}

.pitch-ops-header p,
.search-console-header p {
    max-width: 680px;

    margin: 0;

    color: var(--text-secondary);

    font-size: 10px;
    line-height: 1.5;
}

.pitch-ops-today {
    min-width: 130px;

    padding-top: 3px;

    text-align: right;
}

.pitch-ops-today span,
.pitch-ops-today strong {
    display: block;
}

.pitch-ops-today span {
    color: var(--text-secondary);

    font-size: 6px;
    font-weight: 800;
    letter-spacing: .7px;
}

.pitch-ops-today strong {
    margin-top: 5px;

    font-size: 11px;
}


/* STATUS LEDGER */

.pitch-ops-status-strip {
    display: flex;
    align-items: stretch;

    margin-top: 22px;

    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);

    background: #FFFFFF;
}

.pitch-ops-status-strip a {
    flex: 1;

    min-height: 62px;

    display: flex;
    align-items: baseline;
    gap: 8px;

    padding: 18px 16px;

    border-right: 1px solid var(--border);

    color: var(--text);

    text-decoration: none;
}

.pitch-ops-status-strip a:last-child {
    border-right: 0;
}

.pitch-ops-status-strip a:hover {
    background: #FAFBFC;
}

.pitch-ops-status-strip strong {
    font-size: 20px;
    line-height: 1;
}

.pitch-ops-status-strip strong.danger {
    color: var(--danger);
}

.pitch-ops-status-strip span {
    color: var(--text-secondary);

    font-size: 8px;
}


/* COMMAND GRID */

.pitch-ops-command-grid {
    display: grid;

    grid-template-columns:
        minmax(0, 1fr)
        270px;

    margin-top: 18px;

    border: 1px solid var(--border);

    background: #FFFFFF;
}


/* PANEL HEADING */

.pitch-ops-panel-heading {
    min-height: 58px;

    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 15px;

    padding: 13px 16px;

    border-bottom: 1px solid var(--border);
}

.pitch-ops-panel-heading strong,
.pitch-ops-panel-heading span {
    display: block;
}

.pitch-ops-panel-heading strong {
    font-size: 11px;
}

.pitch-ops-panel-heading div > span {
    margin-top: 3px;

    color: var(--text-secondary);

    font-size: 7px;
}

.pitch-ops-live {
    color: var(--success);

    font-size: 6px !important;
    font-weight: 800;
    letter-spacing: .5px;
}


/* TIMELINE */

.pitch-ops-timeline-panel {
    min-width: 0;

    border-right: 1px solid var(--border);
}

.pitch-ops-axis {
    position: relative;

    height: 34px;

    margin-left: 190px;
    margin-right: 90px;

    border-bottom: 1px solid var(--border);
}

.pitch-axis-marker {
    position: absolute;
    top: 0;

    height: 100%;

    border-left: 1px solid #E9EAEC;
}

.pitch-axis-marker span {
    position: absolute;
    top: 9px;
    left: 5px;

    color: var(--text-secondary);

    font-size: 6px;

    white-space: nowrap;
}


.pitch-timeline-row {
    width: 100%;

    min-height: 52px;

    display: grid;

    grid-template-columns:
        190px
        minmax(0, 1fr)
        90px;

    align-items: center;

    padding: 0;

    border: 0;
    border-bottom: 1px solid #ECEDEF;

    background: #FFFFFF;

    color: var(--text);

    text-align: left;

    cursor: pointer;
}

.pitch-timeline-row:hover {
    background: #FBFCFE;
}

.pitch-timeline-name {
    min-width: 0;

    padding: 10px 13px;
}

.pitch-timeline-name strong,
.pitch-timeline-name span {
    display: block;
}

.pitch-timeline-name strong {
    overflow: hidden;

    font-size: 9px;

    text-overflow: ellipsis;
    white-space: nowrap;
}

.pitch-timeline-name span {
    margin-top: 3px;

    color: var(--text-secondary);

    font-size: 6px;
}

.pitch-timeline-track {
    position: relative;

    height: 100%;

    border-left: 1px solid #F0F1F2;
    border-right: 1px solid #F0F1F2;

    background-image:
        linear-gradient(
            to right,
            transparent 24.8%,
            #F1F2F3 25%,
            transparent 25.2%,
            transparent 49.8%,
            #F1F2F3 50%,
            transparent 50.2%,
            transparent 74.8%,
            #F1F2F3 75%,
            transparent 75.2%
        );
}

.pitch-timeline-bar {
    position: absolute;

    top: 50%;

    height: 7px;

    transform: translateY(-50%);

    border-radius: 2px;

    background: var(--success);
}

.pitch-timeline-bar.closing {
    background: #D99A28;
}

.pitch-timeline-bar.urgent {
    background: var(--danger);
}

.pitch-timeline-bar.upcoming {
    background: var(--blue);
}

.pitch-timeline-bar.open-ended {
    border-right: 2px dashed #757A80;

    background:
        repeating-linear-gradient(
            90deg,
            #AEB3B8 0,
            #AEB3B8 8px,
            #D9DCDF 8px,
            #D9DCDF 14px
        );
}

.pitch-timeline-end {
    padding: 8px 10px;

    text-align: right;
}

.pitch-timeline-end strong,
.pitch-timeline-end span {
    display: block;
}

.pitch-timeline-end strong {
    font-size: 10px;
}

.pitch-timeline-end span {
    margin-top: 3px;

    color: var(--text-secondary);

    font-size: 6px;
}


/* ACTION QUEUE */

.pitch-ops-action-queue {
    background: #FBFBFA;
}

.pitch-action-item {
    position: relative;

    display: block;

    min-height: 91px;

    padding: 13px 34px 13px 15px;

    border-bottom: 1px solid var(--border);

    color: var(--text);

    text-decoration: none;
}

.pitch-action-item:hover {
    background: #FFFFFF;
}

.pitch-action-item div > span,
.pitch-action-item div > strong {
    display: block;
}

.pitch-action-item div > span {
    color: var(--text-secondary);

    font-size: 6px;
    font-weight: 800;
    letter-spacing: .55px;
}

.pitch-action-item div > strong {
    margin-top: 5px;

    font-size: 12px;
}

.pitch-action-item p {
    margin: 5px 0 0;

    color: var(--text-secondary);

    font-size: 7px;
    line-height: 1.35;
}

.pitch-action-item b {
    position: absolute;
    top: 50%;
    right: 13px;

    transform: translateY(-50%);

    color: var(--text-secondary);

    font-size: 11px;
}

.pitch-action-item.urgent {
    border-left: 2px solid var(--danger);
}


/* OPERATIONAL LEDGER */

.pitch-ledger {
    margin-top: 18px;

    border: 1px solid var(--border);

    background: #FFFFFF;
}

.pitch-ledger-toolbar {
    min-height: 54px;

    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;

    padding: 0 14px;

    border-bottom: 1px solid var(--border);
}

.pitch-ledger-tabs {
    display: flex;
    align-items: stretch;

    height: 53px;
}

.pitch-ledger-tabs a {
    display: inline-flex;
    align-items: center;

    padding: 0 12px;

    border-bottom: 2px solid transparent;

    color: var(--text-secondary);

    font-size: 8px;
    font-weight: 650;

    text-decoration: none;
}

.pitch-ledger-tabs a.active {
    border-bottom-color: var(--blue);

    color: var(--text);
}

.pitch-ledger-toolbar form {
    width: min(320px, 38%);
}

.pitch-ledger-toolbar input {
    width: 100%;
    height: 32px;

    padding: 0 10px;

    border: 1px solid var(--border);
    border-radius: 4px;

    background: #FFFFFF;

    font: inherit;
    font-size: 8px;
}

.pitch-ledger-table-wrap {
    overflow-x: auto;
}

.pitch-ledger-table {
    width: 100%;

    border-collapse: collapse;
}

.pitch-ledger-table th {
    height: 34px;

    padding: 0 12px;

    border-bottom: 1px solid var(--border);

    background: #FAFAF9;
    color: var(--text-secondary);

    font-size: 6px;
    font-weight: 800;
    letter-spacing: .4px;

    text-align: left;

    white-space: nowrap;
}

.pitch-ledger-table td {
    min-height: 50px;

    padding: 10px 12px;

    border-bottom: 1px solid #ECEDEF;

    font-size: 8px;

    vertical-align: middle;
}

.pitch-ledger-table tr:last-child td {
    border-bottom: 0;
}

.pitch-ledger-table tbody tr:hover {
    background: #FBFCFE;
}

.pitch-row-urgent td:first-child {
    border-left: 2px solid var(--danger);
}

.pitch-row-closing td:first-child {
    border-left: 2px solid #D99A28;
}

.pitch-row-upcoming td:first-child {
    border-left: 2px solid var(--blue);
}

.pitch-ledger-title {
    padding: 0;

    border: 0;

    background: transparent;
    color: var(--text);

    text-align: left;

    cursor: pointer;
}

.pitch-ledger-title strong,
.pitch-ledger-title span {
    display: block;
}

.pitch-ledger-title strong {
    font-size: 9px;
}

.pitch-ledger-title span {
    margin-top: 3px;

    color: var(--text-secondary);

    font-size: 6px;
}

.pitch-window-cell {
    white-space: nowrap;
}

.pitch-window-cell span {
    margin: 0 4px;

    color: #A2A6AB;
}

.pitch-days {
    font-size: 12px;
}

.pitch-days-urgent {
    color: var(--danger);
}

.pitch-days-closing {
    color: var(--warning);
}

.pitch-ledger-status {
    display: inline-flex;

    padding: 4px 6px;

    border-radius: 3px;

    background: #EFF7F3;
    color: var(--success);

    font-size: 6px;
    font-weight: 750;
}

.pitch-ledger-status.urgent {
    background: #FFF0F0;
    color: var(--danger);
}

.pitch-ledger-status.closing {
    background: #FFF6E2;
    color: var(--warning);
}

.pitch-ledger-status.upcoming {
    background: #EEF4FF;
    color: var(--blue);
}

.pitch-ledger-status.closed {
    background: #F0F1F2;
    color: #747980;
}

.pitch-updated {
    color: var(--text-secondary);

    font-size: 7px;
}

.pitch-updated small {
    display: block;

    margin-top: 2px;

    font-size: 6px;
}

.pitch-row-arrow {
    border: 0;

    background: transparent;
    color: var(--blue);

    font-size: 11px;

    cursor: pointer;
}

.pitch-muted {
    color: #A0A4A9;
}

.pitch-ops-empty {
    padding: 32px !important;

    color: var(--text-secondary);

    font-size: 9px;

    text-align: center;
}


/* DRAWER */

.pitch-ops-overlay {
    position: fixed;
    inset: 0;

    z-index: 120;

    background: rgba(13, 17, 23, .18);

    opacity: 0;
    pointer-events: none;

    transition: opacity .16s ease;
}

.pitch-ops-overlay.open {
    opacity: 1;
    pointer-events: auto;
}

.pitch-ops-drawer {
    position: fixed;
    top: 0;
    right: 0;
    bottom: 0;

    z-index: 130;

    width: min(430px, 92vw);

    padding: 22px;

    border-left: 1px solid var(--border);

    background: #FFFFFF;

    box-shadow: -12px 0 40px rgba(0, 0, 0, .07);

    transform: translateX(101%);

    transition: transform .2s ease;

    overflow-y: auto;
}

.pitch-ops-drawer.open {
    transform: translateX(0);
}

.pitch-drawer-head {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 15px;

    padding-bottom: 17px;

    border-bottom: 1px solid var(--border);
}

.pitch-drawer-head span {
    color: var(--text-secondary);

    font-size: 7px;
    font-weight: 750;
}

.pitch-drawer-head h2 {
    margin: 5px 0 0;

    font-size: 19px;
}

.pitch-drawer-head button {
    width: 30px;
    height: 30px;

    border: 1px solid var(--border);
    border-radius: 4px;

    background: #FFFFFF;

    font-size: 17px;

    cursor: pointer;
}

.pitch-drawer-status {
    padding: 18px 0;

    border-bottom: 1px solid var(--border);
}

.pitch-drawer-status span,
.pitch-drawer-status strong {
    display: block;
}

.pitch-drawer-status span,
.pitch-drawer-label,
.pitch-drawer-grid span {
    color: var(--text-secondary);

    font-size: 6px;
    font-weight: 800;
    letter-spacing: .5px;
}

.pitch-drawer-status strong {
    margin-top: 4px;

    font-size: 14px;
}

.pitch-drawer-section {
    padding: 18px 0;

    border-bottom: 1px solid var(--border);
}

.pitch-drawer-window {
    display: grid;
    grid-template-columns:
        auto
        1fr
        auto;

    align-items: center;
    gap: 10px;

    margin-top: 14px;
}

.pitch-drawer-window span,
.pitch-drawer-window strong {
    display: block;
}

.pitch-drawer-window span {
    color: var(--text-secondary);

    font-size: 6px;
}

.pitch-drawer-window strong {
    margin-top: 3px;

    font-size: 9px;
}

.pitch-drawer-line {
    height: 1px;

    background: #D9DBDE;
}

.pitch-drawer-days {
    margin-top: 15px;
}

.pitch-drawer-days strong {
    font-size: 21px;
}

.pitch-drawer-days span {
    margin-left: 5px;

    color: var(--text-secondary);

    font-size: 8px;
}

.pitch-drawer-grid {
    display: grid;

    grid-template-columns:
        repeat(2, minmax(0, 1fr));

    gap: 18px 14px;

    padding: 19px 0;
}

.pitch-drawer-grid strong {
    display: block;

    margin-top: 5px;

    font-size: 9px;
    line-height: 1.4;
}

.pitch-drawer-note {
    padding: 11px;

    border-left: 2px solid #D7D9DC;

    background: #F7F7F6;
    color: var(--text-secondary);

    font-size: 7px;
    line-height: 1.5;
}

.pitch-drawer-actions {
    display: flex;
    justify-content: space-between;
    gap: 10px;

    margin-top: 22px;
}



/* =========================================================
   SEARCH ACTIVITY INVESTIGATION CONSOLE
========================================================= */

.search-console-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 30px;
}


/* DAY NAV */

.search-console-daynav {
    min-height: 48px;

    display: grid;

    grid-template-columns:
        120px
        minmax(0, 1fr)
        120px;

    align-items: center;

    margin-top: 20px;

    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
}

.search-console-daynav a {
    color: var(--blue);

    font-size: 8px;
    font-weight: 650;

    text-decoration: none;
}

.search-console-daynav > a:last-child,
.search-console-daynav > span:last-child {
    text-align: right;
}

.search-console-daynav > div {
    text-align: center;
}

.search-console-daynav div span,
.search-console-daynav div strong {
    display: block;
}

.search-console-daynav div span {
    color: var(--text-secondary);

    font-size: 6px;
    font-weight: 800;
    letter-spacing: .5px;
}

.search-console-daynav div strong {
    margin-top: 3px;

    font-size: 10px;
}

.search-console-daynav .disabled {
    color: #B3B6BA;

    font-size: 8px;
}


/* DATA STRIP */

.search-console-strip {
    display: flex;

    margin-top: 14px;

    border: 1px solid var(--border);

    background: #FFFFFF;
}

.search-console-strip > div {
    flex: 1;

    min-height: 57px;

    display: flex;
    align-items: baseline;
    gap: 7px;

    padding: 16px 14px;

    border-right: 1px solid var(--border);
}

.search-console-strip > div:last-child {
    border-right: 0;
}

.search-console-strip strong {
    font-size: 18px;
}

.search-console-strip span {
    color: var(--text-secondary);

    font-size: 7px;
}

.search-console-strip .danger {
    color: var(--danger);
}


/* TIMELINE */

.search-console-timeline {
    margin-top: 14px;
    padding: 15px 16px 17px;

    border: 1px solid var(--border);

    background: #FFFFFF;
}

.search-console-timeline-heading strong,
.search-console-timeline-heading span {
    display: block;
}

.search-console-timeline-heading strong {
    font-size: 10px;
}

.search-console-timeline-heading span {
    margin-top: 3px;

    color: var(--text-secondary);

    font-size: 7px;
}

.search-time-scale {
    display: flex;
    justify-content: space-between;

    margin-top: 19px;

    color: var(--text-secondary);

    font-size: 6px;
}

.search-time-line {
    position: relative;

    height: 54px;

    margin-top: 5px;

    border-top: 1px solid #D8DADD;
    border-bottom: 1px solid #ECEDEF;

    background: #FBFBFA;
}

.search-time-grid {
    position: absolute;
    top: 0;
    bottom: 0;

    width: 1px;

    background: #E8E9EB;
}

.search-time-grid.q1 {
    left: 25%;
}

.search-time-grid.q2 {
    left: 50%;
}

.search-time-grid.q3 {
    left: 75%;
}

.search-time-event {
    position: absolute;
    top: 50%;

    width: 10px;
    height: 10px;

    padding: 0;

    transform:
        translate(-50%, -50%);

    border: 2px solid #FFFFFF;
    border-radius: 50%;

    background: var(--blue);

    box-shadow:
        0 0 0 1px var(--blue);

    cursor: default;
}

.search-time-event.event-client_match {
    background: #D79A20;

    box-shadow:
        0 0 0 1px #D79A20;
}

.search-time-event.event-filter {
    background: #7559B8;

    box-shadow:
        0 0 0 1px #7559B8;
}

.search-time-event.event-suggestion_click {
    background: var(--success);

    box-shadow:
        0 0 0 1px var(--success);
}

.search-console-no-events {
    margin-top: 10px;

    color: var(--text-secondary);

    font-size: 7px;
}


/* MAIN INVESTIGATION GRID */

.search-console-grid {
    display: grid;

    grid-template-columns:
        minmax(0, 1fr)
        270px;

    gap: 0;

    margin-top: 14px;

    border: 1px solid var(--border);

    background: #FFFFFF;
}


/* STREAM */

.search-event-stream {
    min-width: 0;

    border-right: 1px solid var(--border);
}

.search-stream-heading {
    min-height: 58px;

    display: flex;
    align-items: center;
    justify-content: space-between;

    padding: 13px 16px;

    border-bottom: 1px solid var(--border);
}

.search-stream-heading strong,
.search-stream-heading span {
    display: block;
}

.search-stream-heading strong {
    font-size: 11px;
}

.search-stream-heading div > span {
    margin-top: 3px;

    color: var(--text-secondary);

    font-size: 7px;
}

.search-real-data {
    color: var(--success);

    font-size: 6px !important;
    font-weight: 800;
}

.search-stream-event {
    position: relative;

    display: grid;

    grid-template-columns:
        76px
        18px
        minmax(0, 1fr);

    padding: 16px;

    border-bottom: 1px solid #ECEDEF;
}

.search-stream-event:last-child {
    border-bottom: 0;
}

.search-stream-time {
    padding-top: 2px;

    color: var(--text-secondary);

    font-size: 7px;
}

.search-stream-marker {
    position: relative;
}

.search-stream-marker::before {
    content: "";

    position: absolute;
    top: 4px;
    left: 4px;

    width: 7px;
    height: 7px;

    border-radius: 50%;

    background: var(--blue);
}

.search-stream-marker::after {
    content: "";

    position: absolute;
    top: 14px;
    bottom: -31px;
    left: 7px;

    width: 1px;

    background: #E1E3E5;
}

.search-stream-event:last-child
.search-stream-marker::after {
    display: none;
}

.search-stream-marker.event-client_match::before {
    background: #D79A20;
}

.search-stream-marker.event-filter::before {
    background: #7559B8;
}

.search-stream-marker.event-suggestion_click::before {
    background: var(--success);
}

.search-stream-content {
    padding-left: 8px;
}

.search-stream-event-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
}

.search-stream-event-head > strong {
    color: var(--text-secondary);

    font-size: 7px;
    font-weight: 600;
}

.search-stream-type {
    color: var(--blue);

    font-size: 6px;
    font-weight: 800;
    letter-spacing: .45px;
}

.search-stream-type.matcher {
    color: #A86E00;
}

.search-stream-type.filter {
    color: #6750A4;
}

.search-stream-type.suggestion {
    color: var(--success);
}

.search-stream-content h3 {
    margin: 7px 0 0;

    font-size: 12px;
    line-height: 1.35;
}

.search-stream-outcome {
    display: inline-flex;

    margin-top: 9px;
    padding: 5px 7px;

    border-radius: 3px;

    font-size: 7px;
}

.search-stream-outcome.success {
    background: #EFF8F3;
    color: var(--success);
}

.search-stream-outcome.gap {
    background: #FFF1F1;
    color: var(--danger);
}

.search-stream-metrics {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;

    margin-top: 10px;
}

.search-stream-metrics span {
    padding: 6px 8px;

    border: 1px solid var(--border);

    background: #FAFAF9;
    color: var(--text-secondary);

    font-size: 7px;
}

.search-stream-metrics strong {
    color: var(--text);
}

.search-stream-privacy {
    margin-top: 9px;

    color: var(--text-secondary);

    font-size: 7px;
    line-height: 1.4;
}

.search-stream-filter-list {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;

    margin-top: 9px;
}

.search-stream-filter-list span {
    padding: 5px 7px;

    background: #F3F4F5;

    font-size: 7px;
}

.search-stream-selected {
    display: flex;
    align-items: center;
    justify-content: space-between;

    margin-top: 11px;
    padding-top: 9px;

    border-top: 1px solid var(--border);
}

.search-stream-selected span {
    color: var(--text-secondary);

    font-size: 6px;
    font-weight: 800;
}

.search-stream-selected a {
    color: var(--blue);

    font-size: 8px;
    font-weight: 650;

    text-decoration: none;
}

.search-stream-empty {
    padding: 50px 20px;

    text-align: center;
}

.search-stream-empty strong {
    font-size: 12px;
}

.search-stream-empty p {
    max-width: 420px;

    margin: 7px auto 0;

    color: var(--text-secondary);

    font-size: 8px;
    line-height: 1.5;
}


/* DAY INTELLIGENCE */

.search-day-intelligence {
    background: #FAFAF9;
}

.search-intelligence-title {
    min-height: 58px;

    padding: 13px 15px;

    border-bottom: 1px solid var(--border);
}

.search-intelligence-title strong,
.search-intelligence-title span {
    display: block;
}

.search-intelligence-title strong {
    font-size: 11px;
}

.search-intelligence-title span {
    margin-top: 3px;

    color: var(--text-secondary);

    font-size: 7px;
}

.search-intelligence-primary {
    padding: 19px 15px;

    border-bottom: 1px solid var(--border);
}

.search-intelligence-primary span,
.search-intelligence-primary strong {
    display: block;
}

.search-intelligence-primary span,
.search-intelligence-section span {
    color: var(--text-secondary);

    font-size: 6px;
    font-weight: 800;
    letter-spacing: .45px;
}

.search-intelligence-primary strong {
    margin-top: 5px;

    font-size: 27px;
}

.search-intelligence-row {
    display: flex;
    align-items: center;
    justify-content: space-between;

    min-height: 38px;

    padding: 0 15px;

    border-bottom: 1px solid #E8E9EB;
}

.search-intelligence-row span {
    color: var(--text-secondary);

    font-size: 7px;
}

.search-intelligence-row strong {
    font-size: 10px;
}

.search-intelligence-row strong.danger {
    color: var(--danger);
}

.search-intelligence-section {
    padding: 14px 15px;

    border-bottom: 1px solid var(--border);
}

.search-intelligence-section span,
.search-intelligence-section strong {
    display: block;
}

.search-intelligence-section strong {
    margin-top: 5px;

    font-size: 10px;
}

.search-intelligence-note {
    margin: 15px;

    padding: 10px;

    border-left: 2px solid #D6D8DB;

    background: #FFFFFF;
    color: var(--text-secondary);

    font-size: 7px;
    line-height: 1.5;
}


/* =========================================================
   RESPONSIVE
========================================================= */

@media (max-width: 1050px) {

    .pitch-ops-command-grid,
    .search-console-grid {
        grid-template-columns: 1fr;
    }

    .pitch-ops-timeline-panel,
    .search-event-stream {
        border-right: 0;
        border-bottom: 1px solid var(--border);
    }

    .pitch-ops-action-queue {
        display: grid;

        grid-template-columns:
            repeat(2, minmax(0, 1fr));
    }

    .pitch-ops-action-queue
    .pitch-ops-panel-heading {
        grid-column: 1 / -1;
    }

    .pitch-action-item {
        border-right: 1px solid var(--border);
    }

    .search-day-intelligence {
        display: grid;

        grid-template-columns:
            repeat(3, minmax(0, 1fr));
    }

    .search-intelligence-title {
        grid-column: 1 / -1;
    }

}


@media (max-width: 760px) {

    .pitch-ops-header,
    .search-console-header {
        flex-direction: column;
    }

    .pitch-ops-status-strip,
    .search-console-strip {
        display: grid;

        grid-template-columns:
            repeat(2, minmax(0, 1fr));
    }

    .pitch-ops-status-strip a,
    .search-console-strip > div {
        border-bottom: 1px solid var(--border);
    }

    .pitch-ops-axis {
        margin-left: 135px;
        margin-right: 65px;
    }

    .pitch-timeline-row {
        grid-template-columns:
            135px
            minmax(0, 1fr)
            65px;
    }

    .pitch-ops-action-queue {
        grid-template-columns: 1fr;
    }

    .pitch-ledger-toolbar {
        align-items: stretch;
        flex-direction: column;

        padding-top: 10px;
        padding-bottom: 10px;
    }

    .pitch-ledger-tabs {
        overflow-x: auto;
    }

    .pitch-ledger-toolbar form {
        width: 100%;
    }

    .search-day-intelligence {
        grid-template-columns:
            repeat(2, minmax(0, 1fr));
    }

}


@media (max-width: 520px) {

    .search-stream-event {
        grid-template-columns:
            58px
            16px
            minmax(0, 1fr);
    }

    .search-day-intelligence {
        grid-template-columns: 1fr;
    }

}


/* =========================================================
   BHARATNXT READABILITY SYSTEM
   Clear operational typography
========================================================= */

html,
body,
button,
input,
select,
textarea {
    font-family:
        "Segoe UI",
        Inter,
        Roboto,
        Arial,
        sans-serif !important;
}


body {
    font-size: 14px;

    font-weight: 400;

    line-height: 1.45;

    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
}


h1,
h2,
h3,
h4,
h5,
h6 {
    font-family: inherit !important;

    font-weight: 700;

    letter-spacing: -.015em;
}



/* =========================================================
   GLOBAL TOP SEARCH
========================================================= */

.topbar {
    display: flex !important;

    align-items: center;

    gap: 20px;

    padding-left: 28px;
    padding-right: 28px;

    overflow: visible;
}


.global-search {
    width: 100% !important;

    max-width: 720px !important;
    min-width: 280px;

    flex:
        1 1 720px !important;

    display: grid !important;

    grid-template-columns:
        auto
        minmax(0, 1fr)
        auto;

    align-items: center;

    margin: 0 !important;

    overflow: hidden;
}


.global-search input {
    width: 100% !important;
    min-width: 0 !important;

    font-size: 13px !important;
}


.global-search .search-icon {
    flex-shrink: 0;
}


.global-search .search-shortcut {
    position: static !important;

    margin-left: 10px;

    white-space: nowrap;

    font-size: 12px !important;
}


.topbar-actions {
    flex:
        0 0 auto !important;

    margin-left: auto !important;

    white-space: nowrap;
}


@media (max-width: 1100px) {

    .global-search {
        max-width: none !important;
    }


    .global-search
    .search-shortcut {
        display: none !important;
    }

}



/* =========================================================
   DAILY BDE ACTIVITY — READABILITY
========================================================= */

.search-console-header h1 {
    font-size: 30px !important;

    font-weight: 700;
}


.search-console-header p {
    font-size: 13px !important;

    line-height: 1.5;
}


.search-console-daynav a,
.search-console-daynav .disabled {
    font-size: 12px !important;
}


.search-console-daynav div span {
    font-size: 10px !important;
}


.search-console-daynav div strong {
    font-size: 14px !important;
}


.search-console-strip {
    min-height: 72px;
}


.search-console-strip > div {
    min-height: 72px;

    padding: 18px 16px;
}


.search-console-strip strong {
    font-size: 24px !important;

    font-weight: 700;
}


.search-console-strip span {
    font-size: 11px !important;

    line-height: 1.25;
}


.search-console-timeline {
    padding: 20px;
}


.search-console-timeline-heading strong {
    font-size: 15px !important;
}


.search-console-timeline-heading span {
    margin-top: 4px;

    font-size: 12px !important;
}


.search-time-scale {
    font-size: 10px !important;
}


.search-stream-heading {
    min-height: 68px;

    padding:
        16px 18px;
}


.search-stream-heading strong {
    font-size: 16px !important;
}


.search-stream-heading
div > span {
    margin-top: 4px;

    font-size: 11px !important;
}


.search-real-data {
    font-size: 10px !important;
}


.search-stream-event {
    grid-template-columns:
        90px
        20px
        minmax(0, 1fr);

    padding: 20px 18px;
}


.search-stream-time {
    font-size: 11px !important;

    font-weight: 600;
}


.search-stream-type {
    font-size: 10px !important;

    letter-spacing: .35px;
}


.search-stream-event-head
> strong {
    font-size: 11px !important;
}


.search-stream-content h3 {
    margin-top: 8px;

    font-size: 16px !important;

    font-weight: 650;
}


.search-stream-outcome {
    margin-top: 10px;

    padding: 7px 9px;

    font-size: 11px !important;
}


.search-stream-metrics {
    gap: 8px;

    margin-top: 12px;
}


.search-stream-metrics span {
    padding: 8px 10px;

    font-size: 11px !important;
}


.search-stream-privacy {
    margin-top: 11px;

    font-size: 11px !important;

    line-height: 1.5;
}


.search-stream-filter-list span {
    font-size: 11px !important;
}


.search-stream-selected span {
    font-size: 10px !important;
}


.search-stream-selected a {
    font-size: 12px !important;
}



/* =========================================================
   DAY SUMMARY
========================================================= */

.search-intelligence-title {
    min-height: 68px;

    padding: 16px;
}


.search-intelligence-title strong {
    font-size: 16px !important;
}


.search-intelligence-title span {
    font-size: 11px !important;
}


.search-intelligence-primary {
    padding: 22px 16px;
}


.search-intelligence-primary span,
.search-intelligence-section span {
    font-size: 10px !important;

    letter-spacing: .4px;
}


.search-intelligence-primary strong {
    font-size: 32px !important;
}


.search-intelligence-row {
    min-height: 48px;

    padding:
        0 16px;
}


.search-intelligence-row span {
    font-size: 12px !important;
}


.search-intelligence-row strong {
    font-size: 14px !important;
}


.search-intelligence-section {
    padding: 17px 16px;
}


.search-intelligence-section strong {
    margin-top: 6px;

    font-size: 14px !important;
}


.search-intelligence-note {
    padding: 12px;

    font-size: 11px !important;

    line-height: 1.5;
}



/* =========================================================
   PITCH OPERATIONS DRAWER READABILITY
========================================================= */

.pitch-ops-drawer {
    font-size: 13px;
}


.pitch-drawer-head > div > span {
    font-size: 10px !important;
}


.pitch-drawer-head h2 {
    margin-top: 7px;

    font-size: 24px !important;

    line-height: 1.25;
}


.pitch-drawer-status {
    padding:
        20px 0;
}


.pitch-drawer-status span,
.pitch-drawer-label,
.pitch-drawer-grid span {
    font-size: 10px !important;

    letter-spacing: .4px;
}


.pitch-drawer-status strong {
    margin-top: 6px;

    font-size: 18px !important;
}


.pitch-drawer-window {
    margin-top: 16px;
}


.pitch-drawer-window span {
    font-size: 11px !important;
}


.pitch-drawer-window strong {
    margin-top: 5px;

    font-size: 13px !important;

    line-height: 1.35;
}


.pitch-drawer-days strong {
    font-size: 27px !important;
}


.pitch-drawer-days span {
    font-size: 11px !important;
}


.pitch-drawer-grid {
    gap:
        22px 18px;
}


.pitch-drawer-grid strong {
    margin-top: 7px;

    font-size: 13px !important;

    line-height: 1.45;
}


.pitch-drawer-note {
    padding: 13px;

    font-size: 11px !important;

    line-height: 1.55;
}



/* =========================================================
   PITCH OPERATIONS MAIN PAGE READABILITY
========================================================= */

.pitch-ops-header p {
    font-size: 13px !important;
}


.pitch-ops-status-strip span {
    font-size: 11px !important;
}


.pitch-ops-panel-heading strong {
    font-size: 15px !important;
}


.pitch-ops-panel-heading
div > span {
    font-size: 11px !important;
}


.pitch-ops-live {
    font-size: 9px !important;
}


.pitch-axis-marker span {
    font-size: 9px !important;
}


.pitch-timeline-name strong {
    font-size: 12px !important;
}


.pitch-timeline-name span {
    font-size: 9px !important;
}


.pitch-timeline-end strong {
    font-size: 13px !important;
}


.pitch-timeline-end span {
    font-size: 9px !important;
}


.pitch-action-item
div > span {
    font-size: 9px !important;
}


.pitch-action-item
div > strong {
    font-size: 14px !important;
}


.pitch-action-item p {
    font-size: 11px !important;

    line-height: 1.45;
}


.pitch-ledger-tabs a {
    font-size: 11px !important;
}


.pitch-ledger-toolbar input {
    font-size: 12px !important;
}


.pitch-ledger-table th {
    font-size: 9px !important;
}


.pitch-ledger-table td {
    font-size: 11px !important;
}


.pitch-ledger-title strong {
    font-size: 12px !important;
}


.pitch-ledger-title span {
    font-size: 9px !important;
}


.pitch-ledger-status {
    font-size: 9px !important;
}


.pitch-updated {
    font-size: 10px !important;
}


.pitch-updated small {
    font-size: 9px !important;
}



/* =========================================================
   SERVICE DETAIL PITCH INFORMATION
========================================================= */

.service-pitch-banner-label {
    font-size: 10px !important;
}


.service-pitch-banner
> div > strong {
    font-size: 15px !important;
}


.service-pitch-banner-dates {
    font-size: 11px !important;
}



/* =========================================================
   BDE NAVIGATION — CLEAR TOOL NAMES
========================================================= */

.sidebar .nav-item {
    font-size: 13px;
}


.sidebar .nav-section-label {
    font-size: 10px;
}



/* =========================================================
   SMALL SCREEN TOPBAR
========================================================= */

@media (max-width: 850px) {

    .topbar {
        gap: 10px;

        padding-left: 14px;
        padding-right: 14px;
    }


    .global-search {
        min-width: 160px;
    }


    .security-status {
        display: none;
    }

}


/* =========================================================
   BHARATNXT TYPOGRAPHY V3
   Large / clean / thin enterprise UI
========================================================= */

:root {

    --bn-font:
        "Segoe UI Variable Text",
        "Segoe UI",
        Arial,
        Helvetica,
        sans-serif;
}


html,
body,
input,
select,
textarea,
button {
    font-family:
        var(--bn-font) !important;
}


body {
    font-size: 14px !important;

    font-weight: 400 !important;

    line-height: 1.45;

    letter-spacing: 0;

    -webkit-font-smoothing:
        antialiased;

    text-rendering:
        optimizeLegibility;
}


h1,
h2,
h3,
h4,
h5,
h6 {
    font-family:
        var(--bn-font) !important;

    font-weight: 600 !important;

    letter-spacing: -.015em !important;
}


.page-title {
    font-size: 28px !important;

    font-weight: 600 !important;
}


.page-description {
    font-size: 13px !important;

    font-weight: 400 !important;

    line-height: 1.55 !important;
}


.page-eyebrow {
    font-size: 10px !important;

    font-weight: 600 !important;

    letter-spacing: .6px !important;
}


/* SIDEBAR */

.sidebar {
    font-family:
        var(--bn-font) !important;
}


.brand-name {
    font-size: 15px !important;

    font-weight: 600 !important;
}


.nav-section-label {
    font-size: 10px !important;

    font-weight: 500 !important;

    letter-spacing: .6px;
}


.nav-item {
    min-height: 42px;

    font-size: 14px !important;

    font-weight: 400 !important;
}


.nav-item.active {
    font-weight: 500 !important;
}


/* BUTTONS */

.primary-button,
.secondary-button,
.topbar-logout-button {
    font-family:
        var(--bn-font) !important;

    font-size: 13px !important;

    font-weight: 500 !important;
}


/* FORM CONTROLS */

input,
select,
textarea {
    font-size: 13px !important;

    font-weight: 400 !important;
}


label {
    font-family:
        var(--bn-font) !important;
}


/* TABLES */

.content table th {
    font-family:
        var(--bn-font) !important;

    font-size: 11px !important;

    font-weight: 600 !important;

    letter-spacing: .15px !important;
}


.content table td {
    font-family:
        var(--bn-font) !important;

    font-size: 13px !important;

    font-weight: 400 !important;
}


.table-title {
    font-size: 16px !important;

    font-weight: 600 !important;
}


.table-subtitle {
    font-size: 12px !important;

    font-weight: 400 !important;

    line-height: 1.45 !important;
}


/* COMMON SMALL METADATA — NEVER MICROSCOPIC */

.stat-label,
.toolkit-service-id,
.matcher-call-label,
.service-pitch-banner-label,
.pitch-drawer-label,
.search-stream-type,
.real-data-label,
.search-real-data,
.pitch-ops-live {
    font-size: 10px !important;

    font-weight: 600 !important;
}


.muted-text,
.search-kpi-meta {
    font-size: 11px !important;
}



/* =========================================================
   GLOBAL TOP SEARCH — FIX WIDTH / CUTTING
========================================================= */

.main-area {
    min-width: 0;
}


.topbar:has(.global-search) {
    display: grid !important;

    grid-template-columns:
        minmax(360px, 1fr)
        auto !important;

    align-items: center;

    gap: 26px !important;

    padding-left: 28px !important;
    padding-right: 28px !important;
}


.global-search {
    width: 100% !important;

    max-width: 760px !important;

    min-width: 0 !important;

    display: grid !important;

    grid-template-columns:
        auto
        minmax(0, 1fr)
        auto !important;

    align-items: center;

    margin: 0 !important;

    overflow: hidden;
}


.global-search input {
    width: 100% !important;

    min-width: 0 !important;

    padding-right: 10px !important;

    font-size: 14px !important;
}


.global-search .search-shortcut {
    position: static !important;

    margin-left: 8px;

    font-size: 12px !important;

    white-space: nowrap;
}


.topbar-actions {
    flex-shrink: 0;

    margin-left: auto;
}


@media (max-width: 1050px) {

    .topbar:has(.global-search) {
        grid-template-columns:
            minmax(220px, 1fr)
            auto !important;

        gap: 12px !important;
    }


    .global-search
    .search-shortcut {
        display: none !important;
    }

}



/* =========================================================
   HOME DASHBOARD V2
========================================================= */

.bn-home-v2 {
    padding-bottom: 48px;
}


/* HEADER */

.bn-home-header {
    display: flex;

    align-items: center;
    justify-content: space-between;

    gap: 24px;

    margin-bottom: 18px;
}


.bn-home-header h1 {
    margin: 0;

    font-size: 28px;

    font-weight: 600 !important;
}


.bn-home-header p {
    margin: 5px 0 0;

    color: var(--text-secondary);

    font-size: 13px;

    font-weight: 400;
}


/* =========================================================
   DATE PICKER
========================================================= */

.bn-date-picker {
    position: relative;

    z-index: 20;
}


.bn-date-picker > summary {
    min-height: 40px;

    display: flex;

    align-items: center;

    gap: 8px;

    padding: 0 12px;

    border: 1px solid var(--border);

    border-radius: 7px;

    background: #FFFFFF;

    color: var(--text);

    font-size: 12px;

    font-weight: 500;

    cursor: pointer;

    list-style: none;
}


.bn-date-picker > summary::-webkit-details-marker {
    display: none;
}


.bn-date-icon {
    color: var(--blue);
}


.bn-date-chevron {
    color: var(--text-secondary);
}


.bn-date-popover {
    position: absolute;

    top: calc(100% + 8px);
    right: 0;

    width: 330px;

    padding: 17px;

    border: 1px solid var(--border);

    border-radius: 8px;

    background: #FFFFFF;

    box-shadow:
        0 14px 35px
        rgba(19, 24, 32, .10);
}


.bn-date-popover-title strong,
.bn-date-popover-title span {
    display: block;
}


.bn-date-popover-title strong {
    font-size: 15px;

    font-weight: 600;
}


.bn-date-popover-title span {
    margin-top: 3px;

    color: var(--text-secondary);

    font-size: 11px;
}


.bn-date-presets {
    display: grid;

    grid-template-columns:
        repeat(3, 1fr);

    gap: 6px;

    margin-top: 14px;
}


.bn-date-presets a {
    padding: 8px 5px;

    border: 1px solid var(--border);

    border-radius: 5px;

    color: var(--text);

    font-size: 11px;

    text-align: center;

    text-decoration: none;
}


.bn-date-presets a:hover {
    border-color: #B6CAFF;

    background: #F4F7FF;

    color: var(--blue);
}


.bn-date-popover form {
    display: grid;

    grid-template-columns:
        repeat(2, 1fr);

    gap: 10px;

    margin-top: 15px;
}


.bn-date-popover form label span {
    display: block;

    margin-bottom: 5px;

    color: var(--text-secondary);

    font-size: 11px;
}


.bn-date-popover input {
    width: 100%;

    height: 38px;

    padding: 0 8px;

    border: 1px solid var(--border);

    border-radius: 5px;
}


.bn-date-popover button {
    grid-column: 1 / -1;

    margin-top: 2px;
}


/* =========================================================
   KPI STRIP
========================================================= */

.bn-home-kpi-strip {
    display: grid;

    grid-template-columns:
        repeat(5, minmax(0, 1fr));

    margin-bottom: 18px;

    border: 1px solid var(--border);

    border-radius: 8px;

    background: #FFFFFF;

    overflow: hidden;
}


.bn-home-kpi {
    min-height: 82px;

    display: flex;

    align-items: center;

    gap: 12px;

    padding: 14px 16px;

    border-right: 1px solid var(--border);

    color: var(--text);

    text-decoration: none;
}


.bn-home-kpi:last-child {
    border-right: 0;
}


a.bn-home-kpi:hover {
    background: #FAFBFD;
}


.bn-kpi-icon {
    width: 35px;
    height: 35px;

    flex-shrink: 0;

    display: flex;

    align-items: center;
    justify-content: center;

    border-radius: 50%;

    background: #EDF4FF;

    color: var(--blue);

    font-size: 14px;
}


.bn-kpi-icon.warning {
    background: #FFF6E4;

    color: var(--warning);
}


.bn-home-kpi span,
.bn-home-kpi strong {
    display: block;
}


.bn-home-kpi span {
    color: var(--text-secondary);

    font-size: 12px;

    font-weight: 400;
}


.bn-home-kpi strong {
    margin-top: 3px;

    font-size: 22px;

    font-weight: 600;
}


/* =========================================================
   PANELS
========================================================= */

.bn-dashboard-panel {
    border: 1px solid var(--border);

    border-radius: 8px;

    background: #FFFFFF;

    overflow: hidden;
}


.bn-panel-header {
    min-height: 70px;

    display: flex;

    align-items: center;
    justify-content: space-between;

    gap: 16px;

    padding: 15px 18px;

    border-bottom: 1px solid var(--border);
}


.bn-panel-header h2 {
    margin: 0;

    font-size: 17px;

    font-weight: 600 !important;
}


.bn-panel-header p {
    margin: 4px 0 0;

    color: var(--text-secondary);

    font-size: 12px;

    font-weight: 400;
}


.bn-panel-link {
    color: var(--blue);

    font-size: 12px;

    font-weight: 500;

    text-decoration: none;

    white-space: nowrap;
}


/* =========================================================
   MAIN GRID
========================================================= */

.bn-home-main-grid {
    display: grid;

    grid-template-columns:
        minmax(0, 1.65fr)
        minmax(300px, .75fr);

    gap: 16px;
}


/* =========================================================
   ACTIVITY CHART
========================================================= */

.bn-chart-legend {
    display: flex;

    gap: 14px;

    color: var(--text-secondary);

    font-size: 11px;
}


.bn-chart-legend span {
    display: flex;

    align-items: center;

    gap: 5px;
}


.bn-chart-legend i {
    width: 9px;
    height: 9px;

    display: inline-block;

    border-radius: 2px;
}


.bn-chart-legend
.search i {
    background: var(--blue);
}


.bn-chart-legend
.client i {
    background: #D79A20;
}


.bn-home-chart {
    min-height: 290px;

    display: flex;

    align-items: stretch;

    gap: 8px;

    padding:
        24px 18px
        16px;

    overflow-x: auto;
}


.bn-chart-day {
    min-width: 66px;

    flex: 1;

    display: flex;

    flex-direction: column;

    align-items: center;

    color: var(--text);

    text-decoration: none;
}


.bn-chart-day.inactive {
    opacity: .45;
}


.bn-chart-values {
    min-height: 24px;

    display: flex;

    gap: 4px;

    margin-bottom: 6px;
}


.bn-chart-values span {
    min-width: 24px;

    padding: 3px 4px;

    border-radius: 10px;

    background: #EEF4FF;

    color: var(--blue);

    font-size: 9px;

    text-align: center;
}


.bn-chart-values
span:last-child {
    background: #FFF4DE;

    color: #9E6900;
}


.bn-chart-track {
    width: 48px;

    height: 190px;

    display: flex;

    align-items: flex-end;

    justify-content: center;

    padding: 0 7px;

    border-radius: 5px;

    background: #F2F3F5;
}


.bn-chart-bars {
    width: 100%;
    height: 100%;

    display: flex;

    align-items: flex-end;

    justify-content: center;

    gap: 5px;
}


.bn-chart-bar {
    width: 12px;

    border-radius:
        3px 3px 0 0;
}


.bn-chart-bar.search {
    background: var(--blue);
}


.bn-chart-bar.client {
    background: #D79A20;
}


.bn-chart-day > strong {
    margin-top: 9px;

    font-size: 11px;

    font-weight: 500;
}


/* =========================================================
   CLOSING SOON
========================================================= */

.bn-closing-list {
    min-height: 290px;
}


.bn-closing-item {
    min-height: 58px;

    display: flex;

    align-items: center;
    justify-content: space-between;

    gap: 12px;

    padding: 11px 15px;

    border-bottom: 1px solid #ECEDEF;

    color: var(--text);

    text-decoration: none;
}


.bn-closing-item:last-child {
    border-bottom: 0;
}


.bn-closing-item:hover {
    background: #FBFCFE;
}


.bn-closing-item
div:first-child {
    min-width: 0;
}


.bn-closing-item
div:first-child strong,
.bn-closing-item
div:first-child span {
    display: block;
}


.bn-closing-item
div:first-child strong {
    overflow: hidden;

    font-size: 12px;

    font-weight: 500;

    text-overflow: ellipsis;
    white-space: nowrap;
}


.bn-closing-item
div:first-child span {
    margin-top: 3px;

    color: var(--text-secondary);

    font-size: 10px;
}


.bn-days-left {
    min-width: 44px;

    text-align: right;
}


.bn-days-left strong,
.bn-days-left span {
    display: block;
}


.bn-days-left strong {
    color: var(--warning);

    font-size: 18px;

    font-weight: 600;
}


.bn-days-left.urgent strong {
    color: var(--danger);
}


.bn-days-left span {
    color: var(--text-secondary);

    font-size: 9px;
}


/* =========================================================
   SECONDARY GRID
========================================================= */

.bn-home-secondary-grid {
    display: grid;

    grid-template-columns:
        minmax(0, 1.3fr)
        minmax(300px, .7fr);

    gap: 16px;

    margin-top: 16px;
}


/* RECENT */

.bn-recent-item {
    min-height: 62px;

    display: grid;

    grid-template-columns:
        72px
        125px
        minmax(0, 1fr)
        24px;

    align-items: center;

    gap: 10px;

    padding: 10px 16px;

    border-bottom: 1px solid #ECEDEF;
}


.bn-recent-item:last-child {
    border-bottom: 0;
}


.bn-recent-item time {
    color: var(--text-secondary);

    font-size: 11px;
}


.bn-recent-type span {
    display: inline-flex;

    padding: 4px 7px;

    border-radius: 12px;

    background: #F1F3F5;

    color: var(--text-secondary);

    font-size: 10px;

    font-weight: 500;
}


.bn-recent-type
span.search {
    background: #EDF4FF;

    color: var(--blue);
}


.bn-recent-type
span.client {
    background: #FFF4DE;

    color: #9E6900;
}


.bn-recent-detail {
    min-width: 0;
}


.bn-recent-detail strong,
.bn-recent-detail span {
    display: block;
}


.bn-recent-detail strong {
    overflow: hidden;

    font-size: 12px;

    font-weight: 500;

    text-overflow: ellipsis;
    white-space: nowrap;
}


.bn-recent-detail span {
    margin-top: 2px;

    color: var(--text-secondary);

    font-size: 10px;
}


.bn-recent-item > a {
    color: var(--blue);

    font-size: 14px;

    text-decoration: none;
}


/* MOST SEARCHED */

.bn-top-search-list > a {
    min-height: 56px;

    display: flex;

    align-items: center;
    justify-content: space-between;

    gap: 12px;

    padding: 10px 15px;

    border-bottom: 1px solid #ECEDEF;

    color: var(--text);

    text-decoration: none;
}


.bn-top-search-list > a:last-child {
    border-bottom: 0;
}


.bn-top-search-list > a:hover {
    background: #FBFCFE;
}


.bn-top-search-list
> a > div {
    min-width: 0;

    display: flex;

    align-items: center;

    gap: 10px;
}


.bn-search-rank {
    width: 23px;
    height: 23px;

    flex-shrink: 0;

    display: flex;

    align-items: center;
    justify-content: center;

    border-radius: 50%;

    background: #F1F3F5;

    color: var(--text-secondary);

    font-size: 10px;
}


.bn-top-search-list strong {
    overflow: hidden;

    font-size: 12px;

    font-weight: 500;

    text-overflow: ellipsis;
    white-space: nowrap;
}


.bn-top-search-list
> a > span {
    flex-shrink: 0;

    color: var(--text-secondary);

    font-size: 10px;
}


/* =========================================================
   SEARCH GAPS
========================================================= */

.bn-search-gap-panel {
    margin-top: 16px;
}


.bn-gap-total {
    min-width: 30px;
    height: 30px;

    display: inline-flex;

    align-items: center;
    justify-content: center;

    border-radius: 50%;

    background: #FFF0F0;

    color: var(--danger);

    font-size: 12px;

    font-weight: 600;
}


.bn-gap-list {
    display: grid;

    grid-template-columns:
        repeat(2, minmax(0, 1fr));
}


.bn-gap-item {
    min-height: 60px;

    display: flex;

    align-items: center;
    justify-content: space-between;

    gap: 12px;

    padding: 12px 16px;

    border-right: 1px solid #ECEDEF;
    border-bottom: 1px solid #ECEDEF;
}


.bn-gap-item
div:first-child span,
.bn-gap-item
div:first-child strong {
    display: block;
}


.bn-gap-item
div:first-child span {
    color: var(--danger);

    font-size: 9px;

    font-weight: 600;
}


.bn-gap-item
div:first-child strong {
    margin-top: 4px;

    font-size: 12px;

    font-weight: 500;
}


.bn-gap-item
> div:last-child {
    color: var(--text-secondary);

    font-size: 10px;
}


.bn-gap-good {
    display: flex;

    align-items: center;

    gap: 12px;

    padding: 20px 18px;
}


.bn-gap-good > span {
    width: 32px;
    height: 32px;

    display: flex;

    align-items: center;
    justify-content: center;

    border-radius: 50%;

    background: #EDF8F2;

    color: var(--success);

    font-weight: 600;
}


.bn-gap-good strong {
    font-size: 13px;

    font-weight: 500;
}


.bn-gap-good p {
    margin: 3px 0 0;

    color: var(--text-secondary);

    font-size: 11px;
}


/* EMPTY */

.bn-panel-empty {
    padding: 30px 18px;

    text-align: center;
}


.bn-panel-empty strong,
.bn-panel-empty span {
    display: block;
}


.bn-panel-empty strong {
    font-size: 13px;

    font-weight: 500;
}


.bn-panel-empty span {
    max-width: 340px;

    margin: 5px auto 0;

    color: var(--text-secondary);

    font-size: 11px;

    line-height: 1.45;
}


/* =========================================================
   RESPONSIVE HOME
========================================================= */

@media (max-width: 1150px) {

    .bn-home-kpi-strip {
        grid-template-columns:
            repeat(3, minmax(0, 1fr));
    }


    .bn-home-kpi:nth-child(3) {
        border-right: 0;
    }


    .bn-home-main-grid,
    .bn-home-secondary-grid {
        grid-template-columns: 1fr;
    }

}


@media (max-width: 760px) {

    .bn-home-header {
        align-items: flex-start;

        flex-direction: column;
    }


    .bn-date-picker {
        width: 100%;
    }


    .bn-date-picker > summary {
        width: 100%;

        justify-content: space-between;
    }


    .bn-date-popover {
        left: 0;
        right: auto;

        width: min(
            330px,
            calc(100vw - 40px)
        );
    }


    .bn-home-kpi-strip {
        grid-template-columns:
            repeat(2, minmax(0, 1fr));
    }


    .bn-home-kpi {
        border-bottom:
            1px solid var(--border);
    }


    .bn-gap-list {
        grid-template-columns: 1fr;
    }


    .bn-recent-item {
        grid-template-columns:
            62px
            minmax(0, 1fr)
            22px;
    }


    .bn-recent-type {
        display: none;
    }

}


/* =========================================================
   HOME V2 — INTERACTIVE TOOLKIT ACTIVITY PIE
========================================================= */

.bn-activity-period {
    color: var(--text-secondary);

    font-size: 12px;

    font-weight: 400;

    white-space: nowrap;
}


.bn-activity-pie-layout {
    min-height: 310px;

    display: grid;

    grid-template-columns:
        minmax(260px, .85fr)
        minmax(320px, 1.15fr);

    align-items: center;

    gap: 28px;

    padding: 24px 28px;
}



/* =========================================================
   DONUT VISUAL
========================================================= */

.bn-pie-visual {
    position: relative;

    width: 250px;
    height: 250px;

    margin: 0 auto;
}


.bn-pie-svg {
    width: 100%;
    height: 100%;

    display: block;

    overflow: visible;

    transform: rotate(-90deg);
}


.bn-pie-track,
.bn-pie-segment {
    fill: none;

    stroke-width: 25;

    vector-effect:
        non-scaling-stroke;
}


.bn-pie-track {
    stroke: #EEF0F3;
}


.bn-pie-segment {
    cursor: pointer;

    transition:
        stroke-dasharray
        .8s
        cubic-bezier(
            .22,
            1,
            .36,
            1
        ),
        stroke-dashoffset
        .8s
        cubic-bezier(
            .22,
            1,
            .36,
            1
        ),
        opacity
        .18s ease,
        stroke-width
        .18s ease;
}


.bn-pie-segment.search {
    stroke: var(--blue);
}


.bn-pie-segment.client {
    stroke: #D79A20;
}


.bn-pie-segment.highlighted {
    stroke-width: 31;
}


.bn-pie-segment.dimmed {
    opacity: .25;
}



/* =========================================================
   CENTER TEXT
========================================================= */

.bn-pie-center {
    position: absolute;

    inset: 0;

    display: flex;

    flex-direction: column;

    align-items: center;
    justify-content: center;

    pointer-events: none;

    text-align: center;
}


.bn-pie-center span {
    color: var(--text-secondary);

    font-size: 10px;

    font-weight: 500;

    letter-spacing: .45px;
}


.bn-pie-center strong {
    margin-top: 4px;

    color: var(--text);

    font-size: 38px;

    font-weight: 500;

    line-height: 1;
}


.bn-pie-center small {
    margin-top: 7px;

    color: var(--text-secondary);

    font-size: 11px;

    font-weight: 400;
}



/* =========================================================
   SUMMARY
========================================================= */

.bn-pie-summary {
    min-width: 0;
}


.bn-pie-summary-heading {
    margin-bottom: 15px;
}


.bn-pie-summary-heading h3 {
    margin: 0;

    font-size: 17px;

    font-weight: 500 !important;
}


.bn-pie-summary-heading p {
    margin: 4px 0 0;

    color: var(--text-secondary);

    font-size: 12px;

    font-weight: 400;
}



/* =========================================================
   INTERACTIVE LEGEND ROWS
========================================================= */

.bn-pie-legend-item {
    width: 100%;

    min-height: 74px;

    display: grid;

    grid-template-columns:
        12px
        minmax(0, 1fr)
        auto;

    align-items: center;

    gap: 13px;

    padding: 12px 14px;

    border: 1px solid var(--border);
    border-radius: 6px;

    background: #FFFFFF;

    color: var(--text);

    text-align: left;

    cursor: pointer;

    transition:
        background
        .15s ease,
        border-color
        .15s ease,
        transform
        .15s ease;
}


.bn-pie-legend-item
+ .bn-pie-legend-item {
    margin-top: 9px;
}


.bn-pie-legend-item:hover,
.bn-pie-legend-item.active {
    border-color: #C7D2E5;

    background: #FAFBFD;

    transform: translateX(2px);
}


.bn-pie-legend-mark {
    width: 10px;
    height: 34px;

    border-radius: 3px;
}


.bn-pie-legend-item.search
.bn-pie-legend-mark {
    background: var(--blue);
}


.bn-pie-legend-item.client
.bn-pie-legend-mark {
    background: #D79A20;
}


.bn-pie-legend-copy {
    min-width: 0;
}


.bn-pie-legend-copy strong,
.bn-pie-legend-copy span {
    display: block;
}


.bn-pie-legend-copy strong {
    font-size: 14px;

    font-weight: 500;
}


.bn-pie-legend-copy span {
    margin-top: 4px;

    color: var(--text-secondary);

    font-size: 11px;

    font-weight: 400;

    line-height: 1.4;
}


.bn-pie-legend-number {
    min-width: 64px;

    text-align: right;
}


.bn-pie-legend-number strong,
.bn-pie-legend-number span {
    display: block;
}


.bn-pie-legend-number strong {
    font-size: 23px;

    font-weight: 500;
}


.bn-pie-legend-number span {
    margin-top: 2px;

    color: var(--text-secondary);

    font-size: 11px;

    font-weight: 400;
}



/* =========================================================
   ACTION LINKS
========================================================= */

.bn-pie-actions {
    display: flex;

    flex-wrap: wrap;

    gap: 14px;

    margin-top: 16px;
}


.bn-pie-action-link {
    color: var(--blue);

    font-size: 12px;

    font-weight: 500;

    text-decoration: none;
}


.bn-pie-action-link:hover {
    text-decoration: underline;
}



/* =========================================================
   EMPTY STATE
========================================================= */

.bn-pie-empty {
    margin-top: 12px;

    padding: 11px 12px;

    border-left: 3px solid #D8DBDF;

    background: #F7F8F9;

    color: var(--text-secondary);

    font-size: 11px;

    line-height: 1.45;
}



/* =========================================================
   RESPONSIVE
========================================================= */

@media (max-width: 880px) {

    .bn-activity-pie-layout {
        grid-template-columns: 1fr;

        gap: 20px;
    }


    .bn-pie-visual {
        width: 225px;
        height: 225px;
    }

}


@media (max-width: 520px) {

    .bn-activity-pie-layout {
        padding:
            20px 16px;
    }


    .bn-pie-visual {
        width: 205px;
        height: 205px;
    }


    .bn-pie-center strong {
        font-size: 32px;
    }


    .bn-pie-legend-item {
        grid-template-columns:
            10px
            minmax(0, 1fr)
            auto;
    }

}


/* =========================================================
   BHARATNXT HUMAN-CENTRIC UI FOUNDATION — FINAL OVERRIDES
========================================================= */


/* =========================================================
   1. LAYOUT STABILITY
========================================================= */

.main-area,
.content,
.bn-home-v2,
.toolkit-v3-page,
.search-console-page,
.pitch-ops-page,
.bn-dashboard-panel,
.bn-home-main-grid,
.bn-home-secondary-grid {
    min-width: 0;
}


.content {
    overflow-x: clip;
}



/* =========================================================
   2. TOPBAR — STABLE THREE-PART LAYOUT
   Search | account information | sign out
========================================================= */

.topbar {
    height: var(--topbar-height) !important;

    display: flex !important;

    flex-wrap: nowrap !important;

    align-items: center !important;

    justify-content: flex-start !important;

    gap: 14px !important;

    padding:
        0 24px !important;

    overflow: visible !important;
}


.topbar > .global-search {
    width: auto !important;

    max-width: 820px !important;

    min-width: 220px !important;

    flex:
        1 1 620px !important;

    margin: 0 !important;
}


.global-search {
    height: 42px;

    display: grid !important;

    grid-template-columns:
        22px
        minmax(0, 1fr)
        auto !important;

    align-items: center;

    gap: 8px;

    padding:
        0 12px !important;

    overflow: hidden !important;
}


.global-search input {
    width: 100% !important;

    min-width: 0 !important;

    padding: 0 !important;

    border: 0 !important;

    background: transparent !important;

    font-size: 14px !important;
}


.global-search input:focus {
    outline: none;
}


.search-shortcut {
    position: static !important;

    flex-shrink: 0;

    margin: 0 !important;

    white-space: nowrap;
}


.topbar > .topbar-actions {
    min-width: 0;

    flex:
        0 1 auto !important;

    margin-left:
        auto !important;
}


.topbar > .topbar-logout-form {
    flex:
        0 0 auto !important;

    margin:
        0 !important;
}


.topbar-logout-button {
    min-width: 78px;

    min-height: 38px !important;

    padding:
        0 14px !important;

    border-radius:
        7px !important;

    font-size:
        12px !important;

    font-weight:
        500 !important;

    white-space: nowrap;
}


/* Admin header has no search.
   Keep all account controls together on the right. */

.topbar:not(:has(.global-search))
.topbar-actions {
    margin-left:
        auto !important;
}



/* =========================================================
   3. FAMILIAR ICONS
========================================================= */

.bn-search-icon,
.bn-nav-svg-wrap {
    display: inline-flex;

    align-items: center;
    justify-content: center;
}


.bn-search-icon svg {
    width: 17px;
    height: 17px;

    stroke: currentColor;

    stroke-width: 1.8;

    stroke-linecap: round;
    stroke-linejoin: round;
}


.bn-nav-svg {
    width: 19px;
    height: 19px;

    stroke: currentColor;

    stroke-width: 1.7;

    stroke-linecap: round;
    stroke-linejoin: round;
}


.nav-icon {
    width: 24px;
    height: 24px;

    display: inline-flex;

    align-items: center;
    justify-content: center;

    flex-shrink: 0;
}



/* =========================================================
   4. CLICK TARGETS
========================================================= */

.nav-item {
    min-height:
        44px !important;
}


.primary-button,
.secondary-button {
    min-height:
        40px !important;

    display: inline-flex;

    align-items: center;
    justify-content: center;
}


button,
a,
input,
select {
    -webkit-tap-highlight-color:
        transparent;
}



/* =========================================================
   5. EASY "VIEW" ACTIONS
   BharatNXT yellow + Amazon-like obvious click affordance
========================================================= */

.bn-row-action {
    min-width: 72px;
    min-height: 36px;

    display: inline-flex;

    align-items: center;
    justify-content: center;

    gap: 6px;

    padding:
        0 13px;

    border:
        1px solid #D8AA18;

    border-radius:
        18px;

    background:
        #F6C945;

    color:
        #17191C !important;

    font-size:
        12px;

    font-weight:
        500;

    line-height:
        1;

    text-decoration:
        none !important;

    cursor:
        pointer;

    transition:
        background .14s ease,
        border-color .14s ease,
        transform .10s ease;
}


.bn-row-action:hover {
    background:
        #F1BE2D;

    border-color:
        #C89A12;

    transform:
        translateY(-1px);
}


.bn-row-action:active {
    transform:
        translateY(0);
}


.bn-action-chevron {
    font-size:
        17px;

    line-height:
        1;
}



/* Pitch table compact View control */

.pitch-row-arrow {
    min-width: 64px !important;

    min-height: 34px !important;

    display: inline-flex !important;

    align-items: center;
    justify-content: center;

    gap: 4px;

    padding:
        0 11px !important;

    border:
        1px solid #D8AA18 !important;

    border-radius:
        17px !important;

    background:
        #F6C945 !important;

    color:
        #17191C !important;

    font-size:
        0 !important;

    font-weight:
        500;

    cursor:
        pointer;
}


.pitch-row-arrow::before {
    content:
        "View";

    font-size:
        11px;
}


.pitch-row-arrow::after {
    content:
        "›";

    margin-left:
        4px;

    font-size:
        16px;
}



/* =========================================================
   6. GLOBAL SUCCESS / FEEDBACK
========================================================= */

.bn-feedback-stack {
    position: fixed;

    top:
        calc(
            var(--topbar-height)
            + 14px
        );

    right:
        22px;

    z-index:
        500;

    width:
        min(
            390px,
            calc(100vw - 32px)
        );

    display: grid;

    gap: 9px;

    pointer-events:
        none;
}


.bn-feedback-toast {
    min-height: 66px;

    display: grid;

    grid-template-columns:
        34px
        minmax(0, 1fr)
        30px;

    align-items: center;

    gap: 11px;

    padding:
        11px 11px 11px 13px;

    border:
        1px solid var(--border);

    border-left:
        4px solid var(--blue);

    border-radius:
        8px;

    background:
        #FFFFFF;

    box-shadow:
        0 10px 28px
        rgba(19, 24, 32, .12);

    opacity: 1;

    transform:
        translateY(0);

    pointer-events:
        auto;

    animation:
        bn-feedback-enter
        .22s ease-out;
}


.bn-feedback-toast.success {
    border-left-color:
        var(--success);
}


.bn-feedback-toast.error {
    border-left-color:
        var(--danger);
}


.bn-feedback-toast.warning {
    border-left-color:
        var(--warning);
}


.bn-feedback-toast.leaving {
    opacity: 0;

    transform:
        translateY(-6px);

    transition:
        opacity .18s ease,
        transform .18s ease;
}


.bn-feedback-icon {
    width: 30px;
    height: 30px;

    display: flex;

    align-items: center;
    justify-content: center;

    border-radius:
        50%;

    background:
        #EDF4FF;

    color:
        var(--blue);

    font-size:
        14px;

    font-weight:
        600;
}


.bn-feedback-toast.success
.bn-feedback-icon {
    background:
        #EAF7F0;

    color:
        var(--success);
}


.bn-feedback-toast.error
.bn-feedback-icon {
    background:
        #FFF0F0;

    color:
        var(--danger);
}


.bn-feedback-toast.warning
.bn-feedback-icon {
    background:
        #FFF6E4;

    color:
        var(--warning);
}


.bn-feedback-copy {
    min-width: 0;
}


.bn-feedback-copy strong,
.bn-feedback-copy span {
    display: block;
}


.bn-feedback-copy strong {
    font-size:
        13px;

    font-weight:
        600;
}


.bn-feedback-copy span {
    margin-top:
        2px;

    color:
        var(--text-secondary);

    font-size:
        12px;

    font-weight:
        400;

    line-height:
        1.4;
}


.bn-feedback-close {
    width: 30px;
    height: 30px;

    display: flex;

    align-items: center;
    justify-content: center;

    padding: 0;

    border: 0;

    border-radius:
        50%;

    background:
        transparent;

    color:
        var(--text-secondary);

    font-size:
        18px;

    cursor:
        pointer;
}


.bn-feedback-close:hover {
    background:
        #F1F3F5;

    color:
        var(--text);
}


@keyframes bn-feedback-enter {

    from {
        opacity: 0;

        transform:
            translateY(-7px);
    }

    to {
        opacity: 1;

        transform:
            translateY(0);
    }

}



/* =========================================================
   7. SUBMIT / PROCESSING FEEDBACK
========================================================= */

.bn-button-loading {
    cursor:
        wait !important;

    opacity:
        .78;
}


.bn-loading-spinner {
    width: 14px;
    height: 14px;

    display: inline-block;

    margin-right:
        7px;

    border:
        2px solid
        rgba(255,255,255,.45);

    border-top-color:
        currentColor;

    border-radius:
        50%;

    animation:
        bn-spin
        .7s linear
        infinite;
}


@keyframes bn-spin {

    to {
        transform:
            rotate(360deg);
    }

}



/* =========================================================
   8. FOCUS / KEYBOARD CLARITY
========================================================= */

a:focus-visible,
button:focus-visible,
input:focus-visible,
select:focus-visible,
textarea:focus-visible,
summary:focus-visible {
    outline:
        3px solid
        rgba(23, 105, 255, .24);

    outline-offset:
        2px;
}



/* =========================================================
   9. RESPONSIVE HEADER
========================================================= */

@media (max-width: 1180px) {

    .topbar {
        gap:
            10px !important;

        padding:
            0 16px !important;
    }


    .global-search
    .search-shortcut {
        display:
            none !important;
    }


    .security-status {
        display:
            none;
    }

}


@media (max-width: 950px) {

    .topbar > .global-search {
        min-width:
            160px !important;
    }


    .topbar-actions {
        gap:
            8px !important;
    }


    .user-info {
        display:
            none !important;
    }


    .topbar-logout-button {
        min-width:
            68px;
    }

}


@media (max-width: 650px) {

    .bn-feedback-stack {
        top:
            calc(
                var(--topbar-height)
                + 8px
            );

        right:
            10px;

        width:
            calc(100vw - 20px);
    }

}

===== FILE: static/css/ui_stability.css =====

/* =========================================================
   BHARATNXT WAVE
   FINAL UI STABILITY / HUMAN INTERACTION LAYER

   Loaded AFTER app.css.
   Do not duplicate these rules back into app.css.
========================================================= */


/* =========================================================
   GLOBAL STABILITY
========================================================= */

html,
body {
    overflow-x: hidden;
}


.main-area,
.content,
.content > *,
.bn-home-v2,
.bn-home-main-grid,
.bn-home-secondary-grid,
.toolkit-v3-page,
.search-console-page,
.pitch-ops-page,
.bn-dashboard-panel {
    min-width: 0;
}


img,
svg {
    max-width: 100%;
}



/* =========================================================
   TOPBAR
   Authoritative layout:
   SEARCH | ACCOUNT CONTROLS | SIGN OUT
========================================================= */

.topbar {
    position: sticky !important;
    top: 0 !important;

    z-index: 100 !important;

    width: 100% !important;
    height: 64px !important;
    min-height: 64px !important;

    display: flex !important;
    flex-direction: row !important;
    flex-wrap: nowrap !important;

    align-items: center !important;
    justify-content: flex-start !important;

    gap: 14px !important;

    padding: 0 24px !important;

    overflow: visible !important;

    background: rgba(255,255,255,.98) !important;

    border-bottom: 1px solid var(--border) !important;
}



/* =========================================================
   GLOBAL SEARCH
========================================================= */

.topbar > .global-search {
    width: auto !important;

    min-width: 0 !important;
    max-width: 780px !important;

    flex: 1 1 580px !important;

    height: 42px !important;

    display: grid !important;

    grid-template-columns:
        22px
        minmax(0, 1fr)
        auto !important;

    align-items: center !important;

    gap: 9px !important;

    margin: 0 !important;

    padding: 0 13px !important;

    border: 1px solid var(--border) !important;

    border-radius: 7px !important;

    background: #FFFFFF !important;

    overflow: hidden !important;
}


.global-search:focus-within {
    border-color: #9CB9FF !important;

    box-shadow:
        0 0 0 3px
        rgba(23,105,255,.08) !important;
}


.global-search input {
    width: 100% !important;

    min-width: 0 !important;

    height: 40px !important;

    margin: 0 !important;

    padding: 0 !important;

    border: 0 !important;

    outline: 0 !important;

    background: transparent !important;

    color: var(--text) !important;

    font-size: 14px !important;

    font-weight: 400 !important;
}


.global-search input::placeholder {
    color: #8A9098 !important;
}


.global-search .search-shortcut {
    position: static !important;

    margin: 0 !important;

    flex-shrink: 0;

    color: var(--text-secondary) !important;

    font-size: 11px !important;

    white-space: nowrap !important;
}



/* =========================================================
   TOPBAR RIGHT
========================================================= */

.topbar > .topbar-actions {
    min-width: 0 !important;

    display: flex !important;

    flex: 0 1 auto !important;

    align-items: center !important;

    gap: 10px !important;

    margin-left: auto !important;

    white-space: nowrap !important;
}


.topbar > .topbar-logout-form {
    display: block !important;

    flex: 0 0 auto !important;

    margin: 0 !important;

    padding: 0 !important;
}


.topbar-logout-button {
    min-width: 82px !important;
    min-height: 38px !important;

    display: inline-flex !important;

    align-items: center !important;
    justify-content: center !important;

    padding: 0 14px !important;

    border: 1px solid var(--border) !important;

    border-radius: 7px !important;

    background: #FFFFFF !important;

    color: var(--text) !important;

    font-size: 12px !important;

    font-weight: 500 !important;

    white-space: nowrap !important;

    cursor: pointer !important;
}


.topbar-logout-button:hover {
    border-color: #E2B5B5 !important;

    background: #FFF8F8 !important;

    color: var(--danger) !important;
}



/* =========================================================
   ADMIN TOPBAR
   No search, so right controls align naturally right.
========================================================= */

.topbar:not(:has(.global-search))
.topbar-actions {
    margin-left: auto !important;
}



/* =========================================================
   USER PROFILE
========================================================= */

.user-profile {
    min-width: 0;

    display: flex !important;

    align-items: center !important;

    gap: 9px !important;
}


.user-info {
    min-width: 0;
}


.user-name {
    max-width: 150px;

    overflow: hidden;

    text-overflow: ellipsis;

    white-space: nowrap;
}



/* =========================================================
   MEMORABLE / RECOGNISABLE ICONS
========================================================= */

.bn-search-icon {
    width: 20px;
    height: 20px;

    display: inline-flex;

    align-items: center;
    justify-content: center;

    color: #6C7480;
}


.bn-search-icon svg {
    width: 17px;
    height: 17px;

    stroke: currentColor;

    stroke-width: 1.8;

    stroke-linecap: round;
    stroke-linejoin: round;
}


.bn-nav-svg-wrap {
    width: 23px;
    height: 23px;

    display: inline-flex;

    align-items: center;
    justify-content: center;
}


.bn-nav-svg {
    width: 19px;
    height: 19px;

    stroke: currentColor;

    stroke-width: 1.7;

    stroke-linecap: round;
    stroke-linejoin: round;
}



/* =========================================================
   CLICK TARGETS
========================================================= */

.nav-item {
    min-height: 44px !important;
}


.primary-button,
.secondary-button {
    min-height: 40px !important;

    display: inline-flex !important;

    align-items: center !important;
    justify-content: center !important;
}



/* =========================================================
   HOME RECENT ACTIVITY
   Replace tiny arrow appearance with a large clear action.
========================================================= */

.bn-recent-item > a {
    width: auto !important;
    min-width: 70px !important;

    min-height: 36px !important;

    display: inline-flex !important;

    align-items: center !important;
    justify-content: center !important;

    gap: 5px !important;

    padding: 0 13px !important;

    border: 1px solid #D4A514 !important;

    border-radius: 18px !important;

    background: #F6C945 !important;

    color: #17191C !important;

    font-size: 0 !important;

    font-weight: 500 !important;

    text-decoration: none !important;

    cursor: pointer !important;

    transition:
        background .13s ease,
        transform .13s ease,
        border-color .13s ease !important;
}


.bn-recent-item > a::before {
    content: "View";

    font-size: 12px !important;
}


.bn-recent-item > a::after {
    content: "›";

    margin-left: 3px;

    font-size: 18px !important;

    line-height: 1;
}


.bn-recent-item > a:hover {
    background: #F0BE2E !important;

    border-color: #C7970B !important;

    transform: translateY(-1px);
}


.bn-recent-item > a:active {
    transform: translateY(0);
}



/* =========================================================
   PITCH OPERATIONS ROW ACTION
========================================================= */

.pitch-row-arrow {
    min-width: 68px !important;
    min-height: 36px !important;

    display: inline-flex !important;

    align-items: center !important;
    justify-content: center !important;

    gap: 5px !important;

    padding: 0 12px !important;

    border: 1px solid #D4A514 !important;

    border-radius: 18px !important;

    background: #F6C945 !important;

    color: #17191C !important;

    font-size: 0 !important;

    font-weight: 500 !important;

    cursor: pointer !important;
}


.pitch-row-arrow::before {
    content: "View";

    font-size: 11px;
}


.pitch-row-arrow::after {
    content: "›";

    font-size: 17px;
}



/* =========================================================
   FEEDBACK / SUCCESS NOTIFICATIONS
========================================================= */

.bn-feedback-stack {
    position: fixed;

    top: 78px;
    right: 22px;

    z-index: 1000;

    width:
        min(
            380px,
            calc(100vw - 32px)
        );

    display: grid;

    gap: 9px;

    pointer-events: none;
}


.bn-feedback-toast {
    min-height: 64px;

    display: grid;

    grid-template-columns:
        34px
        minmax(0, 1fr)
        30px;

    align-items: center;

    gap: 10px;

    padding: 11px 11px 11px 13px;

    border: 1px solid var(--border);

    border-left: 4px solid var(--blue);

    border-radius: 8px;

    background: #FFFFFF;

    box-shadow:
        0 10px 30px
        rgba(19,24,32,.12);

    pointer-events: auto;

    animation:
        bn-feedback-enter
        .22s ease-out;
}


.bn-feedback-toast.success {
    border-left-color: var(--success);
}


.bn-feedback-toast.warning {
    border-left-color: var(--warning);
}


.bn-feedback-toast.error {
    border-left-color: var(--danger);
}


.bn-feedback-icon {
    width: 30px;
    height: 30px;

    display: flex;

    align-items: center;
    justify-content: center;

    border-radius: 50%;

    background: #EDF4FF;

    color: var(--blue);

    font-size: 14px;

    font-weight: 600;
}


.bn-feedback-toast.success
.bn-feedback-icon {
    background: #EAF7F0;

    color: var(--success);
}


.bn-feedback-toast.warning
.bn-feedback-icon {
    background: #FFF6E4;

    color: var(--warning);
}


.bn-feedback-toast.error
.bn-feedback-icon {
    background: #FFF0F0;

    color: var(--danger);
}


.bn-feedback-copy {
    min-width: 0;
}


.bn-feedback-copy strong,
.bn-feedback-copy span {
    display: block;
}


.bn-feedback-copy strong {
    font-size: 13px;

    font-weight: 600;
}


.bn-feedback-copy span {
    margin-top: 2px;

    color: var(--text-secondary);

    font-size: 12px;

    line-height: 1.4;
}


.bn-feedback-close {
    width: 30px;
    height: 30px;

    display: flex;

    align-items: center;
    justify-content: center;

    padding: 0;

    border: 0;

    border-radius: 50%;

    background: transparent;

    color: var(--text-secondary);

    font-size: 18px;

    cursor: pointer;
}


.bn-feedback-close:hover {
    background: #F1F3F5;

    color: var(--text);
}


.bn-feedback-toast.leaving {
    opacity: 0;

    transform: translateY(-6px);

    transition:
        opacity .18s ease,
        transform .18s ease;
}


@keyframes bn-feedback-enter {

    from {
        opacity: 0;

        transform: translateY(-7px);
    }

    to {
        opacity: 1;

        transform: translateY(0);
    }
}



/* =========================================================
   WORKING / PROCESSING FEEDBACK
========================================================= */

.bn-button-loading {
    cursor: wait !important;

    opacity: .76 !important;
}


.bn-loading-spinner {
    width: 14px;
    height: 14px;

    display: inline-block;

    margin-right: 7px;

    border: 2px solid rgba(255,255,255,.45);

    border-top-color: currentColor;

    border-radius: 50%;

    animation:
        bn-ui-spin
        .7s linear infinite;
}


@keyframes bn-ui-spin {

    to {
        transform: rotate(360deg);
    }
}



/* =========================================================
   KEYBOARD / FOCUS CLARITY
========================================================= */

a:focus-visible,
button:focus-visible,
input:focus-visible,
select:focus-visible,
textarea:focus-visible,
summary:focus-visible {
    outline:
        3px solid rgba(23,105,255,.23) !important;

    outline-offset:
        2px !important;
}



/* =========================================================
   PREVENT CONTENT CRUMBLING
========================================================= */

.toolkit-v3-search-form,
.toolkit-v3-filters,
.bn-home-kpi-strip,
.bn-activity-pie-layout,
.bn-home-main-grid,
.bn-home-secondary-grid {
    min-width: 0 !important;
}


.toolkit-v3-search-form > *,
.toolkit-v3-filters > *,
.bn-home-main-grid > *,
.bn-home-secondary-grid > *,
.bn-activity-pie-layout > * {
    min-width: 0 !important;
}


.toolkit-v3-search-input {
    width: 100% !important;
}


.toolkit-v3-filter select {
    width: 100% !important;

    min-width: 0 !important;
}



/* =========================================================
   RESPONSIVE TOPBAR
========================================================= */

@media (max-width: 1180px) {

    .topbar {
        gap: 10px !important;

        padding:
            0 16px !important;
    }


    .security-status {
        display: none !important;
    }


    .global-search
    .search-shortcut {
        display: none !important;
    }

}


@media (max-width: 920px) {

    .user-info {
        display: none !important;
    }


    .topbar > .global-search {
        min-width: 150px !important;
    }


    .topbar-actions {
        gap: 7px !important;
    }


    .topbar-logout-button {
        min-width: 68px !important;

        padding:
            0 10px !important;
    }

}


@media (max-width: 650px) {

    .notification-button {
        display: none !important;
    }


    .bn-feedback-stack {
        top: 72px;

        right: 10px;

        width:
            calc(100vw - 20px);
    }

}


/* =========================================================
   SIDEBAR SIGN OUT
   Final location for authentication exit action
========================================================= */

.sidebar {
    display: flex !important;

    flex-direction: column !important;
}


.sidebar-account-actions {
    width: 100%;

    margin-top: auto;

    padding-top: 14px;

    border-top: 1px solid var(--border);
}


.sidebar-logout-form {
    width: 100%;

    margin: 0;
}


.sidebar-logout-button {
    width: 100%;

    min-height: 44px;

    display: flex;

    align-items: center;

    gap: 10px;

    padding: 0 12px;

    border: 1px solid transparent;

    border-radius: 7px;

    background: transparent;

    color: var(--text-secondary);

    font-family: inherit;

    font-size: 13px;

    font-weight: 400;

    text-align: left;

    cursor: pointer;

    transition:
        background .14s ease,
        border-color .14s ease,
        color .14s ease;
}


.sidebar-logout-button:hover {
    border-color: #F0D5D5;

    background: #FFF7F7;

    color: var(--danger);
}


.sidebar-logout-button:active {
    background: #FDEEEE;
}


.sidebar-logout-icon {
    width: 24px;
    height: 24px;

    display: inline-flex;

    align-items: center;
    justify-content: center;

    flex-shrink: 0;
}


.sidebar-logout-icon svg {
    width: 18px;
    height: 18px;

    stroke: currentColor;

    stroke-width: 1.8;

    stroke-linecap: round;
    stroke-linejoin: round;
}


/* Old header logout must never appear */

.topbar-logout-form,
.topbar-logout-button {
    display: none !important;
}


/* Collapsed sidebar */

@media (max-width: 900px) {

    .sidebar-account-actions {
        display: flex;

        justify-content: center;
    }


    .sidebar-logout-button {
        width: 44px;

        padding: 0;

        justify-content: center;
    }


    .sidebar-logout-button > span:last-child {
        display: none;
    }

}


/* =========================================================
   SOURCE HISTORY — FILE INTERACTIONS
========================================================= */

.source-file-link {
    display: inline-flex;

    align-items: center;

    color: var(--text);

    font-weight: 600;

    text-decoration: none;

    border-bottom:
        1px solid transparent;

    transition:
        color .14s ease,
        border-color .14s ease;
}


.source-file-link::before {
    content: "";

    width: 15px;
    height: 18px;

    flex-shrink: 0;

    margin-right: 7px;

    border: 1.5px solid #6D7682;

    border-radius: 2px;

    background:
        linear-gradient(
            135deg,
            transparent 0 68%,
            #E9EBEE 68% 100%
        );
}


.source-file-link:hover {
    color: var(--blue);

    border-bottom-color:
        rgba(23,105,255,.35);
}



/* =========================================================
   SOURCE ROW ACTIONS
========================================================= */

.source-delete-form {
    display: inline-flex;

    margin-left: 8px;

    vertical-align: middle;
}


.source-delete-button {
    min-height: 32px;

    display: inline-flex;

    align-items: center;
    justify-content: center;

    gap: 5px;

    padding: 0 9px;

    border:
        1px solid transparent;

    border-radius: 6px;

    background: transparent;

    color: #8A4B4B;

    font-family: inherit;

    font-size: 10px;

    font-weight: 500;

    cursor: pointer;
}


.source-delete-button:hover {
    border-color: #E9C8C8;

    background: #FFF5F5;

    color: var(--danger);
}


.source-delete-button svg {
    width: 14px;
    height: 14px;

    stroke: currentColor;

    stroke-width: 1.6;

    stroke-linecap: round;
    stroke-linejoin: round;
}



/* =========================================================
   SOURCE DETAIL DELETE
========================================================= */

.source-detail-delete-form {
    display: inline-flex;

    margin-left: 8px;
}


.source-detail-delete-button {
    min-height: 40px;

    display: inline-flex;

    align-items: center;
    justify-content: center;

    gap: 7px;

    padding: 0 13px;

    border: 1px solid #E6C5C5;

    border-radius: 7px;

    background: #FFFFFF;

    color: #A33A3A;

    font-family: inherit;

    font-size: 12px;

    font-weight: 500;

    cursor: pointer;
}


.source-detail-delete-button:hover {
    border-color: #D99C9C;

    background: #FFF5F5;

    color: var(--danger);
}


.source-detail-delete-button svg {
    width: 16px;
    height: 16px;

    stroke: currentColor;

    stroke-width: 1.7;

    stroke-linecap: round;
    stroke-linejoin: round;
}



/* =========================================================
   SOURCE HISTORY DETAILS LINK
   Larger click target
========================================================= */

.import-history-table a,
.source-history-table a {
    min-height: 32px;
}



/* =========================================================
   MOBILE
========================================================= */

@media (max-width: 760px) {

    .source-delete-form {
        margin-left: 4px;
    }


    .source-delete-button span {
        display: none;
    }


    .source-delete-button {
        width: 34px;

        padding: 0;
    }

}

===== FILE: static/js/dashboard_bar_heights.js =====
(function () {
    "use strict";


    function initializeActivityChart() {

        document
            .querySelectorAll(
                ".search-bar-fill[data-height]"
            )
            .forEach(function (bar) {

                let height = Number(
                    bar.dataset.height
                );

                if (!Number.isFinite(height)) {
                    height = 0;
                }

                height = Math.max(
                    0,
                    Math.min(100, height)
                );

                if (
                    height > 0
                    &&
                    height < 5
                ) {
                    height = 5;
                }

                bar.style.height =
                    height + "%";
            });



        document
            .querySelectorAll(
                ".search-bar-column[data-total]"
            )
            .forEach(function (column) {

                const total = Number(
                    column.dataset.total
                );


                if (
                    !Number.isFinite(total)
                    ||
                    total <= 0
                ) {

                    column.classList.add(
                        "zero-activity"
                    );


                    const link =
                        column.closest("a[href]");


                    if (link) {

                        link.removeAttribute(
                            "href"
                        );

                        link.setAttribute(
                            "aria-disabled",
                            "true"
                        );

                        link.title =
                            "No Toolkit activity recorded";
                    }

                }

                else {

                    column.classList.remove(
                        "zero-activity"
                    );

                    column.title =
                        total
                        +
                        (
                            total === 1
                            ? " recorded action"
                            : " recorded actions"
                        );
                }

            });
    }


    if (
        document.readyState
        === "loading"
    ) {

        document.addEventListener(
            "DOMContentLoaded",
            initializeActivityChart
        );

    }

    else {

        initializeActivityChart();

    }

})();

===== FILE: static/js/dashboard_calendar.js =====
(function () {
    "use strict";

    const form = document.getElementById(
        "calendar-range-form"
    );

    if (!form) {
        return;
    }

    const startInput = document.getElementById(
        "calendar-start"
    );

    const endInput = document.getElementById(
        "calendar-end"
    );

    const rangeLabel = document.getElementById(
        "calendar-range-label"
    );

    const days = Array.from(
        document.querySelectorAll(
            ".calendar-day"
        )
    );

    let rangeStart = null;
    let rangeEnd = null;


    function formatDate(value) {
        const date = new Date(
            value + "T00:00:00"
        );

        return date.toLocaleDateString(
            "en-IN",
            {
                day: "2-digit",
                month: "short",
                year: "numeric"
            }
        );
    }


    function updateSelection() {
        days.forEach(function (day) {
            const value = day.dataset.date;

            day.classList.remove(
                "range-preview"
            );

            if (
                rangeStart
                && rangeEnd
                && value >= rangeStart
                && value <= rangeEnd
            ) {
                day.classList.add(
                    "range-preview"
                );
            }

            else if (
                rangeStart
                && !rangeEnd
                && value === rangeStart
            ) {
                day.classList.add(
                    "range-preview"
                );
            }
        });


        if (rangeStart && rangeEnd) {
            startInput.value = rangeStart;
            endInput.value = rangeEnd;

            rangeLabel.textContent =
                formatDate(rangeStart)
                + " — "
                + formatDate(rangeEnd);
        }

        else if (rangeStart) {
            startInput.value = rangeStart;
            endInput.value = rangeStart;

            rangeLabel.textContent =
                formatDate(rangeStart);
        }
    }


    days.forEach(function (day) {

        day.addEventListener(
            "click",
            function () {

                const value = day.dataset.date;

                if (
                    !rangeStart
                    || (
                        rangeStart
                        && rangeEnd
                    )
                ) {
                    rangeStart = value;
                    rangeEnd = null;
                }

                else {
                    if (value < rangeStart) {
                        rangeEnd = rangeStart;
                        rangeStart = value;
                    }
                    else {
                        rangeEnd = value;
                    }
                }

                updateSelection();
            }
        );

    });

})();

===== FILE: static/js/dashboard_home_v2.js =====
(function () {
    "use strict";


    /* =====================================================
       DATE PICKER
    ===================================================== */

    const picker =
        document.querySelector(
            ".bn-date-picker"
        );


    if (picker) {

        document.addEventListener(
            "click",
            function (event) {

                if (
                    picker.open
                    &&
                    !picker.contains(event.target)
                ) {

                    picker.removeAttribute(
                        "open"
                    );

                }

            }
        );

    }



    /* =====================================================
       INTERACTIVE TOOLKIT ACTIVITY PIE
    ===================================================== */

    const chart =
        document.getElementById(
            "bn-activity-pie"
        );


    if (!chart) {
        return;
    }


    const searchCount =
        Number(chart.dataset.searches) || 0;


    const clientCount =
        Number(chart.dataset.clientChecks) || 0;


    const total =
        searchCount + clientCount;


    const searchCircle =
        document.getElementById(
            "bn-pie-search"
        );


    const clientCircle =
        document.getElementById(
            "bn-pie-client"
        );


    const centerLabel =
        document.getElementById(
            "bn-pie-center-label"
        );


    const centerValue =
        document.getElementById(
            "bn-pie-center-value"
        );


    const centerDetail =
        document.getElementById(
            "bn-pie-center-detail"
        );


    const searchPercentElement =
        document.getElementById(
            "bn-search-percent"
        );


    const clientPercentElement =
        document.getElementById(
            "bn-client-percent"
        );


    const emptyMessage =
        document.getElementById(
            "bn-pie-empty"
        );


    const radius = 82;

    const circumference =
        2 * Math.PI * radius;



    function percentage(value) {

        if (!total) {
            return 0;
        }

        return Math.round(
            (value / total) * 100
        );

    }



    const searchPercent =
        percentage(searchCount);


    const clientPercent =
        percentage(clientCount);



    if (searchPercentElement) {

        searchPercentElement.textContent =
            searchPercent + "%";

    }


    if (clientPercentElement) {

        clientPercentElement.textContent =
            clientPercent + "%";

    }



    function resetCenter() {

        centerLabel.textContent =
            "TOTAL ACTIONS";

        centerValue.textContent =
            total;

        centerDetail.textContent =
            total === 1
                ? "1 Toolkit action"
                : total + " Toolkit actions";

    }



    function showSeries(
        label,
        value,
        percent
    ) {

        centerLabel.textContent =
            label;

        centerValue.textContent =
            value;

        centerDetail.textContent =
            percent + "% of activity";

    }



    function clearActiveSeries() {

        document
            .querySelectorAll(
                ".bn-pie-legend-item"
            )
            .forEach(function (item) {

                item.classList.remove(
                    "active"
                );

            });


        searchCircle.classList.remove(
            "highlighted",
            "dimmed"
        );


        clientCircle.classList.remove(
            "highlighted",
            "dimmed"
        );

    }



    function activateSeries(series) {

        clearActiveSeries();


        const button =
            document.querySelector(
                '[data-pie-series="'
                + series
                + '"]'
            );


        if (button) {

            button.classList.add(
                "active"
            );

        }


        if (series === "search") {

            searchCircle.classList.add(
                "highlighted"
            );

            clientCircle.classList.add(
                "dimmed"
            );


            showSeries(
                "TOOLKIT SEARCHES",
                searchCount,
                searchPercent
            );

        }


        else if (series === "client") {

            clientCircle.classList.add(
                "highlighted"
            );

            searchCircle.classList.add(
                "dimmed"
            );


            showSeries(
                "CLIENT SERVICE CHECKS",
                clientCount,
                clientPercent
            );

        }

    }



    /* =====================================================
       ZERO DATA
    ===================================================== */

    if (total <= 0) {

        searchCircle.style.display =
            "none";

        clientCircle.style.display =
            "none";


        centerLabel.textContent =
            "NO ACTIVITY";

        centerValue.textContent =
            "0";

        centerDetail.textContent =
            "Selected period";


        if (emptyMessage) {

            emptyMessage.hidden =
                false;

        }

        return;

    }



    /* =====================================================
       PIE GEOMETRY
    ===================================================== */

    const searchLength =
        circumference
        * (
            searchCount
            / total
        );


    const clientLength =
        circumference
        * (
            clientCount
            / total
        );



    /*
     * Initial zero-state makes the chart animate
     * into position after the page loads.
     */

    searchCircle.style.strokeDasharray =
        "0 "
        + circumference;


    clientCircle.style.strokeDasharray =
        "0 "
        + circumference;


    clientCircle.style.strokeDashoffset =
        "0";


    resetCenter();



    requestAnimationFrame(
        function () {

            requestAnimationFrame(
                function () {

                    searchCircle.style
                        .strokeDasharray =
                        searchLength
                        + " "
                        + (
                            circumference
                            - searchLength
                        );


                    clientCircle.style
                        .strokeDasharray =
                        clientLength
                        + " "
                        + (
                            circumference
                            - clientLength
                        );


                    clientCircle.style
                        .strokeDashoffset =
                        String(
                            -searchLength
                        );

                }
            );

        }
    );



    /* =====================================================
       LEGEND INTERACTION
    ===================================================== */

    document
        .querySelectorAll(
            "[data-pie-series]"
        )
        .forEach(function (button) {

            const series =
                button.dataset.pieSeries;


            button.addEventListener(
                "mouseenter",
                function () {

                    activateSeries(
                        series
                    );

                }
            );


            button.addEventListener(
                "focus",
                function () {

                    activateSeries(
                        series
                    );

                }
            );


            button.addEventListener(
                "click",
                function () {

                    activateSeries(
                        series
                    );

                }
            );


            button.addEventListener(
                "mouseleave",
                function () {

                    clearActiveSeries();

                    resetCenter();

                }
            );


            button.addEventListener(
                "blur",
                function () {

                    clearActiveSeries();

                    resetCenter();

                }
            );

        });



    /* =====================================================
       PIE SLICE INTERACTION
    ===================================================== */

    searchCircle.addEventListener(
        "mouseenter",
        function () {

            activateSeries(
                "search"
            );

        }
    );


    clientCircle.addEventListener(
        "mouseenter",
        function () {

            activateSeries(
                "client"
            );

        }
    );


    [
        searchCircle,
        clientCircle
    ].forEach(function (circle) {

        circle.addEventListener(
            "mouseleave",
            function () {

                clearActiveSeries();

                resetCenter();

            }
        );

    });

})();

===== FILE: static/js/dashboard_live.js =====
(function () {
    "use strict";

    const REFRESH_MS = 60000;
    const IDLE_REQUIRED_MS = 20000;

    let lastInteraction = Date.now();

    [
        "click",
        "keydown",
        "change",
        "mousemove",
        "touchstart"
    ].forEach(function (eventName) {

        document.addEventListener(
            eventName,
            function () {
                lastInteraction = Date.now();
            },
            { passive: true }
        );

    });


    setInterval(function () {

        if (document.visibilityState !== "visible") {
            return;
        }

        const idleFor = (
            Date.now() - lastInteraction
        );

        if (idleFor < IDLE_REQUIRED_MS) {
            return;
        }

        window.location.reload();

    }, REFRESH_MS);

})();

===== FILE: static/js/dashboard_search_drilldown.js =====
(function () {
    "use strict";


    function getDrilldownDate(url) {

        const params = url.searchParams;


        const start =
            params.get("start")
            ||
            params.get("start_date");


        const end =
            params.get("end")
            ||
            params.get("end_date");


        /*
         * Existing chart drilldown uses the same
         * start/end date when a daily bar is clicked.
         */

        if (
            start
            &&
            end
            &&
            start === end
        ) {
            return start;
        }


        return null;
    }



    function convertExistingLinks() {

        document
            .querySelectorAll("a[href]")
            .forEach(function (link) {

                try {

                    const url = new URL(
                        link.href,
                        window.location.origin
                    );


                    const date =
                        getDrilldownDate(url);


                    if (!date) {
                        return;
                    }


                    /*
                     * Only rewrite dashboard date
                     * drilldown links.
                     *
                     * Do not interfere with unrelated
                     * links elsewhere in the application.
                     */

                    const looksLikeDashboardDateLink =
                        url.searchParams.has("preset")
                        ||
                        url.searchParams.has("start")
                        ||
                        url.searchParams.has(
                            "start_date"
                        );


                    if (
                        !looksLikeDashboardDateLink
                    ) {
                        return;
                    }


                    link.href =
                        "/search-activity/?date="
                        +
                        encodeURIComponent(date);


                    link.dataset.searchDrilldown =
                        "true";


                    link.title =
                        "View search activity for "
                        +
                        date;

                }

                catch (error) {

                    /*
                     * Ignore malformed/non-http links.
                     */

                }

            });

    }



    /*
     * Run after dashboard HTML is ready.
     */

    if (
        document.readyState === "loading"
    ) {

        document.addEventListener(
            "DOMContentLoaded",
            convertExistingLinks
        );

    }

    else {

        convertExistingLinks();

    }



    /*
     * Also catch links that may be rendered
     * dynamically.
     */

    document.addEventListener(
        "click",
        function (event) {

            const link =
                event.target.closest(
                    "a[href]"
                );


            if (!link) {
                return;
            }


            try {

                const url = new URL(
                    link.href,
                    window.location.origin
                );


                const date =
                    getDrilldownDate(url);


                if (
                    date
                    &&
                    !url.pathname.includes(
                        "/search-activity/"
                    )
                ) {

                    event.preventDefault();


                    window.location.href =
                        "/search-activity/?date="
                        +
                        encodeURIComponent(date);

                }

            }

            catch (error) {

                /*
                 * Leave normal navigation untouched.
                 */

            }

        },
        true
    );

})();

===== FILE: static/js/matcher_saved.js =====
(function () {
    "use strict";

    const forms = document.querySelectorAll(
        ".matcher-save-form"
    );

    forms.forEach(function (form) {

        form.addEventListener(
            "submit",
            async function (event) {

                event.preventDefault();

                const button = form.querySelector(
                    ".matcher-save-button"
                );

                const csrf = form.querySelector(
                    'input[name="csrfmiddlewaretoken"]'
                );

                if (!button || !csrf) {
                    return;
                }

                button.disabled = true;

                try {

                    const response = await fetch(
                        form.action,
                        {
                            method: "POST",

                            headers: {
                                "X-Requested-With":
                                    "XMLHttpRequest",

                                "X-CSRFToken":
                                    csrf.value,
                            },

                            body: new FormData(
                                form
                            ),
                        }
                    );


                    if (!response.ok) {
                        throw new Error(
                            "Save request failed"
                        );
                    }


                    const data = await response.json();


                    if (data.saved) {

                        button.classList.add(
                            "saved"
                        );

                        button.textContent =
                            "★ Saved";

                    }

                    else {

                        button.classList.remove(
                            "saved"
                        );

                        button.textContent =
                            "☆ Save";

                    }

                }

                catch (error) {

                    button.textContent =
                        "Try Again";

                }

                finally {

                    button.disabled = false;

                }

            }
        );

    });

})();

===== FILE: static/js/matcher_sort.js =====
(function () {
    "use strict";

    const sortSelect = document.getElementById(
        "matcher-result-sort"
    );

    if (!sortSelect) {
        return;
    }


    const grids = Array.from(
        document.querySelectorAll(
            ".matcher-results-grid"
        )
    );


    grids.forEach(function (grid) {

        const cards = Array.from(
            grid.querySelectorAll(
                ".matcher-result-card"
            )
        );

        cards.forEach(function (card, index) {
            card.dataset.originalOrder = index;
        });

    });


    function sortCards(mode) {

        grids.forEach(function (grid) {

            const cards = Array.from(
                grid.querySelectorAll(
                    ".matcher-result-card"
                )
            );


            cards.sort(function (a, b) {

                if (mode === "type") {

                    return (
                        a.dataset.kind || ""
                    ).localeCompare(
                        b.dataset.kind || ""
                    );

                }


                if (mode === "verified") {

                    return (
                        Number(
                            b.dataset.verified || 0
                        )
                        -
                        Number(
                            a.dataset.verified || 0
                        )
                    );

                }


                return (
                    Number(
                        a.dataset.originalOrder
                    )
                    -
                    Number(
                        b.dataset.originalOrder
                    )
                );

            });


            cards.forEach(function (card) {
                grid.appendChild(card);
            });

        });

    }


    sortSelect.addEventListener(
        "change",
        function () {
            sortCards(
                sortSelect.value
            );
        }
    );

})();

===== FILE: static/js/pitch_operations.js =====
(function () {
    "use strict";


    /* =====================================================
       TIMELINE POSITIONING
    ===================================================== */

    document
        .querySelectorAll(
            ".pitch-axis-marker[data-left]"
        )
        .forEach(function (marker) {

            marker.style.left =
                marker.dataset.left + "%";

        });


    document
        .querySelectorAll(
            ".pitch-timeline-bar[data-left]"
        )
        .forEach(function (bar) {

            bar.style.left =
                bar.dataset.left + "%";

            bar.style.width =
                bar.dataset.width + "%";

        });



    /* =====================================================
       DRAWER
    ===================================================== */

    const drawer =
        document.getElementById(
            "pitch-ops-drawer"
        );

    const overlay =
        document.getElementById(
            "pitch-ops-overlay"
        );

    const closeButton =
        document.getElementById(
            "pitch-drawer-close"
        );


    if (
        !drawer
        ||
        !overlay
    ) {
        return;
    }


    function setText(id, value) {

        const element =
            document.getElementById(id);

        if (element) {

            element.textContent =
                value || "—";

        }

    }


    function openDrawer(button) {

        setText(
            "drawer-title",
            button.dataset.title
        );

        setText(
            "drawer-service-id",
            button.dataset.serviceId
        );

        setText(
            "drawer-sector",
            button.dataset.sector
        );

        setText(
            "drawer-status",
            button.dataset.status
        );

        setText(
            "drawer-effective",
            button.dataset.effective
        );

        setText(
            "drawer-pitch",
            button.dataset.pitch
        );

        setText(
            "drawer-deadline",
            button.dataset.deadline
        );

        setText(
            "drawer-days",
            button.dataset.days
        );

        setText(
            "drawer-updated",
            button.dataset.updated
        );

        setText(
            "drawer-verified",
            button.dataset.verified
        );


        const edit =
            document.getElementById(
                "drawer-edit"
            );

        const detail =
            document.getElementById(
                "drawer-detail"
            );


        if (edit) {

            edit.href =
                button.dataset.editUrl || "#";

        }


        if (detail) {

            detail.href =
                button.dataset.detailUrl || "#";

        }


        drawer.classList.add(
            "open"
        );

        overlay.classList.add(
            "open"
        );

        drawer.setAttribute(
            "aria-hidden",
            "false"
        );

    }


    function closeDrawer() {

        drawer.classList.remove(
            "open"
        );

        overlay.classList.remove(
            "open"
        );

        drawer.setAttribute(
            "aria-hidden",
            "true"
        );

    }


    document
        .querySelectorAll(
            ".ops-row-open"
        )
        .forEach(function (button) {

            button.addEventListener(
                "click",
                function () {

                    openDrawer(
                        button
                    );

                }
            );

        });


    if (closeButton) {

        closeButton.addEventListener(
            "click",
            closeDrawer
        );

    }


    overlay.addEventListener(
        "click",
        closeDrawer
    );


    document.addEventListener(
        "keydown",
        function (event) {

            if (
                event.key === "Escape"
            ) {

                closeDrawer();

            }

        }
    );

})();

===== FILE: static/js/search_activity_console.js =====
(function () {
    "use strict";


    document
        .querySelectorAll(
            ".search-time-event[data-left]"
        )
        .forEach(function (event) {

            let position = Number(
                event.dataset.left
            );


            if (
                !Number.isFinite(position)
            ) {
                position = 0;
            }


            position = Math.max(
                0,
                Math.min(
                    100,
                    position
                )
            );


            event.style.left =
                position + "%";

        });

})();

===== FILE: static/js/session_location.js =====
(function () {
    "use strict";

    if (!navigator.geolocation) {
        return;
    }

    const storageKey = "bharatnxt_location_sent";

    if (sessionStorage.getItem(storageKey) === "1") {
        return;
    }

    function getCookie(name) {
        const cookies = document.cookie ? document.cookie.split(";") : [];

        for (const cookie of cookies) {
            const trimmed = cookie.trim();

            if (trimmed.startsWith(name + "=")) {
                return decodeURIComponent(
                    trimmed.substring(name.length + 1)
                );
            }
        }

        return null;
    }

    navigator.geolocation.getCurrentPosition(
        function (position) {

            fetch("/session/location/", {
                method: "POST",
                credentials: "same-origin",
                headers: {
                    "Content-Type": "application/json",
                    "X-CSRFToken": getCookie("csrftoken")
                },
                body: JSON.stringify({
                    latitude: position.coords.latitude,
                    longitude: position.coords.longitude,
                    accuracy: position.coords.accuracy
                })
            })
            .then(function (response) {
                if (response.ok) {
                    sessionStorage.setItem(storageKey, "1");
                }
            })
            .catch(function () {
                // Location is optional. Do not interrupt the employee.
            });
        },

        function () {
            // Permission denied or location unavailable.
            // The application continues normally.
        },

        {
            enableHighAccuracy: false,
            timeout: 8000,
            maximumAge: 300000
        }
    );
})();


document.addEventListener("submit", function (event) {
    if (
        event.target &&
        event.target.classList.contains("topbar-logout-form")
    ) {
        sessionStorage.removeItem("bharatnxt_location_sent");
    }
});

===== FILE: static/js/toolkit_search.js =====
(function () {
    "use strict";

    const input = document.getElementById("toolkit-search-input");
    const suggestions = document.getElementById("toolkit-suggestions");

    if (!input || !suggestions) {
        return;
    }

    let timer = null;
    let controller = null;

    function hideSuggestions() {
        suggestions.hidden = true;
        suggestions.innerHTML = "";
    }

    function escapeHtml(value) {
        const div = document.createElement("div");
        div.textContent = value || "";
        return div.innerHTML;
    }

    function renderResults(results) {
        if (!results.length) {
            suggestions.innerHTML = `
                <div class="toolkit-suggestion-empty">
                    No matching services
                </div>
            `;

            suggestions.hidden = false;
            return;
        }

        suggestions.innerHTML = results.map(function (item) {
            return `
                <a class="toolkit-suggestion"
                   href="${item.url}">

                    <div class="toolkit-suggestion-main">

                        <div class="toolkit-suggestion-title">
                            ${escapeHtml(item.title)}
                        </div>

                        <div class="toolkit-suggestion-path">
                            ${escapeHtml(item.domain)}
                            ·
                            ${escapeHtml(item.category)}
                        </div>

                    </div>

                    <div class="toolkit-suggestion-meta">
                        ${escapeHtml(item.service_id)}
                    </div>

                </a>
            `;
        }).join("");

        suggestions.hidden = false;
    }

    input.addEventListener("input", function () {
        clearTimeout(timer);

        const query = input.value.trim();

        if (query.length < 2) {
            hideSuggestions();
            return;
        }

        timer = setTimeout(function () {
            if (controller) {
                controller.abort();
            }

            controller = new AbortController();

            fetch(
                "/toolkit/search/suggestions/?q=" +
                encodeURIComponent(query),
                {
                    credentials: "same-origin",
                    signal: controller.signal
                }
            )
            .then(function (response) {
                if (!response.ok) {
                    throw new Error("Search request failed");
                }

                return response.json();
            })
            .then(function (data) {
                renderResults(data.results || []);
            })
            .catch(function (error) {
                if (error.name !== "AbortError") {
                    hideSuggestions();
                }
            });

        }, 300);
    });


    document.addEventListener("click", function (event) {
        if (
            !suggestions.contains(event.target)
            && event.target !== input
        ) {
            hideSuggestions();
        }
    });


    input.addEventListener("keydown", function (event) {
        if (event.key === "Escape") {
            hideSuggestions();
        }
    });

})();

===== FILE: static/js/ui_feedback.js =====
(function () {
    "use strict";


    /* =====================================================
       FEEDBACK NOTIFICATIONS
    ===================================================== */

    document
        .querySelectorAll(
            ".bn-feedback-toast"
        )
        .forEach(function (toast) {


            function dismiss() {

                if (
                    toast.classList.contains(
                        "leaving"
                    )
                ) {
                    return;
                }


                toast.classList.add(
                    "leaving"
                );


                window.setTimeout(
                    function () {

                        toast.remove();

                    },
                    180
                );

            }


            const close =
                toast.querySelector(
                    ".bn-feedback-close"
                );


            if (close) {

                close.addEventListener(
                    "click",
                    dismiss
                );

            }


            const type =
                toast.dataset.feedbackType
                || "";


            const duration =
                (
                    type.includes("error")
                    ||
                    type.includes("warning")
                )
                ? 8000
                : 4300;


            window.setTimeout(
                dismiss,
                duration
            );

        });



    /* =====================================================
       POST ACTION PROGRESS
       Never reports success before server response.
    ===================================================== */

    document.addEventListener(
        "submit",
        function (event) {

            const form =
                event.target;


            if (
                !(form instanceof HTMLFormElement)
            ) {
                return;
            }


            const method =
                (
                    form.getAttribute(
                        "method"
                    )
                    || "get"
                ).toLowerCase();


            if (method !== "post") {
                return;
            }


            if (
                form.dataset.noLoading
                === "true"
            ) {
                return;
            }


            const submitButton =
                form.querySelector(
                    'button[type="submit"]'
                );


            if (!submitButton) {
                return;
            }


            window.setTimeout(
                function () {

                    if (
                        submitButton.disabled
                    ) {
                        return;
                    }


                    submitButton.disabled =
                        true;


                    submitButton.classList.add(
                        "bn-button-loading"
                    );


                    if (
                        submitButton.classList.contains(
                            "topbar-logout-button"
                        )
                    ) {

                        submitButton.textContent =
                            "Signing out…";

                        return;
                    }


                    submitButton.dataset.originalHtml =
                        submitButton.innerHTML;


                    submitButton.innerHTML =
                        '<span class="bn-loading-spinner" aria-hidden="true"></span>'
                        +
                        '<span>Working…</span>';

                },
                0
            );

        }
    );

})();
